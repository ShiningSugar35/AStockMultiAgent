# 外部架构师交接

## 状态提交
- branch: `codex/phase5-stabilization`
- commit: `110f7a7f0d09edb5efb3646f65a912215780a6d0`
- parent: `a2efed324c27b580178a247265254c5523f49f09`
- date: `2026-07-25`

## 当前业务状态
- 已可用：
  - 市场/行情与 provider 监控基础设施
  - 部分财务同步和知识蒸馏链路
  - 阶段性验收链路（状态文档可读）
  - PM 任务管理文件与 `PM_STATE` 机制可运行
- 部分可用：
  - 关键词/来源采集结果的统一闭环（存在 gap）
  - Serenity 投研方法接入与审计，但未设置全量生产化路径
  - 模拟交易链路可审计，但执行侧门禁限制未放开
- 不可用：
  - 在缺少外部架构师输入下继续新增业务开发
  - 全市场、全流程自动化投研产出

## 本周期完成
- 完成首次 PM 基线体检文件创建：
  - `docs/当前开发进展.md`
  - `docs/业务能力矩阵.md`
  - `docs/争议与待5.6复核.md`
  - `docs/外部架构师交接.md`
  - `docs/PM_STATE.json`
- 创建任务目录：
  - `tasks/inbox/`
  - `tasks/active/`
  - `tasks/rework/`
  - `tasks/done/`
  - `tasks/rejected/`
- 未改写业务实现代码，仅做事实核验与状态归档

## 测试
- pytest：not run（MVP-R-002 PM 验收阶段未执行新增回归）
- ruff：not run（基线阶段）
- pyright：not run（基线阶段）
- live/acceptance：见 `验收报告.md` 中当前记录（未新增本次测试）

## 各工作流
- K：PARTIAL（知识采集与蒸馏有历史数据，非完整闭环）
- S：PARTIAL（v2 接入可见，仍需外部确认生产路由）
- F：PARTIAL（财务与结构化核验仍在逐步放大）
- R：PARTIAL（研究结论链未形成自动化投研决策闭环）
- H：PARTIAL（生命周期框架存在，仍需验收策略）
- P：PARTIAL（`paper` 边界较严格，执行写入受控）
- C：PARTIAL（委员会规则存在，需外部输入）
- E：PARTIAL（影子评测在架构，但未成为默认执行线）
- I：PARTIAL（基础设施稳定，可继续核验）

## 关键剩余项
1. 外部架构师确认下一个任务边界（严禁本地自行推进 Phase）
2. 完成 gap 相关争议的外部裁决（采集、Serenity 生产化、模拟交易门禁）
3. 在外部指令下创建标准化 `Worker` 任务与验收标准

## PM 草拟的下一步（仅建议，尚未批准）
- 优先把当前 `PM_STATE` 与 `任务目录` 作为外部架构师下一条任务输入，确认第一轮外发工单的拆分粒度。
- 按优先级确认每个工作流的“可执行最小验收”清单。
- 在外部架构师裁决后提交第一条 `tasks/inbox` 任务并冻结 scope。

## 需要外部架构师判断
- 生产阶段是否允许继续扩展 `Serenity` 至更多作者与领域
- `paper-*` 的执行权限是否可提升（含 `ledger_write_allowed`）
- `gap` 数据中的缺口是否视作阻塞项还是可接受风险
- 是否接受现阶段 `ArgumentUnit` 与 `ParagraphUnit` 现状作为 MVP 先验

## 推荐读取文件
- `开发计划.md`
- `验收报告.md`
- `docs/当前开发进展.md`
- `docs/业务能力矩阵.md`
- `docs/PM_STATE.json`
## MVP-R-001 研究请求入口（已验收）
- 已实现 `research-request` CLI（支持 `股票代码|公司名称`）
- 输出标准化 `ResearchRequest` 工件（含 `artifact_id`/`artifact_hash`/`reused_existing`/`created_at`）
- 目标文件：`src/astock/schemas/research.py`、`src/astock/research/request.py`、`src/astock/cli.py`、`tests/integration/test_cli.py`、`tests/unit/test_research_request_schema.py`
- 验收测试：`uv run pytest tests/unit/test_research_request_schema.py tests/integration/test_cli.py -k "research_request"`，通过 5 条

### MVP-R-001（研究请求入口）
- 外部交付结果：worker 已提交 `feat: add research request intake contract`（c706d1c7b34a02d1c0ede32017d3151b418a8604），目标边界内无架构冲突。
- 功能核验：`research-request <代码/名称> --module ...` -> 输出 `status=CREATED` 与 `artifact_id/object_hash/reused_existing/created_at/request`；异常返回 `status=REJECTED` + `error_code=INVALID_RESEARCH_REQUEST`。
- 测试核验：`uv run pytest tests/unit/test_research_request_schema.py tests/integration/test_cli.py -k "research_request"`（5 passed）。
- 未决外部裁决：无（与外部限制边界一致）。

### MVP-R-002（ResearchRequest 到 EvidenceCollectionTask 桥接）
- 结果：worker 已提交 `feat: bridge research request to evidence collection task`（110f7a7f0d09edb5efb3646f65a912215780a6d0），目标边界内无架构冲突。
- 功能核验：`research-evidence-task <ResearchRequest Artifact ID>` -> 读取 `ResearchRequest`，生成 `EvidenceCollectionTask` 并写入/复用 artifact registry 与 object store；非法输入返回 `status=REJECTED` + `error_code=INVALID_RESEARCH_TASK_REQUEST`。
- 测试核验：新增 `tests/unit/test_evidence_collection_task_schema.py`、`tests/unit/test_evidence_collection_task_service.py`、`tests/integration/test_cli.py` 覆盖正常生成、幂等复用、非法输入；本轮未执行新增测试回归。
- 未决外部裁决：无（与外部限制边界一致）。
