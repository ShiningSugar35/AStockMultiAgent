# 低成本 A 股多 Agent 投研、证据审计与模拟交易系统——完整开发计划

版本：v3.0
制定日期：2026-07-13  
最近修订：2026-07-17
目标平台：Windows 10，Python 3.12  
需求来源：低成本A股多Agent投研系统方案.md  
当前状态：Phase 0、Phase 1 / M1、Phase 2、Phase 3、Phase 4、Phase 6 与 Phase 7 程序层均已完成并通过验收。Phase 7 已交付冻结 study、公平多臂分配、不可变执行观察、确定性市场状态、walk-forward、成对统计、Phase 8 准入、审计、恢复、Parquet 事实和 12 个 CLI 入口；但真实 study、决定和 observation 仍为 0，证据状态是 `NOT_RUN`，绝不是 `EVIDENCE_READY`。Phase 5 的 K5.1～K5.3 确定性内核、K5.5 覆盖审计以及 K5.6a～K5.6d 自动清洗/摘录/候选链已实现，但聚合审计仍有 7 个采集 gap 和 616 个未完成根评论范围，401 条观点仍为 `PENDING`，40 个候选 Skill 仍为 `NOT_RUN/PENDING`，因此 Phase 5 保持 `PARTIAL`。寒武纪 DOCX 是用户确认完整的本地语料，不再联网；私有 PDF/DOCX 均不进入 Git。项目固定使用 Python 3.12；Python 3.11 和 Launcher 已清理。Phase 8 的 P8.0 默认禁用准入状态壳已实现并通过验收；真实硬门仍缺 12 个月、100 个独立决定、5 个合格折和 3 个合格市场状态，因此 P8.1～P8.4 保持未授权、未实现，不能启动动态权重、赌博机或 RL。

### v3.0 变更摘要

1. 交付 `committee-rules-v1`、严格冻结输入解析、无网络 CommitteeAccessPolicy、确定性评估/决策矩阵、不可变 bundle/DecisionPack 和 migration 0027。
2. 固化不可交易、杠杆、财务硬阻断、单股/总仓位/行业/相关性上限、组合回撤与连续亏损冻结；重大公告和数据异常待核验时固定进入 `NEEDS_INFO`。
3. 交付选择性 CounterCase、调查任务闭环和每决定一份 TradeProtocol；全部协议人工确认，券商执行和账本写入永远为 false。
4. 交付 9 个委员会 CLI、审计/恢复、三类严格 Codex 工件和四个 Repo Skill 的真实 Phase 6 路由；外部 narrative 默认关闭且不能覆盖确定性结果。
5. 全仓 222 项实际执行为 214 passed / 8 skipped，ruff、pyright、真实 migration 0027、SQLite 完整性和外键检查通过；真实委员会 8 张表全部为 0 行，未制造公司结论。验收依据见 `docs/验收报告.md（合并来源：Phase6验收报告.md）`。

### v2.9 变更摘要

1. 交付 `codex-run-input-v2`、注册 Artifact 引用、migration 0026 输入/输出安全索引、真实 input_hash 绑定和严格已登记输出门禁。
2. 十种 Phase 4 工件只能按主 ID、registry 类型、ObjectStore 哈希和本 run 冻结输入精确包装；伪造、未登记、未冻结输出和非人工确认 Proposal 均拒绝。
3. 交付 `codex-run-status/audit/recover`、draft hash、中断恢复、固定错误码和隐私脱敏；篡改文件、索引、父工件或损坏对象均会被审计发现。
4. 交付 `research-chain-status/audit` 和真实 Repo Skill 命令路由；完整合成链从 Evidence 跑到 strict Codex audit PASS，缺阶段保持 NOT_RUN/PARTIAL。
5. 全仓 212 项已穷尽执行为 204 passed / 8 skipped，ruff、pyright、真实 migration 0001～0026、SQLite integrity 和外键检查通过；Phase 4 完成。验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.5验收报告.md）`。

### v2.8 变更摘要

1. 交付 `generic-position-lifecycle-v1`、严格监控计划/增量复核 Schema、migration 0025、安全索引、ObjectStore、Artifact/checkpoint、连续窗口和崩溃恢复。
2. 固定 `EXIT > REVIEW > TRIM > ADD > HOLD`；ADD 必须同时有已登记规则和窗口内新增支持 Evidence，冲突、基线失效或缺支持时进入 REVIEW；所有 Proposal 强制人工确认。
3. 交付 `position-lifecycle-schema`、`position-plan-create/status`、`holding-review-run/status/audit`，CLI 和 SQLite 均不暴露 thesis、规则说明、本地路径或私密数量。
4. 合成完整研究链跑通五种动作、异常输入、连续窗口、开放冲突、账本零变化和中断恢复；全仓验收为 195 passed / 8 skipped，ruff、pyright 和真实 migration 0001～0025 通过。
5. 真实运行库只登记规则版本，真实 plan/update/review/proposal 均为 0；没有用户真实持仓决策时不制造示例建议。验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.4验收报告.md）`。

### v2.7 变更摘要

1. 交付 migration 0024、`research_diagnostics.yaml`、6 个严格专家诊断请求、`SpecialistDiagnosticReport`、引用保真 `ResearchMemoArtifact` 和对应安全索引/CLI/审计。
2. 产业瓶颈必须完整验证“系统变化→必要环节→稀缺性→可替代性→上市公司价值捕获”；事件必须落到经营指标、财务方向、时间窗和反证；任一关键链断裂都返回结构化缺口，不产生正向结论。
3. 成长场景 Decimal 概率必须精确守恒，不自动归一化；成长估值明确比较研究增长与市场隐含增长，缺一致预期保留 `CONSENSUS_UNAVAILABLE`，不写成 0，不生成伪精确目标价。
4. 日线只接受 `1d`，HourlySwing 只接受独立 `60m`；两者分别执行质量、样本数和版本化阈值门，只输出趋势/时机背景并明确“不是买入信号或订单”。
5. memo 只组合 12 个 BaseCase section 与同 route 的 Delta/finding/metric/adjustment/evidence 引用；证据并集必须守恒，缺失已选专家明确列出，其他 route 的 Delta 被拒绝。
6. 全仓验收为 187 passed / 8 skipped，ruff、pyright、真实运行库 migration 0001～0024、SQLite integrity 和外键检查全部通过；真实诊断/memo 仍为 0，未制造公司结论。验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.3验收报告.md）`。

### v2.6 变更摘要

1. 交付 migration 0023、`research_skills.yaml`、7 个版本化通用 Skill 合同、确定性专家路由和 `SpecialistDelta`；其中 6 个分析合同占专家名额，`ResearchMemoComposer` 只负责后续组合且不占名额。
2. 用户显式点名超过 3 个专家直接拒绝；自动命中超过 3 个时稳定选择前三并记录 `ROUTE_CAPPED_AT_THREE`，不静默丢弃。日线与独立 60 分钟规则分离，缺小时频率明确 `FREQUENCY_UNAVAILABLE`；缺一致预期明确 `CONSENSUS_UNAVAILABLE`。
3. 专家只能引用同一 FrozenEvidencePack，只能提交增量发现、BaseCase 更正、行业指标、证据请求、失败模式及估值/风险/覆盖调整；未被路由选中的 Skill、冻结范围外证据和没有官方证据的关键判断均被拒绝。
4. 完整正文只进入 ObjectStore；SQLite 只保存 ID、版本、哈希、计数、覆盖和置信度元数据。新增安全 CLI、Codex Artifact 类型、对象/索引/引用/未来信息审计与恢复检查。
5. 全仓验收为 181 passed / 8 skipped，ruff、pyright、真实运行库 migration 0001～0023、SQLite integrity 和外键检查全部通过；真实库只登记 1 份通用 registry，因没有真实公司研究请求而保持 route/delta 为 0。验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.2验收报告.md）`。

### v2.5 变更摘要

1. 交付 migration 0020～0021、版本化私有摘录生成器、观点/Skill 安全引用关系、作者生成报告及 `knowledge-draft-generate/status/audit`；同一蒸馏输入可按生成规则版本并存，不覆盖旧草稿。
2. 自动观点只保存原文摘录并标记 `SOURCE_EXCERPT_NOT_SYNTHESIZED`；适用范围、反证和失效条件均保留显式质量缺口，不替作者补写。
3. 自动 Skill 的正式规则固定为空，并强制 `NOT_RUN/PENDING`；公司事实回到官方证据、PIT、样本外评测和禁止自动交易是固定门禁。
4. 真实结果：MR Dang 148 条摘录/14 个候选，寒武纪 128/12，黄彦臻 0/0，派大星皮皮 125/14；合计 401/40，四位对象与引用审计全部 `PASS`，重复运行报告 ID 不变。
5. 全仓验收为 165 passed / 8 skipped，ruff、pyright、真实运行库 migration 0001～0021、SQLite integrity 和外键检查全部通过；0021 真实迁移前后 401/40/4 及引用计数守恒。验收依据见 `docs/验收报告.md（合并来源：Phase5-K5.6c-K5.6d验收报告.md）`。

### v2.4 变更摘要

1. 交付 migration 0019、跨 PDF 页/DOCX 块/知乎内容/评论的统一定位、规范化文本对象、稳定单元 ID、精确重复链和版本化多标签分类。
2. 交付 `knowledge-distill-plan/run/status/audit` 与 `knowledge-review-queue`；CLI 只输出 ID、哈希、计数和状态，不输出正文或路径。
3. 真实处理结果：MR Dang 5,942 单元、寒武纪 1,856 非空块单元、黄彦臻 1 单元、派大星皮皮 615 单元；四位作者对象、SQLite 与 Parquet 审计全部 `PASS`。
4. 生成 4 个 `PENDING` 私有审核队列、2 个本地清洗报告和 2 个方法覆盖报告；零命中未被写成 `AUTHOR_SILENT`，未产生任何已批准规则。
5. 全仓验收为 163 passed / 8 skipped，ruff、pyright、SQLite integrity、外键和 Git 隐私扫描通过；依据见 `docs/验收报告.md（合并来源：Phase5-K5.6a-K5.6b验收报告.md）`。

### v2.3 变更摘要

1. 新增 `docs/开发计划.md（合并来源：Phase5-K5.6知识蒸馏开发计划.md）`，固定统一定位、私有载荷、确定性分类、重复链、作者覆盖、审核队列、观点/Skill 候选和真实验收顺序。
2. 修正原有设计缺口：DOCX 必须使用块定位而非伪造页码；私有命题和规则载荷只能进入 ObjectStore，SQLite/Parquet/CLI 仅保存哈希与安全元数据。
3. 自动分类只产生待复核候选；零命中在人工批准前不得写成 `AUTHOR_SILENT`，未评测规则不得变为生产 Skill。

### v2.2 变更摘要

1. 交付 K5.3 评论确定性内核：migration 0017、评论页与节点版本、根/子回复独立检查点、Parquet 元数据索引、目标作者参与链和评论响应回放。
2. 评论端点采用“实测后批准”门禁：回答根评论模板已由登录 Chrome 页面资产观察确认；子回复模板保持 `PENDING_OBSERVATION`，任何猜测路径都在持久化前拒绝。
3. 交付 K5.5：`knowledge-local-coverage` 与 `knowledge-coverage-audit`、migration 0018、对象哈希验证、SQLite/Parquet 对账、开放 gap/待消费响应/长时间运行任务检查。
4. 真实 DOCX 核验为 2032/2032 块、正文对象 2032/2032、元数据对象 2032/2032、缺失对象 0；该结论严格标为用户确认完整导出，不冒充知乎分页独立核验。
5. 全仓验收为 162 passed / 8 skipped，ruff、pyright、SQLite 完整性、外键和 Git 隐私扫描均通过；依据见 `docs/验收报告.md（合并来源：Phase5-K5.3b-K5.5验收报告.md）`。

### v2.1 变更摘要

1. 交付 K5.3a 无凭据响应导入：strict 信封、runtime 路径门、同源 HTTPS API 门、migration 0016、PENDING/CONSUMED 队列和安全 CLI。
2. 导入列表响应复用原有解析、版本、Parquet、覆盖报告和检查点；乱序回放拒绝，重复回放幂等，崩溃后可补记消费状态。
3. 全仓验收为 152 passed / 8 skipped，ruff、pyright、SQLite 完整性和 Git 隐私扫描均通过；依据见 `docs/验收报告.md（合并来源：Phase5-K5.3a验收报告.md）`。

### v2.0 变更摘要

1. 实现白名单知识源 Schema、migration 0015、不可变响应/正文存储、SQLite 元数据、Parquet 索引、游标恢复、内容版本链、覆盖报告和安全 CLI；受限响应不会被误判为空。验收依据见 `docs/验收报告.md（合并来源：Phase5-K5.1-K5.2验收报告.md）`。
2. 真实确认黄彦臻为 `huang-wei-yan-30`、派大星皮皮为 `xiao-peng-61-47`，公开索引候选与主页 API 返回的显示名及 url token 均精确匹配，原人工身份任务改为 `RESOLVED`。
3. 低频实测确认：MR Dang 想法 0 条且 `CONFIRMED_EMPTY`；黄彦臻想法 1 条且 `PAGINATION_COMPLETE`；派大星皮皮已不可变保存 615 条，后续游标因平台限制暂停。三位作者的回答/文章均保留访问缺口，等待已登录浏览器通道恢复。
4. 知乎返回的同域 `http://` 分页游标只允许在精确 `www.zhihu.com/api/` 边界内升级为 HTTPS；跨域、用户信息、异常端口和非 API 路径继续拒绝。

### v1.9 变更摘要

1. 落地 `docs/开发计划.md（合并来源：Phase5-知乎采集开发计划.md）`，把白名单、完成定义、Python→MCP→已登录 Chrome/Browser→人工的访问顺序、四层检查点、存储边界、错误暂停条件和验收门写成可执行合同。
2. 再次确认“寒武纪的鳄鱼”不进入线上抓取；MR Dang、黄彦臻、派大星皮皮是仅有的线上作者，后两位先自动发现唯一身份，歧义只阻塞各自来源。
3. 线上原始响应与本地私有正文只进入 runtime/ObjectStore；Git 只保存代码、无真实正文 fixture、Schema、哈希、计数、计划和验收结论。

### v1.8 变更摘要

1. M3.3 已实际交付：冻结数据集、行业与同行 cohort 身份、按时序/同行隔离的训练口径、稳健 Z-score、Isolation Forest、PyOD ECOD、固定随机种子、版本化模型工件、冻结评测、证据支持的正常情境解释、显式失败门禁与 migration 0012～0014。
2. 全仓验收为 131 passed / 8 skipped，ruff 和 pyright 零问题；真实状态库 migration 0001～0014 齐全，完整性 `ok`、外键违规为 0。实现与验收依据见 `docs/开发计划.md（合并来源：Phase3-M3.2-M3.3开发计划.md）` 和 `docs/验收报告.md（合并来源：Phase3-M3.3验收报告.md）`。
3. Phase 3 的 M3.1、M3.2、M3.3 已全部完成；下一开发步骤按用户要求先落 Phase 5 知乎采集/知识处理计划，再继续 Phase 4～8，每一步都用实现、测试和验收工件证明。

### v1.7 变更摘要

1. M3.2 已实际交付：显式期间语义、TTM/同比/环比/每股值、完整 Beneish/Piotroski/Altman/Sloan/DuPont、行业阈值门禁、同行 cohort 与稳定中秩分位、migration 0011 和恢复元数据。
2. M3.2 全仓验收为 124 passed / 8 skipped，ruff 和 pyright 零问题，SQLite integrity `ok`；实现与验收依据见 `docs/开发计划.md（合并来源：Phase3-M3.2-M3.3开发计划.md）` 和 `docs/验收报告.md（合并来源：Phase3-M3.2验收报告.md）`。
3. 按用户 2026-07-17 的确认，把“寒武纪的鳄鱼”改为 `LOCAL_EXPORT_USER_CONFIRMED_COMPLETE`：已解析 DOCX 是完整语料，不再需要主页确认或线上抓取；它仍保留“用户提供完整导出、非平台分页独立核验”的来源语义。
4. 需要线上连续枚举的作者收敛为 MR Dang、黄彦臻、派大星皮皮三位；黄彦臻和派大星皮皮仍先自动发现唯一身份，存在同名歧义时才请求用户确认。

### v1.6 变更摘要

1. 对用户原始架构修订指示的 1,391 行正文完成要求—实现—测试—运行时逐项终验；终验不把后续 Phase 的正式范围误写成 M1 已实现能力。
2. 补严 5m 同柱歧义规则：只有同一根 5m 同时触及止盈和止损时，才允许通过 SourceAccessRouter 尝试抓取该局部窗口的合格 1m 作诊断；1m 不可得或不合格时采用保守 5m 主结果并保留敏感性结果，绝不补造路径。
3. 当前复核基线为 Python 3.12.10、SQLite integrity `ok`、全仓离线测试 113 passed / 8 skipped、当前低频 live smoke 7 passed、ruff 与 pyright 全通过；详情落入 `docs/验收报告.md（合并来源：原始架构指示最终完成性审计.md）`。
4. 本版只确认原始指示和 M1 交付已终验，并记录 M3.1 已完成；M3.2、M3.3、Phase 4～8 的未完成状态不变。

### v1.5 变更摘要

1. 记录用户对 v1.4 的正式确认，开发不再停留于等待状态；Phase 3 在独立分支 `feature/phase3-financial-integrity` 开工，Phase 0、M1、Phase 2 不重复返工。
2. Phase 3 拆成三个可独立验收的子里程碑：M3.1 确定性财务核验与证据/PIT 门禁，M3.2 同公司跨期与同行分位，M3.3 PyOD/稳健异常模型和完整阶段验收；任何后续能力在实际交付前保持禁用。
3. M3.1 强制使用 Decimal 和显式单位归一化；每个复算数值与规则发现必须回链 source_snapshot_id、pit_id、evidence_id，或生成明确的数据缺口/人工任务，禁止补造缺失字段和使用未来信息。
4. 银行、保险、证券、房地产、早期生物科技采用独立行业 Profile；传统 Beneish/Altman 对银行和保险必须输出 `NOT_APPLICABLE`，不得机械套用工业企业阈值。
5. Phase 3 只输出风险信号、勾稽结果、可能的正常解释与补证要求，不输出“造假”判断，不改变模拟盘账本或风险硬阻断。

### v1.4 变更摘要

1. 将《价值投资功法》由 3 页样本解析升级为 249/249 页全量解析：244 页使用原生文本、5 页按需 OCR、0 空页、0 失败页，共 151,014 个解析字符；沿用既有文件版本和清单身份，不制造重复源版本。
2. 正式接纳用户批准的“寒武纪的鳄鱼”知乎文章 DOCX 本地导出为私有蒸馏原料：原件进入 ObjectStore，2,032/2,032 个段落块完成解析，1,909 个非空块、123 个空结构块，共 93,323 个字符；文件内无表格、图片、外链或不支持对象，文本覆盖为 `COMPLETE`。
3. 新增可恢复的 DOCX/WordprocessingML 摄入、稳定块定位、块级证据和通用 Claim—Evidence 关联；PDF 使用页定位，DOCX 使用块定位，不用伪造页码。旧页证据经 migration 迁移后保持关联和外键完整。
4. `.gitignore` 增加 `*.doc` / `*.docx`；PDF、DOCX、正文、Cookie、浏览器 Profile 和 `runtime/` 均不得提交 Git。仓库只保存代码、Schema、测试、哈希、数量和无原文的验收结论。
5. DOCX 是本地种子材料，不等于知乎线上历史全集。它没有文章 URL、发布时间、评论图或分页游标，因此不能证明作者线上覆盖完整，也不能关闭该作者的线上身份确认和 CoverageReport 任务。
6. 本轮离线回归为 88 passed / 8 skipped，`ruff check .` 和 `pyright` 均为 0 问题；2,286 个原件、报告、页/块正文及块元数据对象通过 SHA-256 复核，SQLite 完整性为 `ok`、外键违规为 0。

### v1.3 变更摘要

1. 正式知乎白名单锁定为 MR Dang（`mr-dang-77`）、黄彦臻、派大星皮皮、寒武纪的鳄鱼；后三位身份不得猜测，未确认前保留在范围内并标记 `PENDING_IDENTITY_CONFIRMATION`、禁用采集、生成开放人工任务。
2. 将 MR Dang《价值投资功法》明确列为本地私有正式知识源：原 PDF 不进 Git、不再发布，进入内容寻址 ObjectStore；所有解析、观点和规则必须回到文件哈希、版本、页码或原文片段。
3. Phase 2 增加可复用的私有 PDF 入口和统一 PDF/OCR、页码定位、SourceSnapshot、Evidence、PIT 与解析版本接口；本阶段不做该书的内容清洗和 Skill 蒸馏。
4. Phase 5 明确为四位作者逐一交付 Python 优先的可访问历史全集采集和 CoverageReport，并完成《价值投资功法》的可重建清洗、人工审核、观点卡片、候选/持仓两类 Skill 蒸馏与评测。
5. 增加 BookSourceManifest、BookParseReport、BookCleaningReport、BookMethodCoverageReport、BookViewpointCard、BookSkillCandidate、HumanReviewDecision 工件边界和字段要求。
6. 本地 Git 和 GitHub CLI 身份均为 `ShiningSugar35`；核验时此前没有配置远端，也不存在可识别的错误账号仓库，因此没有无关仓库被删除。现已创建私有仓库 `ShiningSugar35/AStockMultiAgent`，默认分支为 `main`，Phase 2 通过草稿 PR #1 交付。

### v1.2 变更摘要

1. 将“委员会不得联网”从文字约定升级为 `CommitteeAccessPolicy` 硬合约：API、MCP、Browser、全文读取和新研究能力只能为 `false`，缺证据只能 `NEEDS_INFO` 并要求调查任务。
2. 将白名单作者采集断点细化为 `CollectionCheckpoint`，完整保存 author、content_type、listing page/cursor、content_id、comment page/cursor 和 nested reply cursor，并接入通用 SQLite Checkpoint。
3. 抽出独立 `PolicyEngine`；Codex 草稿必须依次通过领域 Schema、证据定位和 Policy 闸门，重复导入同一草稿保持同一工件哈希，并回写完成态 RunManifest。
4. 增加真实断网/超时分类、API→MCP→Browser 路由审计、Provider 失败后游标不推进并在恢复后重新发布、完整 Codex 运行目录和 Repo Skill 可执行命令测试。
5. 完成逐项需求—实现—测试证据矩阵；离线套件更新为 51 passed / 6 live skipped，显式在线套件为 6 passed。

### v1.1 变更摘要

1. 主运行入口由 Manual Packet / OpenAI-compatible API 调整为 `CODEX_INTERACTIVE_MODE`；`DETERMINISTIC_MODE` 继续承担数据、计算、质量、风险和账本，外部 LLM Provider 降为可选扩展。
2. 增加根 `AGENTS.md`、项目级 Repo Skills、Codex 运行目录、Schema 校验与受控写回机制；Codex 不得直接写 SQLite 账本。
3. 模拟回放和持仓监控的默认分钟精度改为未复权原始 5m；东方财富为主 Provider，新浪为备用和交叉验证 Provider，1m 退出默认主路径。
4. Skill 范围从候选筛选扩展到建仓、持有验证、加仓、减仓、退出和复盘的完整持仓生命周期，并明确作者证据缺失时不得推测其观点。
5. 用户指定白名单作者的回答、想法、文章及必要评论链历史全集采集成为正式交付范围；全平台、非白名单采集仍不在范围内。
6. Phase 1 提前交付 Job、Cursor、Checkpoint、ObjectStore、失败状态、SourceSnapshot 接口和 CoverageReport Schema，供后续知乎断点采集直接复用。
7. 用户已明确授权：本次计划修订后无需再次确认，立即进入 M1 开发。

---

## 1. 结论先行

### 1.1 总结

项目可以在当前电脑和免费优先预算上落地。主形态是“Codex 原生交互 + Repo Skills + 本地确定性 Python + 结构化冻结工件”的模块化单体；不把多个开源项目整套拼接，也不把免费分钟行情当成永久可靠的数据源。

推荐路线：

1. Python 统一使用 3.12.x；开发机和 CI 首先锁定已实测的 3.12.10。
2. MVP 日线以 AKShare/东方财富为历史底座，AKShare/新浪为日线独立校验；模拟盘和持仓监控以未复权原始 5m 为默认增量主路径。
3. 东方财富 5m 是主 Provider；新浪 5m 已完成 M1 合约实测，作为备用/交叉验证 Provider。免费 1m 仅作诊断性可选增强，不进入默认回放链。
4. QMT 当前不可用，系统按无 QMT 路线开发；以后安装成功只需增加 Provider，不改核心。
5. 历史正式回测默认关闭。系统上线前的当前供应商值标记为 NOT_PIT_SAFE；官方原文重建的数据才允许进入正式历史评测。
6. 原生 PDF 直接解析；扫描页按页触发 RapidOCR；关键财务表格必须有规则校验或人工复核。
7. 用户直接在项目目录中通过 Codex 自然语言发起研究、持仓检查和恢复任务；本地 CLI 与 Schema 负责确定性执行和受控写回，不要求任何外部大模型 API Key。
8. 模拟交易采用自建 SQLite 双重记账和追加事件日志；VeighNa 只借鉴对象和事件语义。
9. 首要用户界面是 Codex + CLI；Streamlit 只是后续可选的薄控制台，不得拖慢数据、证据和账本核心开发。
10. 不承诺盈利，不接实盘自动下单，不在样本不足时做在线学习或强化学习。

### 1.2 MVP 边界

MVP 包含：

- 根 `AGENTS.md`、项目级 Repo Skills、自然语言路由、Codex 运行清单、上下文预算和受控写回；
- 日线可靠底座、交易日历、未复权价格、复权因子和公司行为；
- 东方财富/新浪双源 5m 接口、对候选股、观察股和开放模拟仓位持续积累 5m 数据；
- 数据质量检查、Provider 健康探测、断点续传和幂等写入；
- 内容寻址原始对象存储、Parquet 事实层、DuckDB 查询层、SQLite 状态与账本；
- 巨潮/交易所官方公告和财报下载、页码级引用、Claim—Evidence 证据关系；
- 确定性财务校验、行业条件化红旗、PyOD 辅助异常线索；
- 人工调查任务卡；
- 统一 BaseCase、最多 1～3 个专家差分、候选筛选和完整持仓生命周期 Skill、冻结工件委员会；
- `CODEX_INTERACTIVE_MODE` 主模式、`DETERMINISTIC_MODE` 后台模式、离线 Manual Packet 和可选 OpenAI-compatible 扩展；
- 白名单作者可访问历史回答/想法/文章全集、必要评论链、覆盖报告、断点续采和增量更新；
- 自建模拟账户、5m 优先降级回放、A 股规则、真实持仓只读监控、影子账户和评测；
- 稳定 CLI、可选 Streamlit 薄控制台、Windows 任务计划启动。

明确推迟：

- 全市场长期历史 1m/5m 回填；
- QMT 强依赖；
- PyGOD 和完整实体图异常检测；
- 全知乎、全平台、非白名单用户采集；
- 大型向量数据库、微服务、Kafka、Kubernetes；
- Tauri/React 桌面端；
- 在线调权、上下文赌博机、PPO/A2C；
- 任何实盘自动下单。

---

## 2. Phase 0 实测结果与架构影响

### 2.1 实测环境

| 项目 | 实测结果 |
|---|---|
| 操作系统 | Windows 10 家庭版 64 位，10.0.19045 |
| CPU | Intel i5-1135G7，4 核 8 线程 |
| 内存 | 15.69 GB |
| GPU | NVIDIA MX450，2 GB；不作为 MVP 依赖 |
| 磁盘 | C 盘约 85 GB 可用，D 盘约 196 GB 可用 |
| Python | 项目和 PATH 已固定为 3.12.10；3.11 实体解释器、安装包缓存、Windows 卸载登记和 `py.exe` Launcher 均已清零 |
| uv | 0.11.25 |
| Git | 2.47.1.windows.1 |

### 2.2 闸门结论

| 闸门 | 结论 | 实测依据 | 开发选择 |
|---|---|---|---|
| 小时/日线底座 | GO | AKShare 对 600519、000001、300750、688981、920015 返回上市日至 2026-07-10 的日线；样本无重复日期、无空值 | 日线进入 MVP；小时线仅近期使用，长期由本地 5m 积累后聚合 |
| MINUTE_DATA | MINUTE_DATA_PARTIAL | 东方财富链路中 1m 只返回 5 个交易日；M1 中东财/新浪对主板、创业板、科创板、北交所和指数均实返 5m；两源均存在免费历史窗口和聚合口径限制 | 默认从双源 5m 开始并持续积累；1m 退出主路径；统计阈值未通过时明确降为单源，缺失时再降级 60m/日线 |
| 1m 字段质量 | PARTIAL | 历史日样本 1m 的开盘列大量为 0，造成 OHLC 关系异常；收盘、高、低、量额可读 | 历史 1m 开盘不得直接使用；优先备用源或按规则降级，禁止静默填值 |
| QMT | QMT_NO_GO | 注册表、安装清单、用户目录、环境目录和常见路径均未发现 QMT/MiniQMT/xtquant | QMT Provider 保留接口但默认禁用；不阻塞 MVP |
| BaoStock | NO_GO（当前网络） | 公共登录返回“黑名单用户，请与管理员联系” | 不作为默认底座；以后只作为可选交叉验证 |
| 日线交叉验证 | GO | 东方财富和新浪对 4 个样本、2026 年 124 个共同交易日的 OHLC 和成交额一致 | 新浪作为日线独立校验源 |
| 成交量口径 | NEEDS_NORMALIZATION | 东方财富成交量以手为主，新浪以股为主，样本相差约 100 倍 | Provider Schema 强制记录 volume_unit，规范层统一换算为股 |
| PIT | PIT_PARTIAL | 巨潮可取得 2001 年起原始年报并记录公告时间；当前行情/财务 Provider 不提供完整修订史；历史成分和退市全集尚无已通过源 | 正式历史回测只接受 CERTIFIED 或 DOCUMENT_RECONSTRUCTED；其余只做探索或前向影子 |
| PDF | PDF_PARTIAL | 143 页原生年报全文约 15.4 万字符；财务表页可提表；leafpaper 章节规则仅命中部分章节 | PyMuPDF 为主、pdfplumber 表格、规则与人工复核；不把章节正则当通用真相 |
| 扫描 PDF/OCR | GO（按页） | 受控扫描财报 3 页无文本层；RapidOCR CPU 约 14.8 秒、额外内存约 39 MB，找回三大表标题 | 只对低文本密度页按需 OCR；缓存页图和 OCR 结果 |
| 开源 Skill | CONDITIONAL_GO | 两个 Serenity 固定 commit 结构校验通过；主要是 Markdown 方法论 | 改造为内部 Skill 规范；权重和阈值必须重新校准 |
| leafpaper | CONDITIONAL_GO | Python 3.12 下 54 测试通过、7 跳过、2 因测试硬依赖 Tushare Token 失败；代码可编译 | 拆用规则和测试思想，不复制数据层和多写手流水线 |
| FinRobot | NO_GO（依赖） | 干跑将安装约 147 包并降级 NumPy/Pandas 等；setup.py 声明 Python 小于 3.12；LICENSE 与 setup.py 许可证字段不一致 | 不安装、不 fork；只借鉴职责分离和 provenance |
| 可选 LLM Provider | OPTIONAL_DISABLED | 当前环境没有项目级 base_url、model_id 或密钥引用；Codex 原生交互不依赖该配置 | 不阻塞任何 MVP 正式投研；仅作为未来自动化扩展，通过独立探针后启用 |
| 知乎 | KNOWLEDGE_INGEST_PARTIAL | 已登录 Chrome 可读取作者目录、回答、发布时间、评论和嵌套回复；未遇验证码；尚未完成历史全集遍历 | 白名单历史全集属于正式 Phase 5；低频、断点、覆盖报告；403/429/验证码立即暂停并保留手工导入 |
| 本机计算 | LOCAL_COMPUTE_GO | 660 万行 Parquet 42.6 MB；DuckDB 扫描 0.05 秒，窗口因子 2.35 秒；Polars 分组 0.08 秒；PyOD 5万×12 约 0.9 秒 | 单机模块化架构足够；限制进程并发和单次 OCR/LLM 批量 |
| 模拟账本 | GO | 1 万事件/2 万分录约 0.06 秒；余额为 0；重复事件拒绝；模拟崩溃无半笔残留 | SQLite WAL + FULL 同步 + 追加事件 + 快照 |

### 2.3 Phase 0 对原方案的实际修正

1. Python 基线由 3.11 改为 3.12；依赖锁必须显式约束 Numba 0.66.x，避免 PyOD 解析到不支持 3.12 的旧 llvmlite。
2. BaoStock 从默认免费底座降为禁用备用。
3. 默认回放由“1m 优先”改为“5m 优先”；小时线只作 5m 缺失时的近似降级，长期小时线由本地持续积累的 5m 聚合。1m 只在同一 5m 同时触及止盈/止损时按局部窗口尝试诊断，且必须先通过质量门禁。
4. 免费 1m 不可用作默认高可信 OHLC 回放；历史日开盘为 0 时必须拦截，不得静默修补。
5. PIT 历史回测不是 MVP 验收条件；前向影子评测先行。
6. OCR 采用 RapidOCR 按页模式，不引入 GPU 或大型 OCR 服务。
7. 白名单知乎历史全集技术路径可行、属于正式 Phase 5；Phase 1 先交付可复用断点基础设施，社区内容仍只能作线索或 Skill 来源，不能直接成为关键事实。
8. FinRobot 完全排除为运行时依赖。
9. 外部 LLM Provider 的未配置状态不再是系统 NO-GO；Codex 是主要交互推理入口，确定性 Python 是唯一数据、计算、策略硬门槛和账本执行入口。

### 2.4 M1 双源 5m 补充实测

2026-07-13 对 600519、000001、300750、688981、920015 和 000300 指数分别请求 2026-07-01 至 2026-07-13 的未复权 5m。东方财富与新浪对每个标的都返回 432 根、时间覆盖 100%，无重复、无 OHLC 内部关系错误、无已观察交易日内缺柱。

| 市场样本 | 收盘相对误差 P95 | OHLC 相对误差 P95 | OHLC 最大相对误差 | 成交量相对误差 P95 | 结论 |
|---|---:|---:|---:|---:|---|
| 600519 上交所主板 | 0.0761% | 0.0911% | 0.3264% | 3.0864% | 双源验证 |
| 000001 深交所主板 | 0.0980% | 0.0984% | 0.2921% | 3.1665% | 双源验证 |
| 300750 创业板 | 0.0611% | 0.0832% | 0.1707% | 2.8729% | 双源验证 |
| 688981 科创板 | 0.1182% | 0.1956% | 0.6494% | 1.9195% | 双源验证 |
| 920015 北交所 | 0.1719% | 0.2947% | 1.5544% | 6.3832% | 降级为东财单源 5m |
| 000300 沪深 300 指数 | 0.000104% | 0.000123% | 0.007123% | 0% | 双源验证 |

因此双源校验不再要求每个开高低逐分相同，而使用版本化的 `cross-5m-empirical-v1`：共同柱至少 48、覆盖至少 98%、收盘相对误差 P95 不高于 0.15%、OHLC 相对误差 P95 不高于 0.20%、OHLC 最大相对误差不高于 1%、规范化成交量相对误差 P95 不高于 5%。所有原始差异照常保留，阈值只决定回放等级，不改写事实。北交所样本越阈值，故不得标为双源验证。

增量实测中，首次完整同步为 432 根；后续按检查点回看 7 天只请求 241 根，但合并后的 canonical 仍保持 432 根和原始起点。再次运行相同增量后 manifest 哈希稳定。详细证据见 `docs/验收报告.md（合并来源：M1验收报告.md）`。

---

## 3. 技术栈和版本策略

首个锁文件使用以下已实测组合；升级必须经过 CI、数据合约和基准回归：

| 类别 | 选择 |
|---|---|
| Python | 3.12.10；项目约束大于等于 3.12、小于 3.13 |
| 包管理 | uv + uv.lock |
| Schema | Pydantic v2 |
| ETL/因子 | Polars 1.42.1 为主；Pandas 3.0.3 只用于第三方接口和兼容 |
| 分析查询 | DuckDB 1.5.4 |
| 列式存储 | Parquet + PyArrow 25.0.0 |
| 状态/账本 | SQLite，WAL，foreign_keys=ON，synchronous=FULL |
| 行情运输 | AKShare 1.18.64；新浪接口做日线校验 |
| PDF | PyMuPDF 1.28.0 + pdfplumber 0.11.10 |
| OCR | RapidOCR ONNXRuntime 1.4.4，仅按页触发 |
| 异常检测 | scikit-learn 1.9.0 + PyOD 3.6.1 + Numba 0.66.0 |
| Codex 项目入口 | 根 AGENTS.md + `.agents/skills/` + 本地 `astock` CLI；不需要项目级模型 API Key |
| 可选 LLM | openai Python SDK 2.45.0，仅在未来 Provider 适配器内部使用，默认不安装到核心依赖也不启用 |
| Web/API | FastAPI 仅作未来内部薄接口；Streamlit 为可选薄 UI |
| 调度 | Windows Task Scheduler 启动；进程内 APScheduler 只做短任务 |
| 测试 | pytest、Hypothesis、golden files、合约测试 |
| 质量 | ruff、pyright、pre-commit、Git |

运行模式固定为：

- `CODEX_INTERACTIVE_MODE`：主要交互和推理入口。Codex 读取项目规则、按需加载 Repo Skill、调用本地 CLI、消费冻结工件并生成待校验结果。
- `DETERMINISTIC_MODE`：数据同步、质量、财务计算、异常检测、候选规则扫描、风险硬门槛、账本和恢复；可由命令行或计划任务独立运行。
- `MANUAL_PACKET_MODE`：离线交换、审计和兼容备用格式，不是日常主入口。
- `OPENAI_COMPATIBLE_MODE`：未来可选自动化扩展；没有 base_url/model/key 时 capability 为禁用，不影响正式投研。

不采用通用 Agent 框架作为 MVP 核心。中央 DAG、ArtifactStore、PolicyEngine、SourceAccessRouter 和本地写回服务用少量自研 Python 实现，降低依赖和隐藏状态。以后若引入 PydanticAI/OpenAI Agents SDK，必须放在可选 LLM 适配层后面，不能接管账本、数据质量、证据校验或风险门槛。

---

## 4. 总体架构、接口和运行 DAG

### 4.1 模块化单体

    Codex Interactive Research Mode
          |
    AGENTS.md + Repo Skills + stable astock CLI
          |
    RunPlanner -> ContextPlanner -> DAG Orchestrator -> PolicyEngine
          |
    +----------------+--------------+---------------+---------------+
    | Source Router  | Providers    | Research      | Paper Trading |
    | API/MCP/Web/UI | market/docs  | evidence      | ledger/replay |
    +----------------+--------------+---------------+---------------+
          |
    Object Store + Parquet + DuckDB Views + SQLite State

所有节点遵循：

    读取带版本的输入工件
      -> 执行确定性工作或由 Codex 生成增量草稿
      -> 验证输出 Schema
      -> 校验证据引用和 PolicyEngine
      -> 原子写入新工件
      -> 记录输入哈希、版本、上下文预算和状态

### 4.2 核心公开接口

**MarketDataProvider**

    probe() -> DataProviderCapability
    fetch_bars(BarRequest) -> RawBarBatch
    fetch_calendar(CalendarRequest) -> RawCalendarBatch
    fetch_corporate_actions(ActionRequest) -> RawActionBatch

**FilingProvider**

    search(DisclosureQuery) -> list[DisclosureRef]
    fetch(DisclosureRef) -> SourceSnapshot

**LLMProvider**

    probe() -> LLMCapability
    generate_structured(ModelRequest[T]) -> ModelResult[T]

这是可选扩展接口，不在 MVP 主路径。若以后启用，Provider 必须支持 Responses 和 Chat Completions 两种请求形状的能力声明；不能假设中转支持所有官方特性。Responses 的结构化输出使用 text.format/json_schema，Chat Completions 使用 response_format/json_schema。官方参考：

- https://developers.openai.com/api/docs/guides/structured-outputs
- https://developers.openai.com/api/docs/guides/error-codes

**Skill**

    screen(UniverseContext) -> list[CandidateDelta]
    analyze(BaseCasePack, EvidenceScope) -> SpecialistDelta
    monitor(PositionMonitoringPlan, IncrementalEvidencePack) -> HoldingReviewPack

**SourceAccessRouter**

    decide(SourceAccessRequest) -> SourceAccessDecision
    execute(SourceAccessDecision) -> SourceAccessResult

固定优先级为 API/本地数据 -> MCP -> Browser -> Manual Task。后两种在 M1 可为禁用 capability stub；所有选择原因、回退链、时间、结果哈希、失败分类和限速状态必须可审计。已由前一层满足的内容不得重复抓取。

**CodexRunService**

    initialize(CodexRunRequest) -> RunManifest
    validate_draft(run_id, result_draft) -> ValidationReport
    import_validated(run_id) -> ArtifactRef | PaperCommandResult

Codex 只写 `runtime/codex_runs/<run_id>/result_draft.json` 等候选工件；服务完成 Pydantic、引用和 Policy 校验后才写入 ArtifactStore。模拟账本只能通过幂等业务命令写入，禁止 Codex 直接修改 SQLite。

**DAGNode**

    plan(RunContext) -> NodePlan
    run(NodeContext, input_refs) -> ArtifactRef

**PaperBroker**

    submit_order(OrderIntent) -> Order
    cancel_order(order_id) -> Order
    on_market_event(MarketEvent) -> list[PaperEvent]
    replay(ReplayRange) -> ReplayReport

### 4.3 单标的研究 DAG

    natural_language_request
      -> route_repo_skill
      -> capability_probe_and_context_plan
      -> sync_incremental_market_and_disclosures
      -> data_quality_and_canonicalize
      -> build_incremental_evidence_pack
      -> load_or_build_base_case
      -> deterministic_skill_router
      -> run_1_to_3_specialist_deltas
      -> conditional_financial_integrity
      -> evidence_gap_resolver
      -> manual_task_or_continue
      -> freeze_artifact_set
      -> Codex_result_draft
      -> schema_citation_policy_validation
      -> committee_on_frozen_inputs
      -> hard_risk_engine
      -> artifact_or_validated_paper_command

委员会只能消费冻结工件哈希。发现关键缺口时返回 NEEDS_INFO，不能自行重新搜索。

### 4.4 Codex 运行工件

每次交互研究在 `runtime/codex_runs/<run_id>/` 生成：

    request.json
    input_manifest.json
    context_budget.json
    result_draft.json
    validated_result.json
    citations.json
    run_summary.md

运行前先生成 `ContextBudgetReport`，默认读取紧凑的 DataQualitySummary、EvidencePack、BaseCasePack、FinancialIntegrityEvidencePack、SpecialistDelta、PositionMonitoringPlan、HoldingReviewPack 和 PortfolioRiskPack。只有某个 claim 需要复核时，才按 evidence_id 精确打开原页或网页快照；同一原始材料不得在多个专家流程中重复输入。

### 4.5 状态机

候选状态：

    DISCOVERED -> SCREENED -> RESEARCHING
      -> NEEDS_INFO -> RESEARCHING
      -> WATCHLIST -> PAPER_PENDING -> PAPER_OPEN -> PAPER_CLOSED
      -> REJECTED
      -> ARCHIVED

任务状态：

    PENDING -> RUNNING -> SUCCEEDED
                    |-> RETRYABLE_FAILED -> PENDING
                    |-> BLOCKED_MANUAL
                    |-> PERMANENT_FAILED
                    |-> CANCELLED

订单状态：

    NEW -> ACCEPTED -> PARTIALLY_FILLED -> FILLED
                   |-> CANCEL_PENDING -> CANCELLED
                   |-> REJECTED
                   |-> EXPIRED

---

## 5. 代码仓库目录和写入所有权

    AStockMultiAgent/
    ├─ AGENTS.md                    # 全项目稳定规则；保持短小
    ├─ .agents/skills/              # Codex 渐进加载的项目级 Skills
    │  ├─ astock-research-orchestrator/
    │  ├─ candidate-scan/
    │  ├─ company-deep-research/
    │  ├─ financial-integrity-audit/
    │  ├─ holding-monitor/
    │  ├─ paper-trading-recovery/
    │  ├─ knowledge-ingest/
    │  └─ evidence-investigation/
    ├─ pyproject.toml
    ├─ uv.lock
    ├─ README.md
    ├─ configs/
    │  ├─ providers.example.yaml
    │  ├─ risk_policy.yaml
    │  ├─ fee_rules.yaml
    │  └─ industry_profiles/
    ├─ src/astock/
    │  ├─ core/                 # DAG、SourceAccessRouter、上下文、策略、哈希
    │  ├─ schemas/              # Pydantic 公共 Schema；不写数据
    │  ├─ providers/            # 外部运输；只写 raw staging
    │  ├─ market_data/          # 质量、规范化、公司行为；唯一行情规范写入者
    │  ├─ documents/            # 下载、解析、OCR；唯一文档解析写入者
    │  ├─ evidence/             # Claim/Evidence；唯一证据关系写入者
    │  ├─ financial_integrity/  # 财务校验、行业规则、PyOD
    │  ├─ candidates/           # 候选注册表状态服务
    │  ├─ skills/               # Skill 执行代码/Schema；Repo Skill 是 Codex 入口
    │  ├─ investigation/        # 证据缺口与人工任务
    │  ├─ committee/            # 冻结工件裁决；禁止联网
    │  ├─ portfolio/            # 程序化风险
    │  ├─ paper_trading/        # 订单、成交、账本、回放；唯一账本写入者
    │  ├─ evaluation/           # 影子账户、评测、归因
    │  ├─ knowledge/            # 白名单采集、清洗、卡片和 Skill
    │  ├─ api/                  # FastAPI 可选薄层
    │  ├─ ui/                   # Streamlit
    │  └─ cli/                  # astock 稳定入口；状态写回经服务层
    ├─ migrations/              # SQLite 迁移
    ├─ tests/
    │  ├─ unit/
    │  ├─ property/
    │  ├─ contract/
    │  ├─ integration/
    │  ├─ golden/
    │  ├─ replay/
    │  └─ performance/
    ├─ evals/
    ├─ third_party/audits/      # 固定 commit 审计清单和许可证，不放可变主分支
    ├─ scripts/
    └─ runtime/                 # 默认 gitignore
       ├─ objects/sha256/
       ├─ data/parquet/
       ├─ artifacts/
       ├─ codex_runs/<run_id>/
       ├─ checkpoints/
       ├─ collection/
       ├─ knowledge/
       ├─ state.sqlite
       ├─ research.duckdb
       └─ logs/

每类事实只有一个写入者。DuckDB 只读 Parquet；SQLite 不保存行情、财务和证据事实副本。Repo Skill 不直接写事实或账本，只编排本地 CLI、读取冻结工件并提交待校验的结构化结果。

---

## 6. 存储、数据库和一致性

### 6.1 内容寻址对象存储

路径：

    runtime/objects/sha256/ab/cd/<完整 sha256>

SourceSnapshot 元数据记录原始 URL、请求时间、响应头、MIME、大小、SHA-256、发布/可得时间和权利状态。对象不可覆盖；相同哈希去重。Windows 写入采用同卷临时文件、flush/fsync、原子 replace；并发写同一哈希必须幂等返回同一对象引用。

### 6.2 Parquet 唯一分析事实源

主要数据集：

| 数据集 | 分区 | 唯一键 | 唯一写入者 |
|---|---|---|---|
| market_observation | provider/frequency/year/symbol_bucket | provider+symbol+frequency+timestamp+adjustment | provider ingest |
| market_canonical | frequency/year/symbol_bucket | symbol+frequency+timestamp+price_basis | market canonicalizer |
| trading_calendar | market/year | market+date | market canonicalizer |
| corporate_action | market/year | event_id+version | corporate action service |
| financial_fact | statement/period_year/symbol_bucket | company+period+concept+source_version | financial normalizer |
| disclosure_index | market/publish_year | disclosure_id | document service |
| document_page | publish_year/document_bucket | document_id+page+parser_version | document service |
| evidence | as_of_year/company_bucket | evidence_id+version | evidence service |
| claim | as_of_year/company_bucket | claim_id+version | evidence service |
| entity_relation | relation_type/year | relation_id+version | entity service |
| factor | factor_version/date | symbol+date+factor_id+version | factor engine |

原始 Provider observation 和 canonical 都属于 Parquet 事实层，但职责不同：前者保留来源原貌，后者保存经过单位换算、质量选择和冲突标记的分析序列。canonical 必须引用 observation_id。

### 6.3 DuckDB

research.duckdb 只保存外部视图、宏和少量物化查询缓存定义；不得复制事实。每次启动校验视图对应的 Parquet manifest 和 Schema 版本。

### 6.4 SQLite

核心表：

- schema_migration(version, applied_at, checksum)
- job(job_id, type, status, priority, input_hash, created_at, updated_at)
- job_attempt(attempt_id, job_id, started_at, ended_at, error_class, retryable)
- cursor(cursor_id, provider_id, scope, value_json, checkpoint_hash)
- checkpoint(checkpoint_id, job_id, scope_type, scope_key, cursor_json, status, object_hash, committed_at)
- lock(lock_key, owner_run_id, lease_until)
- idempotency_key(key, command_type, result_ref, created_at)
- provider_health(provider_id, capability_hash, status, last_probe_at, failure_count)
- run(run_id, mode, status, as_of, plan_hash, started_at, ended_at)
- artifact_registry(artifact_id, type, schema_version, object_hash, input_hashes_json)
- source_snapshot_index(snapshot_id, source_id, object_hash, fetched_at, availability_at, fetch_status)
- collection_scope(scope_id, author_id, content_type, status, last_cursor, terminal_condition)
- collection_gap(gap_id, scope_id, cursor_json, failure_class, retryable, status)
- candidate_state(candidate_id, company_id, state, version, updated_at)
- order_event(event_id, seq, order_id, event_type, payload_json, effective_at)
- fill(fill_id, order_id, event_id, qty, price_fen, fee_fen, occurred_at)
- ledger_account(account_id, account_type, currency, opened_at)
- journal(event_id, seq, event_type, occurred_at, idempotency_key)
- ledger_entry(entry_id, event_id, account_id, debit_fen, credit_fen)
- position_snapshot(snapshot_id, as_of_event_seq, symbol, qty_total, qty_available, cost_fen)
- replay_checkpoint(account_id, symbol, last_event_seq, market_cursor, snapshot_hash)

硬约束：

- 金额使用整数分，数量使用整数股；比率使用 Decimal 或明确精度。
- 每个 journal 事件借方总额等于贷方总额。
- event_id、idempotency_key、order_id+client_request_id 唯一。
- 事务提交后才发布工件引用。
- 迁移前备份；迁移失败自动恢复到原数据库。
- 检查点只在对象、Parquet observation 或业务事务成功落盘后推进；失败响应不得把游标推进到未知位置。
- 原始快照、采集日志和清洗日志分离；更新内容产生新版本，不覆盖旧对象。

### 6.5 跨存储一致性

每日运行：

1. 对象哈希存在性；
2. Parquet manifest 与文件哈希；
3. canonical 引用的 observation 是否存在；
4. artifact 输入哈希是否可解析；
5. SQLite 账本是否平衡；
6. replay checkpoint 是否不超过最后事件；
7. DuckDB 视图 Schema 是否匹配；
8. 孤儿对象只标记，不自动删除。

---

## 7. 核心 Schema

所有 Schema 带 schema_version、created_at、producer_version 和 content_hash。

| Schema | 必要字段与约束 |
|---|---|
| SourceDocument | document_id, title, publisher, document_type, company_ids, published_at, effective_at, disclosure_id, source_url, rights_status |
| SourceSnapshot | snapshot_id, document_id, object_sha256, fetched_at, available_to_system_at, mime, byte_size, headers_hash |
| SourceAccessDecision | source_id, requested_capability, selected_transport, selection_reason, fallback_chain, request_started_at, request_finished_at, result_hash, failure_class, rate_limit_state |
| MarketBar | provider_id, symbol, market, frequency, timestamp, timestamp_semantics, open, high, low, close, volume, volume_unit, amount, amount_unit, adjustment_mode, observation_id |
| MarketDataBatch | provider_id, request, requested_start, requested_end, actual_start, actual_end, bar_count, raw_snapshot_id, bars, cursor, provider_latency_ms, provider_status |
| DataQualityReport | batch_id, missing_sessions, missing_bars, duplicate_bars, ohlc_errors, unit_errors, cross_source_diffs, quality_status, replay_quality, reasons |
| Evidence | evidence_id, snapshot_id, locator(page/section/dom), excerpt_hash, evidence_grade, fact_status, entity_ids, valid_from, valid_to |
| Claim | claim_id, subject_id, predicate, object_json, as_of, claim_type, confidence, status |
| ClaimEvidenceLink | claim_id, evidence_id, relation(SUPPORT/REFUTE/CONTEXT), weight, reviewer_status |
| EvidenceConflict | conflict_id, claim_id, evidence_ids, conflict_type, resolution_status, resolution_note |
| PointInTimeMetadata | period_end, published_at, effective_at, ingested_at, available_to_system_at, revised_at, supersedes_source_id, point_in_time_status, availability_basis |
| EvidencePack | company_id, as_of, evidence_ids, claim_ids, conflicts, coverage, frozen_at, pack_hash |
| BaseCasePack | company_id, as_of, business_model, drivers, cash_flow_quality, capital_returns, reinvestment, governance, competition, industry, valuation_expectations, trend_context, risks, gaps, specialist_tags, base_confidence, coverage, evidence_ids |
| SpecialistDelta | skill_id, skill_version, incremental_findings, base_case_corrections, industry_metrics, evidence_requests, failure_modes, confidence_delta, valuation_adjustments, risk_adjustments, evidence_ids, coverage_delta |
| FinancialIntegrityEvidencePack | company_id, as_of, periods, verified_numbers, recalculated_metrics, rule_findings, time_series_anomalies, peer_anomalies, document_conflicts, governance_findings, benign_explanations, evidence_gaps, risk_level, hard_blocks, rule_versions, model_versions |
| ManualInvestigationTask | task_id, claim_id, importance, decision_impact, priority, expected_minutes, sources, search_terms, steps, required_materials, support_signal, refute_signal, stop_condition, fallback_evidence, status |
| CandidateRecord | candidate_id, company_id, sources, reasons, first_seen_at, last_review_at, state, evidence_strength, data_completeness, liquidity, tradability, exposures, lifecycle_version |
| CoverageScore | data_coverage, evidence_coverage, specialist_coverage, pit_coverage, conflict_penalty, overall, reasons |
| CounterCasePack | decision_scope, challenged_claims, alternative_explanations, downside_paths, missing_evidence, evidence_ids, cost |
| DecisionPack | decision_id, frozen_input_hashes, verdict, expected_return_range, downside_range, confidence, hard_blocks, needs_info, max_position, review_at, rationale, evidence_ids |
| TradeProtocol | protocol_id, decision_id, company_id, strategy_id, skill_versions, signal_time, earliest_executable_time, horizon, entry_rule, position_rule, stop_rules, invalidation_rule, take_profit_rule, review_events, max_holding_period, cost_model_version, fill_model_version, evidence_snapshot_id |
| PositionLifecycleSkillManifest | skill_id, version, stage(entry/holding/add/trim/exit/review), sources, required_evidence, rules, invalidation, failure_modes, output_schema, approval_status |
| AuthorSkillCoverage | author_id, source_snapshot_ids, selection_skill_coverage, entry_skill_coverage, holding_skill_coverage, add_skill_coverage, trim_skill_coverage, exit_skill_coverage, risk_skill_coverage, evidence_count_by_stage, coverage_status, missing_stages, review_status |
| PositionMonitoringPlan | position_id, company_id, decision_id, thesis_summary, entry_assumptions, holding_horizon, key_value_drivers, validation_metrics, monitoring_sources, monitoring_cadence, price_rules, fundamental_rules, event_rules, add_conditions, trim_conditions, exit_conditions, invalidation_conditions, manual_information_needs, last_review_at, next_review_at, skill_versions, evidence_snapshot_id |
| HoldingEvidenceUpdate | position_id, from_as_of, to_as_of, added_evidence_ids, changed_claim_ids, invalidated_evidence_ids, unresolved_conflicts, update_hash |
| HoldingReviewPack | position_id, as_of, new_market_data, new_disclosures, new_regulatory_events, new_industry_data, new_news_leads, manual_evidence_updates, thesis_strength_change, risk_change, triggered_rules, unresolved_conflicts, recommended_action, action_confidence, evidence_ids, next_review_conditions |
| PositionActionProposal | proposal_id, position_id, action(HOLD/ADD/TRIM/EXIT/REVIEW), qty_or_weight_limit, reasons, evidence_ids, hard_blocks, requires_user_confirmation |
| ExitReviewPack | position_id, original_thesis, subsequent_facts, correct_judgments, incorrect_judgments, pnl_attribution, beta_attribution, skill_incremental_value, rule_change_proposals, evidence_ids |
| AuthorCollectionCoverageReport | author_id, content_type, discovered_count, scheduled_count, success_count, failed_count, restricted_count, skipped_duplicate_count, updated_count, missing_count, last_page_or_cursor, terminal_condition, coverage_status, gaps |
| LedgerAccount | account_id, type, currency, normal_balance, status |
| LedgerEntry | entry_id, event_id, account_id, debit_fen, credit_fen, occurred_at |
| Order | order_id, client_request_id, symbol, side, order_type, qty, limit_price_fen, status, submitted_at, effective_rule_version |
| Fill | fill_id, order_id, qty, price_fen, commission_fen, tax_fen, transfer_fee_fen, occurred_at, replay_quality |
| Position | symbol, qty_total, qty_available, avg_cost_fen, realized_pnl_fen, unrealized_pnl_fen, as_of_event_seq |
| CorporateActionEvent | event_id, symbol, event_type, ex_date, record_date, pay_date, ratio, cash_fen, source_id, rule_version |
| PortfolioNAV | account_id, as_of, cash_fen, market_value_fen, receivable_fen, payable_fen, nav_fen, data_quality |
| ReplayCheckpoint | account_id, symbol, requested_resolution, actual_resolution, replay_quality, provider_id, coverage_start, coverage_end, missing_bars, fallback_reason, last_event_seq |
| SkillManifest | skill_id, version, sources, triggers, markets, industries, horizons, required_inputs, required_evidence, reasoning_steps, signals, invalidation, failure_modes, output_schema, cost_class, approval_status |
| RunManifest | run_id, mode(CODEX_INTERACTIVE/DETERMINISTIC/MANUAL_PACKET/OPENAI_COMPATIBLE), request_hash, as_of, node_plans, input_hashes, artifact_hashes, policy_version, provider_versions, status |
| ContextBudgetReport | selected_skills, selected_artifacts, artifact_byte_size, estimated_text_tokens, full_documents_to_open, evidence_excerpts_to_open, expected_browser_steps, expected_mcp_calls, expected_api_calls, duplicate_inputs_avoided |
| CostLedger | run_id, provider, model, prompt_version, input_tokens, cached_tokens, output_tokens, reasoning_tokens, estimated_cost, actual_cost, billing_source, cost_status |
| ModelRun | model_run_id, request_hash, provider_id, model_id, request_shape, timeout, attempts, response_hash, schema_valid, refusal, usage, latency |
| DataProviderCapability | provider_id, markets, symbols_or_types, frequencies, requested_range, actual_range, adjustment, amount_supported, timestamp_semantics, session_rules, volume_unit, rate_limit, auth_dependency, last_probe_at, quality_score, status |
| OpenSourceAuditManifest | repository_url, commit_sha, license_at_commit, license_file_hash, audited_files, audited_file_hashes, audit_date, reuse_decision, local_patch_hash, update_policy |

关键枚举：

- point_in_time_status：CERTIFIED、DOCUMENT_RECONSTRUCTED、APPROXIMATED、NOT_PIT_SAFE
- replay_quality：DUAL_SOURCE_5M_VERIFIED、SINGLE_SOURCE_5M、PROVIDER_1H_APPROX、DAILY_OPEN_MODEL、DAILY_CLOSE_MODEL、DAILY_CONSERVATIVE、UNREPLAYABLE
- decision verdict：REJECT、NEEDS_INFO、WATCH、PAPER_ELIGIBLE、PAPER_HOLD、PAPER_EXIT
- author coverage_status：COMPLETE、AUTHOR_SILENT、INSUFFICIENT_SOURCE、CONFLICTING_SOURCE、PARTIAL、ACCESS_RESTRICTED
- collection terminal_condition：PAGINATION_COMPLETE、CONFIRMED_EMPTY、FETCH_FAILED、ACCESS_RESTRICTED、CURSOR_UNKNOWN、PARTIAL

---

## 8. 数据源矩阵和质量规则

| 数据 | MVP 源 | 备用/手工 | PIT 默认 | 主要风险 | 处理 |
|---|---|---|---|---|---|
| 日线 OHLCVA | AKShare/东方财富 | AKShare/新浪 | NOT_PIT_SAFE（历史导入） | 接口漂移、代理断连、单位 | 重试、缓存、Schema 合约、交叉校验 |
| 5m | 东方财富 5m Provider | 新浪 5m Provider；未来 QMT/商业源 | NOT_PIT_SAFE（历史导入） | 免费历史短、接口漂移、双源口径 | 未复权原始价；候选/持仓增量积累；双源验证或明确单源等级 |
| 60m | 东方财富/可用 Provider | 本地 5m 聚合 | NOT_PIT_SAFE | 仅约 126 根 | 只作 5m 缺失时近似降级 |
| 1m | 非默认诊断 capability | 未来 QMT | NOT_PIT_SAFE | 仅 5 交易日、历史开盘为 0 | 不进入默认回放链；异常直接拒绝 |
| 交易日历 | AKShare/新浪 | 交易所公告 | APPROXIMATED 到核验后 | 未来日历可能调整 | 每次运行刷新，历史冻结 |
| 公司行为 | 交易所/巨潮 + Provider | 手工更正 | DOCUMENT_RECONSTRUCTED | 复权和更正 | 原始事件 + 版本化计算 |
| 公告/年报 | 巨潮、上交所、深交所、北交所 | 用户上传 | DOCUMENT_RECONSTRUCTED | 页面/链接变化 | 保存原文和哈希 |
| 监管处罚 | 证监会、交易所 | 手工导入 | 仅作为当时可得输入或未来标签 | 事后泄漏 | 按发布日期切断 |
| 财务结构化值 | 官方文档重建优先 | AKShare/Tushare 可选 | Provider 当前值默认 NOT_PIT_SAFE | 修订覆盖、单位 | 版本和来源并存 |
| 历史成分/退市 | 官方变更公告重建 | 用户合法数据 | 未核验前 NOT_PIT_SAFE | 幸存者偏差 | 未通过前关闭正式组合回测 |
| 社区内容 | 登录态结构化请求/Chrome 白名单 | MCP、手工 HTML/Markdown | 不属于事实层 | 版权、封禁、编辑、游标不完整 | 白名单历史全集 + 增量；只做线索/Skill 来源；覆盖缺口显式报告 |
| 可选 LLM | 用户未来兼容端点 | Manual Packet | 不适用 | 中断、Schema、成本 | 非主路径；能力探针、预算、禁用降级 |

所有来源均通过 `SourceAccessRouter` 选择运输层，固定优先级为：官方/已验证 API 或本地数据 -> 受控 MCP -> Browser/Chrome -> ManualInvestigationTask。每次选择必须落 `SourceAccessDecision`；上一层已满足请求时不得通过下一层重复取得相同内容。

数据质量硬规则：

- 时间戳统一 Asia/Shanghai，明确 bar 是起始时间还是结束时间。
- 规范成交量统一为股，成交额统一为元；原始单位保留。
- 校验 high 大于等于 max(open, close, low)，low 小于等于 min(open, close, high)。
- 未获取到不得解释为无成交。
- 停牌、午休、涨跌停、除权和上市/退市状态进入预期日历。
- Provider 冲突不自动平均；保存双方值并由来源优先级和质量规则选择。
- 任一批次失败不得覆盖上次 canonical。
- 5m 模拟成交只使用未复权原始价；研究复权序列必须由原始 5m 与版本化公司行为在本地派生。
- 每批 5m 记录 requested/actual 范围、bar_count、缺失时段、重复、OHLC 错误、单位、复权模式、时间戳语义、延迟和 Provider 状态。

---

## 9. 文档、证据和 PIT

文档流水线：

    search index
      -> download original
      -> SHA-256 dedupe
      -> MIME/page validation
      -> native text per page
      -> low-text-density detector
      -> OCR only flagged pages
      -> layout/section/table extraction
      -> deterministic numeric checks
      -> Evidence locator
      -> human review queue for critical conflicts

页码使用 PDF 物理页号和文档印刷页号双字段。任何引用必须能从 evidence_id 定位到对象哈希、页号和文本片段哈希。

PIT 规则：

1. Provider 当前值一律 CURRENT_PROVIDER_VALUE + NOT_PIT_SAFE，除非源能力审计证明相反。
2. 原始公告按公告发布时间和本系统可得时间进入 DOCUMENT_RECONSTRUCTED。
3. 更正报告创建新版本并通过 supersedes_source_id 关联，不能覆盖旧值。
4. 处罚和问询结果可作未来标签，不得进入更早时点输入。
5. 正式评测拒绝 NOT_PIT_SAFE；APPROXIMATED 单独报告。
6. 每份评测披露 PIT 覆盖率、退市股覆盖、历史成分覆盖和排除样本。

---

## 10. 财务可信度与红旗 Agent

### 10.1 分层执行

1. 数据完整度和单位检查；
2. 三表与恒等式确定性校验；
3. TTM/同比/环比/每股值重算；
4. 行业 Profile 选择；
5. 可解释规则和同行分位；
6. 稳健 Z-score/Isolation Forest/PyOD；
7. 命中升级条件后才做跨文档 LLM 审计；
8. 输出风险、正常解释、数据解释和补证要求，不输出“是否造假”。

### 10.2 规则注册表

每条规则包含 rule_id、formula_version、source_reference、applicable_industries、excluded_industries、required_fields、minimum_periods、threshold_source、calibration_status、false_positive_modes、severity、output_type、tests。

先实现：

- 资产负债表恒等式、现金变动和三表勾稽；
- CFO/净利润、应收/收入、存货/成本、预付款、其他应收；
- 非经常损益、研发资本化、商誉减值、资金占用/担保；
- Beneish、Piotroski、Altman、Sloan、DuPont；
- 同公司跨期、同行分位和稳健 Z-score。

银行、保险、证券、房地产、早期生物科技单独 Profile；传统 Beneish/Altman 不直接用于银行保险。

### 10.3 AuditAgent 方法

只借鉴论文 https://arxiv.org/abs/2510.00156 的科目风险先验、混合检索、单文档/跨年度/跨科目专家和证据链聚合。处罚案例只形成训练/评测标签和可版本化先验，不能泄漏到历史输入。

---

## 11. Knowledge/Skill 设计

### 11.1 正式采集边界

不做全知乎、全平台或非白名单用户采集。线上必须完成 MR Dang、黄彦臻、派大星皮皮三位正式白名单作者的“当前登录账号可访问历史内容全集”，再持续增量更新；寒武纪的鳄鱼使用用户确认完整的本地 DOCX，不再访问知乎。白名单配置唯一事实源为 `configs/knowledge_sources.yaml`：

| 作者 | 当前标识 | identity_status | 采集状态 |
|---|---|---|---|
| MR Dang | `mr-dang-77` | `CONFIRMED` | 本地 PDF 已全量解析；想法确认 0 条，回答/文章访问受限 |
| 黄彦臻 | `huang-wei-yan-30` | `CONFIRMED` | 主页 API 精确确认；想法 1 条已完整，回答/文章访问受限 |
| 派大星皮皮 | `xiao-peng-61-47` | `CONFIRMED` | 主页 API 精确确认；想法已保存 615 条后访问受限，回答/文章访问受限 |
| 寒武纪的鳄鱼 | 本地完整导出 | `LOCAL_EXPORT_USER_CONFIRMED_COMPLETE` | DOCX 2,032 块已全量解析；用户确认是完整语料，不做线上采集 |

黄彦臻、派大星皮皮的主页不是按显示名猜测：先由公开索引得到唯一候选，再由真实主页 API 同时校验显示名、url token 和平台用户 ID；对应人工任务已在 `configs/manual_investigation_tasks.yaml` 关闭。每人都有稳定 source_id。MR Dang、黄彦臻、派大星皮皮未经真实连续遍历和 CoverageReport 验证不得标记“知乎采集完成”；寒武纪的鳄鱼则生成“用户确认完整导出”的本地 CoverageReport，明确不是平台分页独立核验。

每位线上作者至少连续枚举：

- 回答全集：同时保存 question_title、question_id、question_url、answer_id、published_at、updated_at 和理解回答所需的问题上下文；
- 想法全集；
- 文章全集；
- 上述内容中识别作者参与所必需的完整评论分页和嵌套回复。

每种内容从第一页开始，按分页、游标或时间轴连续遍历到明确终止条件；不能以搜索结果、近期内容或高赞内容代替全集。空响应、请求失败和确认无内容具有不同语义，任何未取得项目都进入缺口清单，不得被计为 0。

### 11.2 访问、断点和覆盖

采集运输顺序统一为：可验证登录态结构化请求 -> 可用 MCP -> Chrome/Playwright 持久化 Profile -> 用户手工保存 HTML/Markdown。Cookie 不写入代码、日志或 Git；禁止验证码绕过、签名破解和高频风控对抗。

目录层保存 author_id、content_id、type、title、URL、published_at、updated_at、comment_count、content_hash、listing_page、listing_cursor 和 fetch_status；详情层保存正文、问题上下文、图片引用、原始快照、结构化版本和版本关系。

检查点至少细化到 author、content_type、listing_page、listing_cursor、content_id、comment_page、comment_cursor、nested_reply_cursor。每完成一页、一篇内容或一个评论分页立即提交；内容 ID 唯一，重复运行幂等，更新内容创建新版本，原始快照不可覆盖，采集日志与清洗日志分离。

遇到 403、429、安全验证、登录失效或结构异常时，保存当前游标和失败快照，状态置为 `PAUSED_MANUAL` / `ACCESS_RESTRICTED`，暂停相关作者而不是重试风暴。每位作者/类型输出 `AuthorCollectionCoverageReport`，包含 discovered、scheduled、success、failed、restricted、duplicate、updated、missing、last_cursor 和 terminal_condition。

### 11.3 评论图和作者参与路径

原始评论节点至少保存 comment_id、content_id、author_id、parent_id、reply_to_comment_id、root_comment_id、content、created_at、updated_at、page_cursor、fetch_status、source_snapshot_id。原始层保留成功取得的完整评论树，派生知识层只保留：

1. 根评论到目标作者评论之间的全部祖先；
2. 作者直接回复对象；
3. 作者回复后他人追问、且作者后来再次回复时的完整中间分支；
4. 理解作者回复所必需的上下文；
5. 作者在该线程中的全部后续回复。

与目标作者从未互动的其他分支不得进入清洗知识库、蒸馏集或 Codex 默认上下文。`CONFIRMED_EMPTY`、`PAGINATION_COMPLETE`、`FETCH_FAILED`、`ACCESS_RESTRICTED`、`CURSOR_UNKNOWN` 和 `PARTIAL` 必须分别记录。

### 11.4 两类 Skill 与作者覆盖

知识蒸馏必须分别产出：

- Candidate Selection Skills：候选发现、公司分析、行业变量、估值和证据门槛；
- Position Lifecycle Skills：首次建仓、持仓验证、加仓、减仓、退出、时间/价格/论点止损和复盘。

每位作者建立 `AuthorSkillCoverage`。原文没有明确持仓规则时，标记 `AUTHOR_SILENT`、`INSUFFICIENT_SOURCE` 或 `CONFLICTING_SOURCE`，使用“版本化通用持仓内核 + 已验证作者差分”，不得从选股文章凭空推测作者的建仓或退出倾向。

持仓监控先比较上次冻结时点与当前时点，只生成新增行情、公告、监管、财务、行业、新闻线索和人工证据的差异包；只有重大变化才升级完整复核。社区线索支撑关键结论前，必须回到公告、交易所、财报等更强来源验证。

### 11.5 蒸馏和批准

    raw immutable snapshot
      -> deterministic clean
      -> author participation projection
      -> human review
      -> viewpoint card / lifecycle rule
      -> SkillManifest / PositionLifecycleSkillManifest
      -> historical or forward eval
      -> approved or rejected

MR Dang《价值投资功法》是正式知识源，配置见 `configs/book_sources.yaml`，按本地私有研究材料处理：原 PDF 不提交 Git、不对外重新发布；原始字节进入 ObjectStore；BookSourceManifest 保存文件哈希、版本和权利状态；逐页结果保存页码、章节、解析版本、文本/OCR来源和证据定位。任何自动清洗都只生成可重建派生结果，永久保留原始对象。

用户批准的“寒武纪的鳄鱼”知乎文章 DOCX 本地导出同样作为正式私有原料：原件不提交 Git、不对外发布；正文按稳定段落块写入 ObjectStore，SQLite 只保存块序号、结构类型、哈希、解析版本和对象引用。DOCX 没有稳定物理页码，Evidence 必须使用 `BLOCK_TEXT` 定位。该本地导出不含 URL、发布时间、评论或分页游标，因此只证明“文件内文本已解析”，不证明“知乎线上历史已完整采集”。

该书专用处理链固定为：

    原始 PDF
      -> 文本层检测与按页 OCR
      -> 章节恢复
      -> 页码级切片
      -> 重复段落识别
      -> 废话和方法论内容分类
      -> 自动清洗候选
      -> 人工审核
      -> 观点卡片
      -> 方法规则
      -> Candidate Selection Skill
      -> Position Lifecycle Skill
      -> 历史案例或前向评测
      -> 批准或拒绝

自动分类重点降权重复口号、营销宣传、与方法无关的故事、无新增信息的复述、无证据情绪判断和过时即时价格结论；重点保留选股、商业模式、行业、估值、财务质量、建仓、持仓验证、加减仓、退出、风险、失败案例、反证/失效和复盘方法。

正式工件包括 BookSourceManifest、BookParseReport、BookCleaningReport、BookMethodCoverageReport、BookViewpointCard、BookSkillCandidate、HumanReviewDecision。BookCleaningReport 至少报告原始页/字符、成功解析页、OCR页、重复段落、建议删除或降权段落、方法论段落、案例段落、未分类段落和人工审核状态；BookMethodCoverageReport 分别统计选股、建仓、持仓、加仓、减仓、退出、风险和复盘。无明确方法时只能标记 `AUTHOR_SILENT` 或 `INSUFFICIENT_SOURCE`。

Phase 2 已实现通用私有 PDF/DOCX 摄入、逐页/逐块解析与证据定位，并完成两份真实原料的全量解析；具体清洗、人工审核、观点卡片和 Skill 蒸馏仍属于 Phase 5。生产 Skill 复现可验证方法，不模仿作者人格或文风；所有规则必须指向 source_snapshot_id/evidence_id，并单独版本化通用内核与作者差分。

---

## 12. 路由、Codex 运行和上下文成本

### 12.1 四种模式

- `CODEX_INTERACTIVE_MODE`（默认）：用户用自然语言发起任务；Codex 按需加载 Repo Skill、调用 `astock` CLI、读取冻结工件、生成结构化草稿并走校验写回。
- `DETERMINISTIC_MODE`：无需 Codex 或外部模型，独立执行定时同步、质量、指标、扫描、硬风险、账本和恢复。
- `MANUAL_PACKET_MODE`：只作为离线交换、审计和兼容备用；导出/导入均校验 Schema 与输入输出哈希。
- `OPENAI_COMPATIBLE_MODE`：未来可选 Provider；默认 capability 禁用，不是 MVP 启动或正式投研条件。

### 12.2 自然语言与 Skill 路由

根 `AGENTS.md` 只维护稳定边界和常见任务路由；Repo Skill 通过准确的 name/description 支持隐式自然语言触发和 `$skill-name` 显式触发。规则优先识别任务类型、持仓状态、行业、证据缺口和风险，再选择最少必要 Skill；共性 BaseCase 只计算一次，默认最多加载 1～3 个专家差分。

尚未实现的高级能力必须显式报告 capability 不可用。用户请求“最近可以买什么”时，如果研究链未达标，只能返回当前证据支持的观察名单或开发状态，不得伪造完整推荐。

### 12.3 工件优先和增量上下文

同一标的再次运行时，先计算“上次冻结分析时点 -> 当前时点”的新增公告、新闻、行情、财务、监管和人工证据，只生成 IncrementalEvidencePack、IncrementalBaseCaseUpdate、IncrementalSpecialistDelta 或 HoldingReviewPack。默认不重新读取整份年报、全部知乎原文、全量 K 线或旧聊天。

委员会只读已冻结工件，默认没有网络、MCP 或浏览器 capability；缺关键证据只能返回 NEEDS_INFO 并创建调查任务。

### 12.4 ContextBudgetReport

每次 Codex 研究运行前生成上下文预算，至少列出：selected_skills、selected_artifacts、artifact_byte_size、estimated_text_tokens、full_documents_to_open、evidence_excerpts_to_open、expected_browser_steps、expected_mcp_calls、expected_api_calls、duplicate_inputs_avoided。

预算不是官方账单 Token 的精确复刻，而是控制输入规模和重复读取。缓存键至少包括 skill_version、schema_version、evidence_pack_hash、base_case_hash、question_hash；可选 LLM 模式再加入 provider/model/prompt_version。

### 12.5 可选兼容端点探针

以后用户主动启用时，必须验证 TLS/鉴权、最小文本、JSON object、严格 JSON Schema、长上下文、超时取消、429/5xx/Retry-After、usage、声明的流式能力和成本口径；任一失败只降低可选 capability，不影响 Codex Interactive 或 Deterministic Mode。

---

## 13. 委员会、风险和冻结工件

委员会输入只包括已经完成 Schema、引用和 Policy 校验并冻结的 EvidencePack、BaseCasePack、SpecialistDelta、FinancialIntegrityEvidencePack、人工补证、CounterCasePack、PositionMonitoringPlan、HoldingReviewPack、组合和市场状态。Codex 可以在冻结前辅助生成增量分析，但委员会本身不得联网、调用 MCP/浏览器、重抓公告或读取整份原文。

决策顺序：

    hard blocks
      -> data/evidence coverage
      -> deterministic decision matrix
      -> optional Codex one-shot narrative on frozen inputs
      -> risk engine

硬阻断包括三表严重不平、非标审计重大风险、核心来源冲突、数据质量失败、不可交易、低流动性、社区传闻为唯一证据、专业覆盖不足但计划仓位高。任何 Codex 或可选 LLM 叙述都不能覆盖硬阻断；缺证据时结果必须是 NEEDS_INFO，而不是降低措辞后继续交易。

程序化风险参数均带 effective_from 和 version：

- 禁止杠杆；
- 单股、行业、总仓位上限；
- 流动性和成交量上限；
- 组合相关性与回撤；
- 财务红旗和覆盖折扣；
- 重大公告冻结；
- 连续亏损降级；
- 数据异常冻结；
- 手动紧急停止；
- 永久禁止未经用户确认的实盘订单。

---

## 14. 模拟交易和恢复

### 14.1 账本

SQLite 追加 journal + 双重记账 ledger_entry + 可重建 position_snapshot。每个订单、成交、费用、公司行为和现金变化都有 event_id；账本不平立即停止。

### 14.2 A 股规则

按生效日期配置：

- T+1 和可用数量；
- 100 股整数；
- 交易时段和委托有效期；
- 主板、创业板、科创板、北交所和 ST 涨跌幅；
- 停牌、一字板、部分成交和成交量约束；
- 佣金最低收费、印花税、过户费；
- 分红、送转、配股、除权、退市。

### 14.3 回放选择

    EastMoney + Sina verified raw 5m -> DUAL_SOURCE_5M_VERIFIED
    else one provider quality-passed raw 5m -> SINGLE_SOURCE_5M
    else provider 60m -> PROVIDER_1H_APPROX
    else explicit daily open/close/conservative model
    else -> UNREPLAYABLE

1m 不进入默认链。成交和恢复只使用未复权原始 5m；分红、送转、配股、拆并股等由独立 `CorporateActionLedger` 处理。研究复权序列从原始 5m + 版本化公司行为派生，不得把复权历史价当作真实成交价。单源失败不得覆盖已经验证的旧 canonical，也不得静默插值伪造缺失 5m。

只有同一根 5m 同时触及止盈和止损时，才通过 `SourceAccessRouter` 尝试获取该局部窗口的合格 1m 诊断数据；它必须带真实来源、覆盖和质量等级，不能把已知开盘为 0 或覆盖不足的免费 1m 当成成交事实。若 1m 不可得、受限或质量失败，主结果采用对账户更不利的 5m 路径，另存乐观结果供敏感性分析，并标记 `ambiguous_intrabar_path=true` 和诊断失败原因。

### 14.4 启动恢复

1. 校验账本、事件序号和快照哈希；
2. 读取开放订单、持仓、协议和最后市场游标；
3. 计算缺失交易时段；
4. 重新探测 Provider；
5. 分区间选择可用分辨率；
6. 拉取、验证、回放；
7. 每批提交幂等检查点；
8. 处理公司行为和触发条件；
9. 输出恢复报告和降级比例。

### 14.5 持仓生命周期监控

每个开放模拟仓位和用户声明的真实监控仓位都必须有 `PositionMonitoringPlan`。触发源包括启动恢复、每日例行、价格条件、公告、财务披露、监管/风险异常和用户主动复核。系统先生成差异包和 HoldingReviewPack；只有重大变化才重做完整深度研究。

### 14.6 实盘边界

真实持仓能力仅包括只读监控、条件触发、建议输出，以及用户手工录入或导入最终成交。所有真实下单必须由用户在券商端人工确认；项目不得提供自动向券商发单的接口。Repo Skill 也不得绕过这一边界。

---

## 15. 固定版本开源审计与复用决定

审计日期：2026-07-13。

### 15.1 muxuuu/serenity-skill

- commit：c2fe93deedfd0d1bd9fe7ef0601ea1b9c20ea24a
- 许可证：MIT
- LICENSE SHA-256：F1980506005C63EE51B5B9E58FD963F13EA072BADF1273DB98C40F498E468D52
- Windows/Python 3.12：validate_skill 在目录名符合 serenity-skill 时通过；scorecard 可运行。

复用：

- SKILL.md、references/evidence-ladder.md、references/market-source-playbook.md：改造为 IndustryBottleneckSkill。
- scripts/validate_skill.py：可直接复用为开发校验工具，保留许可证。
- scripts/serenity_scorecard.py 和 assets/bottleneck-scorecard.json：只作参考/评测基线，固定权重不进入生产裁决。

### 15.2 haskaomni/serenity-skill

- commit：332037ea5f41ce7f150afbedb3517bcd1f1b2833
- 许可证：MIT
- LICENSE SHA-256：387581AE2CFD9734FAC334D60593873AB957C02340D7A07096449F5274E81DF6
- 5 个 Skill 的 frontmatter、reference 和 agent 文件结构均通过校验；无运行时代码。

复用：

- serenity-alpha -> EventToAlphaSkill；
- bayesian-intrinsic-growth-valuation -> GrowthProbabilitySkill；
- gf-dma-health-index -> DailyTrendHealthSkill 的参考；不得机械迁移到小时线；
- tam-adj-peg -> GrowthValuationLens；
- buy-side-equity-research-memo -> ResearchMemoComposer。

所有 SEC/美股假设改为 A 股公告路径；一致预期缺失时必须降级。

### 15.3 leafpaper/claude-company-analysis

- commit：d908885640cc7d4ff06f1ee48b1beb2ab13012be
- 许可证：MIT
- LICENSE SHA-256：8667398A7845260078C72C1C67D797CDB8014DC865D6A9F5EB6C9BE9E04B68C3
- Python 3.12：54 测试通过、2 失败、7 跳过；失败来自 test_daily_fallback 在 mock 前硬要求 TUSHARE_TOKEN。

改造复用：

- scripts/financial_audit.py：拆出公式和测试，接内部 FinancialFact Schema；重做行业适用性。
- scripts/pdf_reader.py：只借章节正则和页码思路；底层改为 PyMuPDF + pdfplumber + OCR。
- scripts/derived_metrics.py：拆出确定性计算，补单位、版本和属性测试。
- scripts/data_snapshot.py：借鉴修订版本去重测试，改成 PIT 元数据。

不复用 TushareCollector、多写手/Reviewer 长报告和总评分。

### 15.4 FinRobot

- commit：297a8d28d099be328c8a8eb658b4f782b93f3651
- 仓库 LICENSE：Apache-2.0
- LICENSE SHA-256：BBCB4226500B503737713F0C9A9DA17EFCE56FA6989B9F0538071DCB60826AF5
- 风险：setup.py 标注 MIT 且 python_requires 小于 3.12，与仓库许可证和目标环境冲突；干跑依赖会大规模降级核心包。

只借鉴 finrobot/agents/workflow.py、agent_library.py、toolkits.py 的职责分离与追踪思想；不复制、不安装、不 fork。

### 15.5 PyOD、PyGOD、AuditAgent、VeighNa

- PyOD 3.6.1：MVP 条件复用；已在 Python 3.12 实测 ECOD。锁定 Numba 0.66.x。
- PyGOD 1.1.0：仅保留研究依赖位；实体图质量、标签和 PyTorch/PyG 环境未达门槛前禁用。
- AuditAgent：只借论文方法论，无安装依赖。
- VeighNa PaperAccount：只借鉴 Order/Trade/Position 语义和事件顺序；不使用其持久化、资金和撮合实现。

上游升级策略：任何升级先生成新 OpenSourceAuditManifest、文件差异、许可证差异和回归结果；禁止自动跟随主分支。

---

## 16. 分阶段开发路线

### Phase 0：技术验证（已完成主体）

- 已完成：Python 3.12、行情、PDF/OCR、固定 commit、性能、知乎、账本。
- 未配置但不阻塞：QMT、可选兼容 LLM 端点。
- 已在 M1 补测：新浪 5m 的主板、创业板、科创板、北交所和指数能力；北交所双源偏差超阈值，明确保持单源等级。
- 产物：本计划中的闸门和降级决定。
- 验收：所有未测试项都已转化为禁用 capability，而不是理想假设。

### Phase 1：M1 Codex 原生入口、数据/状态/账本基础和双源 5m

- 实施状态：已完成；以 `docs/验收报告.md（合并来源：M1验收报告.md）` 和里程碑提交 `3db43ac` 为验收依据。
- 工期参考：10～15 个开发日。
- 输入：Phase 0 数据合约和性能基线。
- M1.1 工程入口：Python 3.12/uv、ruff/pyright/pytest、Windows 编码/路径/原子写、AGENTS.md、至少六个可触发 Repo Skill。
- M1.2 合约：公共 Schema、错误分类、RunManifest、ContextBudgetReport、SourceAccessRouter 和 capability stub。
- M1.3 存储状态：SHA-256 ObjectStore、SQLite migration、Job/Attempt/Cursor/Checkpoint/Lock/Idempotency/Artifact/ProviderHealth/SourceSnapshot 接口。
- M1.4 行情：EastMoney5mProvider、Sina5mProvider、原始快照、observation Parquet、单位/覆盖/时间语义、增量游标、双源质检和 canonical 保护。
- M1.5 账本：append-only journal、双重记账、订单/成交/持仓/NAV、公司行为接口、5m 限价撮合、replay cursor 和崩溃恢复。
- M1.6 稳定 CLI：init、probe、sync-market、sync-5m、quality-report、paper-status、paper-replay、context-plan、codex-run-init、codex-run-import。
- 产物：无需外部 LLM API、可由 Codex 或命令行运行、可测试且可恢复的首个闭环。
- 验收：不同板块 5m live smoke 与录制夹具；单位统一；重复增量幂等；单源失败不覆盖 canonical；账本逐事件平衡；崩溃无半笔；Repo Skill 能调用 CLI 并生成/导入已校验工件。
- 回滚：Schema 迁移和 canonical manifest 可回退；原始对象不删除。
- 不做：公告语义、专家、UI 美化。

### Phase 2：证据库和官方文档

- 实施状态：已完成；分支 `feature/phase2-evidence`，以 `docs/验收报告.md（合并来源：Phase2验收报告.md）`、`docs/验收报告.md（合并来源：Phase2完成性审计.md）` 和 `docs/验收报告.md（合并来源：私有原料摄入验收报告.md）` 为验收依据。
- 工期参考：8～12 个开发日。
- 任务顺序：①官方公告和财报检索、下载与快照；②通用 PDF/OCR 和页码级证据定位；③Claim—Evidence 关系；④PIT 元数据；⑤私有 PDF/DOCX 摄入、页/块定位与全量解析；⑥代表性公告、原生 PDF、扫描 PDF 和两份真实私有原料实测。
- 私有入口与官方文档共用 ObjectStore、SourceSnapshot、SourceDocument、DocumentPage、Evidence、解析版本和 PIT 规则；权限与导出策略不同，原始私有内容不得出现在 Git、日志、测试夹具或验收报告正文中。
- 验收：标注集 30 份文档；原生页文本召回大于等于 98%；扫描页标题/关键字段召回大于等于 95%；100% 引用可回到对象、文件哈希和页码；同一输入/解析版本幂等，换解析版本保留旧结果；私有源原文零 Git 泄漏。
- 回滚：解析版本并存；可由原始对象重建。
- 不做：自动“是否造假”结论。

### Phase 3：财务可信度 MVP

- 实施状态：已完成；分支 `feature/phase3-financial-integrity`。M3.1、M3.2、M3.3 分别以 `docs/验收报告.md（合并来源：Phase3-M3.1验收报告.md）`、`docs/验收报告.md（合并来源：Phase3-M3.2验收报告.md）`、`docs/验收报告.md（合并来源：Phase3-M3.3验收报告.md）` 为验收依据。
- 工期参考：10～15 个开发日。
- M3.1 确定性核心：财务规范 Schema、Decimal/单位归一化、三表与现金勾稽、基础比率复算、版本化规则注册表、行业 Profile、证据/PIT 门禁、FinancialIntegrityEvidencePack、可恢复运行、稳定 CLI 和人工补证任务。
- M3.2 规则扩展：TTM/同比/环比/每股值、同公司跨期、同行样本口径与分位、Beneish/Piotroski/Altman/Sloan/DuPont 完整字段门禁和行业校准。
- M3.3 异常模型：稳健 Z-score、Isolation Forest、PyOD、模型版本与可重复训练/推理、误报解释、完整 Phase 3 验收。
- 验收：公式 golden case；行业排除；缺字段不编造；异常输出均有证据或明确数据缺口；未来处罚不泄漏。
- 回滚：规则版本可切换；旧工件保留。
- 不做：PyGOD。

### Phase 4：Serenity 和通用投资内核

- 实施状态：P4.1 冻结证据与一次性 BaseCase、P4.2 版本化 Skill/最多三专家路由/增量 Delta、P4.3 七个确定性诊断/组合接口、P4.4 版本化持仓计划/连续增量复核/人工确认 Proposal、P4.5 冻结输入 Codex/Repo Skill/恢复/全链终验均已实现，Phase 4 完成。可执行顺序、工件合同和验收门见 `docs/开发计划.md（合并来源：Phase4开发计划.md）`，验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.1验收报告.md）` 至 `docs/验收报告.md（合并来源：Phase4-P4.5验收报告.md）`。
- 工期参考：12～18 个开发日。
- 任务：BaseCase、产业瓶颈、Event-to-Alpha、成长概率、估值 lens、日线趋势、独立 HourlySwingSkill、专家差分、研究 memo，以及建仓/验证/加仓/减仓/退出/复盘的版本化通用持仓内核。
- 验收：共性分析只执行一次；专家只输出 Delta；最多 3 个专家；缺一致预期能降级；每条关键判断绑定 evidence_id；PositionMonitoringPlan 和 HoldingReviewPack 可由增量证据驱动。
- 回滚：Skill 版本和批准状态回退。

### Phase 5：知识蒸馏

- 工期参考：首位作者 12～20 个开发日，不含人工审核和平台限速等待；后续作者按内容量估算。
- 任务：先把两份本地原料与未来线上快照按来源 ID、内容哈希和版本关系去重；为 MR Dang、黄彦臻、派大星皮皮实现 Python 优先采集器，连续枚举回答（含问题标题/ID/URL/必要上下文）、想法、文章、识别作者参与所需的完整评论分页与嵌套回复，派生作者参与链，支持多级断点、增量更新、缺口恢复和 AuthorCollectionCoverageReport。寒武纪的鳄鱼不再线上采集，直接以用户确认完整且已解析的 DOCX 进入结构分析和蒸馏；黄彦臻、派大星皮皮身份无法唯一确认时只暂停对应来源，不得猜测。
- 私有原料任务：对《价值投资功法》和获准 DOCX 完成可重建的重复/噪声分类、自动清洗候选、人工审核、观点卡片、方法规则、Candidate Selection Skill、Position Lifecycle Skill、历史案例或前向评测与批准/拒绝；每条结果回链 PDF 页或 DOCX 块及片段哈希。
- 验收：三位线上作者分别按内容类型生成 AuthorCollectionCoverageReport；寒武纪的鳄鱼生成 `USER_CONFIRMED_COMPLETE_EXPORT` 本地覆盖报告。线上报告的 discovered/success/failed/restricted/missing 和 terminal_condition 可核对；中断后从精细游标恢复；更新内容产生新版本；验证码自动暂停；任何抓取失败不被计作“内容为 0”。私有原料生成清洗、方法覆盖、观点和候选规则工件；作者未表达的方法明确标记而不推测；Skill 未经人工审核和评测不得批准。
- 回滚：撤回 Skill 不删除来源快照。

### Phase 6：委员会和选择性反方

- 工期参考：10～15 个开发日。
- 实施状态：已完成。冻结合同、确定性硬门槛、组合风险冻结、选择性反方、调查闭环、DecisionPack、TradeProtocol、预算降级、严格 Codex 包装、CLI、恢复和审计均已落地；验收依据见 `docs/验收报告.md（合并来源：Phase6验收报告.md）`。
- 任务：冻结工件、Codex 草稿校验、NEEDS_INFO 闭环、硬门槛、反方触发、DecisionPack、TradeProtocol、上下文预算和可选 Provider 成本上限。
- 验收：委员会无网络能力；相同冻结输入产生相同决定哈希；硬阻断不能被 LLM 覆盖；预算超限自动降级。

### Phase 7：冻结权重影子评测

- 开发工期：5～8 日；观察期至少 6～12 个月。
- 账户：规则基准、BaseCase-only、BaseCase+单专家、完整委员会、沪深300/中证全指、等权候选。
- 最小研究门槛：每个拟调权 Skill 至少 100 个相互独立决策；至少 3 种市场状态、每种至少 30 个；不足时只报告“样本不足”。
- 验收：walk-forward、样本外、置信区间、扣费收益、最大回撤、路径不确定率和 PIT 覆盖率齐全。

### Phase 8：可选自适应权重研究

- 进入条件：Phase 7 门槛全部满足，且专家增量在样本外优于固定权重。
- 顺序：规则状态机 -> 离线动态权重 -> 上下文多臂赌博机影子 -> 最后才研究 RL。
- RL 不直接生成订单，只调整受硬门槛约束的权重/风险预算。
- 任一阶段未优于固定权重即回滚。

---

## 17. 测试与验收矩阵

### 17.1 自动测试

- 每个里程碑必须通过 `pytest`、`ruff check .` 和 `pyright`；失败不得只记录后继续宣称完成。
- Schema：必填字段、枚举、时区、金额/数量精度、序列化稳定性和前后兼容。
- ObjectStore：SHA-256、并发幂等、Windows 原子写、中文路径、坏对象检测。
- SQLite：migration 校验和/回滚、Job/Cursor/Checkpoint/Lock、重复幂等键和崩溃恢复。
- 账本属性：任意合法事件序列逐事件借贷平衡；重复事件拒绝或返回同一结果；进程中断无半笔账。
- Provider 合约：字段、单位、时间戳语义、请求/实际覆盖、错误分类和 capability probe。
- 5m 交叉验证：东方财富/新浪关键字段、单位换算、缺失时段、双源等级、单源失败 canonical 保护和重复增量同步。
- Replay：5m checkpoint、公司行为接口、60m/日线降级、同 K 线歧义和 UNREPLAYABLE。
- Repo Skill：frontmatter、name/description 触发样例、AGENTS/Skill 路径发现、CLI 调用和禁止直接写账本。
- 集成：行情到 ObjectStore/Parquet/QualityReport，Codex draft 到 validated artifact，订单到 NAV。
- 故障注入：断网、429、5xx、超时、磁盘满、进程中断、Cookie 失效、坏 PDF。
- 安全：密钥/Cookie/私有 PDF 不进 Git/日志，路径穿越和恶意文档限制。
- 性能：660 万行基准不得比 Phase 0 慢超过 2 倍，除非有记录原因。

所有外部接口同时维护低频 `live smoke test` 和不联网的 `recorded fixture test`；日常测试不得完全依赖外网。

### 17.2 M1 关键验收

1. 从项目根目录启动 Codex 可读取 `AGENTS.md`、发现 Repo Skills、按自然语言或 `$skill-name` 命中 Skill，并由 Skill 调用稳定 CLI；不要求任何外部 LLM API Key。
2. Codex 结果写到运行草稿，Schema/引用/Policy 校验通过后才进入 ArtifactStore；直接写 SQLite 的路径不存在。
3. 主板、创业板、科创板、北交所（Provider 支持时）和指数样本完成东方财富与新浪 5m 的 live smoke 或明确 capability 失败；报告实际覆盖、时间戳、OHLC、量单位、缺失时段和双源差异。
4. 相同输入和版本重复运行，确定性工件 content_hash 一致；重复增量同步不产生重复 observation。
5. 单个 Provider 失败不覆盖旧 canonical；双源未达一致时只能降级 `SINGLE_SOURCE_5M` 或更低，不能伪装成双源验证。
6. 账本全局与逐事件平衡；订单、成交、持仓可在关闭重启后恢复；崩溃后无半事务；5m replay cursor 准确。
7. ContextBudgetReport 列出预计读取的 Skill/工件/字节/文档/证据片段及浏览器、MCP、API 步数；委员会接口默认无联网 capability。
8. Job、Cursor、Checkpoint、SourceSnapshot、ObjectStore、失败状态和 AuthorCollectionCoverageReport Schema 可被后续白名单采集直接复用。
9. 1m 开盘为 0 被质量规则拦截，且 1m 不进入默认回放链；未复权原始 5m 与公司行为接口职责分离。
10. Windows 中文文件名、路径和原子写测试通过。

### 17.3 后续阶段全局验收

1. 关键 Claim 引用覆盖率 100%；一般 Claim 达到配置阈值。
2. NOT_PIT_SAFE 数据无法进入正式历史评测。
3. 同 K 线歧义主报告采用保守结果。
4. 可选 LLM Schema 失败不污染工件；未配置时不影响 Codex 正式运行。
5. 知乎出现验证/限速立即暂停，不反复请求；任何缺口都反映在 CoverageReport。

---

## 18. 风险和缓解

| 风险 | 缓解 |
|---|---|
| 免费接口不稳定 | capability probe、指数退避、原始缓存、备用源、失败不覆盖 |
| 5m 历史短或单源不稳 | 双源接口、有限标的持续积累、单源等级、60m/日线保守回放、禁止插值 |
| 1m 开盘为 0 | 退出默认链；质量硬门槛，不填理想值 |
| 复权/公司行为错误 | 未复权事实 + 公司行为事件 + 可重建复权 + golden case |
| BaoStock 被封 | 默认禁用，不构成关键路径 |
| PIT 缺失和幸存者偏差 | 前向影子优先；正式回测强制 PIT/历史成分/退市覆盖 |
| PDF 表格/OCR 错误 | 原生优先、按页 OCR、数值勾稽、人工复核关键表 |
| 财务规则误报 | 行业 Profile、同行比较、正常解释、阈值版本 |
| Codex 结论越权或上下文重复 | Repo Skill 单一职责、冻结工件、Schema/引用/Policy 校验、ContextBudgetReport、禁止直写账本 |
| 可选 LLM 幻觉/中断/成本未知 | 非主路径；Provider 探针、Schema、COST_UNKNOWN、硬预算、禁用降级 |
| 白名单历史枚举不完整 | 连续分页终止条件、精细 checkpoint、CoverageReport、失败不计 0、手工补缺 |
| 社区版权和封禁 | 用户白名单、低频、个人本地研究、原文不进 Git/不再发布、验证时暂停 |
| 作者未讲持仓规则 | AuthorSkillCoverage 明示静默/不足/冲突，通用持仓内核与作者差分分离 |
| 过拟合 | 固定权重、样本外、影子账户、多个市场状态 |
| 模拟成交过于乐观 | 分辨率质量、保守路径、成交量和费用、UNREPLAYABLE |
| Windows 兼容 | CI 以 Windows+Python 3.12 为主，路径/编码测试 |
| 复杂度失控 | 模块化单体、MVP 禁止项、每阶段独立验收 |
| 上游许可证变化 | 固定 commit + AuditManifest + 升级重新审计 |

---

## 19. 首批 18 个可直接执行的开发任务

1. [x] 初始化 Git 基线和 `feature/phase1-foundation` 分支；添加不提交 runtime、密钥、Cookie、私有 PDF、缓存和虚拟环境的 `.gitignore`。
2. [x] 初始化 uv/Python 3.12 工程、`pyproject.toml`、`uv.lock`、src layout、ruff、pyright、pytest 和 Windows 路径/UTF-8 约定。
3. [x] 创建精简根 `AGENTS.md`，写明事实源、证据、无未来函数、SourceAccess 优先级、禁止自动实盘、CLI 入口和写回边界。
4. [x] 创建 astock-research-orchestrator、candidate-scan、holding-monitor、paper-trading-recovery、financial-integrity-audit、knowledge-ingest 六个可用 Repo Skill 骨架及触发测试；随后补 company-deep-research、evidence-investigation。
5. [x] 实现错误分类、UTC/Asia-Shanghai 时钟、稳定 JSON 哈希、Windows 原子写和基础结构化日志。
6. [x] 实现 M1 公共 Pydantic Schema、Schema 版本机制和稳定序列化测试。
7. [x] 实现 SHA-256 内容寻址 ObjectStore、SourceSnapshot 元数据和并发/中文路径/幂等测试。
8. [x] 实现 SQLite migration runner 与 job/attempt/cursor/checkpoint/lock/idempotency/artifact/provider_health/collection 状态。
9. [x] 实现 SourceAccessRouter：API -> MCP -> Browser -> Manual；MCP/Browser 先为 capability stub，选择日志统一落库。
10. [x] 实现 MarketDataProvider Protocol、probe、EastMoney5mProvider 和原始响应快照。
11. [x] 实现 Sina5mProvider，实测主板/创业板/科创板/北交所/指数能力并记录不支持项，不编造覆盖。
12. [x] 实现 observation Parquet writer、单位换算、交易时段/OHLC/重复/覆盖质量规则和 DataQualityReport。
13. [x] 实现双源 5m 交叉验证、replay_quality 分级、增量游标和“单源失败不覆盖 canonical”。
14. [x] 实现 append-only journal、双重记账、幂等事件和崩溃恢复属性测试。
15. [x] 实现订单、成交、现金/冻结资金/费用、持仓、NAV、公司行为接口和 5m replay checkpoint 最小闭环。
16. [x] 实现稳定 CLI：init、probe、sync-market、sync-5m、quality-report、paper-status、paper-replay、context-plan、codex-run-init、codex-run-import。
17. [x] 固化 Provider recorded fixtures；运行低频 live smoke、断网/错误响应/重复增量测试；执行 pytest、ruff、pyright 并修复全部失败。
18. [x] 生成 M1 验收与剩余项报告，形成清晰里程碑提交；无阻断时直接进入 Phase 1 下一子里程碑，不重新请求整计划确认。

### Phase 2 当前六个子里程碑

1. [x] 官方公告和财报检索、下载、原始响应/ObjectStore 快照及录制夹具。
2. [x] 通用 PDF 文本层检测、逐页解析、按页 OCR、章节恢复和页码级定位。
3. [x] SourceDocument、DocumentPage、Evidence、Claim 和 ClaimEvidenceLink 公共 Schema 与持久化。
4. [x] 发布/生效/抓取/系统可得时间和修订关系等 PIT 元数据及禁止未来函数校验。
5. [x] 本地私有 PDF/DOCX 摄入接口；《价值投资功法》完成 249 页全量解析，“寒武纪的鳄鱼”DOCX 完成 2,032 块全量解析；两者均可生成统一 Evidence，但尚未进行内容蒸馏。
6. [x] 代表性公告、原生 PDF、扫描 PDF、两份真实私有原料的 recorded + live/本地实测，逐子里程碑运行 pytest、ruff、pyright 并形成真实验收报告。

### Phase 3 当前三个子里程碑

1. [x] M3.1：确定性财务核验、行业适用性、证据/PIT 门禁、可恢复运行、FinancialIntegrityEvidencePack 与稳定 CLI；以 `docs/验收报告.md（合并来源：Phase3-M3.1验收报告.md）` 为验收依据。
2. [x] M3.2：跨期复算、同行口径和分位、完整财务评分公式及版本化阈值校准；以 `docs/验收报告.md（合并来源：Phase3-M3.2验收报告.md）` 为依据。
3. [x] M3.3：稳健异常模型/PyOD、误报解释、冻结评测集与 Phase 3 完整验收报告；以 `docs/验收报告.md（合并来源：Phase3-M3.3验收报告.md）` 为依据。

---

## 20. 用户手动事项和注册事项

这些事项都不阻塞当前已批准的本地开发；需要人工介入时统一在里程碑末报告：

1. **Python 管理员收尾（已完成）**：已运行 `scripts/cleanup-python311-and-launcher.ps1`。复核结果为 Python 3.11/Launcher 卸载登记 0 条、`C:\Windows\py.exe` 不存在、常见 3.11 目录不存在、用户 PATH 无 3.11/Launcher，`D:\Environment\Python312\python.exe` 仍为 Python 3.12.10。
2. **OpenAI-compatible 端点（未来可选）**：日常 Codex 正式投研不需要配置。只有用户以后主动启用自动 Provider 时，才在本机秘密存储配置 base_url、model_id 和 API key 引用；不要把 key 发到聊天、Git 或配置样例。
3. **QMT（可选）**：如果以后希望提高分钟回放质量，需要安装并登录券商 MiniQMT，确认数据目录和账户权限，再运行 QMT probe。当前不需要注册或购买。
4. **BaoStock（可选）**：当前出口被 BaoStock 标记黑名单。可以以后更换正常网络或联系其管理员；不建议为此暂停项目。
5. **知乎**：当前 Chrome 已登录，本轮本地解析无需操作。以后正式采集若出现验证码、安全验证、登录失效或页面授权提示，系统保存检查点并暂停，由用户手动恢复；不会自动绕过。
6. **内容权利白名单**：用户已明确批准当前 PDF 和 DOCX 作为私有蒸馏原料；两份原件均已完成摄入且不提交 Git、不再发布原文。寒武纪的鳄鱼 DOCX 已由用户确认为完整语料，不再查找主页；黄彦臻、派大星皮皮如果自动发现结果存在同名歧义，再由用户确认，不凭显示名猜测。
7. **模拟佣金**：印花税和过户费已有生效日期与官方来源；券商佣金因账户协议不同，正式影子账户前由用户把 `configs/fee_rules.yaml` 改成自己的实际费率。
8. **实盘动作**：如果以后导入真实持仓，用户仍需在券商端人工确认和执行一切真实订单；系统只监控、提示并接受手工成交回录。

所有需要注册、登录、验证码、购买数据或人工授权的事项，在不影响其他开发时统一累积到里程碑报告，不零散打断用户。

---

## 21. 开发启动条件

Phase 0、Phase 1 / M1、Phase 2、Phase 3 和本地私有原料全量解析已完成，不再重复开发。用户要求连续完成 Phase 4～8；每一步先落计划，再以代码、测试和验收工件证明完成，不能用计划文字代替实现。

确认后不等待 QMT、BaoStock、可选 LLM 端点或尚未出现的验证码；这些事项通过 capability 开关、检查点和手工任务后置处理。正式知乎采集必须遵守“结构化接口/MCP/已登录 Chrome/人工任务”的顺序，遇 403、429、验证码、登录失效或结构异常立即暂停，不绕过访问控制。


---

# 合并原始计划与输入说明

> 以下各节保留原文件正文；原文件名用于追溯。合并日期：2026-07-17。


---

## 原文件：《Phase3-M3.2-M3.3开发计划.md》

# Phase 3 M3.2 / M3.3 开发计划

计划日期：2026-07-17
状态：M3.2、M3.3 均已完成并验收；Phase 3 完成
前置基线：M3.1 提交 `884a5b2`，Python 3.12.10，SQLite migration 0001～0010

## 1. 目标与非目标

本阶段把 M3.1 的“单期确定性勾稽”扩展为可审计的跨期、同行和异常检测闭环。所有结果仍是研究线索，不直接修改风险硬阻断、模拟账本或真实订单。

必须完成：

1. M3.2：TTM、同比、环比、每股值；同公司跨期比较；明确同行样本口径与分位；Beneish、Piotroski、Altman、Sloan、DuPont 的完整字段门禁、版本化公式和行业适用性。
2. M3.3：稳健 Z-score、Isolation Forest 和至少一个 PyOD 模型；冻结训练输入、固定随机种子、版本化模型工件、可重复推理、误报解释和冻结评测集。
3. 所有输入继续经过来源快照、PIT 和 Evidence 门禁；缺字段、样本不足、未来数据、行业不适用或模型不可训练时显式 `NEEDS_INFO`/`INSUFFICIENT_DATA`，不得补数。

不做：

- 不输出“财务造假已确认”；异常只表示需要检查。
- 不用 PyGOD，不建设图数据库。
- 不把社区内容当作财务报表事实。
- 不自动生成真实或模拟交易指令。

## 2. M3.2 数据合同

### 2.1 财务字段与期间语义

扩展 `FinancialFieldCode`，覆盖完整公式所需的折旧、销售管理费用、固定资产、长期债务、市值、发行在外股份等字段。持续期间项目必须显式声明：

- `STANDALONE_PERIOD`：该季度自身发生额，可直接滚动为 TTM；
- `YEAR_TO_DATE`：年初至报告期累计值，必须先与同年上一累计期作差；
- `INSTANT`：资产负债表时点值；
- `REPORTED_PERIOD`：来源未给出足够语义，只允许原值和安全的年度比较，不推导 TTM/环比。

### 2.2 同行样本

同行样本必须带 cohort_id、行业 Profile、指标/公式版本、报告期、as_of、公司 ID、值和 Evidence/PIT/SourceSnapshot 引用。服务只接受：

1. 同一行业 Profile；
2. 同一指标和公式版本；
3. 在审计 as_of 前可得；
4. 公司 ID 唯一；
5. 达到配置的最小样本数。

否则不计算分位，并留下具体缺口。分位采用稳定排序和中秩百分位，同值不因输入顺序改变结果。

### 2.3 输出

增加跨期指标、同行分位和完整评分输出；每个输出保存输入 fact/evidence、公式版本、期间、cohort 或缺口。已有 `FinancialIntegrityEvidencePack` 向后兼容。

## 3. M3.2 算法与门禁

| 能力 | 公式/规则 | 最小门禁 |
|---|---|---|
| TTM | 最近四个独立季度之和，或经过同年累计差分后的四季度之和 | 四个连续季度、期间语义明确、无重复/缺口 |
| 同比 | `(本期-上年同期)/abs(上年同期)` | 同口径上年同期，分母非零 |
| 环比 | `(本期-上期)/abs(上期)` | 连续季度、同口径，分母非零 |
| 每股值 | 金额/期末或配置指定股份数 | 股份数大于零、单位明确 |
| Beneish M | 标准 8 变量模型 | 当前与上期完整字段，金融行业排除 |
| Piotroski F | 9 项二元信号 | 当前与上期完整字段，金融行业排除 |
| Altman Z | 上市制造业 5 因子版本 | 市值和完整资产负债字段，金融行业排除 |
| Sloan | `(净利润-CFO)/平均总资产` | 当前/上期资产与当期利润、CFO |
| DuPont | 净利率 × 资产周转率 × 权益乘数 | 平均资产/权益可得且分母非零 |

阈值来自版本化配置。房地产、早期生物科技和 `OTHER` 必须保留行业解释或禁用未经校准的旗标。

## 4. M3.2 实施顺序与验收

1. 扩展 Schema、配置枚举和纯 Decimal 计算函数。
2. 重构规则引擎，使单期与跨期计算都走同一证据索引。
3. 增加同行 cohort 校验和分位输出。
4. migration 0011 保存 cohort/派生输出摘要与恢复检查点，原始事实继续只存 ObjectStore/现有事实输入。
5. CLI Schema 和 Codex 导入保持兼容。
6. 添加 golden、属性、缺字段、期间歧义、同行样本不足、行业排除、PIT 泄漏和重复运行测试。

M3.2 验收门：`pytest`、`ruff`、`pyright` 全通过；冻结 golden 公式逐项匹配；相同输入哈希和输出哈希一致；旧 M3.1 请求仍得到相同语义结果。

## 5. M3.3 模型合同

### 5.1 特征与数据集

模型输入只来自 M3.2 已校验的数值特征。冻结数据集清单包含：dataset_id、feature schema、样本 ID、as_of、训练/评测分割、输入对象哈希和 Evidence 引用。排序规则固定，不依赖文件遍历顺序。

### 5.2 模型

- Robust Z-score：median/MAD；MAD 为零时显式不可训练或使用配置化 IQR 退化，不除零。
- Isolation Forest：固定 `random_state`、树数、抽样和 contamination；保存库版本与参数。
- PyOD：选择确定性、CPU 低成本模型作为第二视角；保存包版本、类名和参数。

任一第三方模型缺包、版本不符、样本不足或特征非有限时必须失败为明确 capability/data gap，不能输出伪分数。

### 5.3 异常解释

每个异常输出保存模型、版本、分数、阈值、触发特征、Evidence 和适用限制。误报解释只在有证据时生成，候选包括高增长、并购/资产重组、业务季节性、地产项目会计、早期研发期和会计政策变化；没有证据则保留为待核查，不自动“洗白”。

## 6. M3.3 实施顺序与验收

1. 增加模型/数据集/推理 Schema 和 migration 0012；已应用状态库需要的作用域、行业与 cohort 元数据只通过追加 migration 0013～0014 补充。
2. 引入固定版本的 scikit-learn、PyOD 及其 Python 3.12 兼容依赖。
3. 实现冻结数据集构建、三类模型训练/推理、模型对象入 ObjectStore 和 checkpoint 恢复。
4. 建立不含未来信息的 frozen fixture，覆盖正常、异常、常量列、缺值、样本不足和重跑。
5. 将异常结果合并进 `FinancialIntegrityEvidencePack`，仍保持 advisory-only。
6. 完成 Phase 3 验收报告和能力状态更新，不再显示 `DISABLED_UNTIL_M3_2/M3_3`。

M3.3 验收门：固定数据集重复训练得到相同模型身份和分数；异常不得断言欺诈；未来样本无法进入历史训练；三种模型有独立测试；旧数据库可升级且外键完整。

实际验收结果见 `docs/验收报告.md（合并来源：Phase3-M3.3验收报告.md）`：固定数据集、按时序/同行隔离的训练口径、行业与 cohort 身份、三类模型、模型工件、评测报告、证据/PIT 门禁、缺样本降级、重复运行和 migration 0012～0014 均已通过；全仓为 131 passed / 8 skipped，ruff 与 pyright 零问题。

## 7. 回滚与提交

- migration 只追加，不覆盖旧工件；规则、cohort、数据集和模型均按版本并存。
- M3.2 与 M3.3 分别形成可回滚提交和验收报告。
- 任一外部依赖无法在 Python 3.12 安装时，先保留已完成的确定性部分并记录真实阻断；不得偷偷降级成“计划中”。


---

## 原文件：《Phase4-P4.3开发计划.md》

# Phase 4 P4.3 七个确定性诊断接口开发计划

计划日期：2026-07-17

状态：开发前计划。P4.2 只登记了可执行合同和路由；本计划完成前，不把任何 manifest 描述当作已经运行的研究分析。

## 1. 目标与边界

P4.3 在同一个 FrozenEvidencePack、BaseCasePack 和 SpecialistRoutePlan 上实现 6 个占专家名额的确定性诊断器，以及 1 个不占名额的引用保真 memo 组合器。诊断器只把结构化观测转换为带证据的 `SpecialistDelta`，不重复写完整公司报告，不输出自动买卖指令，不承诺收益。

作者蒸馏得到的 40 个候选 Skill 继续保持 `NOT_RUN/PENDING`，不得进入本注册表。P4.3 的规则状态只表示代码接口和门禁通过，不表示历史收益或固定阈值已得到投资批准。

## 2. 公共接口和存储

1. 为每个诊断器建立严格请求 Schema；`skill_id` 和 `skill_version` 使用固定字面量，防止把一种输入伪装成另一种 Skill。
2. 建立版本化 `research_diagnostics.yaml`，只保存透明阈值和最低样本数；禁止隐藏模型、embedding 或联网推理。
3. `ResearchDiagnosticsService` 先验证 route plan 的精确 Skill 是否已选中，再执行规则，最后调用 P4.2 的 `build_delta` 做冻结证据、官方证据、幂等和 Artifact 门禁。
4. `SpecialistDiagnosticReport` 记录规则版本、PASS/PARTIAL/INSUFFICIENT、信号码、降级码、指标名、证据请求码、Delta ID 和证据并集。完整报告进入 ObjectStore；SQLite 只保存安全索引和计数。
5. `ResearchMemoArtifact` 只引用 BaseCase finding ID、Delta/finding/metric/adjustment ID、gap code 和 evidence_id，并检查引用并集守恒；不得新增事实或 evidence_id。
6. migration 0024 建立诊断报告和 memo 的安全索引，保留父子外键、对象哈希、输入哈希、版本、计数、覆盖和状态，不保存 statement、rationale、标题正文或本地路径。

## 3. 六个专家诊断器

### 3.1 IndustryBottleneckSkill

- 输入链：系统变化、必要环节、稀缺性、可替代性和上市公司价值捕获，每层单独携带 evidence_id。
- 只有五层全部成立且可替代性不超过版本阈值时，才输出正向增量发现。
- 任一层缺失或未验证时返回 `INSUFFICIENT_EVIDENCE` 和对应证据请求，不用总分掩盖断链。

### 3.2 EventToAlphaSkill

- 输入必须含已验证事件、经营指标方向、财务方向、明确时间窗、可证伪条件和各自证据。
- 只有标题、没有传导指标、没有时间窗或没有反证时，返回 `EVENT_TRANSMISSION_INCOMPLETE`，不得生成 Alpha 结论。
- 完整时只输出“可观察的经营/财务传导假设”，不直接生成订单。

### 3.3 GrowthProbabilitySkill

- 输入 2～5 个互斥场景；每个场景显式给出概率、年化增长、持续年限、驱动、失败条件和证据。
- Decimal 概率必须精确合计为 1；不守恒直接拒绝，不在服务内部偷偷归一化。
- 输出概率加权增长与持续期；一致预期缺失只标 `CONSENSUS_UNAVAILABLE`，不以 0 代替。

### 3.4 GrowthValuationLens

- 分开输入市场隐含增长、研究场景增长、稀释、再投资、估值依据和可选一致预期。
- 只比较“市场已经定价的预期”与研究场景之差，并按版本化中性带生成 INCREASE/DECREASE/NEUTRAL 调整；不生成伪精确目标价。
- 一致预期缺失时保持可运行但降级，置信度不得上调。

### 3.5 DailyTrendHealthSkill

- 只接受 `1d`、质量门 `PASS` 且达到独立最低样本数的派生指标。
- 使用版本化日线规则判断 HEALTHY/MIXED/WEAK 背景，输出价格趋势 section 的非关键增量；不得转成独立买入建议。
- 质量失败、频率错误或样本不足时不计算分数，返回明确缺口。

### 3.6 HourlySwingSkill

- 只接受独立 `60m`、质量门 `PASS` 且达到小时线最低样本数的指标。
- 使用独立小时规则和阈值输出 POSITIVE/NEUTRAL/NEGATIVE timing context；不复用日线分数和阈值，不生成订单。
- 缠入 `1d`、缺小时频率或质量失败时返回 `FREQUENCY_UNAVAILABLE`/`QUALITY_GATE_FAILED`，不得降格冒充。

## 4. ResearchMemoComposer

1. 输入一个 BaseCase、同一个 route plan 和属于该 route 的 Delta ID 列表。
2. 逐项核对 Delta 的 base_case_id、route_plan_id、被选 Skill 和 FrozenEvidencePack；范围外或其他公司的 Delta 拒绝。
3. 生成 12 个 BaseCase section 的 finding/evidence 引用，另列每个 Delta 的增量 finding、correction、metric、adjustment 和 evidence 引用。
4. 总 evidence_ids 必须等于 BaseCase 与所列 Delta 引用并集；未运行的已选专家明确进入 `missing_selected_skill_ids`。
5. memo 只组合引用，不重新联网、不重新研究、不生成新 statement，也不修改账本。

## 5. CLI、恢复和审计

- `research-diagnostic-schema`：列出七种请求类型、规则版本和固定 Skill 版本，不输出私密内容。
- `research-specialist-diagnose <request.json>`：严格解析一个诊断请求，返回 report/delta ID、哈希、状态、信号码、降级码和计数。
- `research-memo-compose <request.json>`：生成引用保真 memo，返回 ID、哈希、覆盖、引用计数和缺失专家。
- `research-diagnostic-status/audit <base_case_id>`：检查对象、索引、父子关系、Skill 选择、证据范围、未来信息、引用并集、计数和 Artifact registry。
- 无效请求只返回固定错误码；异常文本、statement、rationale 和输入路径不得回显。

## 6. 自动化验收

1. 产业链任一层断裂返回不足；完整链生成一次幂等 Delta。
2. 只有新闻标题的事件不产生传导结论；完整事件链生成带时间窗与反证的增量。
3. 成长概率不守恒被拒绝；守恒场景的加权结果可复算；无一致预期明确降级。
4. 估值中性带、上调和下调三条路径均可复算；缺一致预期不被写成 0。
5. 日线与小时线分别通过正确频率/质量/样本门；互换频率、失败质量和不足样本均拒绝或降级。
6. 所有诊断只允许被 route 选中的精确 Skill；冻结范围外证据和关键判断缺官方证据被 P4.2 门禁拒绝。
7. memo 证据并集守恒，不新增 evidence_id，其他 route 的 Delta 被拒绝，缺失已选 Delta 明确显示。
8. 重复运行 ID/对象/索引幂等；SQLite 不含合成 statement/rationale；全仓 pytest、ruff、pyright、真实迁移、完整性、外键和 Git 隐私检查通过。

## 7. 完成定义

只有当七种接口都有实际代码、失败路径测试、CLI、对象/安全索引、审计和验收报告时，P4.3 才能标为完成。仅有 YAML、Schema 或文档不算完成。


---

## 原文件：《Phase4-P4.4开发计划.md》

# Phase 4 P4.4 持仓生命周期与增量复核开发计划

计划日期：2026-07-17

状态：开发前计划。系统尚未获得自动改账本或向券商发单的权限；本阶段只生成可审计、必须由用户确认的动作建议。

## 1. 目标

为模拟持仓和用户主动声明的真实监控持仓建立同一套版本化通用生命周期内核。每个持仓先登记 `PositionMonitoringPlan`，以后只读取上次复核边界之后新增、变化、失效或冲突的证据，生成 `HoldingEvidenceUpdate`、`HoldingReviewPack` 和 `PositionActionProposal`。

本阶段不执行交易、不修改模拟账本、不连接券商。`PositionActionProposal.requires_user_confirmation` 在 Schema 和服务两层固定为 `true`，不存在绕过入口。

## 2. 版本化规则与动作优先级

新增 `generic-position-lifecycle-v1` 配置，固定优先级：

1. `EXIT`：已登记的论点失效或不可交易硬阻断触发。
2. `REVIEW`：证据冲突、基线证据失效、数据质量冻结、重大披露待核或人工信息到期；同时禁止 ADD。
3. `TRIM`：风险上升或估值减仓条件触发，且没有更高优先级动作。
4. `ADD`：显式加仓条件触发、存在窗口内新增支持证据、没有任何硬阻断或待复核条件。
5. `HOLD`：没有更高优先级条件；“没有新证据”只表示 HOLD，不表示论点增强。

动作置信度使用版本化基础值并受研究覆盖上限约束；缺证据或冲突只会下调，不得靠规则数量上调到满置信度。

## 3. 严格 Schema

### 3.1 监控计划请求

`PositionPlanCreateRequest` 必须包含 position/company、decision 引用及其来源状态、BaseCase、route、memo、as_of、持有期限、论点摘要、入场假设、价值驱动、验证指标、监控来源/频率、显式条件、人工信息需求和下一复核时间。

每个 `LifecycleCondition` 固定 rule_id、signal_code、动作、来源类型、说明、是否要求新证据和是否为硬阻断。rule_id/signal_code 唯一；至少有一条 EXIT/失效条件，防止建立“只能加仓、永不退出”的计划。

### 3.2 增量复核请求

`HoldingReviewRequest` 固定 plan_id、from_as_of、to_as_of、新增 evidence_id、变化 claim_id、失效 evidence_id、开放 conflict_id 和触发的 `HoldingRuleSignal`。时间窗必须递增，集合必须唯一；信号只能引用计划中已登记 rule_id。

`HoldingRuleSignal` 只记录 rule_id、观测值、发生时间和 evidence_id；动作由冻结计划查出，调用方不能临时把一个 HOLD 规则改成 EXIT。

## 4. 父子一致性和 PIT 门禁

1. 创建计划时核对 BaseCase、route、memo 的 company、as_of、FrozenEvidencePack 和父子关系；memo 未列出的新事实不得进入计划基线。
2. 首次 review 的 from_as_of 必须等于计划基线时间；后续必须等于上次成功 review 的 to_as_of，不允许跳过或重叠窗口。
3. 新增 Evidence 必须存在、entity_ids 包含计划 company、`available_to_system_at` 严格晚于 from_as_of 且不晚于 to_as_of；未来信息和其他公司证据拒绝。
4. changed claim 必须存在且属于同一 company；signal evidence 必须包含在本次新增 evidence_ids 中。
5. 失效 evidence 和冲突不会被静默删除，分别形成 REVIEW/硬阻断输入。

## 5. Artifact、存储和恢复

- 完整 `PositionMonitoringPlan`、`HoldingEvidenceUpdate`、`HoldingReviewPack`、`PositionActionProposal` 只进入 ObjectStore。
- migration 0025 只保存规则版本、ID、父子引用、时间窗、动作、确认标志、哈希、计数、覆盖和状态；不保存 thesis、原因正文、监控路径或私密仓位数量。
- 同一输入和规则版本生成稳定 ID；重复执行返回已登记对象。对象已写、索引未写时可重跑，不覆盖旧对象。
- 每次成功 review 写 checkpoint；status/audit 从安全索引恢复，不直接编辑 SQLite。

## 6. CLI

- `position-lifecycle-schema`：列规则版本、优先级、动作置信度和安全门。
- `position-plan-create <request.json>`：登记计划，只输出 ID、哈希、计数、覆盖和时间。
- `position-plan-status <position_id>`：返回最新安全索引。
- `holding-review-run <request.json>`：生成 update/review/proposal，只输出 ID、动作、置信度、硬阻断、规则码和计数。
- `holding-review-status <position_id>` 与 `holding-review-audit <position_id>`：检查窗口连续性、对象、父子、Evidence/PIT、确认标志、索引计数和 Artifact registry。
- 无效请求只返回固定错误码，不回显 thesis、条件说明、文件路径或私密数量。

## 7. 自动化验收

1. BaseCase/route/memo 父子不一致时计划拒绝；计划缺 EXIT 条件拒绝。
2. 初始无新证据/无信号为 HOLD，不能写成 STRENGTHENED。
3. ADD 必须同时有已登记 ADD 信号和窗口内新增支持 Evidence；缺支持证据改为 REVIEW，冲突/失效/数据冻结时 ADD 被硬阻断。
4. EXIT、REVIEW、TRIM、ADD、HOLD 同时触发时严格按固定优先级；调用方不能伪造动作。
5. 未来 Evidence、其他公司 Evidence、窗口外 Evidence、未知 rule_id、非连续窗口均拒绝。
6. proposal 显式传 `requires_user_confirmation=false` 在 Schema 层拒绝；任何服务路径都不能修改 paper ledger。
7. 重复计划/review 幂等；完整对象和安全索引、Artifact/checkpoint 对账；SQLite 不含合成 thesis/condition 文字或私密数量。
8. 全仓 pytest、ruff、pyright、真实 migration、完整性、外键和 Git 私密文件扫描通过。

## 8. 完成定义

只有五类动作路径、窗口增量、Evidence/PIT、人工确认、不改账本、CLI、恢复、审计和验收报告全部由实际代码与测试证明后，P4.4 才标为完成。文档或已有宽松 Schema 本身不算实现。


---

## 原文件：《Phase4-P4.5开发计划.md》

# Phase 4 P4.5 Codex/Repo Skill 集成、恢复与终验开发计划

计划日期：2026-07-17

状态：开发前计划。P4.1～P4.4 已有实际实现和验收；本文件只定义最后一段集成工作，不把计划写成已完成能力。

## 1. 目标与非目标

P4.5 不增加新的投资判断规则，而是把已完成的冻结 EvidencePack、BaseCase、专家 route/Delta、诊断、memo、PositionMonitoringPlan 和 HoldingReview 串成一条可恢复、可从 Repo Skill 正确调用、可由 Codex 严格引用既有确定性工件的完整链。

本阶段仍不实现委员会、CounterCase、DecisionPack、真实交易或自动写模拟账本。Codex 只验证和包装已经由确定性服务登记的 Phase 4 工件；不能用一段新生成的文字冒充已运行的 Python 研究链。

## 2. 冻结 Codex 输入清单

新增 `CodexArtifactReference` 和 `CodexRunInputManifest`：

- reference 固定 artifact_id、artifact_type、object_sha256 和 role；object hash 必须为 64 位小写 SHA-256。
- manifest 固定版本、selected_skills、注册工件引用、仅供本地预算使用的旧路径列表，以及 `require_registered_output` 安全门。
- artifact_id、object hash 和 Skill 必须去重；严格模式至少有一个注册工件引用，不能只传本地路径。
- 初始化时逐项核对 Artifact registry、类型、对象哈希和 ObjectStore 完整性；任一不一致时不创建 run。
- `RunManifest.input_hashes` 必须等于清单中冻结对象哈希的稳定集合；完整清单只保存在 runtime，SQLite 不保存路径或正文。

migration 0026 新增安全索引：

- `codex_run_input_index`：run_id、artifact_id、type、role、object hash；
- `codex_run_output_index`：run_id、Codex 验证工件 ID/类型/哈希、原确定性工件 ID/哈希、输入数、引用数、严格门和状态。

两张表只保存 ID、哈希、计数和状态，不保存自然语言请求、citation locator、输入路径或正文。

## 3. 严格“只包装已登记输出”门禁

严格模式支持 P4.1～P4.4 的确定性工件：FrozenEvidencePack、BaseCasePack、SpecialistRoutePlan、SpecialistDelta、SpecialistDiagnosticReport、ResearchMemoArtifact、PositionMonitoringPlan、HoldingEvidenceUpdate、HoldingReviewPack 和 PositionActionProposal。

Codex 导入时：

1. 仍先执行领域 Schema、evidence_id citation 和禁止命令 Policy；
2. 从工件主 ID 推导其确定性 Artifact registry ID；缺主 ID、未登记、类型不符、对象哈希不等或 ObjectStore 损坏均拒绝；
3. PositionActionProposal 继续由 Schema 强制 `requires_user_confirmation=true`；
4. Codex 验证包装对象显式包含 frozen input hashes 和 source artifact ID，避免相同正文在不同输入下发生身份碰撞；
5. Artifact registry 的 input_hashes 写入真实冻结输入，不再一律为空。

非严格兼容路径只用于既有 M1 通用工件测试和非 Phase 4 草稿；Repo Skill 的正式 Phase 4 路径必须开启严格模式。

## 4. Codex run 状态、审计与恢复

新增服务和 CLI：

- `codex-run-status <run_id>`：只返回状态、清单版本、输入/输出 ID 和计数；
- `codex-run-audit <run_id>`：核对 run 行、RunManifest、输入清单、输入安全索引、Artifact registry、ObjectStore、validated_result、citations、输出索引和状态；
- `codex-run-recover <run_id>`：若已有合法 result_draft 但导入在中途失败，幂等重跑导入并补齐文件、Artifact、输出索引和 run 状态；没有 draft 时只报告不可恢复，不伪造输出。

导入写入顺序固定为：验证输入和 draft → 写不可变包装对象 → 幂等登记 Artifact → 原子写 validated/citations/summary/manifest → 同一 SQLite 事务写输出索引并更新 run。故障注入覆盖 Artifact 已登记但输出索引未写、文件已写但 run 未完成两类边界。

## 5. Phase 4 全链状态与审计

新增 `Phase4ChainService` 和：

- `research-chain-status <company_id> [--position-id]`；
- `research-chain-audit <company_id> [--position-id]`。

服务从最新 BaseCase 开始，读取而不重跑研究，组合 P4.1 core audit、P4.2 route/Delta audit、P4.3 diagnostic/memo audit 和可选 P4.4 lifecycle audit。输出只包含阶段状态、父 ID、覆盖、计数和 finding code；任何子审计 NOT_RUN/PARTIAL 都不能被总状态写成 PASS。

该服务不联网、不新增 Evidence、不调用专家、不修改账本。它解决“每个模块各自正常，但整条链是否真的接上”的终验问题。

## 6. CLI 与 Repo Skill 落地

- `probe` 增加已支持的严格 Phase 4 工件类型、输入清单版本和 `registered_output_required` 能力说明。
- `context-plan` 与 `codex-run-init` 增加 `--artifact-id`；注册对象按实际字节计入上下文预算，输出只列 artifact ID，不暴露 ObjectStore 路径。
- `codex-run-init` 增加 `--require-registered-output`；严格模式拒绝只有路径、没有冻结 artifact reference 的初始化。
- 更新 `$astock-research-orchestrator`、`$company-deep-research` 和 `$holding-monitor`：写明实际 research/position/chain 命令、严格 Codex 初始化和导入顺序，删除“尚未实现时假装 DecisionPack”的旧表述。
- 其他 Repo Skill 只在需要时更新能力说明；不扩大其触发范围，也不允许 Skill 直接编辑 SQLite。

## 7. 自动化验收

1. 输入清单去重、未知 artifact、类型/哈希不符、坏 ObjectStore 和严格模式路径-only 全部拒绝，且不留下半个 run。
2. 严格导入十种 Phase 4 工件的主 ID/registry/hash 门禁；伪造 payload、未登记 payload 和 `requires_user_confirmation=false` 拒绝。
3. citation 缺失/空定位、requested_commands、run_id 路径穿越继续拒绝。
4. 相同 run 重复导入幂等；相同输出绑定不同 frozen inputs 时身份不碰撞；Artifact input_hashes 精确等于 RunManifest。
5. 故障注入后 `codex-run-recover` 补齐输出；无 draft、坏 draft 或输入损坏时明确不可恢复。
6. 全链合成案例实际跑通 Evidence → BaseCase → route → diagnostic/Delta → memo → plan → review → strict Codex validation，四个子审计和总审计均 PASS。
7. 删除早期父 Artifact、篡改安全索引或制造子阶段 PARTIAL 时，总审计必须 PARTIAL 并给出阶段化 finding code。
8. Repo Skill 测试验证命令真实存在、严格门已写入正式流程、禁止联网委员会/直接账本/真实订单仍保留。
9. SQLite 0026 表中不出现合成请求、citation、thesis、规则说明、路径或私密数量；真实库 migration、integrity、外键和对象核验通过。
10. 全仓 `pytest`、`ruff check .`、`pyright` 通过，私有 PDF/DOCX、runtime、Cookie 和浏览器 Profile 不进 Git。

## 8. 完成定义

只有当冻结输入、严格输出、Codex 中断恢复、Phase 4 全链审计、Repo Skill 可执行路由、真实 migration 和全仓验收全部由代码与测试证明后，P4.5 和 Phase 4 才标记完成。计划文件、宽松 Schema 或单模块 PASS 都不能替代终验。


---

## 原文件：《Phase4开发计划.md》

# Phase 4：Serenity 改造与通用投资内核开发计划

计划日期：2026-07-17

状态：P4.1～P4.5 已实现并验收，Phase 4 完成。

## 1. 目标

Phase 4 把已经完成的证据、PIT、财务核验和行情基础设施接成一条可审计的研究内核：同一公司、同一信息截面只做一次共性分析，专家只补充真正的行业或方法差分，开放持仓只读取上次冻结时点之后的新增证据。

本阶段交付研究工件和建议，不承诺收益，不批准 Phase 5 的作者 Skill，不自动修改模拟账本，更不向真实券商发单。

## 2. 固定边界

1. `BaseCasePack`、`SpecialistDelta`、研究 memo、监控计划和复核包属于版本化 Artifact，正文只进入 ObjectStore；SQLite 只保存 ID、对象哈希、输入哈希、版本、计数和状态。
2. 每条关键判断必须引用 `evidence_id`；社区内容只能作线索，涉及公司事实时必须有官方或更强证据。
3. 构建时按 `as_of` 校验证据可得时间；晚于研究时点的证据一律拒绝，`NOT_PIT_SAFE` 输入不得伪装成正式历史评测可用。
4. 共性分析只构建一次。专家输入必须引用同一个冻结 BaseCase；专家 Schema 不提供重新书写完整公司介绍、通用财务和整套风险的字段。
5. 路由默认最多 3 个专家；超过上限直接拒绝，而不是悄悄截断。首版只用显式业务规则、标签和关键词，不引入 embedding 或小模型的不确定调用。
6. 缺一致预期、缺小时数据、缺行业证据或专家覆盖不足时返回明确降级码、置信度上限和信息缺口，不插值、不编造。
7. 日线趋势和 HourlySwing 使用独立 manifest、输入频率和规则版本；小时线不得复用日线阈值。
8. 所有持仓动作只是建议；`PositionActionProposal.requires_user_confirmation` 在 Schema 层固定为 `true`。
9. Phase 5 自动摘录和候选 Skill 保持 `PENDING/NOT_RUN`，未经人工综合和评测不得进入 Phase 4 已批准 manifest。

## 3. P4.1：冻结证据与 BaseCase

### 3.1 工件

- `FrozenEvidencePack`：company_id、as_of、claim_ids、evidence_ids、冲突、PIT 状态分布、coverage、输入对象哈希和冻结哈希。
- `CitedResearchFinding`：statement、finding_type、confidence、critical、evidence_ids；关键判断至少一个 evidence_id。
- `ResearchGap`：缺口类别、影响、所需证据和是否阻塞。
- `BaseCasePack`：业务模式、收入/利润驱动、现金流质量、资本回报、再投资、治理、竞争、产业供需、估值预期、趋势背景、风险、缺口、专家标签、coverage、base_confidence 和 evidence_ids。

### 3.2 构建规则

1. 先从 Claim—Evidence 图按 company_id 和 as_of 冻结精确证据集合；开放冲突进入 pack，不能被遗漏。
2. BaseCase 草稿只能引用冻结 evidence_ids；引用集合、section 汇总和总 evidence_ids 必须守恒。
3. 相同冻结输入、内核版本和草稿内容生成相同 base_case_id/object hash；重复运行不得新增逻辑重复工件。
4. BaseCase 不自动给出买卖结论；证据不足时使用 gap 和低 coverage，不用常识补事实。

## 4. P4.2：Skill manifest、专家路由和差分

### 4.1 版本化 manifest

首版配置登记：

- `IndustryBottleneckSkill`
- `EventToAlphaSkill`
- `GrowthProbabilitySkill`
- `GrowthValuationLens`
- `DailyTrendHealthSkill`
- `HourlySwingSkill`
- `ResearchMemoComposer`

每个 manifest 包含来源固定 commit/本地设计来源、触发标签、市场、行业、周期、必需输入、必需证据、推理步骤、正负信号、失效条件、失败模式、成本级别、兼容/冲突 Skill、规则版本和批准状态。首版通用内核可标为 `APPROVED_INTERNAL_RULESET`；来自 Phase 5 作者的候选不得混入。

### 4.2 路由

- 输入：BaseCase 的业务/行业/风险/事件/周期标签、可用数据频率和一致预期可用性。
- 顺序：硬性业务规则 → 显式标签 → 关键词；稳定分数与 skill_id 打破并列。
- 输出：`SpecialistRoutePlan`，记录命中理由、未命中原因、降级码、覆盖状态、置信度上限和最多 3 个 Skill。
- `ResearchMemoComposer` 是最终格式化节点，不计入专家上限；没有合格专家时返回 `INSUFFICIENT`，禁止最高置信度并生成外部证据需求。

### 4.3 差分

`SpecialistDelta` 只允许：增量发现、BaseCase 更正、行业指标、附加证据请求、失败模式、confidence_delta、估值调整、风险调整、coverage_delta 和 evidence_ids。每个发现/更正/调整都必须引用证据；全部引用必须属于专家的冻结 EvidenceScope。

## 5. P4.3：七个通用 Skill 的确定性诊断接口

首版不复制上游固定总分，也不让诊断分直接成为交易信号。

1. 产业瓶颈：验证“系统变化 → 必要环节 → 稀缺性/可替代性 → 上市公司价值捕获”链；缺任一层则 `INSUFFICIENT_EVIDENCE`。
2. Event-to-Alpha：事件必须落到可验证的经营指标、财务方向、时间窗和反证；只有新闻标题时不产生 Alpha 结论。
3. 成长概率：场景概率、增长驱动、持续时间和失败条件显式输入；概率必须守恒。
4. 成长估值：区分当前价格隐含预期和研究场景；一致预期不可得时标 `CONSENSUS_UNAVAILABLE`，不以 0 替代。
5. 日线趋势：只消费通过质量门的日线派生指标，输出趋势背景而非独立买入建议。
6. HourlySwing：独立 60 分钟规则和数据质量门；缺小时数据时 `FREQUENCY_UNAVAILABLE`，不得退用日线阈值冒充。
7. Research memo：只组合 BaseCase 和 Delta 的引用，不重新研究、不引入新 evidence_id。

每个 Skill 都以同一 `SpecialistObservation`/`SpecialistDelta` 合同运行，具体规则由版本化 YAML 定义，测试覆盖缺字段、错误频率、证据不足和降级路径。

## 6. P4.4：版本化通用持仓生命周期内核

### 6.1 规则集

建立 `generic-position-lifecycle-v1`，覆盖 ENTRY、HOLDING、ADD、TRIM、EXIT、REVIEW。规则只定义可审计优先级和安全门，不用一套固定百分比止损强加给所有风格。

- 论点明确失效或不可交易硬阻断：EXIT；
- 核心证据冲突、数据质量冻结、重大公告待核：REVIEW，并禁止 ADD；
- 风险/估值减仓条件触发：TRIM；
- ADD 必须同时有显式加仓条件、新证据支持且没有任何硬阻断；
- 无新增证据和无触发条件：HOLD，不等同于论点增强；
- 到期、重大事件和人工信息需求触发 REVIEW。

### 6.2 工件与增量

- `PositionMonitoringPlan`：引用 decision/base case/evidence snapshot、规则集版本、监控来源、频率、加减仓/退出/复核条件。
- `HoldingEvidenceUpdate`：严格描述上次冻结时点到当前时点的新增、变化、失效和冲突证据。
- `HoldingRuleSignal`：只声明已触发的已登记 rule_id、观测值、时间和 evidence_ids。
- `HoldingReviewPack`：确定性优先级合并后的建议、硬阻断、触发规则、冲突和下次复核条件。
- `PositionActionProposal`：永远需要用户确认；本服务不写模拟账本。

## 7. P4.5：存储、CLI、Codex 和恢复

- migration：只建研究 Artifact 的安全索引、版本和父子引用，不保存 statement、memo、论点或本地路径。
- ObjectStore：保存完整冻结 pack、BaseCase、Delta、route plan、监控计划和复核包。
- Artifact registry：登记对象哈希与输入哈希，重复执行幂等。
- checkpoint：BaseCase、route、delta 和 holding review 分别记录成功边界；对象已写但索引未提交时可重跑恢复。
- CLI 计划交付：`research-evidence-freeze`、`research-base-case-build/status/audit`、`research-specialist-route`、`research-delta-import`、`position-plan-create`、`holding-review-run/status/audit`。CLI 状态输出只含 ID、哈希、计数、覆盖、降级码和状态。
- CodexRunService 支持导入 `FrozenEvidencePack`、`BaseCasePack`、`SpecialistDelta`、`SpecialistRoutePlan`、`PositionMonitoringPlan` 和 `HoldingReviewPack`，继续执行 evidence_id citation 校验。

## 8. 测试与验收

### 8.1 自动测试

- 相同输入稳定 ID/object hash、重复运行幂等；版本升级并存。
- 未来证据、冻结范围外引用、关键判断无 evidence_id、开放冲突丢失均拒绝。
- BaseCase 只注册一次；多个 Delta 引用同一个 base_case_id。
- 路由稳定、最多 3 个专家、无匹配覆盖降级、ResearchMemoComposer 不占专家名额。
- 缺一致预期、缺小时数据、日/小时频率错用均返回明确降级。
- Delta Schema 无完整 BaseCase 字段；所有调整有证据。
- 生命周期动作优先级、ADD 硬门、无新增证据为 HOLD、所有 proposal 强制人工确认。
- SQLite 不含测试 statement/论点/路径；ObjectStore、SQLite 索引和 artifact registry 对账为 0。
- Windows UTF-8/路径、migration 保留数据、崩溃恢复和 CLI 安全输出。

### 8.2 阶段验收

- `pytest`、`ruff`、`pyright` 全通过；SQLite integrity 与外键违规为 0。
- 用合成但具完整证据/PIT 的公司案例跑通冻结证据 → BaseCase → 1～3 个 Delta → memo 引用集合 → 监控计划 → 增量复核。
- 另跑缺一致预期、缺专家和缺小时数据三个降级案例，证明系统不补造。
- Git 不包含私有 PDF/DOCX、runtime、正文、Cookie、浏览器 Profile 或测试中的真实作者原文。

## 9. 明确不在 Phase 4 完成的事项

- 统一委员会、CounterCase、DecisionPack 和硬风险裁决属于 Phase 6。
- 冻结权重影子账户、walk-forward、样本外增量价值属于 Phase 7。
- 动态权重、多臂赌博机和 RL 属于 Phase 8，未满足 Phase 7 门槛时保持禁用。
- Phase 5 的 401 条摘录人工综合、40 个候选 Skill 评测和三位作者采集缺口继续单独跟踪。

## 10. 停止条件

只有当需要用户选择投资风格、提供无法从已冻结证据判断的真实仓位语义，或批准作者候选 Skill 时才暂停对应支路。单个 Skill 输入不足时继续完成其他通用内核，并把该支路标成结构化缺口。

## 11. 实施进度

- [x] P4.1：冻结证据、PIT/冲突门禁、一次性 BaseCase、ObjectStore/安全 SQLite 索引、CLI、审计和验收。
- [x] P4.2：版本化 Skill manifest、确定性专家路由、最多 3 个专家和 SpecialistDelta。
- [x] P4.3：七个通用 Skill 的确定性诊断接口和降级路径。
- [x] P4.4：版本化通用持仓生命周期内核与增量 HoldingReview。
- [x] P4.5：Codex/Repo Skill 集成、整体恢复与 Phase 4 终验。

P4.1～P4.5 验收依据见 `docs/验收报告.md（合并来源：Phase4-P4.1验收报告.md）`、`docs/验收报告.md（合并来源：Phase4-P4.2验收报告.md）`、`docs/验收报告.md（合并来源：Phase4-P4.3验收报告.md）`、`docs/验收报告.md（合并来源：Phase4-P4.4验收报告.md）` 与 `docs/验收报告.md（合并来源：Phase4-P4.5验收报告.md）`。


---

## 原文件：《Phase5-K5.3-K5.5开发计划.md》

# Phase 5 K5.3～K5.5 开发计划

计划日期：2026-07-17

状态：K5.3 确定性内核与 K5.5 已验收；K5.4 登录内容连续枚举仍受平台访问限制

## 1. 已知起点

- 三位线上作者身份已确认；寒武纪的鳄鱼固定为本地完整导出，不再访问知乎。
- Python 公开接口已完成可审计实测：MR Dang 想法 0 条、黄彦臻想法 1 条均到真实终点；派大星皮皮已保存 615 条后被限制。
- 三位作者的回答/文章都需要登录通道。当前 Chrome 已登录，但扩展控制在接管/读取标签页时不稳定；不得复制 Cookie、读取 Profile 或把 401/403 误写成空集合。
- K5.1～K5.2 已有不可变响应、内容版本、Parquet 索引、列表检查点、缺口和覆盖报告基础。

## 2. K5.3：登录响应导入和评论树

### 2.1 无凭据响应导入

新增一个只接受 runtime 本地文件的 `ZhihuBrowserResponseEnvelope`。信封只允许包含作者 source_id、内容类型/内容 ID、请求 URL、HTTP 状态、响应 MIME、原始响应体和采集运输类型；禁止 Cookie、Authorization、请求头全集、Profile 路径和浏览历史。

CLI 分两步：

1. `astock zhihu-response-import <envelope>`：校验白名单、同源 HTTPS API URL、正文大小与 JSON 结构，先存 ObjectStore/SourceSnapshot，再登记“待消费响应”；
2. `astock zhihu-import-replay <source-id>`：按原请求游标把已导入响应送入同一适配器、版本仓储和检查点逻辑，浏览器与 Python 不形成旁路。

导入文件位于被 Git 忽略的 runtime；命令输出只给 envelope_id、snapshot_id、分类和计数。重复导入同一响应幂等。

### 2.2 评论与嵌套回复合同

新增并版本化：

- `ZhihuCommentNode`：comment_id、content_id、author_id、parent_id、reply_to_comment_id、root_comment_id、正文对象哈希、创建/更新时间、点赞数、作者是否为目标作者、原始快照 ID；
- `ZhihuCommentPage`：父内容或根评论、分页/游标、节点 ID、is_end、下一游标、原始对象哈希和运输来源；
- `ZhihuAuthorParticipationChain`：目标作者评论、完整祖先链、作者直接回复对象、理解连续对话所需的中间节点和作者后续回复；
- 评论覆盖报告：根评论页和每个需要展开的 child page 分开计数、设终点和缺口。

原始评论树完整保存在私有 runtime。派生链只选择目标作者参与的分支，但每个节点继续回链原始评论版本和 SourceSnapshot。

### 2.3 端点发现边界

知乎没有项目可依赖的稳定公开评论 API 合同，因此不凭记忆硬编码。先从已登录页面实际观察到的请求 URL 建立 endpoint template，冻结一页真实私有响应，再生成不含真实正文的人工 fixture。只允许 `www.zhihu.com/api/` 且路径必须匹配已批准 template；结构变化返回 `INVALID_RESPONSE`，不推进评论检查点。

## 3. K5.4：三位作者连续枚举

执行顺序固定为：

1. 公开 Python 接口尚可访问的游标；
2. 已导入的登录浏览器响应；
3. 若 Chrome 扩展恢复，逐页观察并导入，低频串行；
4. 若出现验证码、安全验证或扩展持续不可用，为对应作者/类型生成单独人工任务，其他来源继续。

已成功的公开快照不重复从浏览器抓取。浏览器只补开放缺口，并从 SQLite 检查点指定的精确 URL 开始。

## 4. K5.5：覆盖审计

新增聚合命令 `astock knowledge-coverage-audit`，逐作者、逐内容类型检查：

- 身份已确认；
- 列表终点、成功/失败/受限/重复/更新/缺失计数；
- 每个内容版本的正文对象存在且哈希可验证；
- 需要评论的内容是否有根评论终点；
- 需要展开的作者参与根评论是否有 child reply 终点；
- 开放 gap、长期 RUNNING job/attempt 和未消费导入信封；
- Parquet 索引与 SQLite 元数据是否一一对应。

寒武纪的鳄鱼另由 `knowledge-local-coverage` 校验 DOCX 文件哈希、不可变源对象、2,032 个解析块及解析报告，输出 `USER_CONFIRMED_COMPLETE_EXPORT`；该状态不得伪装成知乎分页独立核验。

## 5. 测试与提交顺序

1. [x] K5.3a：导入信封 Schema、migration 0016、仓储、路径/URL/敏感字段拒绝、列表回放和幂等测试。验收见 `docs/验收报告.md（合并来源：Phase5-K5.3a验收报告.md）`。
2. [x] K5.3b 确定性内核：评论页/节点/参与链 Schema、migration、人工 fixture、分页、独立根/子回复检查点与崩溃恢复测试；子回复真实端点仍待观察，不把猜测路径标成可用。
3. [ ] K5.4：登录响应 live smoke 已确认回答根评论端点，但直接 API 返回访问限制；按作者/类型恢复和子回复端点观察仍待后续可用窗口。
4. [x] K5.5：覆盖审计、本地 DOCX 覆盖、对象/Parquet/SQLite 对账和隐私扫描。验收见 `docs/验收报告.md（合并来源：Phase5-K5.3b-K5.5验收报告.md）`。
5. 每个小步分别运行 `pytest`、`ruff`、`pyright`，通过后形成独立提交；最终再跑全仓验收。

## 6. 完成门

只有三位线上作者的每种列表均为 `PAGINATION_COMPLETE` 或结构化 `CONFIRMED_EMPTY`、所有必需评论分页有终点、`missing_count=0` 且开放 gap 为 0，K5.3～K5.5 才能整体完成。平台限制或浏览器不可用时继续标记 `ACCESS_RESTRICTED/PARTIAL`，不阻塞本地材料蒸馏和 Phase 4/6 的确定性内核开发，但会阻止对应作者知识 Skill 获批。


---

## 原文件：《Phase5-K5.6知识蒸馏开发计划.md》

# Phase 5 K5.6 知识清洗与蒸馏开发计划

计划日期：2026-07-17

状态：K5.6a～K5.6d 的确定性自动链与真实运行已实现并验收；人工综合、审核和样本外评测待后续执行，Phase 5 总体仍为 `PARTIAL`

## 1. 目标与真实输入

本步把已获准且已保存的私有/社区原料变成可审计的结构化候选知识，不把自动分类结果直接冒充已批准投资规则。

当前输入固定为：

- MR Dang《价值投资功法》：私有 PDF，全量解析 249/249 页，解析文本 151,014 字符；
- 寒武纪的鳄鱼：用户确认完整的私有 DOCX，全量解析 2,032 块，解析文本 93,323 字符；不再访问知乎；
- 黄彦臻：当前已保存想法 1 条；
- 派大星皮皮：当前已保存想法 615 条；
- MR Dang 线上想法确认 0 条；三位线上作者的回答、文章和评论缺口继续保留，不阻塞现有材料的处理，也不得据此宣布作者全集蒸馏完成。

## 2. 不可突破的边界

1. PDF、DOCX、知乎原文、清洗后全文和浏览器会话只进入 `runtime/objects/sha256/` 或其他被 Git 忽略的 runtime 工件。
2. SQLite 和 Parquet 只保存 ID、来源定位、分类、计数、版本和对象哈希，不保存私有正文、标题、观点原句或文件路径。
3. 自动清洗只生成“保留/降权/待复核”候选，永久保留原始对象与回链，不执行物理删除。
4. 社区内容只能形成研究方法候选或线索；涉及公司事实的规则必须要求回到公告、交易所和财报验证。
5. 未经人工审核与评测，观点卡、规则和 Skill 一律保持 `PENDING/NOT_RUN`，不得批准。
6. 不模仿作者人格和文风，只抽取可复核的方法、适用条件、反证和失效条件。

## 3. 先修正的工件合同

现有 `BookViewpointCard` 和 `BookSkillCandidate` 只接受 PDF 页码，不能合法引用 DOCX 块或知乎内容；明文 proposition/rule 若直接进入 SQLite 也会破坏私有内容边界。因此先新增统一引用与私有载荷合同：

- `DistillationSourceLocator`：`PAGE_TEXT`、`BLOCK_TEXT`、`ZHIHU_CONTENT` 或 `ZHIHU_COMMENT`，保存 source_snapshot_id、源单元 ID、页码/块序号/content_id、字符区间、parser/version 和对象哈希；
- `DistillationUnit`：稳定 unit_id、作者/来源、规范化文本对象哈希、原始定位、重复关系、内容分类、方法分类、规则版本、理由码和置信度；
- `PrivateViewpointDraft`：命题、反证和失效条件只保存在 ObjectStore 载荷中，SQLite 只登记 draft_id、对象哈希、证据引用数量和审核状态；
- `PrivateSkillCandidate`：规则载荷同样只在 ObjectStore，必须引用证据定位，默认 `NOT_RUN/PENDING`；
- `AuthorDistillationReport`：按作者汇总输入覆盖、分类、重复、方法类别、缺口和审核队列，不含正文。

原有 PDF 专用 Schema 保留兼容，但 K5.6 新链统一走上述跨 PDF/DOCX/知乎的合同。

## 4. K5.6a：确定性单元目录与清洗分类

### 4.1 单元化

- PDF：按已解析页文本中的非空段落切分，记录页码与字符区间；
- DOCX：每个非空稳定块作为一个单元，引用 `BLOCK_TEXT`；过长块只在对象层切片，保留父块定位；
- 知乎：对已保存正文做 HTML/空白规范化后按段落切分，引用内容版本和原始 SourceSnapshot；评论只处理已取得的目标作者参与链；
- 全部规范化采用固定 Unicode NFKC、换行/空白规则和版本号；同一输入重复运行必须产生相同 unit_id 与文本哈希。

### 4.2 重复与噪声

- 先做同作者范围内的规范化文本精确重复；首次出现为 canonical，后续单元只记录 duplicate_of；
- 短文本、纯链接/目录/页码、无正文 HTML 等使用明确理由码降权；
- “营销、情绪、故事、过时价格、无新增复述”等只能由版本化通用规则标成候选，不能自动删除；规则未命中则进入 `UNCLASSIFIED`，不得强行归类。

### 4.3 方法分类

版本化词典和组合规则覆盖：选股、商业模式、行业、估值、财务质量、建仓、持仓验证、加仓、减仓、退出、风险、失败案例、反证/失效和复盘。允许多标签，保存命中理由码和分数；自动结果统一标为待人工复核。

## 5. K5.6b：覆盖报告与人工审核队列

每个来源生成：

- `BookCleaningReport` 或等价跨来源清洗报告；
- `BookMethodCoverageReport`：八个核心阶段分别给出段落/证据计数；自动阶段零命中只能写 `INSUFFICIENT_SOURCE`，不能写 `AUTHOR_SILENT`；
- `AuthorDistillationReport`：列出来源覆盖状态，线上缺口与本地完整导出语义分开；
- 私有 review queue：包含待审核单元对象引用和上下文对象引用，只写 runtime，不在 CLI 输出正文。

审核前 `human_review_status=PENDING`。人工可以批准、拒绝或要求修改分类；决定以 `HumanReviewDecision` 追加保存，不覆盖自动结果。

## 6. K5.6c：观点与 Skill 候选

仅从“保留且方法分类明确”的单元生成私有草稿：

1. 观点候选必须包含命题、适用范围、证据引用、反证和失效条件；
2. Candidate Selection 候选覆盖候选发现、公司/行业分析、估值和证据门槛；
3. Position Lifecycle 候选覆盖建仓、持仓验证、加减仓、退出、风险和复盘；
4. 没有明确原文依据时不补写作者观点；
5. 每条候选默认 `evaluation_status=NOT_RUN`、`approval_status=PENDING`；
6. 后续评测必须验证可执行字段、未来函数、证据门槛、作者差分和通用内核冲突。

本步只交付候选与审核包，不在没有用户审核的情况下批准生产 Skill。

## 7. K5.6d：真实运行顺序

1. MR Dang PDF：全量单元化、分类、清洗/方法覆盖报告和审核队列；
2. 寒武纪 DOCX：全量 2,032 块处理，生成块级引用与独立作者报告；
3. 黄彦臻 1 条想法：处理现有版本并标记线上历史缺口；
4. 派大星皮皮 615 条想法：批量处理、断点提交并保留后续游标 gap；
5. MR Dang 线上想法 0 条只记录已确认空集合，不从 PDF 结果反推线上内容；
6. 汇总跨作者类别分布、未分类比例和审核工作量，不合并不同作者的观点身份。

## 8. Migration、CLI 与恢复

- migration 0019：distillation run、unit metadata、分类版本、重复关系、作者报告和初步审核队列；
- migration 0020：私有摘录草稿、候选 Skill、安全引用关系和作者生成报告；
- migration 0021：同一蒸馏输入按生成规则版本并存，升级规则不得覆盖旧草稿；
- Parquet：只保存安全元数据索引，正文/命题/规则载荷只存 ObjectStore；
- CLI：`knowledge-distill-plan/run/status/audit`、`knowledge-review-queue` 和 `knowledge-draft-generate/status/audit`，输出仅含 ID、哈希、计数和状态；
- 检查点按 run、source、source_unit 递进；ObjectStore 已写而 SQLite 未提交时可幂等恢复；
- 相同源快照与规则版本重复运行不重复生成单元和候选。

## 9. 测试与验收门

自动化测试至少覆盖：

- PDF 页、DOCX 块、知乎内容三种定位；
- 中文 Unicode/空白规范化、稳定 ID、精确重复链和多标签分类；
- 分类计数守恒，所有非空输入单元恰好进入 canonical 或 duplicate；
- 对象先写、元数据后提交、崩溃恢复、重复运行幂等；
- DOCX 结果不伪造页码；零命中在未审核前不得成为 `AUTHOR_SILENT`；
- 未审核候选不能变为 `APPROVED`；
- SQLite/Parquet/CLI/Git 不泄露正文、文件路径、Cookie 或浏览器 Profile。

真实验收必须证明：

- PDF 249 页、DOCX 2,032 块和线上 616 条现有内容全部纳入或给出结构化缺口；
- 输入对象和派生对象哈希全部可验证，SQLite/Parquet 对账为 0；
- 每位作者独立生成分类和方法覆盖报告；
- 所有观点/Skill 仍为待人工审核和待评测，不提前批准；
- `pytest`、`ruff`、`pyright`、SQLite integrity/foreign key 和 Git 隐私扫描通过。

## 10. 停止条件

只有遇到无法从源对象、现有 Schema 或明确项目规则判断的语义选择，才暂停对应候选并进入人工审核队列；单个作者或单个类别不确定时继续处理其他来源。平台访问限制不阻止本地材料和已保存线上内容的确定性蒸馏。

## 11. 实施进度

- [x] K5.6a：统一定位、规范化单元、精确重复链、版本化多标签分类、SQLite/Parquet/ObjectStore 和断点恢复。
- [x] K5.6b：作者清洗/方法覆盖报告、私有审核队列、四位作者真实运行与逐作者对象/索引审计。
- [x] K5.6c 自动链：私有原文摘录草稿、空规则 Skill 候选、来源回链、人工审核与评测门禁；未经审核不批准。
- [ ] K5.6c 人工链：把摘录综合为可测试观点、补适用范围/反证/失效条件，执行评测并作批准或拒绝决定。
- [x] K5.6d：四位作者真实自动运行、幂等复跑、对象/引用/审批门禁和运行库终验。

K5.6c～K5.6d 自动链真实结果为 401 条 `PENDING` 摘录草稿和 40 个 `NOT_RUN/PENDING` Skill 候选；黄彦臻现有唯一材料未达到生成门槛，结果保持 0/0。验收依据见 `docs/验收报告.md（合并来源：Phase5-K5.6c-K5.6d验收报告.md）`。

K5.6a～K5.6b 验收依据见 `docs/验收报告.md（合并来源：Phase5-K5.6a-K5.6b验收报告.md）`。


---

## 原文件：《Phase5-知乎采集开发计划.md》

# Phase 5 白名单知乎采集开发计划

计划日期：2026-07-17

状态：K5.1～K5.2 已实现并实测；K5.3～K5.5 因知乎登录/平台限制继续推进；K5.6 待先落专项蒸馏计划

K5.3～K5.5 的实现细化与完成门见 `docs/开发计划.md（合并来源：Phase5-K5.3-K5.5开发计划.md）`。

## 1. 已确认范围

线上采集只覆盖三位用户明确授权的白名单作者：

1. MR Dang：已确认主页 `https://www.zhihu.com/people/mr-dang-77`，可立即实测。
2. 黄彦臻：先自动发现并核对唯一身份；若存在同名歧义，只暂停这一位并在其他任务完成后报告。
3. 派大星皮皮：先自动发现并核对唯一身份；若存在同名歧义，只暂停这一位并在其他任务完成后报告。

“寒武纪的鳄鱼”不再访问知乎。用户确认本地 `寒武纪的鳄鱼 知乎文章.docx` 已包含该作者全部内容，系统只为它生成 `USER_CONFIRMED_COMPLETE_EXPORT` 本地覆盖报告，并进入后续结构分析和蒸馏。

线上内容类型为回答、想法、文章，以及理解作者参与所必需的问题上下文、评论分页和嵌套回复。范围不扩展到其他作者、全知乎或绕过当前账号不可访问的内容。

## 2. 完成定义

“采集完成”必须同时满足：

- 从第一页连续枚举到平台明确的终止条件；
- 每个成功响应先进入 ObjectStore，再推进对应检查点；
- 每个内容 ID 都有成功、失败、受限、重复或更新状态，不能凭空消失；
- 回答带问题 ID、标题、URL 与必要问题上下文；
- 评论原始分页完整保存，作者参与链另行派生，不能只留摘要；
- 每位作者、每种内容类型都有 `AuthorCollectionCoverageReport`；
- `403`、`429`、登录失效、安全验证或结构异常不能被算成“内容为 0”或“已经到底”；
- 原文、Cookie、浏览器 Profile 和私有材料不进入 Git、日志或测试夹具。

只有报告达到 `PAGINATION_COMPLETE` 或经过结构校验的 `CONFIRMED_EMPTY`，且 `missing_count=0`，才允许标记完整；否则保持 `PARTIAL` 或 `ACCESS_RESTRICTED`。

## 3. 访问顺序与认证边界

每次访问都通过 `SourceAccessRouter` 记录决策，固定顺序为：

1. Python `httpx` 的已验证结构化请求：优先尝试无需复制浏览器 Cookie 的公开/当前可访问接口，单线程、低频、可缓存。
2. MCP：只有存在已验证、能返回同一结构化内容的连接器时才使用；没有可用能力就记录并继续。
3. 已登录 Chrome/Browser：若结构化请求需要现有登录态，在页面上下文中发起同源请求并保存响应；不导出、打印或持久化明文 Cookie。
4. 人工任务：只处理身份歧义、验证码、必须由用户确认的登录或平台限制。

不伪造签名，不绕验证码，不轮换账号或代理规避限速，不使用重试风暴。默认每个作者串行采集、请求间隔可配置且不低于平台实测安全下限；收到限制后立即停止相关来源。

## 4. 数据合同

新增并版本化以下合同：

- `KnowledgeSourceDefinition`：白名单身份、主页、url token、权限、允许内容类型与本地替代来源。
- `ZhihuAuthorIdentity`：平台用户 ID、url token、显示名、主页、头像/简介哈希、确认依据和状态。
- `ZhihuListingPage`：作者、内容类型、请求游标、下一游标、`is_end`、内容 ID 列表、原始对象哈希和结构版本。
- `ZhihuContentRecord`：回答/想法/文章的稳定 ID、作者、标题或问题上下文、URL、创建/更新时间、正文对象哈希、原始快照 ID和版本关系。
- `ZhihuCommentPage` 与 `ZhihuAuthorParticipationChain`：父内容、根评论/回复游标、作者参与位置、上下文节点和原始对象哈希。
- `AuthorCollectionCoverageReport`：发现、调度、成功、失败、受限、重复、更新、缺失、最后游标、终止条件与缺口。

正文不放入 SQLite。SQLite 只保存任务、游标、版本、对象哈希、计数和错误元数据；原始 JSON/HTML 进入不可变 ObjectStore；规范化内容索引写入 runtime 下的 Parquet。DuckDB 只读这些 Parquet 事实，不复制正文。

## 5. 检查点与恢复

检查点分四层：

1. 作者 + 内容类型的列表游标；
2. 单个内容详情状态；
3. 单个内容的根评论分页游标；
4. 单个根评论的嵌套回复游标。

一次边界提交的顺序固定为：接收响应 → 分类 HTTP/结构状态 → 原始字节写 ObjectStore → 校验对象哈希 → 登记 SourceSnapshot/内容版本 → 更新计数 → 提交检查点。任一步失败都不推进游标。崩溃恢复从最后已提交边界重试，同一响应保持幂等，不重复计数。

内容的 `updated_time` 或正文哈希变化时保留旧版本并新增版本；列表重复但正文未变计入 `skipped_duplicate_count`。失败游标进入 `collection_gap`，成功补回后关闭缺口，但不删除历史失败记录。

## 6. Python 采集器实现

模块放入 `src/astock/knowledge/`，拆成：

- 配置加载与白名单门禁；
- HTTP/Chrome 响应传输接口；
- 知乎响应结构适配器；
- 作者身份发现与验证；
- 列表、详情、问题上下文、评论和回复枚举器；
- ObjectStore/Parquet/SQLite 仓储；
- 覆盖报告与恢复服务。

稳定 CLI 计划为：

- `astock knowledge-source-list`
- `astock zhihu-author-probe <source-id>`
- `astock zhihu-author-sync <source-id> [--content-type ...] [--max-pages ...]`
- `astock zhihu-coverage <source-id>`
- `astock knowledge-local-coverage <source-id>`

`--max-pages` 只用于低频 smoke，不得把主动截断报告成分页完成。生产全量同步从检查点继续，直到真实终止、受限或明确失败。

## 7. 身份发现规则

黄彦臻和派大星皮皮先通过搜索结果与公开主页候选发现，再用显示名、url token、简介、历史主题和用户已有材料交叉核对。自动确认至少需要一个唯一主页且无相互冲突候选；仅靠相同显示名不得确认。若不唯一，保留 `PENDING_IDENTITY_CONFIRMATION`，生成一条只阻塞该作者的人工任务，其他作者继续。

MR Dang 仍会读取主页元数据验证 `mr-dang-77` 与平台用户 ID，但不会因为显示名轻微变化而另造作者。

## 8. 测试与验收门

日常测试只使用人工构造的 recorded fixture，不提交真实作者正文。必须覆盖：

- 成功响应先存对象、后推进游标；
- 写对象后崩溃、写元数据前崩溃和检查点后重跑；
- 正常分页、确认空、重复游标、意外空页、字段缺失和结构变化；
- `403`、`429`、登录页、验证码页、超时和网络中断分类；
- 内容更新保留旧版本，重复内容不重复计数；
- 回答问题上下文、评论分页和嵌套回复恢复；
- 两个同名候选不能自动确认；
- Windows 中文路径、原子写、Parquet Schema 与 SQLite 外键；
- Git 隐私扫描确认无正文、Cookie、Profile、PDF、DOCX 或 runtime 泄漏。

低频 live smoke 必须显式启用，只抓最小页数验证结构并保存私有 runtime 快照。全量采集完成后，再运行全仓 `pytest`、`ruff`、`pyright`、SQLite 完整性、对象哈希和每作者覆盖审计。

## 9. 实施顺序

1. [x] K5.1：完善 Schema、配置校验、migration、仓储和 recorded fixture。
2. [x] K5.2：实现 Python 结构化请求、错误分类、游标恢复和覆盖报告。
3. [ ] K5.3：实测 MR Dang；Python 已确认想法为空、回答/文章需登录，Chrome 通道仍需恢复稳定采集。
4. [ ] K5.4：黄彦臻、派大星皮皮身份已自动确认；公开想法已分别完成 1 条、保存 615 条，后者及回答/文章仍有访问缺口。
5. K5.5：生成三位线上作者的逐类型覆盖报告，以及寒武纪的鳄鱼本地完整导出覆盖报告。
6. K5.6：在采集覆盖可审计后，另行先落“结构分析、差异化清洗与蒸馏计划”，再处理线上快照、MR Dang PDF 和寒武纪的鳄鱼 DOCX。

每个子里程碑形成独立测试与可回滚提交。遇到某位作者的真实访问限制，只暂停该来源并继续不依赖它的开发和本地材料分析；所有需要用户手动操作或注册的事项在其他可完成工作结束后统一报告。


---

## 原文件：《Phase6开发计划.md》

# Phase 6 委员会与选择性反方开发计划

计划日期：2026-07-17

状态：已完成并通过终验。本文保留开发前冻结的范围和顺序，实际交付及验证证据见 `docs/验收报告.md（合并来源：Phase6验收报告.md）`。Phase 5 的确定性蒸馏内核虽已实现，但线上覆盖及人工审核仍为 `PARTIAL`，不会因 Phase 6 完成而被提前标记完成。

## 1. 阶段目标

Phase 6 要交付一条可审计、可恢复、完全基于冻结工件的投资判断链：

```text
冻结输入核验
  -> 不可覆盖的硬门槛
  -> 覆盖度与证据缺口
  -> 确定性决策矩阵
  -> 选择性反方
  -> DecisionPack
  -> TradeProtocol
  -> 可选的 Codex 单次文字说明
```

这里的“委员会”不是多个 Agent 投票，也不负责重新找资料。它只读取已经通过 Schema、引用、PIT 和 Policy 校验并登记到 ArtifactStore 的工件。相同输入哈希、规则版本和请求必须产生相同的 `decision_id`、决定内容和对象哈希。

## 2. 明确不做

- 不联网，不调用 API、MCP、Browser/Chrome，不打开完整 PDF/DOCX，不重抓公告或知乎。
- 不让 Codex、外部 LLM 或自然语言字段修改硬门槛、数值结果、仓位上限和 verdict。
- 不因缺证据而“降低语气后继续”；关键缺口只能是 `NEEDS_INFO`。
- 不让社区内容成为关键事实的唯一证据。
- 不直接创建模拟订单、修改账本或连接券商；TradeProtocol 只是冻结规则工件。
- 不实现真实下单、杠杆、动态权重、上下文赌博机或强化学习。
- Phase 5 未经人工批准的候选 Skill 不能进入委员会的生产 Skill 版本集合。
- 不把私有 PDF、DOCX、Cookie、浏览器 Profile、原文或 `runtime/` 提交到 Git。

## 3. 冻结输入边界

### 3.1 允许的工件

首版只接受已登记、可重算哈希且类型在白名单中的工件：

- `FrozenEvidencePack`
- `BaseCasePack`
- `SpecialistDelta`
- `SpecialistDiagnosticReport`
- `ResearchMemoArtifact`
- `FinancialIntegrityEvidencePack`
- 已审核的人工补证工件
- `PositionMonitoringPlan`
- `HoldingEvidenceUpdate`
- `HoldingReviewPack`
- `PositionActionProposal`
- `CounterCasePack`
- 结构化的组合状态与市场状态快照

输入引用固定 `artifact_id`、`artifact_type`、`object_sha256`、角色和是否必需。服务必须同时核对 ArtifactRegistry、对应安全索引、ObjectStore 实际字节哈希和领域 Schema。未知 ID、类型不符、索引哈希不符、对象损坏、公司不一致、`as_of` 穿越或未批准 Skill 一律拒绝，不能留下半个委员会运行。

### 3.2 CommitteeAccessPolicy

每个委员会请求都固化现有 `CommitteeAccessPolicy`：network、API、MCP、browser、全文读取和新研究全部只能是 `false`；缺证据动作只能是 `NEEDS_INFO`，且必须生成调查任务。该 Policy 同时进入请求哈希和审计结果，不能只存在于文档里。

### 3.3 冻结集合

新增版本化 `CommitteeInputBundle`，保存排序后的输入引用、公司、决策范围、`as_of`、规则版本、Skill 版本、访问策略和输入集合哈希。完整内容进入 ObjectStore；SQLite 只保存 ID、类型、状态、数量、哈希和时间等安全索引。

## 4. 结构化决策输入

委员会不从自由文本里猜数值。新增 `CommitteeDecisionRequest`，要求调用方显式提供并用冻结 evidence/artifact 引用支持以下信号：

- 决策范围：新候选、现有模拟仓位或用户声明的只读监控仓位；
- 预期收益区间、下行区间、置信度和持有期；
- 数据、证据、PIT、专家覆盖和流动性分数；
- 可交易性、数据质量、核心来源冲突、社区唯一证据、财务硬阻断；
- 专家分歧、重大新公告、证伪条件接近、组合风险变化；
- 当前仓位、计划仓位和持仓建议；
- TradeProtocol 所需的入场、仓位、退出、复核和版本化成本/成交规则。

所有比例使用有界 Decimal，所有时间使用带时区时间，所有列表排序去重。收益、下行、仓位和信号必须引用冻结集合中的证据或父工件；缺引用不能变成投资结论。

## 5. 不可覆盖的硬门槛

新增 `configs/committee_rules.yaml`，配置带 `rule_set_id`、`version`、`effective_from` 和内容哈希。首版硬门槛及固定优先级为：

1. `REJECT`：不可交易、明确禁止标的、手动紧急停止、杠杆请求、严重财务硬阻断或确定的核心失效条件。
2. `NEEDS_INFO`：数据质量失败、关键 PIT 不安全、核心来源冲突、关键事实只有社区证据、必需证据/专家覆盖不足、触发反方但缺少合格 CounterCasePack。
3. `PAPER_EXIT`：现有仓位触发已冻结的退出或 thesis invalidation；不得被较乐观收益区间覆盖。
4. `PAPER_HOLD`：现有仓位无退出/减仓硬信号且通过前述门槛；只表示模拟持有/继续观察，不自动成交。
5. `PAPER_ELIGIBLE`：新候选通过全部门槛，覆盖、流动性、风险收益和仓位均满足版本化阈值。
6. `WATCH`：研究材料完整但没有达到模拟候选阈值，或已有减仓/复核信号但不构成强制退出。

规则引擎只读取结构化字段。可选 narrative 即使给出相反建议，也只能产生审计 warning，不能修改 verdict、hard_blocks、needs_info、max_position 或 TradeProtocol 状态。

首版组合级风险闸门也固化在同一版本化规则中：计划后总暴露不高于 80%、单一行业暴露不高于 25%、与现有组合的最大绝对相关性不高于 80%；组合回撤达到 20% 或连续亏损达到 5 次时冻结新增风险。重大公告待核验或数据异常冻结进入 `NEEDS_INFO`。这些是可审计的首版保守默认值，不是收益承诺；以后只能创建新规则版本，不能回写历史决定。

## 6. 选择性反方

CounterCase 只在下列任一条件发生时触发，不对每个候选无条件增加成本：

- 计划仓位超过配置阈值；
- BaseCase、专家或持仓建议存在实质冲突；
- 财务异常或证据覆盖接近最低线；
- 潜在收益高但关键假设仍弱；
- 多专家严重分歧、重大新公告、证伪条件接近；
- 组合风险显著变化。

新增 `CounterCaseTriggerReport` 和 `CounterCasePack`。CounterCasePack 只能挑战冻结集合里的 claim/evidence，列出替代解释、下行路径、未决问题、引用和估算成本，不能引入未登记事实。触发后没有合格反方包时，结果固定为 `NEEDS_INFO` 并生成调查任务；未触发时不要求构造“形式上的反方”。

## 7. NEEDS_INFO 闭环

新增通用、委员会专用的 `CommitteeInvestigationTask`，字段覆盖 task_id、关联 decision/bundle、reason code、重要性、决策影响、优先级、预估时间、建议来源、搜索词、步骤、所需材料、支持/反驳信号、停止条件、替代证据、状态和解决工件引用。

流程为：

```text
委员会发现缺口
  -> 冻结 NEEDS_INFO DecisionPack
  -> 创建 OPEN 调查任务
  -> 上游或人工取得新证据并单独冻结
  -> 创建新的 CommitteeInputBundle
  -> 产生新的 decision_id
```

旧决定和旧任务不回写历史。解决任务只登记新的解决工件 ID/哈希；委员会不会自行执行调查步骤。

## 8. DecisionPack 与 TradeProtocol

### 8.1 DecisionPack

新增不可变 `DecisionPack`，至少包含：decision_id、bundle_id、冻结输入哈希、规则/Skill 版本、verdict、预期收益区间、下行区间、置信度、hard_blocks、needs_info、反方触发结果、最大仓位、复核时间、结构化 rationale code、evidence IDs、决定哈希和创建时间。

`decision_id` 由规范化请求、冻结输入集合哈希、规则版本和引擎版本确定，不含运行时随机数。相同请求幂等；相同 ID 对应不同内容时按身份冲突拒绝。

### 8.2 TradeProtocol

每个 DecisionPack 都生成一份版本化 TradeProtocol：

- `PAPER_ELIGIBLE`、`PAPER_HOLD`、`PAPER_EXIT` 可生成对应的模拟资格/持有/退出协议；
- `WATCH`、`NEEDS_INFO`、`REJECT` 仍生成 `BLOCKED` 协议，明确为什么不可执行，避免“没有协议就被旁路”；
- 包含 signal_time、earliest_executable_time、持有期、入场与仓位规则、价格/波动/移动/时间止损、thesis invalidation、止盈、复核事件、最大持有期、成本模型、成交模型和 evidence snapshot；
- 所有协议 `requires_user_confirmation=true`，不得携带真实券商命令，不写订单、成交、持仓或账本表；
- 修改规则只能创建新 protocol_id 和有效时间，不能覆盖旧对象。

## 9. 上下文预算与可选 Provider 降级

在现有 `ContextBudgetReport` 上增加委员会预算结果，实际统计冻结工件字节、估算文本 Token、重复输入和可选 narrative 成本。委员会的 full_documents、browser、MCP 和 API 步数必须恒为 0。

预算超限按固定顺序降级：

1. 禁用可选 Codex/Provider narrative；
2. narrative 只读取 DecisionPack 和必要引用摘要，不读取所有父工件；
3. 外部 Provider 未启用、探针失败或估算成本超过 ceiling 时保持 `DETERMINISTIC_ONLY`；
4. 必需确定性输入不能为了省预算被静默删除；若必需结构化工件本身不完整则 `NEEDS_INFO`。

首版不主动调用任何外部 LLM Provider，只交付配置、能力状态和确定性降级证明。Codex 可通过 P4.5 已有的严格运行清单包装已登记的 CounterCasePack、DecisionPack 或 TradeProtocol，不能导入一个未登记的替代决定。

## 10. 持久化与恢复

新增 migration `0027_committee.sql`，预计包含：

- `committee_bundle_index`
- `committee_bundle_input_index`
- `counter_case_index`
- `committee_decision_index`
- `committee_trade_protocol_index`
- `committee_investigation_task_index`

SQLite 不保存 thesis、全文 rationale、原始摘录、搜索词、规则正文或私有内容；这些只进不可变 ObjectStore。各表使用外键、唯一约束、状态检查和父哈希，支持在“对象已写、索引未写”的故障后恢复。恢复只能根据已存在的请求/对象补登记，不能重新研究或换输入。

## 11. CLI、审计与 Repo Skill

计划增加稳定命令：

- `committee-plan`：只校验输入、展示预算和反方触发，不落决定；
- `committee-decide`：生成/幂等读取 bundle、DecisionPack、TradeProtocol 和必要调查任务；
- `committee-status`：读取安全索引状态；
- `committee-audit`：核对 registry、ObjectStore、父哈希、决定矩阵、硬门槛和协议；
- `committee-recover`：补齐可证明的中断写入；
- `committee-task-status`：只读调查任务状态；
- `probe`：公开委员会规则版本、冻结输入类型、Provider/narrative capability 和无网络策略。

更新 `$astock-research-orchestrator`、`$company-deep-research`、`$holding-monitor` 和 `$evidence-investigation` 的真实命令路由。Repo Skill 只能调用稳定 CLI，不能直接写 SQLite、绕过委员会或声称已成交。

## 12. 分段实现顺序

### P6.1 冻结合同与配置

交付 Schema、规则配置、严格输入解析、bundle 哈希、migration 和仓储；先证明委员会无法获得联网 capability。

### P6.2 硬门槛与选择性反方

交付确定性 hard gate、覆盖度门槛、决策矩阵、CounterCase 触发和缺失反方的 `NEEDS_INFO`。

### P6.3 决策、协议与调查闭环

交付 DecisionPack、BLOCKED/ACTIVE TradeProtocol、调查任务、幂等写入、Codex 严格包装和不可覆盖性测试。

### P6.4 CLI、预算、恢复与全链终验

交付预算降级、status/audit/recover、Repo Skill、真实库 migration 和 Phase 4 → Phase 6 合成全链验收。

## 13. 自动化测试与验收

1. CommitteeAccessPolicy 任一联网/全文/新研究字段为 true 时 Schema 直接拒绝。
2. 未登记、类型/哈希不符、对象损坏、公司不一致、未来工件、未批准 Skill 和不在白名单的输入全部拒绝且无半写。
3. 相同冻结输入、请求、规则和引擎版本重复运行，bundle、decision、protocol 与对象哈希完全相同。
4. 不可交易、紧急停止、杠杆、严重财务硬阻断不能被乐观 narrative、收益区间或高置信度覆盖。
5. 关键证据缺失、核心冲突、社区唯一关键事实、低 PIT/覆盖和必需反方缺失固定为 `NEEDS_INFO`，并生成可审计 OPEN 任务。
6. EXIT 优先级高于 HOLD/高收益；现有仓位与新候选分别走正确 verdict 分支。
7. CounterCase 只在配置触发条件出现时要求；包内引用超出冻结集合、引入未知 claim/evidence 或公司不一致时拒绝。
8. 每个 DecisionPack 都有 TradeProtocol；非可执行 verdict 必须是 BLOCKED，所有协议必须人工确认且不能写账本。
9. 预算超限禁用可选 narrative，但确定性决定保持相同；Provider disabled/探针失败/超成本不影响主链。
10. 故障注入后 recover 能补齐安全索引；坏对象、变更规则、输入不全时明确不可恢复。
11. audit 可发现输入索引、ArtifactRegistry、对象、决定、协议或任务的篡改；不会通过重新联网修复。
12. P4 冻结 Evidence → BaseCase → 专家/诊断/memo → 可选持仓复核 → P6 bundle/decision/protocol 合成链实际跑通。
13. SQLite migration、`PRAGMA integrity_check`、外键检查、隐私字段检查通过；私有原料和 runtime 仍被 Git 忽略。
14. 全仓 `uv run pytest`、`uv run ruff check .`、`uv run pyright` 通过；live/昂贵测试继续由明确环境开关控制。

## 14. 可回滚点

- 每个 P6 子阶段独立提交；规则配置和 Schema 先于服务代码提交。
- 新表只追加，不修改既有 Phase 1～5 表语义；回滚代码时旧 Artifact/ObjectStore 仍可读取。
- 规则升级创建新版本，不改旧规则文件对应的历史决定。
- 错误 DecisionPack 通过新输入/新版本重算，不删除或覆盖旧决定。
- 调查任务解决或取消只改变安全状态索引并引用新工件，不擦除历史对象。

## 15. 预算与完成定义

工期参考 10～15 个开发日；本地自动化主路径不产生外部模型费用，网络调用预算为 0。可选 Provider 默认关闭，只有未来用户主动配置并通过探针后才可能计费。

冻结输入、不可覆盖硬门槛、选择性反方、NEEDS_INFO 闭环、DecisionPack、每决定一份 TradeProtocol、预算降级、Codex 严格包装、恢复审计、真实 migration 和全仓验收均已由代码与测试证明，Phase 6 于 2026-07-17 标记完成。计划文件、Schema 存在或单个 happy-path 测试不能代替终验；详细证据见 `docs/验收报告.md（合并来源：Phase6验收报告.md）`。


---

## 原文件：《Phase7开发计划.md》

# Phase 7 冻结权重影子评测开发计划

计划日期：2026-07-17

状态：程序层已于 2026-07-17 实现并通过终验；本段保留开发前冻结的原始口径。真实 study、委员会决定、协议和 observation 仍为 0，当前证据状态为 `NOT_RUN`。Phase 5 的线上覆盖与人工 Skill 审核仍为 `PARTIAL`；合成测试样本没有写入真实投资证据。

## 1. 阶段目标与完成口径

Phase 7 不是寻找“近期最赚钱的专家”，而是回答一个更窄、可检验的问题：在同一批候选、同一时间点、同一 TradeProtocol、同一成本和成交假设下，加入某个专家或完整委员会后，是否在严格样本外对 BaseCase 产生了稳定、扣费后仍为正的增量价值。

主链为：

```text
冻结评测定义和权重
  -> 同一候选集的多臂影子分配
  -> 决策时点前 PIT/引用/版本核验
  -> 决策后不可变成交与结果观察
  -> 确定性市场状态标签
  -> 按独立决策聚类和成熟期过滤
  -> 逐时点 walk-forward 样本外折
  -> 成对指标、置信区间和稳定性检验
  -> ShadowEvaluationReport
  -> Phase8AdmissionReport
```

完成状态必须分成两层：

1. `IMPLEMENTED`：Schema、配置、存储、计算、CLI、审计、恢复和自动测试已交付。
2. `EVIDENCE_READY`：真实前向观察达到时间、样本、市场状态、PIT、成交质量和统计门槛。

代码完成不等于证据成熟。少于 6 个月只能是 `COLLECTING`；6～12 个月最多为 `PROVISIONAL`；Phase 8 的研究准入要求至少 12 个月。当前真实决定为 0，因此本阶段开发完成后的真实状态预期是 `NOT_RUN` 或 `COLLECTING/INSUFFICIENT_SAMPLE`，绝不能提前写成 `EVIDENCE_READY`。

## 2. 明确不做

- 不根据短期盈亏改权重，不在线学习，不人工回填“当时应该用的权重”，不删除表现差的臂或决定。
- 不实现规则动态权重、上下文多臂赌博机、PPO/A2C/RL；它们属于 Phase 8，且必须先通过本阶段准入门。
- 不用 `NOT_PIT_SAFE` 的当前 Provider 值冒充正式历史样本，不用今天的指数成分或幸存公司列表覆盖历史。
- 不读取未来公告、未来修订值、未来市场状态或未来收益来决定候选、权重、分组、排除项和协议。
- 不让不同臂使用不同候选集、成本、成交模型、信号时间或执行时点来制造优势。
- 不把 Phase 5 的 `PENDING/NOT_RUN` 候选 Skill 当作批准 Skill；未批准 Skill 只能在隔离的研究标签下运行，不能取得 Phase 8 准入。
- 不连接券商、不生成真实订单、不修改用户主模拟账户。影子“账户”是隔离的不可变评测臂与 NAV 工件，不复用真实/主模拟账本身份。
- 不让 Codex、外部 LLM 或自由文本计算收益、置信区间、市场状态、样本数、最大回撤或准入结论。
- 不把私有 PDF、DOCX、知乎正文、Cookie、浏览器 Profile、原始 runtime 或真实私密持仓写入 Git、测试夹具或报告正文。

## 3. 版本化配置和冻结研究定义

新增 `configs/shadow_evaluation.yaml`，首版至少冻结：

- `policy_version`、`engine_version`、`effective_from`、配置内容哈希；
- 初始资金、固定名义仓位、最大单股/行业/总暴露；
- 评测周期 5、20、60 个交易日，最终准入只使用已成熟的 60 日结果；
- 手续费、印花税、过户费、滑点、参与率、成交和公司行为版本；
- 市场状态规则版本、独立决策规则版本、统计方法版本和确定性随机种子；
- 最小样本、观察期、折数、每折样本、PIT、路径不确定、回撤和稳定性门槛；
- 正式、探索和隔离 Skill 的允许状态。

新增 `ShadowStudyManifest`。创建后固定 study ID、创建时间、观察起止边界、候选选择规则、候选集合快照规则、比较臂、权重、Skill 版本、BaseCase/委员会规则版本、成本/成交/公司行为版本、基准定义和配置哈希。任何字段变化必须创建新 study/version，旧研究只读。

权重只能是预先声明且总和严格等于 1 的 Decimal。`created_at/effective_from` 晚于某个决定时，该研究不得把该决定算作正式样本。历史重放即使 PIT 合格，只要规则/权重没有在结果发生前冻结，也只能标为 `EXPLORATORY_RETROSPECTIVE`。

## 4. 必需比较臂和公平性

每个正式研究至少配置以下逻辑影子账户：

1. `RULE_BASELINE`：只用确定性规则基准。
2. `BASE_CASE_ONLY`：只用冻结 BaseCase，不读取 SpecialistDelta。
3. `BASE_CASE_PLUS_SPECIALIST`：每个被评测专家单独一个臂；不得把多个专家混在“单专家”臂里。
4. `FULL_COMMITTEE`：使用 Phase 6 的完整决定和协议。
5. `APPROVED_SKILL`：只为已人工批准且版本冻结的重要 Skill 建独立臂；未批准候选只能使用 `RESEARCH_ISOLATED` 标签。
6. `CSI300_BENCHMARK` 或 `CHINA_ALL_BENCHMARK`：至少一个可在相同时点重建且成分/PIT 合格的指数基准。
7. `EQUAL_WEIGHT_CANDIDATE`：对当时可见的同一候选集做简单等权基准。

新增 `ShadowArmDefinition` 和 `FrozenWeightProfile`。所有可比较研究臂必须绑定相同：

- `candidate_set_id` 和当时可见的候选成员快照；
- `signal_time`、`earliest_executable_time` 和持有期；
- TradeProtocol 家族、成本、成交、公司行为、复核与退出模型；
- 初始资金、固定名义风险和再平衡日历；
- 股票停牌、涨跌停、T+1、100 股整数、退市和不可成交处理。

臂之间只允许研究组件不同。若候选、时点、协议或成交口径不相同，比较状态固定为 `NOT_COMPARABLE`，不能计算“专家增量”。

## 5. 决策分配、独立性和防止事后挑样本

新增 `ShadowDecisionAssignment`，在结果可见前登记每个 decision/candidate 应进入的全部臂。分配对象保存 decision、BaseCase、SpecialistDelta、DecisionPack、TradeProtocol、候选集、权重、Skill 版本和对象哈希。

同一个正式 study 中：

- 分配一旦登记不能删除；拒绝、`NEEDS_INFO`、`WATCH` 和“无动作”也必须保留，避免只统计买入样本。
- 同一候选必须同时进入所有有资格比较的臂；某臂缺输入时记录 `ARM_INPUT_UNAVAILABLE`，不能悄悄删掉该候选。
- 每个公司/研究事件使用冻结的 `independence_key`。同一 thesis episode 或 60 个交易日结果窗口重叠的决定只算一个独立聚类；只有出现新的官方事件/新 thesis 版本且前一暴露结束后，才允许新独立 key。
- 服务强制 `(study_id, independence_key, arm_id)` 唯一；统计按 independence key 成对，不把同一事件的多个复核当成多个样本。
- 新臂、新 Skill、新权重或排除规则只能创建新 study，不能追溯加入已经看见结果的旧研究。

## 6. 不可变执行与结果观察

新增 `ShadowExecutionObservation`，只接受可重算的结构化执行事实：

- study/arm/assignment/decision/protocol/independence key；
- symbol、market、signal/entry/exit/valuation 时间；
- 动作、股数、入场/退出价格、公司行为现金流、毛盈亏；
- commission/tax/transfer/slippage、净盈亏、成交额、换手和 NAV 前后值；
- MFE、MAE、持有交易日、流动性、成交参与率；
- replay quality、行情 observation/manifest 哈希、成本和成交版本；
- `ambiguous_intrabar_path`、保守结果和乐观敏感性结果；主结果永远使用保守值；
- 所有决策输入的 PIT 状态、可得时间、source snapshot 和排除原因。

服务重新计算金额恒等式、费用、收益率和 NAV，拒绝直接提交一个无法回到成交/估值事实的“收益率”。入场不得早于 `earliest_executable_time`；观察和退出不得早于入场；60 日结果未成熟时只能是 `PENDING_MATURITY`。

正式样本要求：决定及其输入在 signal time 前已冻结，结果行情在 signal time 后观测，关键输入均为 `CERTIFIED` 或 `DOCUMENT_RECONSTRUCTED`，或是有实时 fetch 时间证明的前向不可变快照。`APPROXIMATED` 单列敏感性结果；`NOT_PIT_SAFE` 只进入探索报告，绝不能取得 Phase 8 准入。

## 7. 确定性市场状态

新增 `MarketRegimeSnapshot`，至少保存指数 60 分钟/日线趋势、市场宽度、新高新低、成交额、行业扩散、波动率分位、回撤、风格相对表现和模拟策略表现；“多数策略亏损”只能是一个特征，不能单独决定状态。

首版 `market-regime-v1` 使用固定优先级：

1. `PANIC`：指数回撤不高于 -12%，且波动率分位至少 85% 或市场宽度不高于 20%。
2. `HIGH_VOL_BULL`：日线趋势分数至少 0.20、60 分钟趋势非负、宽度至少 55%、波动率分位至少 70%。
3. `TREND_BULL`：日线趋势分数至少 0.20、60 分钟趋势非负、宽度至少 55%。
4. `TREND_BEAR`：日线趋势分数不高于 -0.20、60 分钟趋势非正、宽度不高于 45%。
5. `RANGE`：核心字段齐全但不满足以上条件。
6. `UNCLASSIFIED`：核心字段缺失、PIT 不安全或时间线冲突。

特征和状态都必须引用当时可得的冻结快照。规则版本改变只产生新状态版本，不覆盖旧标签。

## 8. Walk-forward 和样本外窗口

固定权重不需要训练，因此不设置可调训练权重。所有正式前向决定从 study 生效起即为样本外；评测按 decision time 排序，禁止随机打乱。

- 每个拟评价/调权 Skill 至少 100 个已成熟、相互独立、可成对比较的决定。
- 至少 5 个连续 walk-forward 样本外折，每折至少 20 个独立决定。
- 折边界按时间和 independence key 切分；跨边界且结果窗口重叠的 episode 整体归入较晚折，形成最长 60 交易日 purge，避免结果泄漏。
- 至少覆盖 3 个非 `UNCLASSIFIED` 市场状态，每个至少 30 个独立决定。
- 观察跨度少于 6 个月为 `COLLECTING`；6～12 个月为 `PROVISIONAL`；Phase 8 准入至少 12 个月。
- 5/20 日结果只作早期诊断；正式专家增量和准入只使用已成熟 60 日结果和完整退出结果。
- 同一 independence key 在基准和对照臂缺任一成熟结果时，从成对效应中整体排除并报告原因，不能只保留表现较好的单边。

## 9. 指标、置信区间和多重比较

每个臂、折、市场状态和 Skill 至少报告：

- 扣费后收益、超额收益、NAV、最大回撤；
- MFE、MAE、盈亏比、胜率、持有周期、换手和流动性；
- 总费用、未成交/部分成交、路径不确定比例和保守/乐观差异；
- 1 分钟与 5 分钟差异（仅在合格 1 分钟局部数据真实存在时，不要求默认抓取 1 分钟）；
- PIT 覆盖率及四类状态分布、退市覆盖、历史成分覆盖、被排除样本和原因；
- 决策数量、独立聚类数、成熟数、各状态数和各折数。

专家增量使用同一 independence key 的成对净收益差。`shadow-statistics-v1` 使用固定 hash seed、2,000 次、长度 5 的移动块 bootstrap 计算 95% 百分位置信区间；输入排序和抽样索引完全确定，同一输入哈希必须得到相同结果。胜率同时报告 Wilson 区间。多个 Skill 同时申请升级时，以家族显著性 5% 做 Holm 校正，不能从许多候选中只挑偶然赢家。

所有指标保留精确分母、排除数和 Decimal 序列；报告不能只给一个平均收益数字。

## 10. Phase 8 准入硬门槛

新增不可变 `Phase8AdmissionReport`。只有同时满足以下条件，状态才可为 `ELIGIBLE_RULE_STATE_MACHINE_RESEARCH`：

1. 至少 12 个月、100 个独立成熟决定、5 折且每折至少 20 个、至少 3 个市场状态且每状态至少 30 个。
2. 正式决定输入 PIT 安全率 100%，`NOT_PIT_SAFE=0`；退市/历史成分/公司行为关键覆盖无未解释缺口。
3. 可回放率 100%，`UNREPLAYABLE=0`；双源验证或等价官方重建覆盖至少 90%；路径不确定比例不高于 5%，主结果使用保守路径。
4. 相对 BaseCase-only 的扣费后成对净收益差，Holm 校正后的 95% 置信区间下界大于 0。
5. 至少 4/5 walk-forward 折的增量点估计为正；三个必需市场状态中没有一个呈现置信区间完全低于 0 的明确伤害。
6. 增量臂最大回撤不高于 25%，且不比 BaseCase-only 恶化超过 2 个百分点。
7. 单个决定贡献不超过总正收益的 20%，单一市场状态贡献不超过 70%，避免结果由一个偶然事件或单一行情支配。
8. Phase 6 的所有硬风险门仍成立，权重/候选/协议/成本/成交版本无事后改变，审计为 `PASS`。

未满足样本门返回 `NOT_ELIGIBLE_INSUFFICIENT_SAMPLE`；数据/PIT/比较完整性失败返回 `NOT_ELIGIBLE_INTEGRITY`；样本足够但没有稳定增量返回 `NOT_ELIGIBLE_NO_INCREMENT`。这些都是正常研究结果，不允许人工改成通过。准入只允许进入 Phase 8 的“规则状态机研究”，不允许直接进入赌博机、RL 或真实交易。

## 11. 存储、恢复和唯一事实源

新增 migration `0028_shadow_evaluation.sql`，计划包含：

- `shadow_policy_index`
- `shadow_study_index`
- `shadow_arm_index`
- `shadow_assignment_index`
- `market_regime_index`
- `shadow_observation_index`
- `shadow_evaluation_run_index`
- `shadow_report_index`
- `phase8_admission_index`

完整 manifest、权重、分配、成交观察、折明细、指标序列、bootstrap 结果和准入报告只进入 ObjectStore；SQLite 只保存 ID、版本、哈希、状态、时间、数量和安全统计，不保存私有 thesis、原文、完整成交路径或本地路径。

Parquet 保存可重建的结构化事实序列；DuckDB 只建视图。每个对象记录父哈希和配置哈希。对象已写而索引未写的中断可以补登记；索引存在但对象损坏、输入改变、规则版本改变或成对集合不完整时明确不可恢复，必须新建 run/study。

## 12. CLI 和 Repo Skill 路由

计划增加稳定命令：

- `shadow-schema`：输出 Schema/规则版本和边界。
- `shadow-study-plan`：只校验定义、公平性和预计样本需求，不持久化。
- `shadow-study-create`：创建冻结 study 和臂。
- `shadow-assign`：在结果可见前登记成对分配。
- `market-regime-classify`：从冻结特征生成确定性状态。
- `shadow-observation-record`：校验并登记不可变执行观察。
- `shadow-evaluate`：只读取冻结对象，生成折、指标、置信区间和报告。
- `shadow-status`：返回安全状态、样本/成熟/状态/折计数。
- `shadow-audit`：核对对象、索引、时间线、PIT、公平性、成对集合和计算哈希。
- `shadow-recover`：补齐可证明的中断索引，不重算研究输入。
- `phase8-admission`：生成或读取确定性准入报告；默认预期为不具备资格。

`$astock-research-orchestrator` 负责宽泛状态路由；`$candidate-scan`、`$company-deep-research` 只生成上游冻结研究；`$paper-trading-recovery` 仍只管理用户模拟账户，不得被影子评测借用来绕过隔离；Repo Skill 不直接写 SQLite。

## 13. 分段实现顺序

### P7.1 合同、配置和持久化

交付 Schema、`shadow_evaluation.yaml`、配置哈希、migration 0028、repository 和对象/索引身份；先证明当前真实状态是 `NOT_RUN`。

### P7.2 冻结 study、公平分配和市场状态

交付 study/arm/weight、同候选同协议约束、独立 key、结果前分配和 `market-regime-v1`；未批准 Skill、未来输入和事后加臂必须拒绝。

### P7.3 执行观察、成熟度和成对集合

交付结构化执行观察、金额/费用/NAV 复算、PIT/时间线/回放质量、保守路径、5/20/60 日成熟状态和成对排除报告。

### P7.4 Walk-forward、统计和准入

交付连续折、成对指标、确定性 block bootstrap、Wilson、Holm、多状态/稳定性/回撤门和 Phase8AdmissionReport。

### P7.5 CLI、审计、恢复、Repo Skill 和终验

交付全部 CLI、审计/恢复、状态探针、Repo Skill、合成性质测试、真实 migration 和空真实库终验。真实 observation 不足时必须保留 `NOT_RUN/INSUFFICIENT_SAMPLE`。

每个子阶段独立提交并推送；计划文件本身先于任何 Phase 7 代码提交。

## 14. 自动化测试与验收矩阵

1. 权重和为 1、版本/时区/Decimal/排序/唯一性、状态机和金额恒等式 Schema 测试。
2. 相同 study/输入/config 产生相同 ID、对象哈希、折、bootstrap 索引、指标和准入结果。
3. study 生效前决定、决定后才可得的输入、未来市场状态、未来修订和 `NOT_PIT_SAFE` 正式样本均被拦截。
4. 结果出现后添加臂、删除失败决定、改变权重/候选/协议/成本或只排除对照单边均被拒绝。
5. 同一 episode 多次复核只计一个独立聚类；跨折重叠结果窗口被整体 purge，不发生泄漏。
6. 六类市场状态固定优先级、边界值、缺字段和相同输入稳定性测试。
7. 5/20/60 日成熟边界、交易日而非自然日、T+1、停牌、涨跌停、100 股、部分成交、退市和公司行为测试。
8. 毛盈亏、费用、净盈亏、收益率、NAV、MFE/MAE、回撤、换手和路径敏感性由原始字段重算；直接伪造收益拒绝。
9. 同时触发止盈/止损时主结果取保守路径；合格局部 1m 不存在时不补造。
10. 成对集合缺一臂时整体排除且计数；BaseCase 与专家使用不同候选/协议时 `NOT_COMPARABLE`。
11. bootstrap、Wilson、Holm 用手算小样本、常量序列、全赢/全输、负收益和空样本验证；空/不足样本不产生伪置信区间。
12. 99、100 个独立决定，29/30 个状态样本，5.9/6/12 个月，4/5 折和回撤/路径阈值边界测试。
13. 有正均值但 CI 跨 0、单一事件/状态贡献过高、回撤恶化或一折严重亏损时不得准入。
14. 故障注入后 recover 只补安全索引；对象、索引、父哈希、计算结果或准入报告篡改被 audit 发现。
15. 影子服务不能接受用户主模拟账户 ID，不能连接券商，不能修改已有 paper account/order/fill/position/journal 数量。
16. 私有原料、正文、Cookie、Profile、runtime 和真实成交路径不进入 Git；CLI 错误不回显私密 payload。
17. 全仓 `uv run pytest`、`uv run ruff check .`、`uv run pyright` 通过；live/长观察测试继续由显式环境门控制。

## 15. 可回滚点

- migration 只新增表，不改 Phase 1～6 的既有语义；停用 Phase 7 代码不影响已有 Artifact、委员会或模拟账本。
- 配置和统计方法升级必须新建 policy/study 版本，旧报告和准入结论永久保留。
- 错误观察通过新版本 supersede，不物理删除；正式报告明确使用的版本集合。
- 某专家失败只回退/禁用对应研究版本，不删除 BaseCase、其他专家或历史样本。
- Phase 8 研究失败时回到本阶段固定权重基线；不回写历史权重或重选样本。

## 16. 预算、人工事项和阶段交付边界

本地确定性开发不产生外部 LLM/API 费用；日常测试使用合成完整 PIT/成交夹具，不联网。真实前向观察沿用已有 Provider、对象缓存和低频采集策略，所有外部 live smoke 由显式开关控制。

开发期预计 5～8 个开发日；真实证据观察期至少 6～12 个月，不能压缩成一次回测。Phase 7 的程序交付时会报告当前真实样本数、观察跨度和缺口。只有用户未来批准候选 Skill、处理上游平台访问限制或提供真实人工监控仓位语义时，才需要人工介入；这些事项统一在全部开发工作后汇总，不在本阶段中途打断其他可完成任务。

## 17. Phase 7 实施结果与当前边界

- P7.1～P7.5 均已交付；migration 为 `0028`～`0030`，程序提交为 `a9331c8` 和 `3259524`。
- 观察事实明确区分未下单、未成交、部分成交和全部成交；金额、费用、收益率、NAV、MFE/MAE、换手、成交参与率和保守/乐观路径均能从结构化事实复算。
- 成对比较显式报告缺失基准或缺失实验臂，不允许只取有利单边；市场状态贡献按 `PANIC`、`RANGE` 等状态聚合，而不是按单条快照 ID 分散。
- 报告和准入结果会从冻结输入重新计算；对象、父链、ArtifactRegistry、SQLite 索引和 Parquet 任一层损坏都进入审计 finding。恢复只补可证明的索引/Parquet，不重写研究输入。
- 12 个 CLI 入口已可用：Schema、study plan/create、独立 key、分配、市场状态、观察、评估、状态、审计、恢复和 Phase 8 准入。
- 自动化结果为 `226 passed / 8 skipped`，Ruff 与 Pyright 全通过；8 个跳过项仍是显式环境门控制的 Phase 2 真实 OCR 基准和外网 live probes。
- 真实运行库已迁移到 `0030`，完整性为 `ok`、外键错误为 0；9 张 Phase 7 表全部为 0，状态为 `NOT_RUN`。主模拟账户保持 1 个账户、1 条初始化 journal、0 订单、0 成交、0 持仓，资金和 NAV 均未改变。
- Phase 7 的程序完成状态是 `IMPLEMENTED`；真实证据成熟状态是 `NOT_RUN`。在至少 12 个月、100 个独立成熟决定、5 个合格折和 3 个合格市场状态等硬门满足前，Phase 8 自适应研究保持禁用。

# Phase 8 可选自适应权重研究开发计划

计划日期：2026-07-17

状态：开发前冻结计划。Phase 7 程序层已通过终验，但真实 Phase 8 admission 为 `NOT_RUN`，真实观察月份、独立决定、成熟观察、合格折和合格市场状态均为 0。本轮只能实现 P8.0 安全状态壳；P8.1～P8.4 是达到硬门且再次获得明确研究批准后的条件计划，不得提前编码成可运行算法。

## 1. 先给结论：当前可做与不可做

当前可以落地：

1. 从不可变 `Phase8AdmissionReport` 读取准入事实，输出一个确定性、只读、默认禁用的 `AdaptiveResearchStatusReport`。
2. 在 CLI、`probe` 和 Repo Skill 中明确显示为什么 Phase 8 没有进入，以及在线改权、主模拟账本写入和券商执行始终为 false。
3. 为未来规则状态机研究冻结 Schema、输入、门槛、审计、回滚和测试计划，但不创建权重、不训练模型、不产生新的投资决定。

当前禁止落地：

- 不创建规则动态权重，不根据近期盈亏切换专家，不把市场状态直接映射成新仓位。
- 不实现网格搜索、贝叶斯优化、回归调权、上下文多臂赌博机、PPO、A2C 或其他 RL。
- 不以合成数据、历史回测、当前 Provider 值或手工改数据库冒充 Phase 7 前向证据。
- 不修改 Phase 7 admission，不允许命令参数或自由文本覆盖 admission 结果。
- 不写主 paper account/order/fill/position/journal，不连接券商，不生成真实订单。
- 不让 Codex/LLM 计算权重、奖励、propensity、离线策略价值或准入结论。

## 2. 阶段目标与状态机

Phase 8 不是“自动找到赚钱权重”，而是分层回答：在固定权重专家增量已经被真实前向证据证明后，使用更复杂但仍受硬约束的权重规则，能否在新的样本外期间稳定优于固定权重，并保持同等或更低风险。

状态顺序固定为：

```text
NOT_ENTERED_BY_DESIGN
  -> AWAITING_EXPLICIT_RULE_RESEARCH_APPROVAL
  -> RULE_STATE_MACHINE_SHADOW_RESEARCH
  -> OFFLINE_DYNAMIC_WEIGHT_RESEARCH
  -> CONTEXTUAL_BANDIT_SHADOW_RESEARCH
  -> RL_SANDBOX_RESEARCH
```

任何状态都不能跳级。任一阶段未优于上一层基准、审计失败、输入版本改变或证据不足，必须回到上一层已验证固定策略；不能在原 run 上改阈值或重选样本。

P8.0 当前完成状态只能是 `IMPLEMENTED_DISABLED_BOUNDARY`。它不等于进入 P8.1，更不等于 Phase 8 算法已经开发完成。

## 3. P8.0 进入条件与只读事实源

唯一准入事实源是 Phase 7 产生并通过审计的不可变 `Phase8AdmissionReport`。P8.0 不自行重算一套较宽松规则，也不读取聊天结论。

进入 P8.1 前必须同时满足：

1. admission 状态为 `ELIGIBLE_RULE_STATE_MACHINE_RESEARCH`。
2. admission 引用的 `ShadowEvaluationReport`、study、policy、对象和索引审计均为 `PASS`。
3. 至少 12 个月、100 个独立成熟决定、5 个合格 walk-forward 折且每折至少 20 个、3 个合格市场状态且每状态至少 30 个。
4. 正式 PIT、退市、历史成分、公司行为和回放完整性门通过，未解释缺口为 0。
5. 至少一个已批准生产合同专家臂的 Holm 校正后增量 CI 下界大于 0，稳定性、回撤、路径不确定和集中度门全部通过。
6. 用户针对具体 admission/report/study 版本明确批准“只进入规则状态机影子研究”；历史上的一般开发授权不能代替本次批准。

任一条件不满足时，状态固定为 `NOT_ENTERED_BY_DESIGN` 或 `AWAITING_EXPLICIT_RULE_RESEARCH_APPROVAL`，所有自适应能力保持 false。

## 4. P8.0 本轮交付合同

新增 `AdaptiveResearchStatusReport`，至少包含：

- `study_id`、`shadow_report_id`、`phase8_admission_id`、admission 状态和对象哈希；
- `capability_status`：`NOT_ENTERED_BY_DESIGN`、`AWAITING_EXPLICIT_RULE_RESEARCH_APPROVAL`、`RULE_STATE_MACHINE_SHADOW_RESEARCH`、`OFFLINE_DYNAMIC_WEIGHT_RESEARCH`、`CONTEXTUAL_BANDIT_SHADOW_RESEARCH`、`RL_SANDBOX_RESEARCH`；
- 当前观察月份、独立决定、成熟观察、折数和市场状态覆盖；
- `reason_codes` 和缺口计数；
- `adaptive_weights_enabled=false`、`online_learning_allowed=false`、`main_paper_ledger_write_allowed=false`、`broker_execution_allowed=false`；
- `next_permitted_stage`，未满足 admission 时只能是 `PHASE7_FORWARD_EVIDENCE_COLLECTION`；
- Schema/engine/policy 版本和确定性 `status_sha256`。

P8.0 服务规则：

1. study 不存在时返回 `NOT_RUN/NOT_ENTERED_BY_DESIGN`，不创建任何数据库记录。
2. study 存在但没有 report/admission 时返回 `PHASE8_ADMISSION_NOT_AVAILABLE`。
3. admission 不合格时逐项转述确定性 gate/reason，不生成建议权重。
4. admission 合格但没有新的明确研究批准时返回 `AWAITING_EXPLICIT_RULE_RESEARCH_APPROVAL`，仍不生成权重。
5. 对象、哈希、索引或审计异常时返回 `BLOCKED_BY_INTEGRITY` 语义的 reason code，全部能力保持 false。
6. 输出不包含 thesis 正文、私有内容、本地路径、Cookie、持仓细节或收益承诺。

本轮不新增 migration：P8.0 是对已登记 Phase 7 工件的只读派生状态，不形成新的研究事实。未来 P8.1 真正创建研究时，才单独设计 migration 和不可变 Artifact。

## 5. P8.0 CLI、探针和 Repo Skill

新增稳定命令：

- `adaptive-research-status [--study-id]`：只读返回最新或指定 study 的 Phase 8 能力状态和缺口。

`astock probe` 增加：

- `adaptive_research.status`；
- admission 最低层级；
- `adaptive_weights=false`；
- `online_learning=false`；
- `main_paper_ledger_write=false`；
- `broker_execution=false`；
- 当前允许的下一阶段。

`$astock-research-orchestrator` 在宽泛“是否能动态调权/进入 Phase 8”请求中先调用只读状态命令。`$paper-trading-recovery` 明确拒绝把自适应研究当作主账户恢复或改账入口。Repo Skill 不直接读写 SQLite。

## 6. P8.1 条件计划：规则状态机影子研究

只有 P8.0 全部门和明确批准通过后才实施。

计划 Schema：

- `AdaptiveResearchPolicy`：policy/study/admission/report 版本、允许市场状态、权重上下界、单次变化上限、专家总权重上限、BaseCase 最低权重、风险预算上限、冷却期、成本/成交/公司行为版本和生效时间。
- `RegimeWeightRule`：市场状态、触发前置条件、固定 Decimal 权重映射、失败回退、禁用条件、规则证据和人工批准身份。
- `AdaptiveWeightDecision`：决定前可见的状态快照、旧权重、新权重、触发规则、边界裁剪、输入哈希和创建时间。
- `AdaptiveShadowStudyManifest`：相对固定权重基准的新 study 身份、比较臂、冻结规则、候选集、样本窗口和回滚版本。

规则状态机只能从有限、有序、事前冻结的市场状态选择有限权重模板。权重参数必须在看到新 study 结果前冻结；任何阈值变化创建新 policy/study。状态缺失、PIT 不安全、审计异常或进入冷却期时，确定性回退到 Phase 7 固定权重。

P8.1 只运行隔离影子账户，不写主模拟盘。它必须与固定权重基准共享候选、signal time、TradeProtocol、成本、成交、公司行为、退出模型和名义本金。

## 7. P8.1 独立验收门

规则状态机版本至少重新积累一轮完整前向样本；不得复用用于设计规则的 Phase 7 样本作为最终样本外证据。最低门不得低于：

- 12 个月、100 个独立成熟决定；
- 5 个连续样本外折且每折至少 20 个；
- 3 个市场状态且每状态至少 30 个；
- 相对 Phase 7 固定权重的扣费成对增量 CI 下界大于 0，并通过 Holm 校正；
- 至少 4/5 折为正，无状态呈现明确伤害；
- 最大回撤、回撤恶化、路径不确定、单事件/状态集中度不劣于 Phase 7 硬门；
- 权重切换次数、费用、换手和未成交率单独报告，不能用忽略调仓成本制造优势。

未通过即回退固定权重；不得自动进入 P8.2。

## 8. P8.2 条件计划：离线动态权重

只有 P8.1 真实前向通过且再次明确批准后才实施。首版只能是可复现的离线批处理：

- 训练窗口、验证窗口和最终测试窗口严格按时间切分并带 purge；
- 只用决策时点可得的结构化特征，不读正文、未来修订、未来收益或当前幸存者集合；
- 目标函数同时包含扣费收益、回撤、换手、权重变化和集中度惩罚；
- 参数、随机种子、求解器版本、训练输入和输出全部冻结为 Artifact；
- 训练完成后权重在下一段前向窗口固定，禁止逐决定在线学习；
- 必须同时比较 Phase 7 固定权重和 P8.1 规则状态机。

模型复杂度从透明线性/凸优化开始；不能直接跳到深度网络。解释不清、解不唯一、结果对小扰动敏感或审计无法复算时，状态为 `NOT_ELIGIBLE_INTEGRITY`。

## 9. P8.3 条件计划：上下文赌博机影子研究

只有离线动态权重真实样本外显著优于规则状态机后，才允许单独立项。进入门不得低于 24 个月和 300 个独立成熟决定，并须另行冻结。

必须先具备：

- 每个动作的候选集合、选择概率/propensity、上下文版本和奖励可得时间日志；
- 明确探索预算、最小 BaseCase 权重、最大专家权重、风险预算和 kill switch；
- IPS/SNIPS/DR 等离线策略评估的确定性实现、重叠/支持域检查和方差报告；
- 固定策略、规则状态机和离线动态权重三类基准；
- 只在影子研究中产生动作，不影响主 paper account。

支持域不足、propensity 缺失、离线估值方差过大或任一硬风险门失败时，不启动探索。

## 10. P8.4 条件计划：RL 沙箱

RL 永远是最后一层，且需要新的专项计划和用户明确批准。即使未来实施，也只能在隔离、可重放的研究沙箱内调整受上层硬门限制的权重或风险预算：

- 不直接选股，不直接生成订单，不连接券商，不修改主模拟账本；
- 状态、动作、奖励、终止、成本、成交和公司行为语义必须版本化；
- 必须有固定策略、规则状态机、离线动态权重和 bandit 基准；
- 使用严格时间切分、多个随机种子、压力市场、奖励黑客和分布漂移测试；
- 任何结果只形成研究报告，不能形成自动晋级。

当前状态固定为 `RL_RESEARCH_NOT_AUTHORIZED`，本轮不安装 RL 框架，不增加 GPU 依赖。

## 11. 未来存储、审计与恢复原则

P8.1 以后若获准实施：

- 原始/完整 policy、rule、decision、训练输入清单、模型参数、评估和准入报告进入 ObjectStore；
- 结构化时序事实进入 Parquet；SQLite 只保存 ID、版本、状态、哈希和安全统计；
- DuckDB 只建视图，不复制事实；
- 每个权重决定必须引用 Phase 7 admission、研究 policy、市场状态快照和父权重哈希；
- 结果出现后不得覆盖规则或训练输入；修订产生新版本和 supersedes 链；
- audit 重算权重裁剪、成本、奖励、统计和准入；recover 只补可证明索引，不重新训练或重新选参数。

任何未来 migration 必须只新增表/列，不改变 Phase 1～7 语义；停用 Phase 8 后，固定权重 Phase 7 和主模拟账户仍可独立运行。

## 12. 实现顺序

### P8.0 默认禁用的能力状态壳（本轮）

交付 Schema、只读服务、CLI、probe、Repo Skill、无写入/无网络测试和真实 `NOT_ENTERED_BY_DESIGN` 验收。

### P8.1 规则状态机影子研究（条件未满足）

先追加独立实施计划和 migration，再交付冻结规则/权重/影子 study；没有明确批准不得开始。

### P8.2 离线动态权重（条件未满足）

只在 P8.1 新前向证据通过后设计；不复用调参样本冒充最终测试。

### P8.3 上下文赌博机（条件未满足）

只在 propensity、支持域和离线策略评估合同齐全后立项。

### P8.4 RL 沙箱（未授权）

需要单独专项计划、依赖审计、资源实测和用户批准；默认永久禁用。

## 13. P8.0 自动化测试与验收矩阵

1. study 不存在、没有 report、没有 admission、样本不足、完整性失败、合格但未获新批准等状态均返回正确原因。
2. `NOT_RUN` 或任一不合格 admission 下，四个能力布尔值必须全部为 false。
3. 相同 admission/输入产生相同状态、reason 排序和 `status_sha256`。
4. CLI 不接受“强制开启”、自选权重、覆盖 admission 或跳级参数。
5. P8.0 调用前后所有 Phase 7 表和 paper account/journal/order/fill/position 数量不变。
6. 不联网、不调用 Browser/MCP/外部 LLM、不读取私有 PDF/DOCX/Cookie/Profile。
7. 对象、报告、admission 或审计异常时 fail closed，不回显 payload、本地路径或密钥。
8. `probe` 明确显示 adaptive weights、online learning、paper ledger write 和 broker execution 均为 false。
9. Repo Skill 只能调用稳定 CLI，不得直接写 SQLite。
10. 全仓 `uv run pytest`、`uv run ruff check .`、`uv run pyright` 通过；既有 Phase 1～7 回归不变。

## 14. 回滚点与完成定义

P8.0 无 migration、无新运行工件、无账本写入；回滚只需移除只读 Schema/服务/CLI/探针字段，不影响 Phase 7。

本轮完成定义：

- P8.0 程序为 `IMPLEMENTED_DISABLED_BOUNDARY`；
- 真实运行返回 `NOT_ENTERED_BY_DESIGN`，并准确显示 Phase 7 样本缺口；
- 所有自适应、在线、账本和券商能力为 false；
- P8.1～P8.4 继续标为 `CONDITION_NOT_MET/NOT_AUTHORIZED`，不以文档冒充实现。

## 15. 预算与人工事项

P8.0 是本地只读确定性功能，不产生 API/LLM 费用，也不需要注册、购买或浏览器操作。Phase 7 真实前向观察是自然时间门，不能付费加速或用回测替代。

未来第一个可能需要人工确认的节点，是某个真实 `Phase8AdmissionReport` 已合格后，用户针对具体 study/report/admission 版本批准是否进入 P8.1 规则状态机影子研究。在此之前不打扰用户，也不提前开发算法。


---

## 原文件：《财务审计输入说明.md》

# 财务可信度审计输入说明（M3.1）

## 这条命令解决什么问题

`financial-audit` 不负责猜数字，也不直接从网页抄表。它接收已经完成官方来源登记、不可变快照、PIT 时间标注和 Evidence 定位的财务数字，然后用 Decimal 做单位归一化、三表勾稽和描述性比率复算。

完整机器可读格式以命令输出为准：

```powershell
uv run astock financial-audit-schema
```

## 前置顺序

1. 通过 Phase 2 的官方公告/财报入口登记并解析文件；
2. 给每个拟使用的数字建立页或块级 `evidence_id`；
3. 建立对应 `source_snapshot_id` 和 `pit_id`，其中报告期、发布时间、系统可得时间必须真实；
4. 生成 `FinancialAuditRequest` JSON；
5. 运行审计并用返回的确定性 `audit_run_id` 查询恢复状态。

```powershell
uv run astock financial-audit .\request.json
uv run astock financial-audit-status <audit_run_id>
```

## 每个数字必须说明的内容

- `fact_id`：本次输入中唯一且稳定的数字身份；
- `company_id`：公司代码或系统统一公司 ID；
- `period_start` / `period_end` / `period_type`：数字覆盖的报告期；
- `statement_type` / `field_code`：来自哪张表、是什么科目；
- `reported_value` / `unit`：原披露数字和原披露单位；
- `source_snapshot_id`：不可变原始文件快照；
- `pit_id`：发布时间、可得时间和修订链；
- `evidence_ids`：能准确回到原文件位置的官方证据。

`CNY`、`THOUSAND_CNY`、`TEN_THOUSAND_CNY`、`MILLION_CNY` 和 `HUNDRED_MILLION_CNY` 会统一换算成元。不同单位表达的同一个数字不会被误判成冲突；同一报告期同一科目存在不同官方值时，系统不会擅自选一个，而会生成 `document_conflicts` 和人工核验任务。

## 返回状态

- `SUCCEEDED`：所选 M3.1 规则需要的数字、证据和 PIT 均可用；这不代表公司“没有风险”。
- `NEEDS_INFO`：存在缺字段、弱证据、未来信息、来源链不一致、官方值冲突，或用户点名了尚未实现的 M3.2/M3.3 能力。
- `risk_level`：只由已完成且证据充分的确定性规则触发；缺资料本身不伪装成高风险。

社区文章只能作为调查线索，不能作为已报告财务数字的主证据。M3.1 的比率仅做复算，不使用尚未校准的行业阈值；同行分位、时间序列异常、Isolation Forest 和 PyOD 仍明确禁用。

---

# Phase 8 P8.0 落地结果

落地日期：2026-07-17

程序状态：`IMPLEMENTED_DISABLED_BOUNDARY`

真实研究状态：`NOT_ENTERED_BY_DESIGN / NOT_RUN`

## 1. 已交付内容

1. 新增严格 `AdaptiveResearchStatusReport`，包含 study/report/admission 身份与对象哈希、冻结 policy 版本、真实样本计数、四项硬门缺口、审计状态、排序 reason code、下一许可阶段和确定性 `status_sha256`。
2. 新增只读 `AdaptiveResearchStatusService`。它只读取并核验 Phase 7 的冻结索引和对象；不新建 migration、Artifact、权重、训练结果、研究决定或账本事件。
3. 新增稳定命令 `uv run astock adaptive-research-status [--study-id]`，并在 `astock probe` 中公开 Phase 8 状态、样本缺口和四个永久为 false 的安全能力。
4. 更新研究调度与模拟盘恢复 Repo Skill：讨论动态调权前必须先读只读状态；即使 admission 合格，也只能等待用户针对具体 study/report/admission 版本明确批准 P8.1，不能据此启用算法或改账。
5. 新增无 study、无 report、无 admission、不合格 admission、审计/对象异常、合格但待批准、确定性哈希、CLI 禁止强制开启和全表零写入测试。

## 2. 当前真实缺口

| 硬门 | 当前值 | 最低要求 | 仍缺 |
|---|---:|---:|---:|
| 前向观察月份 | 0 | 12 | 12 |
| 独立成熟决定 | 0 | 100 | 100 |
| 合格 walk-forward 折 | 0 | 5 | 5 |
| 合格市场状态 | 0 | 3 | 3 |

当前 `Phase8AdmissionReport` 不存在，Phase 7 审计状态为 `NOT_RUN`。因此下一许可阶段只能是 `PHASE7_FORWARD_EVIDENCE_COLLECTION`；`adaptive_weights_enabled`、`online_learning_allowed`、`main_paper_ledger_write_allowed` 和 `broker_execution_allowed` 全部为 false。

## 3. 后续条件计划状态

- P8.1 规则状态机影子研究：`CONDITION_NOT_MET / NOT_AUTHORIZED`。
- P8.2 离线动态权重：`CONDITION_NOT_MET / NOT_AUTHORIZED`。
- P8.3 上下文多臂赌博机：`CONDITION_NOT_MET / NOT_AUTHORIZED`。
- P8.4 RL 沙箱：`NOT_AUTHORIZED`。

本轮没有开发任何自适应算法。只有真实 Phase 7 硬门全部通过、冻结对象审计为 `PASS`，并且用户对具体版本再次明确批准后，才允许先追加 P8.1 的独立实施计划。
