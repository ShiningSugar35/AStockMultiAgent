# 当前开发进展（首次体检基线）

## 项目状态快照

- 当前分支：`codex/phase5-stabilization`
- HEAD：`adaee07f70d5a524baaa9736359bf26eec71e67e`
- parent：`1e7155780dac0d3f71c6e912bdb0a3221fe8e2d8`
- git status：clean（无未提交文件）
- 检查时间（本地时区）：`2026-07-25`
- 远程：`origin`（`ShiningSugar35/AStockMultiAgent`）

## 本次体检结论（只做状态核验，不开发新业务）

### 用户目前可以做的事
- 读取、维护项目状态文件和任务目录；
- 运行本地事实核验与审计链路；
- 下载/校验部分 5m 行情与参考市场数据（受 provider 可用性与参数范围影响）；
- 使用现有 CLI/脚本进行知识蒸馏、候选扫描、财务数据核验流程的重放与复核；
- 查看阶段性验收与开发计划中的可复用统计。

### 用户目前还不能做的事
- 在无外部架构师任务下发前推进新增研究功能开发；
- 将作者蒸馏 Skill 全量自动化导入生产；
- 宣称“全市场全链路实时”可用；
- 以测试通过即代表投研链路完整闭环（目前仍存在待确认/待核验项）。

## 本次核验结果

### 关键项目目录
- 已存在：`开发计划.md`、`验收报告.md`、`runtime/`、`src/astock`、`tests/`、`migrations/`、`configs/`、`.agents/skills`、`docs/`
- 新建/补齐：`docs/当前开发进展.md`、`docs/外部架构师交接.md`、`docs/业务能力矩阵.md`、`docs/争议与待5.6复核.md`、`docs/PM_STATE.json`、`tasks/*` 五个任务目录

### 业务能力（可核验）
| 能力 | 状态 | 证据来源 | 当前限制 |
|---|---|---|---|
| 5m 行情与市场基准数据 | 部分可用 | `验收报告.md`、`configs`、`src/astock` | 部分 provider/参数场景仍受限 |
| 财务结构化能力 | 部分可用 | `验收报告.md`、`financial-source-*` 相关入口 | `financial` 与 `source_snapshot` 的完整性链仍有审计边界 |
| 知识采集与参数化清洗 | 部分可用 | `验收报告.md`、`runtime` | 全量覆盖仍未达到可交付 Mvp 标准 |
| 论文/观点蒸馏（ArgumentUnit/ParagraphUnit） | 部分可用 | `验收报告.md`、`migrations`（0035~0040） | 规则边界与审计通过态仍持续演化 |
| Serenity 投研集成 | 部分可用 | `验收报告.md`、`configs/research_skills.yaml` | 尚未达到完整可投研默认链路 |
| 候选扫描（candidate-scan） | 部分可用 | `验收报告.md`、`src/astock` | 需要外部任务触发与验证闭环 |
| 持仓生命周期监控 | 部分可用 | `验收报告.md`、`migrations/0025_position_lifecycle.sql` | 未形成统一闭环策略 |
| 模拟交易与账本 | 部分受限 | `验收报告.md`、`src/astock` | `ledger_write_allowed` 与执行网关仍有限制 |

### 真实采集统计（从本地验收报告抽取）
- 作者采集总量（report 标注）：
  - MR Dang：`3789`/`3788`（按阶段报告口径）
  - 榛勉溅（含评论/文章/观点的子统计行）：`831 / 8 / 1`（口径见 report）
  - 寒武纪鳄鱼：`895 / 24 / 1896`（口径见 report）
  - 其余三位作者：表内缺失（需外部任务/后续复核）  
- Report gap：`9`
- Parquet 已发现问题：`0`

### Serenity 集成统计（基线）
- 当前可见接口与方法：S 系列接入已存在且标注为 v2 使用中（5 个来源/技能维度）
- 当前校验态势：验收报告中对应模块为 `PASS` 或 `PROVISIONAL`（含历史版本与待验证项）

### 争议与待复核项（启动期）
- 当前无新增争议工单，所有新争议集中记录在 `docs/争议与待5.6复核.md`
- 需外部架构师确认的边界：`paper/order` 联动、Serenity 全量生产化开关、作者蒸馏全量导入策略

## 距离第一个可用投研 MVP 的剩余步骤（基线版）
1. 由外部架构师确认下一阶段任务边界；
2. 对 `Serenity` 全量到生产链路做一轮闭环验收；
3. 将知识采集缺口（gap）和审核失败/受限项做闭环；
4. 明确模拟交易 `paper-*` 全链路的安全门禁策略；
5. 输出下一批 Worker 任务并补齐验收标准。

