"""External quant framework pattern benchmarks for AStockMultiAgent.

Isolated experiment comparing Qlib, RD-Agent, LEAN, and RQAlpha patterns
against existing project implementations.  No production dependencies;
all fixtures are local and deterministic.  Deletable after completion.
"""
