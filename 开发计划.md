# AStockMultiAgent 当前开发计划

> 状态：CURRENT / RELEASE REVIEW
> 本文件只保存尚未完成的工作；已验证事实归档于《进度验收》与现行架构文档。
> 版本：investor-orchestration-and-regime-plan-v1
> 产品主线：full-research-recommendation-v1 / package v0.4.0
> 更新日期：2026-09-12
> 机器索引：`planning/work_packages_v1.yaml`
> durable run：`lr_mtw92jn9_aabe249465fb`；owner lane：`main/full-research-recommendation-p0`。

## 1. 当前唯一投资决策架构

所有可直接形成证券选择、买卖判断、价格、数量或仓位的用户请求统一归一为 `FULL_RESEARCH_RECOMMENDATION`。当前设计不存在并行的轻量荐股、旧 BUY/HOLDING/PORTFOLIO decision 或独立 Recommendation Readiness 发布路径；历史名称若被旧输入带入，只能在 request construction 时一次性迁移到 Full Research，不能拥有 Planner、Executor、Response 或 Publication 权威。

正式公共投资建议的唯一发布对象是不可变 `RecommendationResearchReceipt`，且必须通过 Publication Gate。上游 `FullResearchInputReadinessReport` 只证明 Full Research 输入就绪，不具有正式荐股权。Mandatory Research DAG、PIT/source lineage、Financial/Governance CriticalVeto、multi-model valuation、candidate ranking、portfolio/risk/execution、freshness SLA、receipt replay 与 tracking/re-evaluation 均属于同一 Full Research 闭环。

永久边界保持：`broker_execution_allowed=false`；actual/paper 分 lane；`EA_PROVISIONAL` 只允许冻结不完整持仓声明，真实 `EA_WRITE` 必须独立走 `ACCOUNT_FACT_WRITE`；无新鲜行情不得生成即时成交数量；系统允许零 BUY、少于目标数量与现金持有；历史 receipt 不因后续 tracking 重评而改写。

## 2. 已完成实现，不再作为 active work package

WP-12→WP-19 已完成实现并从 active manifest 移出：Full-Research Intent Router、PIT Data & Evidence Graph、Mandatory Research DAG、Candidate & CriticalVeto、Multi-model Valuation & Ranking、Portfolio & Execution、Publication/Audit Receipt、Continuous Tracking。对应验收证据写入《进度验收》和 durable run，不在本文件保留旧 `PLANNED`/兼容设计。

性能与复杂度审计已完成有证据的低风险优化：候选与证据索引由重复扫描改为一次构建复用；删除无用 `DocumentRepository` 与死 Challenger 失效条件路径；portfolio shortlist 使用 bounded `islice`；`build_portfolio` 拆分为 position/factor/constraint 纯 helper 并退出 C901 超限列表。Full Research 28 项 profile 显示纯计算主要为 0.01–0.02s 级，持久化 receipt/replay/tracking 的主要耗时来自必要审计 I/O；50 次五候选 tracemalloc 测试峰值约 1.46 MiB，因此不通过删证据、弱化 receipt 或扩大缓存换取表面速度。

## WP-20：E2E / Shadow / Release

> 状态：IN_PROGRESS｜优先级：P0｜owner lane：main/full-research-recommendation-p0｜版本目标：v0.4.0｜依赖：无
> 恢复锚点：durable run `lr_mtw92jn9_aabe249465fb`，step `s5`。断网/新会话后必须先读取 `AGENTS.md → docs/architecture/ → 开发计划.md → 进度验收.md → durable run → Git/tests`，禁止依赖聊天记忆重建现场。

### 3.1 冻结树发布门

当前恢复点说明（2026-09-11 断网前）：WP-12→WP-19 已完成；WP-20 尚未发布。Full Research 路由、PIT/证据图、Mandatory DAG、CriticalVeto、估值/排名、组合/执行、不可变 receipt/replay/tracking 已在前序门取得通过证据。70 场景 business E2E 已通过；PIT temporal audit 已 PASS 且 `finding_codes=[]`；receipt 持久化/离线 replay/防篡改/重评不可变性专项 3/3 通过；文档/Skill 合同在修复 `RECOMMENDED` 跟踪契约后 14/14 通过；全仓 Ruff 与 Pyright 曾在复杂度重构前分别通过 `All checks passed` 与 `0 errors / 0 warnings`，因此**复杂度重构后的冻结树必须重新跑这些门**。

复杂度收敛已完成并做了当前代码专项验证：`FullResearchRecommendationService.verify_receipt` 已拆为身份、DAG lineage、估值 replay、组合 replay、Publication Gate helper；`VerifiedAnswerProjector._full_research_decision` 已拆为 receipt/index、单标的理由/风险/动作与组合摘要 helper；`FullResearchReceiptAssembler.assemble` 的候选投影已拆入 candidate outputs / rejection / ranking helper。当前 C901 复核中本轮新增三个热点均退出超限清单，仅剩历史 `inputs=12`、`_paper_status=13`；相关 Pyright 为 `0 errors / 0 warnings`，Full Research + rendering + orchestration + domain-contract 专项为 `93 passed`。因此下一步直接进入同一冻结树的全仓 L3；旧历史复杂度不在无性能证据时扩大重构面。

接下来固定顺序：① 在当前冻结树重跑 Ruff 全仓、Pyright 全仓、文档合同、PIT/replay 与 `git diff --check`；② 从头跑全仓 pytest；③ defect-first diff Review；④ 清理仅本任务可证明可再生的 runtime helper/test temp；⑤ secret/private/runtime 审计；⑥ release baseline commit/push/tag `v0.4.0`/GitHub Release；⑦ 发布后 docs-only closeout、远端/clean/durable Review/complete。任何代码变更都使之前冻结树门失效并要求受影响门重跑。

在不再修改业务逻辑的同一冻结树完成：

1. Full Research / orchestration / Research Team / DomainContract / Closure / rendering targeted regression；
2. 70 场景 business E2E 与用户指定故障矩阵；
3. receipt seal/tamper/offline replay、tracking/re-evaluation、PIT temporal/non-interference；
4. Ruff 全仓、Pyright 全仓、文档/Workflow/Skill 合同、`git diff --check`；
5. 全仓 pytest；
6. secret/private/runtime/项目外输出审计；
7. defect-first independent Review，任何真实缺陷修复后重跑受影响门。

### 3.2 Release

冻结门全部 PASS 后：显式 staging → release baseline commit → push `main` → 创建不可移动 annotated tag `v0.4.0` → 按仓库既有版本规范创建非 draft、非 prerelease GitHub Release，并核验 tag/release 均指向 release baseline。构建 wheel/sdist 时只写项目内受控目录并记录 SHA-256。

### 3.3 发布后 docs-only closeout

Release 完成后只允许文档/机器索引状态迁移：从 active manifest 移出 WP-20，更新《进度验收》为 v0.4.0 已发布事实，核验远端 HEAD/tag/release、clean worktree、无 secret/private/runtime 泄漏，最后执行 durable Review PASS 与 complete。若 docs-only closeout 产生 commit，则再次 push，但不得移动 v0.4.0 tag。

## 4. 发布否决

验收证据必须来自真实执行与可复现工件，**不得伪造**样本、PASS、覆盖率、receipt、release 或远端状态。

任一以下条件成立即不得发布：Full Research Publication Gate 可被旁路；Mandatory 节点非 PASS 仍能输出证券+价格+数量/权重；PIT leakage/source lineage 不完整；CriticalVeto 漏放；stale quote 生成即时股数；receipt tamper/replay 失败；真实账户写入混入 Full Research；全仓测试/静态/文档门不绿；active Skill/Workflow/架构文档仍把旧轻量决策路径描述为当前设计；输出或临时文件越出 `D:\AStockMultiAgent`。
