# AStockMultiAgent 当前开发计划

> 状态：CURRENT / PLANNED FOR NEXT IMPLEMENTATION
> 版本：investor-orchestration-and-regime-plan-v1
> 更新日期：2026-09-07
> 范围：统一投资请求编排、实际/模拟持仓前置恢复、跨会话研究登记、宏观 current data、市场状态与风险预算、68 个业务问题验收、ChatGPT / Codex 定时语义研究适配
> 本文件只保存尚未完成的工作；历史由 Git/Release、当前架构和 `进度验收.md` 追溯。
> 机器索引：`planning/work_packages_v1.yaml`；工作包 ID、顺序、依赖、状态、优先级与 owner lane 必须和本文通过合同测试保持一致。

## 1. 目标与硬边界

下一阶段交付一条可机器验证的投资请求主链：

```text
用户问题
  → actual / paper / orders / material events / latest-regime availability preflight
  → intent + entity resolution
  → REQUIRED / CONDITIONAL / PROHIBITED capability DAG
  → typed outputs + coverage receipt
  → Investor Answer Gateway
```

市场状态只作为风险覆盖层：健康牛市可以在用户风险上限内提高**已通过正式准入**的仓位预算、建仓节奏和推荐供给；熊市、恐慌或投机性高波动牛市收紧总风险、单股风险、流动性与安全边际。任何状态都不得放松公司质量、财务、治理、估值、证据、交易规则和人工确认硬门。

本计划不包含真实券商连接/自动下单，不用大模型生成账本或宏观数值，不把 K 线估值伪造成真实成交，也不制造 Phase 7/8、长期自然场景或外部资格样本。就 E-02 等外部资格而言，**无未完成开发任务**；未达生产资格属于外部证据/授权门，不作为本轮开发 backlog。

## 2. 冻结输入

- `AGENTS.md`
- `docs/README.md`
- `docs/adr/0001-documentation-as-code-with-machine-contracts.md`
- `docs/adr/0002-market-regime-as-risk-overlay.md`
- `docs/architecture/investment-request-orchestration-v1.md`
- `docs/architecture/market-regime-control-v1.md`
- `docs/architecture/scheduled-research-orchestration-v1.md`
- `docs/acceptance/business-question-capability-matrix-v1.md`
- `planning/work_packages_v1.yaml`
- 当前 Schema、migration、versioned config、CLI、SQLite/Parquet/ObjectStore 和测试事实

改变安全边界、事实源、状态语义或正式验收门时，必须新增或替代 ADR；不能只改提示词或本文。

## 3. 依赖与实施顺序

```text
WP-01 机器合同与场景 manifest
  ├─→ WP-02 统一增量 preflight
  ├─→ WP-03 研究标的登记与 provisional facts
  └─→ WP-04 Capability Planner / Executor / Coverage

WP-05 Official current macro data
  └─→ WP-06 Market Regime V2
         └─→ WP-07 Regime Decision Overlay

WP-02 + WP-03 + WP-04 + WP-07
  └─→ WP-08 Investor Answer Gateway 与代表性纵向切片
         └─→ WP-09 全部 68 场景 E2E

WP-02 + WP-03 + WP-04 + WP-08 + 现有 Continuous Monitor
  └─→ WP-10 ChatGPT / Codex Scheduled Research Adapter

WP-09 + WP-10
  └─→ WP-11 Controlled live、shadow、启用与回滚
```

WP-05/06 可与 WP-02/03/04 并行；WP-11 前 `market_regime_v2_enabled`、`regime_decision_overlay_enabled` 默认关闭，WP-10 在统一 preflight/gateway 可用前仅做 recorded dry-run。

## WP-01：机器合同、migration 与场景 manifest

> 状态：PLANNED｜优先级：P0

交付 `InvestorRequestEnvelope`、`InvestorSessionPreflightReceipt`、`UnifiedPortfolioContext`、`ResearchSubjectEvent`、provisional position/cost、`CapabilityExecutionPlan/Receipt`、Market Regime V2/Policy Schema，以及 `configs/business_scenarios_v1.yaml`；将现有 `planning/work_packages_v1.yaml` 纳入派工/状态一致性校验，不让 `.ai-bridge` 瞬时交接形成第二份计划。

验收：

- 68 个问题 ID 唯一、齐全，并具备 required/conditional/prohibited 能力、side effect 和输出义务；
- 工作包机器索引可解析，11 个 ID、标题、依赖、状态、优先级和 owner lane 与本文一致；过期 `.ai-bridge` handoff 被识别为 `SUPERSEDED`；
- actual、paper、research metadata 三条写入 lane 独立；
- date-only、month-only、exact timestamp 和 unknown 显式区分；“今天/昨天”按冻结的 user timezone 解析并映射 A 股交易日历；
- 多账户仅在唯一 active account 或已显式配置默认账户时自动选择；多个账户且无默认时必须阻断写入而非猜测；
- estimated cost 不进入 exact P&L、税费或账本；
- Schema round-trip、migration、FK/唯一性、幂等、rollback 和只读 audit CLI 全绿；
- active policy 可切回旧版本，不删除历史事件。

## WP-02：统一、分 lane、增量的请求前置恢复

> 状态：PLANNED｜优先级：P0｜依赖：WP-01

实现 `InvestorSessionPreflightService`，统一恢复外部账户投影、paper ledger、open order、pending settlement、本地兼容投影、重大监控增量，并只读返回 latest valid market-regime identity/age/availability；使用 event/ledger revision、snapshot hash、policy hash 和代码版本增量缓存。preflight 不联网构建市场状态，纯账户事实写入不以 current regime 为提交前置条件。

验收：

- actual/paper/open order/fill/position/unknown cash 不混淆；
- 相同 revision 同时只重建一次，重启后缓存可验证或安全失效；
- 当前约 66 秒的完整 `paper-status` 不再成为每问前置路径；同一请求只构建一次账本状态，`portfolio_nav()` 不得再次触发完整 `status()`；
- 全库 `PRAGMA integrity_check` 只允许显式审计或低频运维任务调用，暖 preflight 使用有界只读健康检查；
- 100 次无 revision 变化暖读取 p95 ≤ 2 秒；
- 超时/审计失败不得伪造空持仓；
- 空持仓用户回复静默率 100%，重大变化必须给影响和动作；
- preflight 不产生任何真实或模拟经济写入，也不发起宏观/行情网络采集；
- current regime 缺失时，账户记录/更正/现金/转托管/去重仍可完成；买卖判断、持仓、组合和荐股则正确降级或阻断；
- feature flag 可回到现有逐命令恢复。

## WP-03：跨会话研究标的登记与不完整持仓事实

> 状态：PLANNED｜优先级：P1｜依赖：WP-01

实现 append-only `ResearchSubjectRegistry`，登记 `MENTIONED/RESEARCHED/RECOMMENDED/HELD/EXITED/MONITOR_*`；实现 `ProvisionalPositionAssertion` 和 `EstimatedCostBasisRange`。

验收：

- 问题 88/89/90 可得到完整、可解释的研究/持有/监控集合；
- 搜索噪声、未解析实体和仅作可比公司的证券不误记为正式研究；
- 重跑不重复登记，actual/paper 来源始终保留；
- K 线只能生成估算区间和方法，状态为 `ESTIMATED_NOT_LEDGER_TRUTH`；
- 用户确认或券商导入后，以 append-only replacement/reversal 升级；
- 禁止 UPDATE/DELETE 历史事实。

## WP-04：Capability Planner、Executor 与覆盖回执

> 状态：PLANNED｜优先级：P0｜依赖：WP-01

实现机器可读的能力注册、DAG 规划、typed 执行、freshness/reuse、并行组、停止/降级与 `CapabilityCoverageReceipt`。固定安全门、side effect 和 public gateway 由代码控制；Agent 只负责语义研究和证据解释。

验收：

- 68 个 planner fixture 的 required coverage=100%，prohibited calls=0；
- conditional 能力每次触发/跳过均有机器理由；
- valuation、bull/bear、red team、Committee、portfolio 依赖顺序不可绕过；
- read-only 意图无法获取经济写权限；既存已确认模拟订单的确定性回放必须显式规划为 `PT_REPLAY`，不得伪装成 `READ`；
- required output 不完整时不能发布“已全方位研究”或正式 BUY；
- 相同事实不重复外部抓取，checkpoint 重跑不重复写入；
- provider failure、stale price、财务缺口、模型冲突均正确降级。

## WP-05：官方 current macro 数据面

> 状态：PLANNED｜优先级：P0

把 NBS/PBOC/MOF/NDRC 从 recorded-first 扩展为“recorded contract + official current capture + PIT vintage”。首批聚焦 NBS PMI/月季度具名 release、PBOC 货币信贷/社融/政策 release；MOF/NDRC 优先保存 official document，不强依赖脆弱 API。

验收：

- 原始响应先入 ObjectStore，保存 published/effective/ingested/available/revision timeline；
- first-release 与 latest-vintage 可分别重放；
- 每个首批 family 有 recorded、malformed、revision 和 controlled-live 证据；
- schema drift/超时/旧 snapshot 不会静默写错字段；
- 至少 NBS、PBOC 各一类 representative current release 通过受控 live；
- 部分覆盖不得宣称“宏观数据已完整”；
- active route 可回到 latest valid official snapshot。

## WP-06：Market Regime V2 特征、透明基线与概率 challenger

> 状态：PLANNED｜优先级：P0｜依赖：WP-01、WP-05、可认证市场/财务历史

状态包括 `HEALTHY_BULL`、`SPECULATIVE_BULL`、`NEUTRAL_RANGE`、`RISK_OFF_RANGE`、`TREND_BEAR`、`PANIC`、`TRANSITION`、`UNCLASSIFIED`。特征覆盖多宽基趋势、宽度、尾部/相关性、流动性/拥挤、估值、盈利扩散、宏观/信用/政策和 OOD。

候选：

1. 当前 `market-regime-v1` 最低复杂度基线；
2. transparent multi-family scorecard；
3. 预注册状态数的 Markov/HMM challenger。

验收：

- 使用最大可认证 PIT 历史并分 `CORE_LONG_HISTORY` / `ENRICHED_HISTORY`；长历史层尽量覆盖 2015 年等极端阶段，增强层目标至少 8 年，不用短历史高级特征回填早期；
- time-ordered train/dev/holdout、walk-forward、first-release vintage 主评；
- point-in-time Universe 覆盖上市/退市/停牌，复权与 total-return 口径分离，板块/ST/涨跌停/注册制等制度变更按 effective date 处理；
- 特征、权重、状态数、mapping 和门在 holdout 前冻结；
- 只有存在事前冻结 observable outcome/专家标签协议时才报告 calibration/Brier/log loss；无标签 hidden state 报告熵、稳定性、转移、coverage、false/missed panic、revision/OOD 和下游决策价值；
- 指数涨但宽度窄或高波动时不得判 `HEALTHY_BULL`；
- 覆盖不足为 `UNCLASSIFIED`，模型冲突进入 `TRANSITION` 或更保守状态；
- 所有新模型仅 shadow，保留 v1 和静态风险回滚。

## WP-07：市场状态到风险预算和推荐供给的只读覆盖层

> 状态：PLANNED｜优先级：P0｜依赖：WP-06

实现 `RegimeDecisionPolicy`：总风险、新仓、单股、并发新仓、正式推荐数量、最低流动性、安全边际、分批、no-trade band、red-team depth 和不确定性 fallback。

验收：

- 用户风险上限先于 regime；
- 复用现有 `PortfolioIntentProfile` 及正式用户约束作为绝对上限；约束不完整时使用保守默认值，禁止另建相互冲突的风险画像；
- `PANIC ≤ TREND_BEAR ≤ RISK_OFF_RANGE ≤ NEUTRAL_RANGE ≤ HEALTHY_BULL`；
- `SPECULATIVE_BULL` 的单股风险、流动性和安全边际不宽于 neutral；
- 推荐数量为正式通过数量与 regime cap 的较小值；
- 公司质量、财务、治理、估值、证据、规则和确认门永不放宽；
- 市场状态变化不能单独触发已有持仓的强制买卖；必须与 thesis、估值、组合风险或交易规则中的具名条件共同成立；
- 已在公司预测/估值中使用的宏观或行业信息不得被 regime overlay 再次隐式加权；需要改变情景权重时必须有独立验证的因果合同和重复计数审计；
- 对静态基线做成本后配对比较：回撤、CVaR/CDaR、波动、换手、收益牺牲和机会成本；
- 数值门在 holdout 前预注册，prospective shadow 通过前不得启用；
- 一键关闭 overlay 不修改历史快照或账本。

## WP-08：Investor Answer Gateway 与 10 类纵向切片

> 状态：PLANNED｜优先级：P0｜依赖：WP-02、WP-03、WP-04、WP-07

代表问题：1、11、22/26、31、42、52、61/65、75/77、81/90、92/95/96。

验收：

- 所有 public investment entry point 必须消费 preflight 与 coverage receipt；
- `InvestorDecisionAssembler` 只从 typed outputs 构建结论；
- 绕过 gateway、内部字段泄漏、伪精确价和空持仓噪声的反例全部被拒绝；
- actual/paper 分列，重大持仓变化在结论附近说明；
- current price stale 时不输出“现在的精确买卖点”；
- 正式 BUY/组合权重只来自 readiness 通过的证券；
- 账户写入 append-only、幂等、重启恢复；
- 10 类 positive/negative/error-injection recorded E2E 全绿。

## WP-09：68 个业务问题全量 E2E 与缺陷修复

> 状态：PLANNED｜优先级：P0｜依赖：WP-08

每个场景至少包括 positive、缺数据/冲突、stale/PIT、actual/paper、empty-holding、side-effect/幂等、capability missing/prohibited 和 public-answer fixture。

硬门：

- scenario coverage=68/68；
- required capability coverage=100%；
- prohibited call count=0；
- economic side-effect match=100%；
- idempotent rerun=100%；
- account/ledger audit=PASS；
- actual/paper confusion、future leakage、fabricated price/position/macro/event、gateway bypass、未准入正式推荐均为 0；
- empty-holdings silence=100%；
- 每个 P0/P1 缺陷有测试与修复，或保持明确 fail closed。

任何错账、重复持仓、转托管记买卖、open order 记持仓、估算成本写真实成交、牛市放松硬门、纯 `READ` 运行写经济账本，均使整包失败，不能用平均分豁免。`PT_REPLAY` 只允许回放既存已确认订单，重跑必须保持 fill 幂等。

## WP-10：ChatGPT / Codex Scheduled Research Adapter

> 状态：PLANNED｜优先级：P1｜依赖：WP-02、WP-03、WP-04、WP-08、现有 Continuous Monitor

实现 `ScheduledResearchPolicy`、`ScheduledTaskBinding`、`ScheduledResearchRunRequest/Receipt` 和 `MaterialChangeDigest`。Desktop Work/Codex 本地项目作为读取 `D:\AStockMultiAgent` 的默认执行面；Web 任务只允许公共信息研究，不能声称完成本地持仓、订单或账本复核。平台侧优先由用户在原生界面创建，项目只保存 opaque binding；除非实施时重新核实到稳定官方 API，否则不得假设代码可直接创建任务。先提供创建预览、数据披露级别和用户确认，再提供交易时段每小时重大变化、收盘后摘要、周度研究清单和有限期催化剂跟踪模板。

验收：

- 创建前普通聊天 dry-run 通过，任务名、时区、频率、执行面、数据范围、权限、通知、停止条件和回滚均显式；
- 原生创建/绑定路径可验证；没有稳定官方 API 时 direct-create 调用数为 0，平台 opaque task id 与本地 binding 可审计；
- 默认只向语义任务披露经过 allowlist/脱敏的最小上下文，不读取整库 SQLite 或完整交易历史；披露级别、云端上下文提示与用户同意绑定 policy hash；
- 同一 `binding_id + schedule_bucket` 重复触发 100 次只产生一个 accepted receipt 和至多一份通知；
- scheduled run 的 external-account、paper order/fill、broker execution 经济写入均为 0；
- actual/paper/open order 分 lane，空 lane 静默，本地事实不可读时声称“已检查持仓/订单”的次数为 0；
- watermark 只在完整持久化后前移，missed-run 有界 catch-up，超期进入 `DEGRADED/BLOCKED`；
- 无重大变化时不重跑全市场或完整公司研究，模型/外部调用满足版本化预算；
- 手工会话与定时任务同时命中同一 event/subject 时只有一个语义研究 owner；
- 当前账户计划、活动任务槽、权限和桌面运行条件不满足时在预览阶段阻断或降级，不伪装已创建；
- 暂停/删除 Scheduled binding 不删除本地 Monitor、账本、ObjectStore、研究 registry 或历史 receipt。

## WP-11：Controlled live、prospective shadow、启用与回滚

> 状态：PLANNED｜优先级：P0｜依赖：WP-09、WP-10

受控 live 覆盖当前公司/价格/公告、NBS/PBOC、三市场 Universe、ETF、账户更正/转托管、paper limit replay、monitor、scheduled research、provider failover/schema drift 和 current regime。

最终启用门：

- 68/68 recorded E2E 硬门全绿；
- live 原始 snapshot、时点、来源、回退和审计齐全；
- warm preflight 性能达标；
- Scheduled adapter 的本地/云端执行面、幂等、missed-run、通知和零经济写入门全绿；
- public gateway、side effect、账本和 PIT 安全门全绿；
- regime overlay 通过预注册样本外与 prospective 门；
- `pytest`、`ruff`、`pyright` 全绿；
- activation/rollback drill 不改变账本；
- 项目所有者显式批准 active policy。

启用顺序固定为：subject registry metadata → unified read-only preflight → coverage receipt → gateway hard gate → planner/executor → current macro → regime snapshot read-only → recommendation budget shadow → Scheduled adapter recorded dry-run → Scheduled adapter controlled-live → regime risk overlay。

## 4. 统一测试与证据

| 层级 | 内容 | 可否替代下一层 |
|---|---|---|
| L0 | Schema/config/docs contract | 否 |
| L1 | Planner/router/unit/property | 否 |
| L2 | Repository/service/migration/side-effect | 否 |
| L3 | 68 场景 recorded E2E | 否 |
| L4 | 代表性 controlled-live | 否 |
| L5 | regime/orchestration prospective shadow | 否 |
| L6 | activation/rollback/operational audit | 否 |

原始外部材料进入 ObjectStore；typed output 带 source/hash/PIT；benchmark 记录冷暖状态、样本量和分位数；live 记录 available time 与 route/fallback；失败证据同样保留，不以重跑挑绿。

## 5. 多 Agent 写入边界

| 工作包 | 唯一写入 lane |
|---|---|
| WP-01 | `schema-migration-and-planning-contracts` |
| WP-02、WP-03 | `account-and-session-state` |
| WP-04、WP-08、WP-09 | `orchestration-gateway-and-scenario-harness` |
| WP-05 | `official-macro-provider-and-data-contract` |
| WP-06、WP-07、WP-11 | `regime-evaluation-and-policy` |
| WP-10 | `scheduled-research-adapter` |

文档索引、ADR 状态、根计划和验收矩阵由架构/集成席统一收口；
`planning/work_packages_v1.yaml` 只能由架构/集成席更新；其他 Agent 通过 durable run/task receipt 报告状态，不直接并发改根计划。

并行 Agent 使用独立 worktree/任务范围；禁止同时改同一 migration、active policy、根计划或验收矩阵。

## 6. 不作为开发 backlog 的门

Phase 7/8 真实前向样本、长期自然 Continuous Monitor 场景、外部生产资格/数据权利、regime prospective 时间覆盖、用户未提供或券商未导入的真实成交，保持 `SHADOW`、`NOT_ADMITTED`、`UNKNOWN` 或 fail closed；不得为清计划造样本、猜交易或降低门槛。

对 E-02 等外部生产资格门，沿用既有合同术语：资格证据尚未满足本身可以是“无未完成开发任务”的安全稳定终态，不进入开发 backlog；只有出现可归因于本仓库且可实施的工程缺陷时，才建立具名工作包。

## 7. 完成定义

本计划只有在统一 preflight、能力覆盖、subject registry、public gateway、current macro、regime overlay 和 Scheduled Research Adapter 均形成机器合同与证据，68 个场景硬门通过，controlled-live/shadow/rollback 完成且所有者批准后才算完成。完成的工作包同时从本文和 active machine manifest 移除，稳定事实迁入架构，最近证据写入 `进度验收.md`。
