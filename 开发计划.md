# AStockMultiAgent 当前开发计划

> 状态：CURRENT
> 本文件只保存尚未完成的工作；已验证事实归档于《进度验收》与现行架构。
> 版本：investor-orchestration-and-regime-plan-v1
> 更新日期：2026-09-10
> 机器索引：`planning/work_packages_v1.yaml`
> 最终集成：durable run `lr_mtv2kqdo_ede506756fd4`；接手既有未提交工作，不 reset/restore/stash，不建立平行主线。

## 1. 当前开发状态

**无未完成开发任务。** active manifest 为空；历史实现与验证事实只保留在《进度验收》、现行 architecture/workflow、canonical Repo Skills、机器合同与 Git 历史中，本计划不保存关闭清单或过去进度。

运行期启用、正式投资结论、外部授权、长期样本和自然场景观察仍按各自机器门禁独立判断；条件未满足时保持对应 SHADOW / observation-only / fail-closed 状态，不因运行期门禁重新创建软件开发 backlog。

## 2. 永久边界

- `broker_execution_allowed=false` 永久保持；正式账户只允许研究、提醒和 append-only 用户事实，不自动下真实券商订单。
- actual / paper 永久分 lane；模拟操作仍要求既有准备、独立确认、确定性回放与审计，只有 fill 才改变模拟持仓。
- PIT、来源身份、对象 hash、请求/实体/账户绑定、财务/治理/估值/Universe/推荐门均 fail closed；不得伪造或放宽验收门求绿。
- recorded/smoke/DEGRADED 不能冒充 controlled-live 或 prospective；外部来源异常时保留待重试 bucket，不写经济事实、不激活功能。
- 原始 ObjectStore、生产 SQLite、`user_state/`、历史事件和未知归属文件不回写、不清理；所有开发/测试/验收产物只允许在 `D:\AStockMultiAgent` 内生成。

## 3. 发布验收

本轮风险等级为 L3。发布只接受最终冻结树上的文档合同、Ruff、Pyright、全仓 pytest、三市场真实 live/回退 smoke、`git diff --check`、归属/secret/private/runtime 审查与 defect-first Review。随后显式 staging、commit + push 并核对远端 HEAD；在这些步骤真实发生前，不在本文件预写 commit SHA 或“已发布”结论。
