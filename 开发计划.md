# AStockMultiAgent 开发计划

## 维护契约

- 本文件只保存当前未完成任务、尚未完成的真实运行/数据义务及其验收口径。
- 新任务编码前必须写明需求、根因、权威依据、方案、实施步骤、验收标准、测试与独立 Review 门、风险和回滚。
- 子任务通过独立 Review 与专业测试后，必须立即从本文件删除，并把当时已经成立的实现、代码、测试、性能和迁移事实写入《验收报告》；正式 release 结束后还必须执行一次发布后归档，把最终测试、release baseline SHA、tag/Release、远端校验等发布后事实补回《验收报告》，不得在本文件保留完成态项目名、step id、进度或发布过程。
- 架构事实只维护在 `docs/architecture/`；不得另建重复的《项目架构》状态副本。

## 当前状态

**当前没有未完成的代码发布主线**。本文件只保留下列尚未完成的长期运行/数据义务；任何已经通过独立 Review、专业测试并完成发布的实现任务，均不得继续作为计划项、完成清单或发布过程留在本文件中。

继续冻结的不可退化边界：PIT/source availability、provenance、Universe 完整性、正式财务/估值证据、独立 Bull/Bear/Reviewer/模型风险/投委会门、模拟确认，以及全局 `broker_execution_allowed=false`。Search/Web 只可发现或补充经 SourcePolicyGate/正式 capture 冻结的证据，不得冒充 Universe、negative proof、连续行情、ETF NAV/iNAV 或 hedge effectiveness 的正式证明。

## 独立长期运行/数据义务（非当前代码发布主线）

### Phase 7 / Phase 8 Prospective Study

- 正式 prospective study 仍需积累真实独立前向样本：100 个独立事件、12 个月、5 个 folds、3 个 regimes。
- 历史回放、fixture、补录 trial 均不得增加正式 forward-event count。
- Phase 8 在正式样本和治理门满足前继续 `NOT_ADMITTED`；不得因本轮 Portfolio/Holding 能力上线而提前准入在线学习、自适应生产调权或自动交易。

### Phase 11 / Phase 12 长期统计与 Skill 证据

- 随真实研究持续登记 all-trials、independence clusters、SkillUsageEvent、Catalyst/KPI 前瞻证据和真实 end-to-end duration。
- 无正式 prospective artifact 时，Skill 只能继续观察/评估，不得自动修改、退休或调权。

### 认证行业 taxonomy

- 当前组合研究可使用 caller-supplied risk group 做明确标注的 provisional 分组，但不能冒充官方行业分类。
- 后续只有取得具备 PIT/provenance 的稳定认证来源并完成版本化 taxonomy 后，才可正式执行用户 `max_industry_exposure`、行业集中度和行业 hedge 等强约束。
- taxonomy 未完成前，用户显式行业上限必须 fail closed，而不是静默忽略或使用未验证标签替代。

### ETF NAV / iNAV、折溢价与产品深度数据

- 当前 ETFResearchMetrics 已正式覆盖同一 `as_of` 的日线波动、平均成交额、绑定基准 tracking error，以及官方产品 profile 中的管理费/托管费/总费率。
- 当前 reference 体系没有正式、可审计的 ETF NAV/iNAV 连续序列，因此 `premium_discount_rate` 保持不可用；不得用二级市场价格或 Web 摘要自行反推正式折溢价。
- 后续如引入 NAV/iNAV、份额、申赎清单或更细的 ETF 产品事实，必须单独建立正式来源、PIT、质量与 provenance 合同。

### ETF paper execution / replay

- 当前 `InstrumentType.ETF` 只开放正式研究、产品 profile、PIT metrics、组合迁移和自然对冲评估基础。
- `PaperOperation` 与 `PaperReplay` 当前仍严格只准入 STOCK；ETF 不得套用股票 100 股/T+1/费用/公司行动规则。
- 若未来开放 ETF 模拟执行，须独立完成 instrument-specific 交易单位、价格单位、涨跌幅、结算、费用、申赎/二级市场边界、公司行动/分红和 replay 规则后另立开发主线。

### 显式空头与衍生品对冲

- 融券卖空、股指期货、ETF/股指期权、杠杆/反向工具、保证金和逐日盯市均不在当前系统能力内。
- 只有完成合约主数据、乘数/到期、保证金、适当性、行权/交割、费用、风险限额和独立模拟账本后，才可单独立项；当前 long-only 系统永不把普通股票或 ETF 组合描述为已经实现 `EXPLICIT_HEDGE`。

### Continuous Monitor 真实运行观察

- 长期 daemon、自然语言场景、crash/source-degradation、同一事件多持仓影响和 Agent worker 不可用时的持久任务恢复，仍需继续积累真实运行样本。
- 确定性 monitor 可持续更新行情/规则/replay/事件 lead；需要语义判断的任务允许保持 PENDING，但不得声称无人值守 Agent 已经完成研究。

### 外部既成交易更正与账户级现金事实

- 当前完整且无歧义的外部既成交易可 exactly-once 落入本地 trade lane；重复与 paper-fill 经济冲突已 fail closed。
- 用户主动更正历史成交、券商批量导入、转托管/非交易过户、账户级现金流和多账户隔离尚未形成正式统一导入协议时，不得通过手工改 Markdown 伪造事实；此类需求出现后另立状态迁移/对账主线。

## 后续新主线启动条件

只有出现新的真实用户需求、长期运行证据达到预注册门槛、外部规则/来源发生重大变化，或上述长期义务具备可正式开发的输入时，才在本文件新增代码发布主线。新增主线必须重新经过：需求/根因 → 权威依据 → 架构 → 计划 → 实现 → 独立 Review → 专业测试/全仓门 → 文档迁移 → release。
