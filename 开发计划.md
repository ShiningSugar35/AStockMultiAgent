# AStockMultiAgent 开发计划

## 维护契约

总方案只写长期设计；开发计划只写未完成项；验收报告只写已实现且有证据的当前事实。任务验收
通过时，必须在同一次修改中从开发计划删除并写入验收报告。被替代的方案从现行文档删除，由
Git 历史追溯。所有运行计数必须附时间、run ID 或工件身份。

当前只保留 Phase 5 视觉论证收口、视觉增强包重建和 OpenCode/DeepSeek 审核。已完成的程序合同、
纯文本运行和本地书视觉覆盖已迁入验收报告。

## 当前依赖顺序

`论证规则金标收口 → 本地书 REVIEW AU 收口 → 知乎图片证据冻结 → 三位作者视觉增强重跑 → OpenCode/DeepSeek →
候选审计与人工批准`

## K5-D3-R：论证规则金标收口

- 独立人工 gold 已冻结为 25 个跨段场景和 29 个有价值 AU；当前真实召回为 20/29，19 个场景存在
  修辞角色过度多标。
- Spark 一次返工后 Sol 二审仍为 P1，现由 Sol 接管语义漏斗生产规则。不得修改 gold 或指标公式
  迁就当前输出。
- 验收要求：设问—回答和 AU 边界 Jaccard 均为 1.0，有价值 AU 召回不低于 0.90，错误拆分、
  错误合并和仅上下文内容的下游泄漏均为 0，逐段角色与 gold 精确一致。

## K5-D4-R：本地书视觉 REVIEW AU 收口

- 《价值投资功法》视觉运行已覆盖全部 74 个 placement；55 个视觉相关 AU 中 11 个结构闭合并进入
  本地 DeepSeek 包，44 个保持 `NEEDS_REVIEW`，不得直接转成 Skill。
- 对 30 个 `DANGLING_CONTEXT_DEPENDENCY` 逐项检查跨页前后文；只有能从相邻页稳定定位到同一论证
  的，才以新 run 重建跨页上下文。不能闭合的保持 REVIEW。
- 对 9 个 `BOOK_VISUAL_NO_METHOD_KEYWORD_REVIEW_REQUIRED` 和 1 个 `OCR_NO_TEXT` 边界只做人工复核，
  不放宽关键词门、不猜图中内容。各 reason 可重叠，不能把分项相加冒充 AU 总数。
- 完成后生成新的 immutable semantic run、Embedding manifest 和 packet；旧 run 与覆盖报告不改写。

## K5-D5：知乎图片证据链

- 从三位白名单作者已冻结的回答、文章、专栏正文和想法快照中枚举图片；优先使用当前已登录 Chrome
  与 AStock 采集插件，只有本地链不可用时才进入 MCP，且不得绕过登录、验证码或访问控制。
- 每张图片先写 immutable snapshot，再记录 `source_snapshot_id`、URL hash、DOM locator、图片 hash、
  OCR input/text hash、引擎版本、置信度、类型、前后 Paragraph 和受影响 AU。
- 图片 Paragraph 固定 `standalone_distillable=false`。图表位于论点与结论之间时
  `MERGE_WITH_BOTH`；OCR 失败、低置信、无文字但疑似信息图、UNKNOWN 或上下文不完整均为
  `REVIEW_REQUIRED`。
- 现有三个纯文本 v3 包继续标记 `PROVISIONAL_TEXT_VIEW`。图片证据冻结后生成新的视觉增强 run，
  不覆盖旧对象，不把 `[图片]` 占位符当证据。

## K5-D6：OpenCode/DeepSeek 与候选审核

- 先处理已冻结的本地书 READY 包；三位知乎作者只在视觉增强包完成后启动。
- DeepSeek 逐 AU 输出严格 JSONL；视觉候选必须引用真实 Paragraph 和 visual evidence ID。结果经
  stage/import 原子校验后仍为 `PENDING / NOT_RUN`。
- Sol 审计去重、适用范围、反证、失效条件和来源链；最终批准或拒绝由用户确认，不自动进入研究
  路由、模拟账本或实盘。

## 全局门禁

执行定向测试、`uv run pytest`、`uv run ruff check .`、`uv run pyright` 和
`git diff --check`。不得删除运行证据、原始对象、用户改动、SQLite、密钥、Cookie、浏览器 Profile
或私有来源材料。
