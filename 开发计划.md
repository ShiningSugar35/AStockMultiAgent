# AStockMultiAgent 开发计划

> 基线：2026-08-31《AStockMultiAgent 系统体验与投研能力深度研究报告》
> 本轮主线：系统体验、正式报告、来源治理与外部生产备用能力，目标版本 `v0.3.0`
> 用户裁决：高星、持续维护的社区 Skill、MCP、抓取器和开源 Provider 可以成为生产备用，但必须先通过统一准入和既有证据链，不得凭 stars 自动获得正式性。

## 维护契约

- 本文件只保存当前未完成任务；同时只保留尚未完成的实现、验证和真实运行义务。完成并通过独立 Review 的工作包必须迁移到《验收报告》并从本文件删除。
- 此前“当前没有未完成的代码发布主线”的状态已由本轮 `v0.3.0` 主线取代；正式 release 结束后还必须执行一次发布后归档，完成验收、架构、Workflow、Skill 和远端发布事实的终局迁移。
- 所有新增能力复用现有 SourcePolicyGate、Provider Registry、SourceSnapshot、ObjectStore、Evidence、PIT、Research Team、Committee、模拟账本和 checkpoint，不建立第二套事实源、Router、Evidence、用户状态或 Agent 调度。
- 普通投资咨询默认 Investor Mode；Developer Mode 只能由明确诊断意图或显式开关进入。任何模式都不得暴露 Secret、Cookie、Token、私人材料或无关绝对路径。
- 外部社区能力的 stars、下载量、Registry 上架和演示成功只作维护/采用度信号。只有完成端点级 License/ToS、PIT、provenance、Secret、供应链、故障、成本、固定版本和退出测试，才允许以 `PRODUCTION_BACKUP` 进入现有 fallback。
- 本轮涉及跨模块状态、Provider、报告和正式推荐语义，最高测试等级为 L3；不得以单元测试替代受控外部、迁移、故障和发布验证。

## 依赖顺序与发布边界

1. 先冻结公共输出、日志、报告、来源准入和覆盖语义 schema。
2. 再实现统一出口、报告服务、配置一致性和来源资格验证。
3. 然后补官方来源、ETF/基金/指数、存储 SLO 与外部账户事实能力。
4. 最后运行 Humanize、社区 Skill/MCP/抓取器和量化框架实验；通过者只按其资格进入生产备用或影子层。
5. 每个工作包独立 Review；全部完成后执行全仓专业门、真实受控 smoke、版本升级、commit、push、annotated tag、GitHub Release、发布后文档归档和终局核验。
6. 真实券商自动下单、融券、期货、期权和执行型券商 MCP 永久不在本轮范围内。

---

## 必须做

### M-02｜结构化日志、错误摘要、脱敏、关联号与自动解决预算收敛

- **需求**：外部平台不可用时，Investor Mode 只用一句自然语言说明影响；详细错误、重试、fallback、熔断和调用链进入可关联日志。当前研究和 Research Team 使用唯一 1800 秒自动解决预算。
- **根因**：日志仅有简单 Formatter 且未形成统一业务入口；错误处理分散；`current_research_policy.yaml` 为 1800 秒而 `research_team.yaml` 为 7200 秒。
- **方案**：新增 `StructuredLogEvent`、correlation/run/request id、Secret/路径/隐私脱敏器、Operational/Audit/Security 事件分类、轮转文件 sink 和保留策略；`PublicErrorMapper` 将稳定失败分类映射为用户影响；统一自动解决政策源并在启动/合同检查时拒绝漂移。
- **涉及文件或模块**：`src/astock/core/logging.py`、`errors.py`、Provider/monitor/research 调用边界、`configs/current_research_policy.yaml`、`configs/research_team.yaml`、设置与 CLI、日志测试。
- **性能与兼容评估**：使用标准库异步/队列或轻量批量写入，避免引入常驻日志平台；Windows/Linux 路径兼容；按大小和日期轮转。
- **验收标准**：
  1. 429、超时、5xx、解析失败、数据冲突和陈旧数据均有一条稳定用户摘要。
  2. 每个摘要可通过 correlation id 定位内部重试、fallback 和熔断记录。
  3. 自动化扫描证明 Token、Cookie、Authorization、密码、私人目录和完整请求体不进入日志。
  4. 所有当前研究入口读取同一 1800 秒预算；配置不一致时 fail closed。
  5. 日志轮转、保留、损坏恢复和磁盘上限有测试。
- **风险**：过度脱敏影响排障；日志事件基数过高。
- **回滚**：禁用文件 sink，仅保留安全 stderr JSON；预算统一回退到 1800 秒。
- **测试等级**：L0/L1/L2。

### M-04｜正式报告服务、展示偏好与跨会话报告恢复

- **需求**：深入投研默认生成专业 DOCX；用户可选 MD/PDF。网页只返回核心结论、关键发现和路径。报告可恢复、可审计且默认不进入 Git。
- **根因**：项目无 ReportRequest/Manifest、模板、Known Folder、引用/图片清单、原子发布、报告偏好和失败恢复。
- **方案**：新增 `ReportRequest`、`ReportManifest`、`CitationManifest`、`AssetManifest`、`ReportPublishResult` 和 `PresentationPreferences`；使用 `python-docx` 作为首选 renderer，MD 为稳定回退，PDF 通过可选转换器；Windows 使用 Known Folder/注册表安全解析桌面，服务器使用 `report_root`；staging 写入、完整性验证、哈希和原子改名；按幂等键恢复。
- **涉及文件或模块**：新增 `src/astock/reports/`、schema、设置、迁移、CLI、模板/样式、`.gitignore`、用户偏好 repository、README/Workflow/Skills。
- **性能与兼容评估**：无 Office 依赖；Windows 7/10/11 和 Linux；图片去重和大小上限；用户报告默认放 Git 外。
- **验收标准**：
  1. DOCX 默认可在无 Office 环境生成并通过 zip/OpenXML 完整性检查。
  2. 路径不硬编码用户名；桌面不可写时原子回退到配置目录。
  3. 报告记录输入工件哈希、模板版本、引用、图片权利、隐私等级和输出哈希。
  4. 中断不会留下伪成功文件；同一请求可从 manifest 恢复且不重复发布。
  5. 展示偏好跨会话恢复、可覆盖、可删除，不进入模拟账本。
  6. DOCX 失败时稳定生成 MD 并准确告知降级。
- **风险**：字体、图片版权、路径权限、PDF 环境差异。
- **回滚**：禁用相应 renderer，保持 MD-only；不影响研究工件。
- **测试等级**：L0/L1/L2。

### M-05｜Universe 正式覆盖证据、覆盖状态分级与推荐门加固

- **需求**：正式全市场推荐只能在官方或等价强度的分母对账完成时成立；二级源自报 99.5% 只能表示工程高覆盖。
- **根因**：当前下游门能校验冻结 Seed Report，但上游 `formal_full_market_coverage_allowed` 可由二级源自报分母达到 0.995 后为真。
- **方案**：新增覆盖证明等级 `UNAVAILABLE / PARTIAL / ENGINEERING_HIGH_COVERAGE / OFFICIAL_DENOMINATOR_RECONCILED`、`UniverseCoverageProof` 和市场级对账；保留 99.5% 工程门，正式 full 仅认官方分母或经准入的授权主数据；Recommendation Gate、组合和投资者措辞消费新等级。
- **涉及文件或模块**：reference/seed/team schemas、market reference service、候选/推荐门、配置、迁移、报告/CLI/测试。
- **性能与兼容评估**：对账在主数据同步时一次完成；旧报告读取映射到安全的工程覆盖等级；不增加每只股票逐项网络调用。
- **验收标准**：
  1. 二级源 100% 自报覆盖也不能自动产生正式 full 语义。
  2. 每个市场记录分母来源、分子、差异、版本、可得时间和对象哈希。
  3. 正式推荐门只接受 `OFFICIAL_DENOMINATOR_RECONCILED`；其余可继续观察/研究但措辞准确。
  4. 零候选和 Universe 不可用继续保持不同状态。
  5. 旧工件迁移/读取不会被错误升级。
- **风险**：正式推荐率暂时下降；官方分母源短期不可用。
- **回滚**：保持工程覆盖和观察功能，禁用“全市场正式推荐”措辞，不回退为弱证明。
- **测试等级**：L0/L1/L2/L3。

### M-06｜外部能力统一资格验证、GDELT 回收与生产备用路由

- **需求**：官方、商业、高星社区 Skill、MCP、抓取器、解析器和开源 Provider 使用同一准入框架；通过资格验证的社区能力可以进入现有生产备用链。GDELT 等直接外部 I/O 统一进入 Provider Registry 和能力级韧性治理。
- **根因**：外部能力目前主要依赖人工文档判断；GDELT 在监控模块直接构造；社区工具缺少统一的权利、PIT、provenance、Secret、供应链、性能、有效期和退出证据。
- **方案**：新增 `ExternalCapabilityRegistry`、`CapabilityQualificationRequest/Report`、资格有效期、撤销记录和 `PRODUCTION_BACKUP` 路由属性，支持 API、Python library、MCP、crawler、parser 与 Skill。资格结果绑定固定版本、仓库/包哈希、License/ToS、SBOM、真实上游、PIT、provenance、凭证处理、资源成本、故障行为和回滚合同；具体运行范围由版本化 capability metadata、有效资格报告与任务上下文共同确定。首批登记 Arelle、Docling、Microsoft Playwright MCP、AKShare、Crawl4AI、changedetection.io，以及项目自有 `source-qualification-auditor`、`report-visual-qa`、`schema-drift-recorder`；GDELT 注册为现有监控能力的 Provider。SourceAccessRouter 只在资格与当前路由条件满足时选择生产备用。
- **涉及文件或模块**：Provider config/factory/router/resilience、monitoring/news、documents、financial integrity、reports、SourcePolicyGate、ObjectStore、schema、CLI、Repo Skills、tests、`configs/external_capabilities.yaml`。
- **性能与兼容评估**：资格验证离线或低频；线上只读取冻结注册表、资格状态和有效期，不实时扫描上游仓库；适配器懒加载、可单独撤销并具备退出测试。
- **验收标准**：
  1. 新外部能力无有效资格报告时只能 `DISCOVERY_ONLY` 或 `SHADOW`。
  2. 每项首批能力均有独立 capability、固定版本、License/ToS、数据权利、PIT/provenance、Secret/SBOM、性能、故障、成本和退出结论。
  3. 合格社区能力可被标记 `PRODUCTION_BACKUP`，但不会改变原始上游的权威级别、完整性语义或推荐资格。
  4. 外部结果沿用现有 ObjectStore、SourceSnapshot、Evidence 和 Provider lineage，不建立第二套事实源或路由。
  5. GDELT 受 Provider Registry、capability breaker、快照和既有证据合同控制。
  6. 撤销、到期或卸载任一候选后，主源、Router、Evidence、报告和账本仍可正常运行。
  7. 执行型券商 MCP 永久拒绝。
- **风险**：配置复杂、供应链和许可证变化、能力权限配置错误、备用源产生错误信任。
- **回滚**：撤销资格 release、停用对应 capability 或删除可选适配器；既有主源和正式工件保持不变。
- **测试等级**：L0/L1/L2/L3。

---

## 建议做

### R-01｜官方主数据、宏观 authority 与北交所正式来源补齐

- **需求**：提高沪深证券分母、宏观/政策和北交所财报/公司行动的正式覆盖。
- **根因**：沪深主数据正式分母主要依赖二级源；authority registry 未系统覆盖国家统计局、人民银行、财政部、发改委等；北交所官方财报/公司行动配置为不可用。
- **方案**：扩展官方域、能力和独立性组；新增 recorded-first 的上交所/深交所证券清单、国家统计局/人民银行/财政部/发改委宏观发布和北交所 exact-item/枚举 adapter；无法形成完整枚举时严格保留 exact-item 语义。
- **涉及文件或模块**：authority/provider/transport config、reference、documents、financial_sources、macro evidence、fixtures、contracts。
- **性能与兼容评估**：低频官方数据本地冻结；分页有上限和终止证明；不把网页搜索当连续序列。
- **验收标准**：
  1. 沪深官方证券分母可冻结并与现有 Master 对账。
  2. 核心宏观序列区分观察期、发布日期、修订版和可得时间。
  3. 北交所至少具备正式 exact-item 财报和公司行动证据路径；完整枚举能力如未证明不得声明完整。
  4. 单一官方站点失败时合格生产备用可接管相同 capability。
- **风险**：页面漂移、反爬、历史范围和条款变化。
- **回滚**：关闭对应 adapter，退回当前观察/NEEDS_INFO 语义。
- **测试等级**：L1/L2/L3。

### R-02｜ETF、基金、指数产品深度数据与 ETF 模拟执行

- **需求**：正式支持产品身份、基准、费率、规模、份额、NAV/iNAV、溢折价、成分和跟踪误差，并在规则完整时开放 ETF paper order/replay。
- **根因**：现有 ETF 只覆盖部分研究指标；NAV/iNAV 和模拟执行尚未准入，不能复用股票交易规则。
- **方案**：扩展 `ETFProductProfile/ETFResearchMetrics` 和产品 release；引入正式或合格备用来源；独立定义 ETF 交易单位、价格单位、涨跌幅、费用、结算、现金/实物申赎边界、分红和回放规则；PaperOperation 按 instrument-specific policy 路由。
- **涉及文件或模块**：schemas、portfolio、market/reference、provider registry、paper_trading、fee/rule config、monitor、reports、migrations/tests。
- **性能与兼容评估**：按关注/持仓增量同步；不全市场高频抓 NAV；股票路径保持不变。
- **验收标准**：
  1. 代表性 ETF 的身份、基准、费率、规模、NAV/iNAV、溢折价和跟踪误差可追溯且 PIT 正确。
  2. 缺 NAV/iNAV 时不计算伪溢折价。
  3. ETF 订单、费用、结算和 replay 使用独立规则；股票规则不会被误用。
  4. 只有确认订单和回放成交才改变模拟持仓。
  5. 关闭 ETF execution 后仍保留只读研究。
- **风险**：产品数据授权、申赎口径、交易所规则差异。
- **回滚**：禁用 ETF paper execution，保留研究和组合评估。
- **测试等级**：L0/L1/L2/L3。

### R-03｜业务 SLO、磁盘配额、日志/报告/ObjectStore 生命周期与长稳恢复

- **需求**：量化 Universe 覆盖、Provider 降级、证据新鲜度、正式研究转化、报告成功/恢复、任务积压、单位成本和磁盘增长；长期运行不会无限膨胀。
- **根因**：已有部分 archive/vacuum/orphan prune，但日志、报告、临时目录和 ObjectStore 缺统一保留/配额/SLO；真实长稳证据不足。
- **方案**：新增 `storage-lifecycle-plan/audit/run` 和 `operations-slo-report`；引用感知 dry-run、分层 TTL、配额、水位告警、报告/临时目录清理、日志轮转、ObjectStore 未引用候选审计；Continuous Monitor 记录 MTTR、积压和恢复。
- **涉及文件或模块**：settings、core/object_store/logging、reports、monitoring、observability、CLI、config、tests。
- **性能与兼容评估**：使用索引/manifest，禁止每周期全盘扫描；删除操作必须显式 `--confirm`；Windows 文件占用安全处理。
- **验收标准**：
  1. dry-run 明确每类对象数量、字节、引用和删除原因。
  2. 正式 SourceSnapshot/Evidence、账本、用户状态和活跃工件不会被误删。
  3. 长稳/故障演练中日志、报告、临时目录和队列有界。
  4. SLO 报告可显示覆盖、降级、新鲜度、报告、恢复、积压和成本。
- **风险**：引用判定错误、指标高基数、Windows 锁文件。
- **回滚**：禁用自动清理，只保留审计和告警。
- **测试等级**：L1/L2/L3。

### R-04｜外部交易更正、账户现金事实、批量导入和多账户隔离

- **需求**：在不接真实券商下单的前提下，安全导入用户已有交易、交易更正、转托管/非交易过户、现金流和多账户事实。
- **根因**：当前只支持完整单笔既成交易 exactly-once，历史更正、现金和多账户缺正式协议。
- **方案**：新增 append-only `ExternalAccountEvent`、更正/冲销关系、账户身份和现金事件；CSV/JSON 批量 importer 先校验再确认；与 paper fill 经济冲突 fail closed；本地 Markdown 继续只是投影。
- **涉及文件或模块**：local_portfolio、schemas、state migration、portfolio evaluation、CLI、reports、tests。
- **性能与兼容评估**：事件回放 O(n)，按账户索引；旧单账户数据迁移到默认账户；不保存券商凭证。
- **验收标准**：
  1. 批量导入幂等，错误行不产生部分提交。
  2. 更正通过冲销/替代事件完成，不覆写历史。
  3. 现金、持仓和成本可按账户对账；不同账户不串数据。
  4. 与 paper fill 冲突继续阻断。
- **风险**：用户源文件口径、时区、重复和现金分类错误。
- **回滚**：关闭 importer；保留已确认 append-only 事件和旧投影兼容。
- **测试等级**：L1/L2。

---

## 实验性验证

### E-01｜受约束中文 Humanize A/B

- **需求**：验证规则审计后的小幅润色是否确实提升自然度，而不改变金融事实。
- **根因**：纯规则能去除硬问题，但不能保证所有文本自然；自由 Humanizer 又有事实漂移和隐私风险。
- **方案**：新增离线评测 harness；比较“仅规则”“项目受约束提示”“高星 Humanizer Skill”三组；只允许编辑非事实文本片段；结构化事实、数字、实体、方向、时间、引用和风险强度锁定；人工盲评和自动漂移检测并行。
- **涉及文件或模块**：`experiments/presentation_humanize/`、fixtures、qualification registry、评测报告；生产默认关闭。
- **性能与兼容评估**：离线运行，有成本上限，不增加线上延迟；输入先脱敏。
- **验收标准**：
  1. 事实漂移率必须为 0。
  2. 自然度、清晰度和简洁度相对规则基线有预注册的实际提升。
  3. License、隐私、成本和退出均通过后，才能提交受控生产备用提案。
- **风险**：主观偏差、过度口语化、提示注入。
- **回滚**：删除实验开关/依赖；生产继续使用确定性风格合同。
- **测试等级**：L0/L1（实验）。

### E-02｜已批准社区 Skill、MCP、抓取器、解析器和开源 Provider 生产备用试点

- **需求**：对已批准的九项候选逐项完成真实资格验证、适配、recorded 验证、受控 live 验证与退出演练；通过者进入可撤销的生产备用，未通过者保留影子或拒绝状态。
- **根因**：外部生态可补足 XBRL 校验、复杂文档解析、动态页面访问、结构化金融端点、网页提取、站点变化监测、来源资格审计、报告视觉验收和 Schema 漂移记录，但不能直接依赖社区热度或演示结果决定生产准入。
- **方案**：
  1. **Arelle**：作为 XBRL taxonomy、context、unit、dimension 与 calculation consistency 的专项验证器，结果写入现有 Financial Integrity/Evidence 合同。
  2. **Docling**：作为复杂 PDF、Office、HTML、版面和表格结构的解析备用，原始材料和解析结果继续进入现有 ObjectStore/文档管线。
  3. **Microsoft Playwright MCP**：作为浏览器 transport 候选，通过 ExternalCapabilityRegistry 登记具体 capability、版本、运行范围、资源预算和退出合同。
  4. **AKShare**：按 endpoint/capability 分别登记金融 Provider 备用，不以包级资格替代端点级上游、权利和字段验证。
  5. **Crawl4AI**：作为网页抓取与结构化提取候选，完成依赖、许可证、安全记录、资源成本、输出一致性和故障恢复验证。
  6. **changedetection.io**：作为站点变化监测候选，将变化事件接入现有监控、任务与证据调查链。
  7. **`source-qualification-auditor`**：新增 Repo Skill，封装资格取证、裁决、到期复核、撤销与卸载验证；Skill 本身不授予来源权威性。
  8. **`report-visual-qa`**：新增 Repo Skill，执行 DOCX/PDF 渲染、分页、表格、字体、引用、图片权利、隐私和输出哈希验收。
  9. **`schema-drift-recorder`**：新增 Repo Skill，保存 raw-first 响应、生成差异与 fixture、提交受控修复提案，不在线自改生产解析器。
- **涉及文件或模块**：`configs/external_capabilities.yaml`、optional dependency groups、adapters、Provider/transport factories、documents、financial integrity、monitoring、reports、`.agents/skills/`、recorded fixtures、qualification reports、docs/scouting、SBOM/License 输出。
- **性能与兼容评估**：所有候选懒加载并按 capability 单独启用；资格与维护核验低频执行；大依赖和浏览器运行时不进入核心最小安装；缺失、到期或撤销时按现有路由安全降级。
- **验收标准**：
  1. 九项候选分别形成固定版本、来源、维护快照、License/ToS、数据权利、SBOM、Secret、PIT/provenance、权限范围、性能、故障和退出报告。
  2. Arelle 与 Docling 通过固定材料对拍；错误、分歧和缺字段不会静默覆盖现有正式结果。
  3. Playwright MCP、Crawl4AI 与 changedetection.io 分别通过与其 capability 对应的 recorded/受控 live 验证和资源边界测试。
  4. AKShare 至少有一个明确 endpoint 达到 `PRODUCTION_BACKUP`，且原始上游、字段语义、可得时间和失效行为可审计。
  5. 三个 Repo Skill 均有触发正例、否定例、冲突路由、事实/权限保护、恢复和卸载回归，并进入现有 Skill observability。
  6. 每个通过项关闭或卸载后，主仓、主源、正式推荐门、报告链和账本保持正常。
- **风险**：供应链、服务条款、数据权利、依赖体积、浏览器资源、上游漂移和候选间功能重叠。
- **回滚**：撤销资格 release、关闭单项 capability、删除可选依赖或 Repo Skill；不迁移现有正式事实源和账本。
- **测试等级**：L0/L1/L2/L3（试点）。

### E-03｜Qlib、RD-Agent、LEAN/RQAlpha 的最小模式对拍

- **需求**：验证实验记录、自动研发、事件顺序、回测鲁棒性等外部模式是否提供本项目没有的边际价值。
- **根因**：整套引入会复制现有架构，但部分模式可能提升研究效率和验证质量。
- **方案**：使用固定本地数据和独立沙箱，对 Recorder、假设迭代、事件/成交顺序、鲁棒性报告做最小基准；禁止写生产 Provider、Evidence、Committee、组合权重和模拟账本；只输出采用/不采用的小型模式建议。
- **涉及文件或模块**：`experiments/external_quant_patterns/`、固定 fixtures、资格报告、scouting 文档；不进入默认依赖。
- **性能与兼容评估**：隔离虚拟环境和磁盘上限；实验结束可完整删除。
- **验收标准**：
  1. 每个候选有可复现基准、依赖/许可/退出报告。
  2. 明确量化相对现有实现的新增价值和重复部分。
  3. 仅将小型模式写入后续提案，不导入第二套平台。
- **风险**：实验污染、依赖冲突、对海外市场假设误迁移。
- **回滚**：删除沙箱和实验依赖，主仓不保留运行依赖。
- **测试等级**：L0/L1/L2（沙箱）。

---

## 暂不采用

| 方案 | 原因 | 边界 |
|---|---|---|
| 无约束 Humanizer 直接重写正式投资结论 | 可能改变事实、数字、引用和结论强度 | 只允许 E-01 事实锁定后的离线/受控备用 |
| 以 stars、下载量或 Registry 上架自动批准生产 | 无法证明上游数据权利、PIT、provenance 和安全 | stars 只作候选排序；生产资格由 M-06 决定 |
| OpenBB/MCP/AKShare 作为第二数据总线或第二 Router | 会造成双写、冲突和审计漂移 | 合格能力只能作为现有 Provider/transport 的生产备用 |
| 外部能力脱离资格报告和 capability 合同直接提升事实权威 | 会绕过 SourcePolicyGate、PIT/provenance 与正式推荐门 | 结果只按有效资格报告、来源证据和任务上下文解释 |
| 执行型券商 MCP、真实券商自动下单、融券、期货、期权 | 与全局安全边界冲突 | 永久拒绝；真实交易仅由用户在券商端人工执行 |
| 网页端默认粘贴完整长报告 | 首屏无结论、重复、泄漏风险 | 默认短答并给正式报告路径 |

## 独立长期运行/数据义务

以下事项不能靠一次开发或 fixture 冒充完成，但本轮软件应为其提供统计和准入能力：

- Phase 7/8 正式前向样本仍需 100 个独立事件、12 个月、5 个 folds、3 个 regimes；Phase 8 在门槛前保持 `NOT_ADMITTED`。
- Phase 11/12 继续积累 all-trials、independence clusters、SkillUsageEvent、Catalyst/KPI 和真实端到端时长。
- 认证行业 taxonomy 只有取得可许可、PIT/provenance 完整的数据 release 后，才可正式执行行业强约束。
- Continuous Monitor 需持续积累自然场景、来源降级、崩溃接管、Agent worker 不可用和磁盘增长的长稳证据。
- 商业数据源只有用户具备相应授权、Secret 和再分发权利时才可启用；代码完成不等于外部合同完成。

## 全局完成定义

- 所有工作包达到各自最高测试等级，独立 defect-first Review 通过；L3 不得只用 recorded fixture 代替必要的受控真实验证。
- 所有新增外部能力具备固定版本、资格报告、License/ToS、SBOM/漏洞、Secret、PIT/provenance、成本、故障和退出证据。
- 公共输出事实、数字、证券身份、时间、方向、引用和推荐权限在展示转换中零漂移。
- 正式研究、推荐、组合和模拟交易硬门不得因体验层或生产备用源放松。
- 完成项从本文件删除，实施/测试/性能/迁移/安全事实写入《验收报告》，现行架构、Workflows、Skills、README 同步更新。
- 完成版本升级、全仓门、release baseline commit/push、annotated tag、GitHub Release、远端资产校验和发布后 docs-only closeout；最终工作区干净、无未知后台任务、唯一 durable run 完成。
