# AStockMultiAgent 当前开发计划

> 状态：CURRENT / NO OPEN DEVELOPMENT WORK PACKAGES
> 本文件只保存尚未完成的工作；已验证事实归档于《进度验收》与现行架构文档。
> 版本：investor-orchestration-and-regime-plan-v1
> 当前已发布主线：full-research-recommendation-v1 / package v0.4.0
> 更新日期：2026-09-13
> 机器索引：`planning/work_packages_v1.yaml`
> durable run：`lr_mtzhhvph_32d4e46b16a8` 已完成开发与验证收口，最终 completion 以 durable gate 为准。

## 1. 当前状态

当前**无未完成开发任务**。WP-22（Current-Only Research Recovery Refactor）已经完成实现与 L3 验证，验收事实记录于《进度验收》；因此机器索引 `planning/work_packages_v1.yaml` 的 `work_packages` 为空。后续只有出现新的、明确授权的开发需求时才新增 Work Package，不把运行时自然样本积累、外部授权等待或普通监控状态重新包装成开发 backlog。

## 2. 当前唯一投资决策架构

所有可直接形成证券选择、买卖判断、价格、数量或仓位的用户请求统一归一为 `FULL_RESEARCH_RECOMMENDATION`。当前设计不存在并行的轻量荐股、旧 BUY/HOLDING/PORTFOLIO decision 或独立 Recommendation Readiness 发布路径；历史名称若由旧输入带入，只能在 request construction 时迁移到 Full Research，不能拥有 Planner、Executor、Response 或 Publication 权威。

CURRENT 研究是**当前信息向前运行**：系统在同一请求中持续采集、修复、交叉验证公开事实，并在 bounded acquisition/recovery 完成后冻结最终当前决策快照；不得以用户提问时刻截断随后正常取得的新数据，也不得要求旧 reference release 保持历史 PIT head。显式 historical/backtest/replay 才使用独立 anti-lookahead/PIT/source-availability 约束，且不得反向约束 CURRENT。

Candidate 只表示“值得进入深研”。CURRENT Candidate 的核心发现输入是证券身份、交易日历、最低充分日线与数据质量；公司行动、公告与财务完整性属于 enrichment，缺失时保留自动恢复任务而不是把核心候选直接删除。任何 Candidate、Seed、Promotion 或 Web/LLM 补证结果都没有 BUY 权威。

正式公共投资建议的唯一发布对象仍是不变的 `RecommendationResearchReceipt`，并必须通过 Publication Gate。Financial/Governance CriticalVeto、完整公司深研、估值、独立 Bull/Bear/Reviewer、Committee、组合风险、价格 freshness 与 Mandatory Research DAG 均继续生效；`broker_execution_allowed=false` 永久保持。

## 3. 自动恢复与硬边界

CURRENT 的可恢复缺口统一按“重读/重建 canonical object → allowlisted provider fallback → bounded authoritative Web 多源补证并回灌 Evidence/ObjectStore → 回到原节点重跑”处理。`NEEDS_INFO` 是内部恢复状态；只有公共自动恢复预算真实耗尽，或剩余事实只能由用户私人输入提供时，才允许升级为用户侧输入请求。

保留的硬边界包括：证券/公司身份必须一致；关键对象必须可读且来源可追溯；重复、损坏、事实冲突和伪造证据不能静默进入正式结论；财务/治理 CriticalVeto、Committee 与 Publication Gate 不得绕过；无新鲜行情不得生成即时成交数量；actual/paper 分 lane；真实券商执行始终禁止。恢复预算是有界策略参数，不再把默认 1800 秒硬编码为身份合同。

## 4. 已发布 v0.4.0 边界

v0.4.0 release baseline 为 `a511d64c46076ccaaf6dbc9eabd7056c66d96369`；annotated tag `v0.4.0` 固定指向该 baseline。WP-22 只推进后续 `main`，不得移动该 tag，也不把本次开发误写成新的 v0.4.0 release。

## 5. 永久发布否决

验收证据必须来自真实执行与可复现工件，**不得伪造**样本、PASS、覆盖率、receipt、release 或远端状态。任一以下条件成立即不得发布后续版本：Full Research Publication Gate 可被旁路；Mandatory 节点非 PASS 仍能输出证券+价格+数量/权重；CURRENT source lineage / 决策快照不完整或存在未解决重大冲突；显式 historical/replay 发生 PIT leakage；CriticalVeto 漏放；stale quote 生成即时股数；receipt tamper/replay 失败；真实账户写入混入 Full Research；全仓测试/静态/文档门不绿；active Skill/Workflow/架构文档把旧历史式 CURRENT 截断或旧轻量决策路径描述为当前设计；输出或临时文件越出 `D:\AStockMultiAgent`。
