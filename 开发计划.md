# AStockMultiAgent 开发计划

## 维护契约

- 本文件只保存当前未完成任务、尚未完成的真实运行/数据义务及其验收口径。
- 新任务编码前必须写明需求、根因、权威依据、方案、实施步骤、验收标准、测试与独立 Review 门、风险和回滚。
- 子任务通过独立 Review 与专业测试后，必须立即从本文件删除，并把实现、代码、测试、性能、迁移和发布证据写入《验收报告》。
- 当前 External Dependency Resilience 的唯一 durable run 为 `lr_mtb4gekw_ff5540ff05d2`；不得创建平行 long-run。已完成并已迁移事实的 s1–s7 只保存在《验收报告》、durable run 与运行工件中，不在本文件重复登记；s7 只有在收敛后的最终冻结树五门全部 0 failed 后才在 durable run 中正式标记完成。
- 架构事实只维护在 `docs/architecture/`；不得另建重复的《项目架构》状态副本。

## 当前未完成主线：External Dependency Resilience v1 收尾与正式发布

### s8 — 显式提交、推送、Tag/GitHub Release 与 clean worktree

**需求与根因**

- 当前 `main` 的 `HEAD == origin/main == 5dae1381d14d88b12f310aba75a4c3afc3a34c4b`；无 staged、无 tag、无 GitHub Release、无 `.github/workflows/`，大量有效成果仍为 unstaged/untracked。
- 必须依据仓库真实版本语义确定下一版本和发布方式，不得猜版本或虚构 CI/CD。

**方案与实施步骤**

1. 审计版本来源、remote、migration 0060、GitHub CLI/权限、tag/release 约定、secret/private/runtime/untracked 和发布策略。
2. 只用显式路径暂存；完整审阅 staged diff、文件清单、secret scan 与 private/runtime 命中。
3. 所有门通过后 commit、push，并验证远端分支 commit 与本地一致。
4. 创建并验证正式 tag 和 GitHub Release；不得 force push 或移动已发布 tag。
5. 远端 release 验证成功后，把架构状态更新为 `RELEASED`，提交并推送该状态迁移；再次验证远端一致。
6. 验证 `git status --short` 为空，且本地、远端、release/tag 关系有明确证据。
7. 把最终测试、live、commit、push、tag、release、clean worktree 证据写入唯一 durable run；执行 `long_run_review`，只有 PASS 后才执行 `long_run_complete`。

**验收、测试与 Review 门**

- staged diff 与最终 commit 不含 secret、Cookie、token、私有绝对路径、`runtime/`、`.ai-bridge/`、缓存或用户本机状态。
- commit/push/tag/release/远端验证均真实成功；最终 worktree clean。
- durable run 所有旧失联 task 均有显式终态解析，s6–s8 均为 done 且有证据；最终 review PASS、complete 成功。

**风险与回滚**

- 风险：错误版本、GitHub 权限不足、remote/tag 冲突、误暂存运行工件、发布后文档状态漂移。
- 控制：先审计后暂存；不使用 `git add .`/`git add -A`、不 force push；Tag/Release 创建前再次核对 commit。
- 回滚：commit 前可从 index 取消单个误暂存路径而不改工作树；push 后只允许正常追加修复提交。已公开 tag/release 不强移，必要时创建明确的修复版本。

## 独立长期运行/数据义务（非本轮代码开发）

- **Phase 7 / Phase 8**：正式 prospective study 仍需积累 100 个真实独立前向事件、12 个月、5 个 folds、3 个 regimes；当前不能用历史回放或 fixture 伪造，Phase 8 继续 `NOT_ADMITTED`。
- **Phase 11 / Phase 12**：随真实研究持续登记 all-trials、independence clusters、SkillUsageEvent 与 Catalyst/KPI 前瞻证据；无正式 prospective artifact 时不得自动修改、退休或调权 Skill。
- **认证行业 taxonomy**：取得具备 PIT/provenance 的稳定来源后再发布版本化 taxonomy；此前 caller-supplied group 不能冒充官方行业分类。
- **Continuous Monitor 真实运行**：长期 daemon、自然语言场景、crash/source-degradation 观察仍需真实运行积累；确定性 monitor 可保留语义任务为 PENDING，但不得声称无人值守 Agent 已完成研究。
