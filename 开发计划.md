# AStockMultiAgent 开发计划

## 维护契约

总方案只写长期设计；**开发计划只写未完成项**、尚未完成的运行/数据义务及必要边界；验收报告只写已实现且有证据的当前事实。任务验收通过时，必须在同一次修改中从开发计划迁移状态并写入验收报告。被替代的执行路径须明确标记 `SUPERSEDED`，既有 run、工件和审计记录不得删除。尚未产生运行证据的能力统一使用 `PENDING/NOT_RUN`，不得用计划状态冒充已运行事实。

## 当前真实状态（2026-08-12）

- Git：当前开发主线已经收敛到 `main`。本轮发布完成后只保留 `main` 本地/远端分支；提交继续采用显式文件清单，不使用 `git add -A` 混入运行时或诊断材料。
- **Phase 5：COMPLETE**。baseline registry 保持 `231 admitted`，visual overlay `422 admitted`，composite registry `653 admitted`；正式委员会权重仍为 `false`。知识主链继续固定为 `SourceItem → ParagraphUnit → ArgumentUnit → SkillCandidate`；`ParagraphUnit` 是原文存储和定位单位，最终方法判断以完整 AU 为单位。
- **Phase 6：DEVELOPMENT COMPLETE**。通用 Research Runtime、输入自动发现、TradingClassification、分阶段 checkpoint/hash/耗时/cache、ContextBudget/Performance、classification-bound `ClassifiedTradeProtocol` 与 `ResearchPaperDecision` 已实现。当前 LIVE 投资咨询先完成 `research-acquire-current`，在本轮采集结束后冻结统一 decision snapshot；historical/recorded/backtest/正式前瞻研究仍显式绑定 `as_of` 与 source availability，保持防未来数据边界。缺决策相关正式证据时仍 fail closed，不猜测、不写模拟账本、不连接券商。
- **Phase 7：DEVELOPMENT COMPLETE / EVIDENCE COLLECTING**。正式 forward study 已创建：`shadow-study:1a45f7ee4b35d23b545bae881c751566ef5d91f60c65aa2b7b234a22089fc9de`，6 个 arm，`shadow-audit=PASS`。真实独立前向事件仍为 `0/100`、assignment=`0`、observation=`0`；这些计数只能由未来真实研究事件增长。
- **Phase 8：DEVELOPMENT COMPLETE / USER STATUS NOT_ADMITTED**。活动 policy 合同统一为 12 个月、100 个独立决策、5 个 walk-forward folds、3 个 market regimes；内部保留精确 reason code，用户态固定映射 `NOT_ADMITTED`。adaptive weight、online learning、main paper ledger write、automatic Skill modification、broker execution 均保持 `false`。
- **自然语言产品面：DEVELOPMENT COMPLETE**。9 个 canonical Repo Skills 固定在 `.agents/skills/` 供 Agent discovery，顶层 `skills/README.md` 只做人类可见能力目录，不复制第二套 `SKILL.md`；`docs/workflows/` 已建立 10 条跨 Skill Workflow，覆盖当前单股、候选、证据、财务、Committee/TradePlan、组合、持仓、模拟盘、知识采集与前瞻评估，并由文档合同测试验证 Skill→Workflow 链接。`$astock-research-orchestrator` 将“公司怎么样 / 能不能买 / 买卖计划 / 评估组合 / 推荐组合”等问题路由到确定性 CLI 与冻结工件。广泛荐股探索先合并已有 `RESEARCH_READY` Candidate、市场流动性/规模 Seeds 与由当前已发布大 V Skills 动态推导的 Expert Domain Seeds，再由 `research-seeds-promote` 对有限集合自动冻结 `CandidateInstrumentUniverseProof`、reference/质量/官方公司行动/公告/财务输入并运行 Candidate Scan；ResearchSeed 本身无推荐权。Serenity 当前为 `research-skills-v3`；所有上游方法只形成 evidence-bound Delta，不直接产生交易权重。组合层四套提案仍以受约束等权为默认，优化器不能覆盖单股投委会结果。
- **Phase 9：DEVELOPMENT COMPLETE**。机构级基本面主链已实现为 `EvidenceSufficiencyReport → IndustryProfile / CompanyEconomicsProfile → DriverTree → ForecastPack → ValuationPack → FundamentalModelBundle → InstitutionalDecisionContext`。Forecast/DCF/reverse-DCF/sensitivity 均由 Python 确定性复算；裸市场价格不再进入 expected-return/reverse-DCF，只有绑定注册 artifact/hash 且满足 PIT 的 `MarketPriceAnchor` 才可参与。新当前公司研究默认要求该链，历史 Phase 6 recorded 路径仅保留兼容；Bundle/DecisionContext 作为共享 PRIMARY 冻结输入进入 Committee，不新增投票席位或权重。

## Phase 5 已完成后的不可变边界

1. 不删除或改写旧 semantic run、AU、Embedding、direct 243 Skill、ShadowBundle、baseline/composite knowledge registry、VisualEvidencePack、visual Skill generation/review/release 或对应 ObjectStore 对象。
2. Research Runtime 只能经 `KnowledgeSkillProvider` 使用已发布 registry；不得在 runtime 内直接查询 knowledge 表或绕过 registry/hash 校验。
3. 社区作者观点、OCR 和图片分类只作为方法与研究线索；上市公司关键事实仍必须回到公告、交易所、财报等更强来源。
4. Committee 只读冻结工件；新当前公司研究的 runtime 主顺序固定为 `Evidence → Financial Integrity → Institutional Fundamental Model / Decision Context → BaseCase → Serenity Delta → Knowledge Skill Delta → Committee → TradingClassification → ClassifiedTradeProtocol / ResearchPaperDecision`。历史 recorded Phase 6 链只作兼容，不得覆盖该 vNext 默认路径。
5. Committee 原生 `TradeProtocol` 在通用 runtime 中只是 `committee_protocol_draft`；只有 TradingClassification status 与 audit 均通过后，才可冻结最终 `ClassifiedTradeProtocol`。最终协议必须绑定 DecisionPack、committee protocol 与 TradingClassification 的 exact object hash。
6. `broker_execution_allowed=false` 是全局硬边界。系统没有券商 API、自动 BUY/SELL 或自动实盘入口；真实交易只能由用户在券商端人工执行。
7. Phase 7 的历史回放、fixture、回测、事后补登记均不能增加正式 forward-event count。
8. Phase 8 即使未来满足 admission，也不会自动开启动态权重、在线学习或主模拟账本；任何后续 research authorization 必须另行显式、版本化批准。

历史索引：`Priority 1：可恢复的单股票 Research Runtime` 已于 2026-08-10 完成并迁入《验收报告》，**不是当前待办**。

## 当前未完成主线

Phase 9–12 的代码开发与回归验收已迁入《验收报告》；Phase 7/8 的真实前瞻样本仍受时间约束，不能靠补代码伪造完成。2026-08-13 新增一条独立工程主线：**Adaptive Edge / Deterministic Core 架构重构**。目标不是把确定性内核改成 LLM 黑箱，而是消除“YAML 看似可配、Python 又复制一份常量/路由”的双轨事实源，让外围数据获取、provider/schema 适配、研究调度和用户呈现具备可发现、可恢复、可扩展能力；PIT、不可变证据、数学恒等式、人工确认、账本与 broker 禁用等安全门继续 deterministic/fail-closed。

ARCH-R1/R2/R3/R4/R5 架构重构已完成并迁入《验收报告》。当前没有剩余 Adaptive Edge / Deterministic Core 代码开发项；后续只能通过新的 versioned policy/registry/dialect/plugin/proposal 发布变更，禁止重新引入业务 Service 与配置双轨事实源。当前未完成事项仅为下述受真实时间/样本约束的数据义务。

### 明确不进入当前开发主线

- 不建设 GraphRAG/GNN/Agent swarm；Claim DAG 足够解决当前 provenance 问题。
- 不建设 LLM expected-return → Max Sharpe、深度 RL 组合/执行、在线自动 Skill mutation、house investment LLM 或无假设的大型另类数据湖。
- 不继续堆几十个风险指标；Portfolio vNext 以 ES/CVaR、drawdown、factor、stress、liquidity、implementation、attribution 为主表面。

## 剩余真实运行/数据义务

### P7-DATA1：前瞻登记真实研究事件

- 在正式 study `shadow-study:1a45f7ee4b35d23b545bae881c751566ef5d91f60c65aa2b7b234a22089fc9de` 下，持续以前瞻方式登记真实 `ResearchMemo → ShadowDecision → TradeProtocol`。
- 每个事件必须在结果可见前冻结 assignment、independence key、输入工件与 market-regime snapshot；超过策略登记窗口或使用未来可见数据必须拒绝。
- 当前真实计数：`0 / 100`。这是时间和真实研究事件约束，不是待开发代码项。

### P7-DATA2：冻结未来观察并达到评估门槛

- 对已登记事件等待真实未来行情，按 5/20/60 个交易日要求冻结 Future Observation，并从冻结未复权 5m OHLCV 复算价格、成交量、MFE/MAE、成本与观点状态。
- 使用 `shadow-forward-market-freeze`、`shadow-observation-record`、`shadow-status`、`shadow-audit` 持续推进；正式计数始终读取 SQLite/ObjectStore 当前状态，不再维护可漂移的根目录状态副本。
- 只有达到至少 100 个独立事件，并满足活动 policy 的 observation months、5 folds、3 regimes 及完整性要求后，才运行正式 Phase 7 evaluation。

### P7-DATA3：组合方法的真实样本外比较

- Portfolio Construction 当前 active `portfolio-allocators-v1` policy 把 `EQUAL_WEIGHT_CONSTRAINED` 作为默认稳健基准，并启用 `INVERSE_VOLATILITY / HIERARCHICAL_RISK / SHRINKAGE_MIN_VARIANCE` 作为对照；这四种方法均通过 `PortfolioAllocatorRegistry` 插件实现，不是代码待办。
- 在真实 Phase 7 事件和未来行情积累后，须用相同候选集、相同单股准入、相同费用/PIT/约束做 walk-forward 对照，评估净收益、最大回撤、CVaR/CDaR、换手、集中度和尾部损失。未有稳定样本外证据前不得因为回测或单次表现替换默认等权。
- 组合方法评价不得增加或伪造现有正式 forward-event count；若未来需要新增组合方法比较臂，必须创建新的版本化 prospective study，不改写当前 6-arm study。

### P11-DATA1：持续登记 all-trials 与独立性 cluster

- Phase 11 软件合同已经完成；从下一条真实研究漏斗事件开始，Seed rejected、Promotion blocked、Candidate rejected、Committee `REJECT / NEEDS_INFO / WATCH / APPROVE_SIMULATION` 等都应通过 `prospective-trial-register` 冻结到 all-trials registry，并绑定 study-specific governance config。
- 每条 trial 必须冻结 stock / industry / theme / decision-date / shared-catalyst 中适用的 cluster IDs、independence unit、PIT 输入和 ex-ante market-regime rule。该 registry 用于选择偏差和相关性治理，`formal_trade_event` 永远为 false，不能增加 Phase 7 正式 forward-event count。
- 随样本积累持续运行 `prospective-trials-report` 与 `prospective-statistics-plan`；只有 independence contract 真正达到门槛，才能说“独立性样本门槛达到”，仍不等价于 Phase 7/8 已通过正式评估或准入。

### P12-DATA1：积累 Skill 使用效果与 Catalyst 前瞻证据

- 新 Research Production 路由、usage instrumentation 与 Catalyst Registry 已完成；后续真实研究运行应冻结 `SkillUsageEvent`，记录是否修正 claim、发现 gap、改变 driver、提供 falsifier、改变 IC、token cost，以及有正式前瞻评估来源时的 prospective lift。
- 没有 prospective evaluation artifact/hash 时，Skill 只能返回 `INSUFFICIENT_PROSPECTIVE_EVIDENCE`，不得根据主观印象或 token cost 自动退休、改写或调权。
- Catalyst/KPI 事件应登记预期窗口、KPI 规则和受影响模块；状态变化后只重跑受影响模块。不得为了“有更新”而无差别重跑完整研究链。

### P8-ADMISSION：等待真实 Phase 7 证据满足准入

- 当前用户态：`NOT_ADMITTED`；内部原因是 Phase 7 尚无 evaluation/admission 工件。
- 达到真实门槛后运行 deterministic admission。若状态达到 `ELIGIBLE_RULE_STATE_MACHINE_RESEARCH`，仍只进入“等待显式 research approval”，不会自动启用 adaptive weights 或 online learning。
- 未满足门槛前，Phase 8 继续返回精确 gap/reason code；不得通过修改阈值、补假样本或历史回放提前准入。

### PROD-DATA1：认证行业 taxonomy

- 当前 Portfolio Analysis 可以读取明确提供的 `industry_tag/risk_group`，但项目尚无已验收的申万/中证等正式行业分类 release；因此当前组暴露会显式标记 provenance 风险，不能冒充官方行业分类。
- 后续如取得稳定、可审计且具有 `available_to_system_at` 的行业分类源，应新增版本化 taxonomy release，并让组合行业集中度硬门绑定 exact release/hash。完成前，单股 10%、总敞口 80%、相关性、回撤及单股投委会仍是正式硬门，group constraint 仅作辅助约束。

## live 数据前置条件

- TradingClassification 的板块、风险警示、停牌、涨跌幅制度与有效期已有版本化 resolver；但 exact live 公司行为基准必须是**在决策 `as_of` 之前已经冻结且可审计的官方事实**。二级结构化接口返回 0 行不能被解释为“官方证明没有除权除息”。未取得 certified corporate-action baseline 时正确状态为 `NEEDS_INFO`。
- 这属于 point-in-time 数据可得性约束，不得通过事后网页查询反向声称历史时点已知。

## 全局门禁

每次后续真实数据推进或代码增量仍执行与改动成比例的定向测试、完整测试覆盖、`uv run ruff check .`、`uv run pyright`、`git diff --check`、SQLite integrity/FK 和 Git 安全扫描。不得删除运行证据、原始对象、用户改动、SQLite、密钥、Cookie、浏览器 Profile 或私有来源材料。
