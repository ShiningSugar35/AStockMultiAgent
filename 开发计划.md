# AStockMultiAgent 当前开发计划

> 状态：CURRENT / NO OPEN DEVELOPMENT WORK PACKAGES
> 本文件只保存尚未完成的工作；已验证事实归档于《进度验收》与现行架构文档。
> 版本：investor-orchestration-and-regime-plan-v1
> 当前已发布主线：full-research-recommendation-v1 / package v0.4.0
> 更新日期：2026-09-13
> 机器索引：`planning/work_packages_v1.yaml`
> durable run：`lr_mtw92jn9_aabe249465fb`；owner lane：`main/full-research-recommendation-p0`。

## 1. 当前唯一投资决策架构

所有可直接形成证券选择、买卖判断、价格、数量或仓位的用户请求统一归一为 `FULL_RESEARCH_RECOMMENDATION`。当前设计不存在并行的轻量荐股、旧 BUY/HOLDING/PORTFOLIO decision 或独立 Recommendation Readiness 发布路径；历史名称若被旧输入带入，只能在 request construction 时一次性迁移到 Full Research，不能拥有 Planner、Executor、Response 或 Publication 权威。

正式公共投资建议的唯一发布对象是不可变 `RecommendationResearchReceipt`，且必须通过 Publication Gate。上游 `FullResearchInputReadinessReport` 只证明 Full Research 输入就绪，不具有正式荐股权。Mandatory Research DAG、PIT/source lineage、Financial/Governance CriticalVeto、multi-model valuation、candidate ranking、portfolio/risk/execution、freshness SLA、receipt replay 与 tracking/re-evaluation 均属于同一 Full Research 闭环。

永久边界保持：`broker_execution_allowed=false`；actual/paper 分 lane；`EA_PROVISIONAL` 只允许冻结不完整持仓声明，真实 `EA_WRITE` 必须独立走 `ACCOUNT_FACT_WRITE`；无新鲜行情不得生成即时成交数量；系统允许零 BUY、少于目标数量与现金持有；历史 receipt 不因后续 tracking 重评而改写。

## 2. Active work package

当前没有 active work package。已完成主线已按治理规则从 `planning/work_packages_v1.yaml` 的 active manifest 移出；实现、缺陷修复、L3 验证、release baseline、annotated tag、GitHub Release 与资产校验事实统一归档于《进度验收》和 durable run。

后续若启动新开发，必须先创建新的 WP 与 durable run，重新经过 scope/root-cause → architecture/implementation → defect-first review/validation → risk-based release → docs-only closeout 全流程；不得把已发布 v0.4.0 的历史任务重新伪装为 active 计划。

## 3. 已发布 v0.4.0 边界

v0.4.0 release baseline 为 `a511d64c46076ccaaf6dbc9eabd7056c66d96369`；annotated tag `v0.4.0` 固定指向该 baseline。GitHub Release 已创建为非 draft、非 prerelease，并附带 `astock_research-0.4.0-py3-none-any.whl` 与 `astock_research-0.4.0.tar.gz`。发布后的 docs-only closeout 只能更新文档/机器索引并继续推进 `main`，不得移动 `v0.4.0` tag。

性能与复杂度审计已完成有证据的低风险优化：候选与证据索引由重复扫描改为一次构建复用；删除无用对象和死路径；portfolio shortlist 使用 bounded `islice`；`build_portfolio` 与本轮新增复杂度热点均已拆分退出本轮 C901 超限范围。性能优化不得通过删证据、弱化 receipt、绕过 Publication Gate 或扩大隐式缓存换取表面速度。

## 4. 永久发布否决

验收证据必须来自真实执行与可复现工件，**不得伪造**样本、PASS、覆盖率、receipt、release 或远端状态。任一以下条件成立即不得发布后续版本：Full Research Publication Gate 可被旁路；Mandatory 节点非 PASS 仍能输出证券+价格+数量/权重；PIT leakage/source lineage 不完整；CriticalVeto 漏放；stale quote 生成即时股数；receipt tamper/replay 失败；真实账户写入混入 Full Research；全仓测试/静态/文档门不绿；active Skill/Workflow/架构文档把旧轻量决策路径描述为当前设计；输出或临时文件越出 `D:\AStockMultiAgent`。
