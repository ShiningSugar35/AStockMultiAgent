# AStockMultiAgent 开发计划

> 基线：2026-08-31《AStockMultiAgent 系统体验与投研能力深度研究报告》
> 当前状态：**没有未完成的代码发布主线**；这里只保留下列长期运行、数据积累与外部资格义务。
> 文档职责：本文件只保存当前未完成任务；完成事实进入《进度验收》与对应架构/Workflow/Skill，历史过程由 Git 追溯。

## 维护契约

- 本文件只保存当前未完成任务，不保存完成清单、历史发布流水或中间进度。
- 所有新增能力继续复用 SourcePolicyGate、Provider Registry、SourceSnapshot、ObjectStore、Evidence、PIT、Research Team、Committee、模拟账本和 checkpoint；不得建立第二套事实源、Router、Evidence、用户状态或 Agent 调度。
- 普通投资咨询默认 Investor Mode；Developer Mode 只能由明确诊断意图或显式开关进入。任何模式都不得暴露 Secret、Cookie、Token、私人材料或无关绝对路径。
- 外部社区能力的 stars、下载量、Registry 上架和演示成功只作维护/采用度信号；生产备用必须先通过端点/能力级 License/ToS、数据权利、PIT、provenance、Secret、供应链、故障、成本、固定版本和退出门。
- 真实券商自动下单、融券、期货、期权和执行型券商 MCP 永久不在范围内；`broker_execution_allowed=false` 不得放宽。

## 独立长期运行/数据义务

以下事项不能靠一次开发、fixture 或历史回放冒充完成；软件能力存在不等于真实证据门已经满足：

### E-02｜AKShare endpoint 生产备用外部资格

- E-02 的软件试点、M-06 fail-closed 资格框架、九项候选固定版本/证据/撤销/退出合同和三个 Repo Skills 已完成；当前没有任何候选被无证据提升为生产数据源。
- 当前 AKShare 1.18.94 明确声明其数据仅供学术研究和数据分析使用；包级 MIT License 不构成 endpoint-specific 上游数据权利。现有证据因此不能合法证明生产数据权利，`akshare` 必须保持 `SHADOW`。
- 未满足的外部资格门原文保留：**AKShare 至少有一个明确 endpoint 达到 `PRODUCTION_BACKUP`**。只有取得明确 endpoint 的上游使用许可/权利证明，并补齐 PIT、provenance、recorded/live、SBOM/安全、延迟/缓存/故障和退出证据后才可重新评估。
- 本项自 2026-09-02 起重分类为**外部数据权利/授权义务**，不再是可由代码修改闭合的 `v0.3.0` 软件发布阻断项；在权利门未满足前 Router 必须 fail closed，任何计划配额都不能覆盖该门。

- Phase 7/8 正式前向样本仍需 **100 个独立事件、12 个月、5 个 qualifying folds、3 个 qualifying regimes**；门槛前 Phase 8 保持 `NOT_ADMITTED`。
- Phase 11/12 继续积累真实 all-trials、independence clusters、SkillUsageEvent、Catalyst/KPI 与端到端时长；没有 prospective evidence 时不得自动修改/退休 Skill 或调整生产权重。
- 认证行业 taxonomy 只有取得可许可、PIT/provenance 完整的数据 release 后，才可作为正式行业强约束；caller-supplied risk group 不能冒充认证 taxonomy。
- Continuous Monitor 需持续积累自然场景、来源降级、崩溃接管、Agent worker 不可用和磁盘增长的长稳证据。
- 商业数据源只有用户具备相应授权、Secret 和再分发/使用权利时才可启用；代码完成不等于外部合同完成。

## 全局完成定义

- 当前发布主线达到其最高测试等级，独立 defect-first Review 通过；L3 不得只用 recorded fixture 替代必要的受控真实验证。
- PIT、官方性、完整性、Recommendation Gate、人工确认和账本权限不得因体验层、ETF 或生产备用能力放松。
- 完成项从本文件删除；当前稳定结构写入 `AGENTS.md`、`docs/architecture/`、`docs/workflows/`、README/canonical Skills，《进度验收》只保留最近一次任务有证据的验收记录。
- 正式 release 结束后还必须执行一次发布后归档与 docs-only closeout；最终工作区干净、无未知后台任务、唯一 durable run 完成。
