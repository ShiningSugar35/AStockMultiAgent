# AStockMultiAgent 开发计划

## 维护契约

总方案只写长期设计；开发计划只写未完成项及其必要历史边界；验收报告只写已实现且有证据的当前事实。任务验收通过时，必须在同一次修改中从开发计划迁移状态并写入验收报告。被替代的执行路径须明确标记 `SUPERSEDED`，既有 run、工件和审计记录不得删除。所有运行计数必须附时间、run ID 或工件身份。

## 当前真实状态（2026-08-10）

- Git：当前分支 `codex/phase5-stabilization`，上游为 `origin/codex/phase5-stabilization`。本轮开始前工作树已有用户持有的删除、方案文件修改和若干未跟踪诊断文件；不得回退，也不得用 `git add -A` 混入 Phase 5 提交。
- Migration：`runtime/state.sqlite` 已真实应用 `0051_visual_skill_overlay.sql`；旧 migration `0001..0050` 未改写 checksum。0051 只新增 append-only visual Skill generation/review/composite release，不修改旧 0050 baseline registry。
- **Phase 5：COMPLETE**。direct run=`direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef` 仍为 `FINALIZED`，243 条 direct Skill、旧 ShadowBundle 与 baseline registry 均保持不变。baseline admitted=`231`，object hash=`e12a1787138589f501a35406d43c59c5b1a0887566c7d922eb42110426f3b9aa`。
- 三位知乎作者真实视觉证据已完成：`2,503/2,503` placements、`2,306` unique assets、`2,503 READY / 0 NEEDS_REVIEW / 0 BLOCKED`；三份 `VisualEvidencePack` 均为 `READY`。精确工件身份见《验收报告》。
- 视觉增强 Skill generation run=`visual-skill-run:e34f4a5c0d00eb8c3adcded5e6a462de694c3919969caefd634e0c2848c9924e`：评估真实视觉关联 AU=`951`，严格候选=`422`，明确 `NO_SKILL=529`；append-only review=`APPROVE 422 / REJECT 0 / pending 0`；audit=`PASS / findings=[]`。
- composite registry 已真实发布：`base 231 + visual overlay 422 = admitted 653`；release=`knowledge-registry-v2:abbd793d4a3dc08bb1dd239cf2822297e6f46451142882734da8849f5fbb5c53`，object hash=`cac3c6ac8c5f1c4b37fba7f913d7fca3d83eef1d1695e97e38f5ba7f9897e82b`。`KnowledgeSkillProvider=READY / COMPOSITE_REGISTRY_READY / eligible=653`，正式委员会权重仍为 `false`。
- Phase 5 最终工程门：`778` tests collected；分组全覆盖为 `760 passed / 18 skipped / 0 failed`；`uv run ruff check .` PASS；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；全库 `PRAGMA integrity_check=ok`、`foreign_key_check=0`。完整分组与 skip 原因见《验收报告》。
- Phase 6：300750 recorded 软件闭环已通过；通用 Research Runtime、精确 live TradingClassification 与完整 live single-stock orchestration 仍有下述剩余项。
- Phase 7：程序接口完成，正式独立真实前向事件仍为 `0/100`，正确状态 `COLLECTING`；不得用 fixture、回测、历史回放或事后登记改变计数。
- Phase 8：内部边界为 `NOT_ENTERED_BY_DESIGN`；adaptive weight、online learning、main paper ledger write、broker execution 均为 `false`。不得因接口完成而启用。

## Phase 5 已完成后的不可变边界

Phase 5 的完整实现、历史失败/返工、两份本地原文 direct distillation、20 条 direct 终审、三作者视觉资产、视觉增强 Skill、composite registry、测试和数据库证据均已迁入《验收报告》，不再作为开发计划待办。后续开发必须遵守：

1. 不删除或改写旧 semantic run、旧 AU、旧 Embedding、direct 243 Skill、旧 ShadowBundle、0050 baseline registry、三份 `VisualEvidencePack`、0051 generation/review/release 或对应 ObjectStore 对象。
2. Research Runtime 只能经 `KnowledgeSkillProvider` 使用 composite registry；不得在 runtime 内直接查询 knowledge 表或绕过 registry/hash 校验。
3. visual overlay 中社区作者观点、OCR 和图片分类只作为方法与研究线索；任何上市公司关键事实仍必须由公告、交易所、财报等更强来源验证。
4. visual Skill 固定 `community_source_only=true / factual_use_requires_stronger_source=true / standalone_visual_distillation=false / MERGE_WITH_BOTH / formal_committee_weight_allowed=false`。
5. 当前已验收的 Phase 5 范围不以完整评论链为发布前置；如未来总方案仍要求作者参与评论链，将作为新的增量知识采集任务，不回开已完成的 Phase 5 release。

## 剩余任务与优先级

### Priority 1：可恢复的单股票 Research Runtime

- 收敛版本化 `ResearchRunRequest/ResearchRunReport` 与 `research-run plan/run/status/audit/recover` staged coordinator，使 `company_id + as_of` 能驱动现有 service/Protocol，而不是要求调用者手工预造全部中间工件。
- 每一阶段冻结 artifact ID/hash、checkpoint、耗时、cache 状态和结构化 `NEEDS_INFO` 原因；证据、财务、知识或分类不足时停在可恢复节点，不猜测、不写模拟账本。
- 目标顺序为 `Evidence → Financial Integrity → BaseCase → Serenity Delta → Knowledge Skill Delta → Committee → TradingClassification → TradeProtocol / Paper Decision`。TradingClassification 必须在最终协议生成前成为约束，而不是事后完成门。
- Committee 继续只读冻结工件；只有既有人工确认合同通过后才允许模拟账本写入。不得添加券商接口。

### Priority 2：live TradingClassification

- 冻结板块、风险警示/ST、停牌、特殊无涨跌停制度、公司行动基准与有效期，形成不可变、PIT、可审计 `TradingClassificationRelease`。
- 缺失、冲突、过期或 hash 漂移一律 `NEEDS_INFO`；不得通过默认值或猜测静默放行。
- 增加 live resolver/provider mapping，但不得把二级网页或社区来源直接升级成交易制度事实。

### Priority 3：Research Runtime 可观测性与恢复

- 完成 `ContextBudgetReport/PerformanceSummary`：single-stock cold/warm wall time、Knowledge Skill top-k latency、context bytes/token estimate、token budget、cache hit、provider call 和分阶段耗时。
- 检索保持 `filter → candidate → top-k → verify`；禁止 O(N²) 全量两两比较。
- 补 provider readiness、holding due/prepare、paper replay-checkpoint/recovery-plan 的只读聚合；复用现有 holding/paper service，不重写账本或回放核心。

### Priority 4：真实前向评估

- Phase 7 保持 `COLLECTING`，持续冻结真实 `ResearchMemo → ShadowDecision → Future Observation`。
- 达到至少 100 个独立事件后才允许评估 Skill/Committee/Research Quality；最终期限事件、MFE/MAE 和观点失效必须从冻结未来行情复算。
- 禁止制造样本、用历史回放替代未来证据、在线调权或自动 Skill 修改。

### Priority 5：Phase 8 门禁一致性

- 用户态统一映射为 `NOT_ADMITTED`，内部继续保留精确 reason code；统一活动 shadow policy 的 regimes/folds 合同。
- 不实现或启用动态权重、在线学习、规则研究、主模拟账本写入或券商执行。

## Phase 6：live 公司研究与模拟 reference 剩余项

### P6-L1：live 公司研究与模拟 reference 工件

当前事实：

- 300750 recorded 软件闭环已通过验收，不在此处重复。
- 通用 live paper execution 仍缺一个可审计的精确分类工件和完整自动 staged orchestration。

未完成内容：

1. 冻结板块、风险警示、停牌、特殊无涨跌停制度和公司行为基准。
2. 将精确 TradingClassification 前置到 TradeProtocol/Paper Decision 约束。
3. `company_id + as_of` 真实驱动 Evidence/Financial/BaseCase/Specialist/Knowledge/Committee，而非调用者手工提供全部冻结输入。
4. 缺失、冲突或超出适用期时保持 `NEEDS_INFO`，不得退化为猜测。

输入：已注册 provider、已同步公司行为、交易所分类表、当前 composite KnowledgeSkillProvider。

输出：可审计的分类工件、ResearchRunReport/checkpoint 和对应 `NEEDS_INFO` 处理逻辑。

验收条件：精确分类工件有 schema、冻结对象 hash 和审计路径；完整 recorded 输入可从研究请求运行至最终模拟协议；任一 live 缺项返回 `NEEDS_INFO`，账本零写入。

禁止越过的边界：不得在缺精确工件时猜测值或通过默认值静默通过。不得增加券商接口或自动 BUY/SELL。

## Phase 7：真实前向采集

### P7-F1：真实前向研究事件持续采集

当前事实：

- 当前状态：`COLLECTING`，真实独立前向事件 0/100，成熟最终期限事件 0。
- 0044 历史迁移身份已恢复，后续语义通过 0047 追加；SQLite checksum 未被改写，迁移阻断已解除。

未完成内容：

1. 创建正式 study。
2. 前瞻登记真实 ResearchMemo → ShadowDecision → TradeProtocol。
3. 等待真实未来行情后冻结 Future Observation。
4. 复算 MFE/MAE，记录观点状态和失效原因。
5. `uv run astock phase7-status-update` 持续更新计数。
6. 达到至少 100 个独立事件后评估 Skill/Committee/Research Quality。

验收条件：独立事件计数≥100；评估状态从 `COLLECTING` 提升。

禁止越过的边界：不得以测试 fixture、历史回放、回测或事后补登记填充计数。不启用强化学习、动态调权或自动 Skill 修改。

## 跨阶段缺陷与技术债

- DuckDB 在代码中未直接使用（仅为设计原则声明）。
- 总方案称“6 个 research-skills-v2 方法合同”，但代码实际为 5 个具体合同 + Hourly Swing 枚举，需确认。
- `tests/unit/test_repo_skills.py` 在用户现有工作树中包含额外 Phase 7 命令预期修改；该用户改动不属于本轮 Phase 5 提交，后续提交前需单独处理其归属。

## 方案差异（不阻塞已完成的 Phase 5）

### 默认自动连接券商下单

总方案中的自动券商表述与当前系统安全边界冲突。当前代码和已验收边界均要求：`broker_execution_allowed=false`，委员会最多输出模拟资格，模拟账本仍需人工签名确认；系统没有券商 API、自动 BUY/SELL 或实盘入口。后续若修改总方案，应以“所有真实交易由用户人工确认并在券商端执行”为准。

### Committee 结果枚举

总方案曾包含 `PAPER_HOLD/PAPER_EXIT`，当前 `TradeProtocolOutcome` 为 `WATCH / REJECT / NEEDS_INFO / APPROVE_SIMULATION`。`PAPER_HOLD/PAPER_EXIT` 属于持仓生命周期概念而非委员会直接输出；后续文档应统一到当前四值合同。

### 作者参与评论链

评论链 schema 与基础采集能力仍存在，但当前 Phase 5 已验收知识范围为回答、文章、专栏正文和想法。用户本轮明确以“三作者视觉入库 + 视觉增强 Skill 重生成与最终发布”为 Phase 5 完成条件，故评论链不再是本次 release blocker；若未来要恢复总方案的完整评论链要求，应作为新版本增量采集，不改写当前 composite registry。

## 全局门禁

后续每个阶段继续执行与实际改动成比例的定向测试、完整测试覆盖、`uv run ruff check .`、`uv run pyright`、`git diff --check`、SQLite integrity/FK 和 Git 安全扫描。不得删除运行证据、原始对象、用户改动、SQLite、密钥、Cookie、浏览器 Profile 或私有来源材料。
