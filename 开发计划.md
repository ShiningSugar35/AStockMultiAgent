# AStockMultiAgent 开发计划

## 维护契约

总方案只写长期设计；开发计划只写未完成项及其必要历史边界；验收报告只写已实现且有证据的当前事实。任务验收通过时，必须在同一次修改中从开发计划迁移状态并写入验收报告。被替代的执行路径须明确标记 `SUPERSEDED`，既有 run、工件和审计记录不得删除。所有运行计数必须附时间、run ID 或工件身份。

## Final Completion Plan

### 1. 当前真实状态（2026-08-04 只读基线）

- Git：当前分支 `codex/phase5-stabilization`，上游为 `origin/codex/phase5-stabilization`，HEAD=`187df223c5068363049559abc2afcb23633ca994`（`feat: complete direct source skill distillation`）。工作树在本任务开始前已包含用户持有的删除、方案文件状态变化、`new_file.patch` 和 `tmp/`；本计划不把这些既有改动归因于 Final Completion，也不得回退或用 `git add -A` 混入提交。
- Migration：`runtime/state.sqlite` 已真实应用 `0050_knowledge_completion.sql`；旧 migration `0001..0049` 保持只读且未改写 checksum。0050 的真实准入运行与表级完整性/FK 证据已迁入《验收报告》。
- Phase 5 direct distillation：权威 run=`direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef`，状态 `FINALIZED`，冻结来源 2、batch 144、final Skill 243，其中 `READY_FOR_SHADOW=223`、`NEEDS_USER_REVIEW=20`；现有 ShadowBundle 为 `all=243 / shadow=223`，`formal_committee_weight_allowed=false`。243 个 Skill JSON、对象 hash、ObjectStore 对象和 bundle membership 已只读复核一致。
- Phase 5 direct 准入已闭合：20 条 `NEEDS_USER_REVIEW` 已由用户授权 GPT-5.6 Sol 完成 `APPROVE=8 / REJECT=12` 的 append-only 终审，immutable admitted registry 已真实发布，`KnowledgeSkillProvider` 已在真实 runtime 返回 `READY`；精确工件身份迁入《验收报告》。Phase 5 当前仅保留知乎视觉证据支线：三位作者纯文本 v3 仍为 `PROVISIONAL_TEXT_VIEW`，真实图片资产 `asset_count=0 / placement_count=0`，专栏真实无凭据枚举仍为 `BLOCKED_EXTERNAL_OBSERVATION`。
- Phase 6：Evidence、Financial Integrity、BaseCase、Specialist Delta、ResearchMemo、Committee、TradeProtocol、人工签名模拟执行等模块已存在；Research Runtime 已通过窄 `KnowledgeSkillProvider` Protocol 接入 immutable admitted registry，并冻结 `KnowledgeSkillDelta` 供委员会只读消费。通用 live 单股票闭环与精确 live paper classification 的剩余边界仍按下文任务验收，不因知识 Provider READY 自动视为完成。
- Phase 7：程序接口和禁止未来函数门已完成；当前正式独立真实前向事件为 `0/100`，正确状态是 `COLLECTING`。该状态是“程序完成、真实时间证据持续采集”，不得用 fixture、回测、历史回放或事后登记改变计数。
- Phase 8：admission/evaluation/status 边界已实现，内部详细状态为 `NOT_ENTERED_BY_DESIGN`；Final Completion 对用户统一报告为 `NOT_ADMITTED`，同时保留内部 reason code。adaptive weight、online learning、main paper ledger write、broker execution 全部为 `false`，不得因接口完成而启用。
- 2026-08-04 测试基线：`uv run pytest` 为 `714 passed, 30 failed, 18 skipped`；29 个失败由 `src/astock/cli.py` 的 open-source adaptation hash drift 触发，1 个失败为《验收报告》缺少 `OpenCode 边界` 契约文字。`uv run ruff check .` 为 31 项（tracked `reviewed_repository.py` 5 项、未跟踪 `tmp/` 26 项）；`uv run pyright` 为 0 errors/0 warnings；`git diff --check` 通过；SQLite `integrity_check=ok`、`foreign_key_check=0`。

### 2. 已完成能力（本轮不得重做）

1. Phase 0—4 的 ObjectStore、Parquet、SQLite、PIT、Claim—Evidence、行情/reference、财务审计、BaseCase、Serenity 路由、诊断、持仓生命周期基础合同。
2. 离线委员会、DecisionPack、TradeProtocol、模拟账本、5m 回放、幂等和人工签名确认；没有券商接口或自动 BUY/SELL。
3. Phase 5 本地两份冻结原文的完整 direct distillation、去重、审计和 ShadowBundle；旧 semantic run、AU、Embedding、Skill 仅作不可变历史审计。
4. Phase 6 的 300750 recorded 软件闭环、Phase 7 的前向评测状态机、Phase 8 的禁用准入边界。
5. Provider recorded/live 显式探针、候选、holding plan/run/status/audit、paper status/replay/recover 等现有稳定入口。

### 3. 剩余任务与优先级

#### Priority 1：可恢复的单股票研究闭环

- 新增版本化 `ResearchRunRequest/ResearchRunReport` 与 `research-run plan/run/status/audit/recover` staged coordinator。输入至少为 `company_id + as_of`；每一阶段只调用现有 service/Protocol，并冻结 artifact ID/hash、checkpoint、耗时、cache 状态和 `NEEDS_INFO` 原因。证据、财务或分类不足时停在可恢复节点，不猜测，不写模拟账本。
- 闭环顺序固定为 `Evidence → Financial Integrity → BaseCase → Serenity Delta → Knowledge Skill Delta → Committee → TradeProtocol/Paper Decision`。委员会继续只读冻结工件；只有既有人工确认合同通过后才允许模拟账本写入。
- 新增不可变、PIT、带有效期的 `TradingClassificationRelease` 及 status/audit；覆盖板块、风险警示、停牌、特殊无涨跌幅制度和公司行动基准。缺失、冲突、过期均返回 `NEEDS_INFO`。不得添加券商接口。
- 补 provider readiness、holding due/prepare 和 paper replay-checkpoint/recovery-plan 的最小只读聚合接口；复用现有 holding/paper service，不重写账本或回放核心。

#### Priority 2：知乎视觉知识补全

- direct 20 条人工终审、0050、immutable admitted registry 与 `KnowledgeSkillProvider` 的真实发布已验收并迁入《验收报告》，不再列为待办。
- 为知乎图片执行真实的精确 asset allowlist、ObjectStore-first snapshot、DOM locator、逐图 OCR 尝试、分类、双侧上下文和受影响 AU rebuild。recorded 合同代码已存在，但三位作者真实资产当前仍为 `asset_count=0 / placement_count=0`；真实完成前继续 `PROVISIONAL_TEXT_VIEW/NEEDS_INFO`，不得把 `[图片]` 或 `<img>` 占位符当证据。

#### Priority 3：真实前向评估

- 保持 Phase 7 `COLLECTING`，只验证 study/assignment/observation/status/audit/recover 接口和真实事件独立性。最终验收允许 `0/100` 或届时真实计数；禁止制造样本。

#### Priority 4：Phase 8 门禁

- 只收敛用户态 `NOT_ADMITTED` 映射、准入评估和门禁一致性；统一活动 shadow policy 的 3 regimes/5 folds 合同，移除未接线 schema 的 5 states/3 folds 分裂。
- 不实现动态权重算法，不启用在线学习，不启动规则研究，不写主模拟账本，不连接券商。

#### 全局质量与文档

- 先修复 open-source adaptation hash drift、验收文档契约和 tracked Ruff finding；`tmp/` 从质量扫描/提交范围中明确隔离，不修改其中历史资产来“修 Ruff”。
- 新增 ContextBudgetReport/PerformanceSummary：记录单股票分阶段耗时、Knowledge Skill top-k 检索耗时、context bytes/估算 tokens、预算、cache hit 和 provider 调用数；检索必须为 `filter → candidate → top-k → semantic verify`，禁止 O(N²) 全量比较。
- 每个通过验收的阶段在《验收报告》追加六行短记录：`任务 / 状态 / 证据 / commit / 测试 / 剩余`。未通过能力继续留在本计划，不提前迁移。

### 4. Agent 分工与文件归属

- **Root Agent（架构/PM/最终验收）**：冻结 Protocol/Schema/Artifact ID 边界，维护本计划、`tmp/final-completion/` 和《验收报告》，处理共享导出/CLI 的最终小范围集成，审查 Agent A/B 完整 diff；不代替两个实现 Agent 完成主体代码。
- **Agent A — Knowledge Completion**：唯一负责 direct 人工决策/admitted release、知乎图片证据合同、knowledge registry 和 knowledge-side `KnowledgeSkillProvider`；输出 `knowledge completion report`，必须含作者覆盖、Skill 总数、READY/NEEDS、来源链和 registry 版本。禁止修改 Phase 6、committee、paper/ledger。
- **Agent B — Research Runtime**：唯一负责通用 staged research runtime、KnowledgeSkillProvider 的 runtime port 使用、TradingClassification、provider readiness、holding/paper 只读恢复聚合、Phase 7/8 状态一致性；禁止修改 knowledge 内部表或直接读取 Skill 数据库。
- **Agent C — Evaluation & Quality**：全程只读，负责 pytest、ruff、pyright、性能、token/context、Schema/文档一致性、SQLite integrity/FK 和 Git 安全扫描；不开发、不修文件。
- 同一文件同一时刻只有一个 writer。共享 `src/astock/cli.py`、`src/astock/schemas/__init__.py` 和文档由 Root 在 Agent A/B API 冻结后串行集成；Agent 不得回退用户或其他 Agent 的改动。

### 5. 最终验收标准

1. `research-run` 对完整 recorded 输入可从股票身份运行至冻结 TradeProtocol，重复运行 artifact ID/hash 一致并报告 cache hit；对 live 缺证据/缺分类/知识未准入返回结构化 `NEEDS_INFO` 和可恢复 checkpoint，账本零写入。
2. Research Runtime 源码静态门不含 knowledge repository/table 名、裸 SQL 或 `StateStore.connect`；Knowledge Skill 只经 `KnowledgeSkillProvider` 的版本化 Schema/Artifact ID 进入 top-k 摘要。
3. direct 243 条与旧 bundle hash 不变；用户已明确授权 GPT-5.6 Sol 完成 20 条最终裁决，runtime 必须逐条 append-only 写入并精确得到 `APPROVE=8 / REJECT=12 / pending=0 / admitted=231`。knowledge completion report 必须具有真实 decision、registry、来源链对象 hash 和 admission 状态。
4. 知乎图片 recorded 合同覆盖 snapshot/locator/OCR/classification/context/AU rebuild、`MERGE_WITH_BOTH` 和 fail-closed；真实图片未冻结时不声称作者视觉包完成。
5. TradingClassification 对缺失、冲突、过期和篡改全部 fail closed；Phase 7 保持 `COLLECTING`；Phase 8 用户态为 `NOT_ADMITTED` 且所有执行/学习布尔值为 false。
6. 性能报告至少含 single-stock cold/warm wall time、Skill top-k latency、context bytes/token estimate、token budget、cache hit/provider call；无全量两两比较。
7. 最终门全部通过：`uv run pytest`、`uv run ruff check .`、`uv run pyright`、`git diff --check`、SQLite `integrity_check`、`foreign_key_check`；Git 安全扫描确认本次提交不含 PDF/DOCX/XLSX/SQLite/ObjectStore/Parquet/Embedding/Cookie/Profile/密钥。
8. 只显式 stage Final Completion 允许文件，提交信息固定为 `feat: complete astock multi-agent investment system`，push 当前分支；20 条 direct 最终裁决的用户授权已取得并由 GPT-5.6 Sol 完成，不再作为外部 blocker。若真实知乎图片或专栏枚举仍未取得，相关状态必须继续为 `NEEDS_INFO/BLOCKED_EXTERNAL_OBSERVATION`，不能伪报 PASS。

### 6. 禁止修改范围

- 禁止删除或改写旧 semantic run、旧 AU、旧 Embedding、旧 Skill、现有 direct 243 条对象、旧 ShadowBundle、旧 ObjectStore 对象及 migration `0001..0049`。
- 禁止为了统一接口重构已验收 Phase 0—4、委员会裁决核心、模拟账本双重记账或 5m 回放核心；只允许 additive adapter、Protocol、Schema、Artifact binding 和必要 fail-closed 修复。
- 禁止券商 API、自动 BUY、自动 SELL、自动真实交易、绕过人工确认、动态调权上线、在线学习或 Phase 8 规则研究。
- 禁止提交 `runtime/`、`tmp/`、PDF、DOCX、XLSX、SQLite、ObjectStore、Parquet、Embedding、Cookie、浏览器 Profile、签名 URL、密钥或私有绝对路径。
- 禁止修改 gold/指标公式迁就现有输出，禁止用 fixture/回测/历史回放补 Phase 7 真实事件，禁止把社区内容或图片占位符提升为公司事实。
- 禁止回退本任务开始前的用户工作树改动；如与 Final Completion 文件重叠，Root 必须隔离并在提交前逐项核对。

## 当前依赖顺序

Phase 5 本地直读主线：

`冻结原文指纹与自然阅读批次 → Sol 按批原文直读 → 六投资模块 Skill 候选 → 全文后去重与审计 → 人工批准`

知乎在线图片证据是另一条独立主线：

`知乎图片证据冻结 → 三位作者视觉增强重跑 → 候选审计与人工批准`

旧 reviewed AU 逐批返工不再是本地直读主线的前置条件。Phase 7 继续作为独立真实时间轨道运行：

`冻结 ResearchMemo/ShadowDecision → 等待真实未来行情 → 冻结 Future Observation → 持续评估`

## Phase 5：知识工厂剩余项

### K5-D4-DIRECT：两份本地原文直读与六模块 Skill 蒸馏

#### 已完成锚点（恢复时先读）

- **已完成 checkpoint**：PDF `1..249`、DOCX `123/123` 已全部处理；direct run `direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef` 已 FINALIZED，最终 Skill `243` 条，其中 `READY_FOR_SHADOW=223`、`NEEDS_USER_REVIEW=20`。
- **当前工程首动作（NEXT）**：独立 `xhigh` 只读审查本轮 release 重建；通过后才执行 `prepare 第一次 → prepare 第二次幂等复验 → verify → 正式 init`。prepare 只冻结旧 run 顶层行的 canonical aggregate fingerprint；任何一步失败即停止。
- **后续模型路由**：PDF 优先 `Codex 5.6 Luna xhigh`，文本清晰批次按显式授权可转 `GPT-5.3 Spark xhigh`，核心图表只由 `GPT-5.6 Terra high` 局部解析；DOCX 使用 `GPT-5.3 Spark xhigh`，单回合上下文门为 `12.8k tokens`。语义 Agent 不得重读 `b01..b12`。

#### 恢复前提与不可变边界

- 当前分支为 `codex/phase5-stabilization`。旧知识资产仅作历史审计，禁止修改、删除或作为新 direct run 的语义输入。
- 当前 PDF 为 `docs/《价值投资功法》Mr.Dang.pdf`，SHA-256=`31579b347d6ccd178322bc5d31c46bc888e069927591dcee8d7a2e5e1dc3e86f`，共 249 页。
- 当前 DOCX 为 `docs/寒武纪的鳄鱼 知乎文章.docx`，SHA-256=`197ec18e6fabac4401f6412331e9aa50f919498d4e40cfddb481eeab9788852d`，共 123 篇、OOXML 正文 2032 段；文章以标题和 1-based 正文段落定位，禁止跨标题重组。
- 历史 checkpoint `b01..b12` 保留作审计；本次 v4 run 已覆盖 PDF `1..249` 和 DOCX `123/123`，共 `144` 个 batch，raw candidate `243` 条，Sol 去重确认无可合并重复组。最终六模块关联分布（同一 Skill 可关联多个模块）为 `FUNDAMENTAL_RESEARCH=156`、`PORTFOLIO_CONSTRUCTION=71`、`POSITION_RISK_MANAGEMENT=144`、`PSYCHOLOGY_BEHAVIOR=96`、`SOURCING_SCREENING=66`、`VALUATION_PRICING=61`。
- `b12` 的 146..169 页、11 条 READY、33 条来源切片和 3 条视觉引用已通过该批确定性门，不得重读或重做语义；current PDF snapshot、manifest 与显式 visual relineage 已通过只读验收。DOCX article 34 的唯一 audited marker 与 article 33/34/35 精确邻接错误码已通过独立 `xhigh` 复核。
- DOCX `123` 篇、`2032` 段已按文章边界完成直读蒸馏；旧 `accepted_docx_sections=[]` 仅为历史 checkpoint，不代表当前状态。
- `0049_direct_source_distillation_state.sql` 的 fresh/runtime 兼容与现有迁移事实已通过当前来源注册验收；继续禁止重放、改写既有 migration checksum 或把“再次应用 0049”列为待办。
- 禁止 DeepSeek 和原文向量预筛选。Embedding 只允许在全部 Skill 生成后用于去重候选和检索，合并决定仍须由语义审查确认。

#### Agent 配置、推理强度与文件归属

- **Agent A — PM / 架构师 / release 接管 / 最终验收（GPT-5.6 Sol）**：纯读取、架构判断、里程碑审查和最终只读验收默认 `xhigh`；本轮事故清理与工程写入固定使用 `high`。当前是 release 合同唯一 owner，完成后必须交独立 `xhigh` 只读审查。
- **Agent B — 集成与疑难语义（GPT-5.6 Terra，`xhigh`）**：本轮 release 工程已移交 Agent A，不持有并行写权限；后续仍负责全文完成后的集成、去重和复杂语义协助。
- **Agent C — 已退出本任务（GPT-5.3 Spark）**：首次交付及唯一一次返工均未执行约定四文件工程合同，状态保持 `FAILED_AFTER_SINGLE_REWORK / RETIRED`；历史失败保留，不再分配或持有文件写入权。
- **Agent D — 已停止并等待重新分配**：曾越权修改工程代码、运行 prepare 并写入 placeholder/handoff，相关无效临时工件已删除；该历史失败保留，但 `b13..b19` 已由后续恢复流程完成。后续 PDF 路由保持 `Luna xhigh → Spark xhigh 纯文本 → Terra high 核心图表`；DOCX 使用 `Spark xhigh`、`12.8k tokens` 上下文门。
- 同一文件同一时刻只允许一个 Agent 写。需要接管时，Agent A 先终止原责任人的写权限，再把明确文件清单转交下一 Agent；不得并行修改同一文件或回退他人改动。
- Agent C、Agent B 的历史失败及 R1/R2/R3 返工记录继续保留在验收报告和状态中；R3 工程 finding 已标记 `RESOLVED_BY_SOL_TAKEOVER`，不得删除历史或重新打开同一代码合同。

#### 已完成合同：`DIRECT_RELEASE_SOL_REBUILD_FINALIZED`

- 新 direct run 直接读取当前来源合同，不依赖旧 reviewed 数据。prepare/verify 对旧知识子表不执行查询、计数、指纹或 ObjectStore 存在性校验。
- 历史保护只保留固定源 semantic run 顶层行的 ID、精确状态与 canonical aggregate fingerprint；该身份仅用于证明历史未修改，不进入 Skill 解释输入。顶层行漂移必须 fail closed。
- 当前来源、视觉、DOCX、article 34 精确 marker、123 篇/2032 段和空段连续性门均保持原强度。v4 run 已完成 144 批导入、finalize 和 ShadowBundle 发布；旧 run、旧 AU 与旧 embedding 未被修改或复用。
- DOCX boundary 已独立 `xhigh` 验收；历史 `CURRENT_SOURCE_FRAGMENT_EMPTY`、`CURRENT_DOCX_ARTICLE_BOUNDARY_DRIFT` 及 Spark 越权 prepare 仍保留，但不再是开放 finding。

#### 已完成阶段与门禁

1. PDF `249/249` 页、DOCX `123/123` 篇（2032 段）已完成原文直读蒸馏。
2. 144 个 batch 已原子导入；raw candidate、去重后 final skill 均为 `243`，Sol 确认无重复合并组，未复用旧 Embedding。
3. 已生成并发布 ShadowBundle；`all_rule_ids=243`，`shadow_rule_ids=223`，Shadow 实际返回非 READY 规则数为 `0`。
4. 20 条 `NEEDS_USER_REVIEW` 已在用户明确授权下完成最终语义裁决并真实 append-only 写入：`APPROVE=8 / REJECT=12 / pending=0`；immutable registry 已发布、admitted=`231`、Provider=`READY`。该完成事实及工件 hash 已迁入《验收报告》；正式委员会生产权重仍未开启。

每批 `READY_FOR_SHADOW`/`NEEDS_USER_REVIEW` 判定继续遵循直接原文合同：只有作者原意无法确定、同源矛盾无法协调、核心图表不可恢复，或不同合理解释会产生显著不同投资行动时进入 NEEDS；单纯缺少可选字段、纯案例叙述或没有可迁移方法不得进入 NEEDS。

#### 低额度与轮询规则

- Agent A 不按固定短周期轮询，不在 Worker 运行时反复追问；仅在 Agent 完成、主动报告阻塞，或 Spark 超过 20 分钟、Terra 超过 30 分钟时检查一次，并只索取一句结构化状态。
- 不启动第二个 Sol 或第二个 Terra，不让多个 Agent 重读同一原文。已验收 `b01..b12` 只读取摘要、hash 和统计；普通批次只跑 schema、来源和目标测试，完整回归只在里程碑及最终执行。
- Python 负责 hash、覆盖、manifest、Schema、幂等、去重候选和数据库审计；模型只做自然论证边界、方法抽象、疑难去重和审查。

#### 最终质量门、清理与提交

- 完整覆盖必须为 PDF 249/249 页、DOCX 123/123 篇且 2032 段定位可审计；报告六模块分布、原始/去重后 Skill、READY、NEEDS 逐条理由、SKIP、图表参与、无法识别图表和无来源 Skill 数。
- 运行目标测试、相关集成测试、必要全量 `uv run pytest`、`uv run ruff check .`、`uv run pyright`、`git diff --check`、SQLite `integrity_check` 与 `foreign_key_check`；所有门必须引用真实命令结果。
- 全部门通过后删除 `tmp/phase5-direct-distill/` 及本任务旧临时状态；更新两份进度文档，将已完成项从本计划迁移至验收报告，不删除失败/返工历史来掩盖事实。
- 不提交 `tmp/`、`runtime/`、PDF、DOCX、XLSX、SQLite、ObjectStore、Parquet、Embedding、私有原文、本地绝对路径或凭据；禁止 `git add -A`，只显式添加本任务代码、追加 migration、测试及两份文档。
- 建议提交信息为 `feat: complete direct source skill distillation`；显式提交后 push 到 `codex/phase5-stabilization`，最终报告 commit SHA、四 Agent 交付、真实 checkpoint、来源覆盖、Skill/状态统计、测试与数据库结果、临时文件清理、git status 和 push 结果。

### K5-D3-R：旧语义漏斗金标遗留项

当前遗留事实：

- 独立人工 gold 为 25 个跨段场景和 29 个有价值 AU。
- 旧语义漏斗真实召回为 20/29，19 个场景存在修辞角色过度多标。

该缺陷不再阻塞 `K5-D4-DIRECT`，也不得把 gold 或旧 AU 作为本地直读输入。如未来恢复旧语义漏斗生产路线，仍须完成：设问—回答与 AU 边界 Jaccard=1.0、有价值 AU 召回≥0.90、错误拆分/合并/泄漏均为 0。不得修改 gold 或指标公式迁就当前输出。

### K5-D5：知乎图片证据链

当前事实：

- 三位白名单作者的采集已完成；真实正文图片仍未形成不可变资产包，因此三个纯文本 v3 包继续标记 `PROVISIONAL_TEXT_VIEW`。
- `0050` 与 `ZhihuVisualCompletionService` 已实现 recorded 合同：精确 `zhimg.com` HTTPS allowlist、ObjectStore-first bytes、DOM locator、OCR 尝试、分类、双侧上下文、`MERGE_WITH_BOTH` 和受影响 AU rebuild；该代码合同不等于三位作者真实图片已经完成。

未完成内容：对三位作者真实图片逐张取得授权可访问字节并执行 snapshot/locator/OCR/classification/context/AU rebuild，生成新的视觉增强 run；任何真实资产缺失继续 fail closed。

输入：已冻结 SourceItem 快照、正文 DOM、已登录 Chrome 与 AStock 采集插件。

输出：按顺序 `IMAGE_URL_INVENTORIED → ACCESS_POLICY_VERIFIED → IMAGE_SNAPSHOT_FROZEN → DOM_LOCATED → OCR_ATTEMPTED → VISUAL_CLASSIFIED → CONTEXT_ASSEMBLED → AFFECTED_AU_REBUILT → PACKET_READY`。

验收条件：逐作者报告 SourceItem、图片引用、唯一图片对象、OCR 状态、分类、READY/REVIEW/BLOCKED 数和真实对象 hash。对象缺失、跨作者引用和图片孤立候选均为 0。

停止条件：`BLOCKED_ACCESS_POLICY`、`SNAPSHOT_FAILED`、`LOCATOR_STALE`、`REVIEW_REQUIRED` 必须 fail closed。

禁止越过的边界：不得绕过登录、验证码或访问控制；不得把 Cookie、Profile、签名 URL 或私有路径写入工件；不得把 `[图片]` 占位符当证据；不得在新证据冻结前启动 DeepSeek。

### K5-D6：OpenCode/DeepSeek 与候选审核

当前事实：

- 《价值投资功法》旧知识资产只作历史审计，新 direct run 不依赖旧 reviewed AU，也不得把旧批次导入直接原文路线。
- 三位知乎作者仍须先完成图片证据冻结和受影响 AU 重建。

未完成内容：

1. 三位知乎作者视觉增强后，重新冻结完整 AU 输入。
2. 如继续使用外部离线审核，须新建批次并执行 stage/import；不得复用本地书旧批次。
3. 审计去重、适用范围、反证、失效条件和来源链。
4. 用户确认批准或拒绝每个候选。
5. 已批准候选进入 ResearchSkillRegistry 或生产路由。

验收条件：导入后的候选状态为 `PENDING/NOT_RUN`；审计通过后由用户最终确认。

停止条件：缺行、重复 AU、哈希不符或越界引用时整批零导入。

禁止越过的边界：不得自动批准；不得自动进入研究路由、模拟账本或实盘；不得自评、自批或生成交易指令。

三位知乎作者只在视觉增强包完成后才能启动新的 DeepSeek 批次。

## Phase 6：live 公司研究与模拟 reference 剩余项

### P6-L1：live 公司研究与模拟 reference 工件

当前事实：

- 300750 recorded 软件闭环已通过验收，不在此处重复。
- 通用 live paper execution 仍缺一个可审计的精确分类工件。

未完成内容：

1. 冻结板块、风险警示、停牌、特殊无涨跌停制度和公司行为基准。
2. 缺失、冲突或超出适用期时保持 `NEEDS_INFO`，不得退化为猜测。

输入：已注册 provider、已同步公司行为、交易所分类表。

输出：可审计的分类工件和对应 `NEEDS_INFO` 处理逻辑。

验收条件：精确分类工件有 schema、冻结对象 hash 和审计路径；任何缺项返回 `NEEDS_INFO`。

禁止越过的边界：不得在缺精确工件时猜测值或通过默认值静默通过。不得增加券商接口或自动 BUY/SELL。

## Phase 7：真实前向采集

### P7-F1：真实前向研究事件持续采集

当前事实：

- 当前状态：`COLLECTING`，真实独立前向事件 0/100，成熟最终期限事件 0。
- 0044 历史迁移身份已恢复，后续语义通过 0047 追加；SQLite checksum 未被改写，迁移阻断已解除。

未完成内容：

1. 创建正式 study。
2. 前瞻登记真实 ResearchMemo → ShadowDecision → TradeProtocol。
3. 等待真实未来行情后冻结 Future Observation。
4. 复算 MFE/MAE，记录观点状态和失效原因。
5. `uv run astock phase7-status-update` 持续更新计数。
6. 达到至少 100 个独立事件后评估 Skill/Committee/Research Quality。

验收条件：独立事件计数≥100；评估状态从 `COLLECTING` 提升。

禁止越过的边界：不得以测试 fixture、历史回放、回测或事后补登记填充计数。不启用强化学习、动态调权或自动 Skill 修改。

## 跨阶段缺陷与技术债

- DuckDB 在代码中未直接使用（仅为设计原则声明）。
- 总方案称“6 个 research-skills-v2 方法合同”，但代码实际为 5 个具体合同 + Hourly Swing 枚举，需确认。
- `shadow-forward-market-freeze` CLI 命令未列入 `test_repo_skills.py` 的预期命令清单（测试覆盖缺口）。

## 待用户确认的方案差异

### 默认自动连接券商下单（总方案 1.1 节第 31 行）

总方案当前写：“系统允许大模型绕过风险规则，保留默认自动连接券商下单接口。”

当前代码和已验收边界：

- `broker_execution_allowed` 在所有 schema 中固定为 `Literal[False]`。
- Committee 输出为 `APPROVE_SIMULATION` 时只允许模拟准备，账本写入门禁和人工签名确认缺一不可。
- 系统没有任何券商 API 接口、自动 BUY/SELL 或实盘入口。
- Phase 6、Phase 7、Phase 8 均通过字面量 `Literal[False]` 永久禁用券商执行。

冲突：上述总方案表述与系统当前“人工确认、无券商接口”的实现直接冲突。

建议：修改总方案删除“默认自动连接券商下单接口”或改为“系统保留只读券商监控接口，所有交易必须用户人工确认并在券商端执行”。

### Committee 结果中含 PAPER_HOLD、PAPER_EXIT（总方案 13 章第 1259-1260 行）

总方案当前写 TradeProtocol 结果为 `REJECT / NEEDS_INFO / WATCH / PAPER_ELIGIBLE / PAPER_HOLD / PAPER_EXIT`。

当前代码：TradeProtocolOutcome 仅为 `WATCH / REJECT / NEEDS_INFO / APPROVE_SIMULATION`（四值而非六值）。`PAPER_HOLD` 和 `PAPER_EXIT` 是持仓生命周期概念而非委员会直接输出。

建议：确认总方案是否应更新为与代码一致的四值结果。

### 总方案 Phase 5 包含“评论链”（8.1 节）

总方案要求在采集范围内包含“作者参与的评论链”。

当前代码和已验收边界：评论链的 schema 和基础模型存在，但采集范围当前仅为回答、文章、专栏正文和想法。评论链的完整采集尚未执行。验收报告中已标记“当前范围为回答、文章、专栏正文和想法”。

建议：确认评论链是 Phase 5 必需项还是可后置。若必需，保留在开发计划；若非必需，从总方案八章调整。

## 全局门禁

执行与实际改动成比例的定向测试、`uv run pytest`、`uv run ruff check .`、`uv run pyright` 和 `git diff --check`。不得删除运行证据、原始对象、用户改动、SQLite、密钥、Cookie、浏览器 Profile 或私有来源材料。
