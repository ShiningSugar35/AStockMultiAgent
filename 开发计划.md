# AStockMultiAgent 开发计划

## 维护契约

总方案只写长期设计；开发计划只写未完成项；验收报告只写已实现且有证据的当前事实。任务验收
通过时，必须在同一次修改中从开发计划删除并写入验收报告。被替代的方案从现行文档删除，由
Git 历史追溯。所有运行计数必须附时间、run ID 或工件身份。

当前分支为 `codex/phase5-stabilization`。Phase 5 论证链、Serenity v2、Provider 注册表、免费
reference 数据底座、财务事实源和候选注册表已经迁移到验收报告；本文不再重复列为待开发项。

## 当前依赖顺序

`K5 外部蒸馏/人工审核 → production-recorded 候选纵切 → Serenity 用户路由 →`
`研究/委员会 → 人工确认 → 模拟交易 → Phase 7 前向积累`

Phase 8 P8.1～P8.4 继续保持 `NOT_ENTERED_BY_DESIGN`。

## K5：论证级知识链收口

### K5-E：真实域阈值校准

- 统计三位作者 14 类最高/次高相似度分布、长度分桶和多标签间隔。
- 建立真实域正例、负例、模糊例标注集；阈值绑定 calibration manifest。
- 校准前低分只进入 `CALIBRATION_REQUIRED`，不得永久删除。

验收：标注集有冻结清单和来源；校准与 held-out 集按内容项隔离；阈值给出 precision/recall、置信
区间和适用范围，失败时继续保持 `automatic_exclusion_enabled=false`。

### K5-F：DeepSeek 执行、结果导入与 SkillCandidate

- 按 `OPENCODE_DEEPSEEK_TASK.md` 在 OpenCode/DeepSeek V4 Flash 运行三个已冻结离线批次。
- 使用已实现的 result stage/import 校验并导入结果。
- 只由 `KEEP` 结果生成 AU 级候选；全部保持 `PENDING/NOT_RUN`。
- 生成用户审核包，最终适用范围、反证、失效条件和批准/拒绝由用户确认。

验收：三个批次均逐条闭合；导入审计通过；候选不能直接引用 Paragraph；没有候选自动批准或进入
生产路由。

### K5-H：真实全量审计与旧链隔离

- 删除已硬停用的 `knowledge-distill-run`、`knowledge-draft-generate` 不可达旧实现和对应死测试断言。
- 旧表、对象和报告只读保留并标记 `LEGACY/STALE`。
- 重跑全量语义审计、隐私扫描、Windows 中文路径和崩溃恢复。
- 解释所有 live/真实 OCR skip，形成干净提交基线。

验收指标至少包括：ArgumentUnit exact-boundary、角色/关系 macro-F1、依赖闭合率、singleton 泄漏
率、引用重建率、关键词整篇分区守恒、三视图覆盖率和正文泄漏数。

### K5-I：线上覆盖最后边界

- 保持回答、文章和想法增量补全。
- 补一个真实“专栏容器到文章”的枚举快照，用于证明专栏归属覆盖；不重复抓已验证文章正文。
- 访问受限或关闭历史保留精确人工边界，不能计为完整覆盖。

生产范围继续排除评论与子回复。

## SER：Serenity 后续路由

### SER-3：用户路由

- 冻结候选宇宙可路由到 IndustryBottleneck。
- company-deep-research 使用 v2 Event/Growth/Valuation/Daily 合同。
- memo 只组合冻结工件，不重新联网。

验收：每个合同覆盖正向、缺证据、PIT、降级和反例 golden case；完整 recorded 链审计 PASS。

## P5X：Phase 5 后续全链

### P5X-5：模拟交易操作层

实现 `paper-order-place`、`paper-order-cancel`、`paper-settle`、`paper-mark`、`paper-recover`。
补齐真实交易日历 T+1、板块/ST 涨跌停、停牌、交易时段、委托有效期、版本化费用、公司行为
入账、缺行情降级、部分成交和同 K 线保守语义。

委员会 TradeProtocol 继续 `ledger_write_allowed=false`；新增用户确认后的独立模拟执行请求。
本批次预计使用 additive migration `0042`，以开工时冻结合同为准。

### P5X-6：真实端到端 recorded 链

打通：数据同步 → 候选 → 官方证据/财务 → 冻结证据 → BaseCase/专家/诊断 → 委员会 → 用户
确认模拟请求 → 委托 → 5m 回放 → NAV。每一步可中断恢复，委员会阶段保持断网。

其中必须补一条不使用 `CandidateTestInputVerifier` 的完整 production-recorded 候选正向纵切，证明
P5X-2/P5X-3 typed release、公司全集覆盖证明、候选审计和下游冻结宇宙在生产 verifier 下闭合。

### P5X-7：Phase 7 真实前向积累

补日常调度、结果可见前冻结 arm、到期观察和定期 shadow evaluate。真实状态从 `COLLECTING`
开始，不得用测试夹具或回测冒充前向证据。

### P5X-8：可选模型接口与操作界面

OpenCode/DeepSeek 手工包优先。自动 OpenAI-compatible Provider、预算控制和轻量 UI 最后实现，
不阻塞确定性数据、研究和模拟交易闭环。

## 全局门禁

每个工作包执行定向测试、`uv run pytest`、`uv run ruff check .`、`uv run pyright`、
`git diff --check`。Provider 额外执行 recorded contract；live smoke 必须显式开启。未经专项授权不
开发 Phase 8 算法，不自动实盘，不删除历史对象、用户改动或运行证据。
