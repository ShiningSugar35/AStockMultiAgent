# AStockMultiAgent 当前开发计划

> 状态：CURRENT
> 本文件只保存尚未完成的工作；已验证事实归档于《进度验收》与现行架构。
> 版本：investor-orchestration-and-regime-plan-v1
> 更新日期：2026-09-09
> 机器索引：`planning/work_packages_v1.yaml`
> 最终集成：沿用 durable run `lr_mtqys4vy_81c26a9e8c93`；接手既有未提交工作，不 reset/restore/stash，不建立平行主线。

## 1. 当前开发状态

**无未完成开发任务。** WP-01→WP-11 的实现缺口已经按原依赖、owner lane 与安全边界完成修复并从 active manifest 移出；68 个业务问题的真实领域 E2E、统一请求编排、current macro、Market Regime 风险覆盖、三域定时研究、语义唯一 owner、通知恢复、controlled-live fail-closed 与 rollback 均已形成机器可验证路径。

完成开发不等于自动启用生产能力。`configs/investor_orchestration_activation_v1.yaml` 的正式启用门继续独立生效：真实 prospective shadow 样本、持续天数、current macro controlled-live、scheduled research controlled-live 与 owner approval 必须来自版本化 canonical 证据，不得由调用者自报、测试夹具、recorded 回放、空跑或文档声明替代。当前未满足这些外部运行门时，相关 feature flag 继续为 disabled/SHADOW；这属于运行准入条件，不重新打开已完成 WP。

## 2. 永久边界

- `broker_execution_allowed=false` 永久保持；正式账户只允许研究、提醒和 append-only 用户事实，不自动下真实券商订单。
- actual / paper 永久分 lane；模拟操作仍要求既有准备、独立确认、确定性回放与审计，只有 fill 才改变模拟持仓。
- PIT、来源身份、对象 hash、请求/实体/账户绑定、财务/治理/估值/Universe/推荐门均 fail closed；不得伪造或放宽验收门求绿。
- recorded/smoke/DEGRADED 不能冒充 controlled-live 或 prospective；外部来源异常时保留待重试 bucket，不写经济事实、不激活功能。
- 原始 ObjectStore、生产 SQLite、`user_state/`、历史事件和未知归属文件不回写、不清理；所有测试/验收产物只在项目内隔离路径生成。

## 3. 发布验收

最终发布只接受同一稳定树上的机器证据：68 场景 E2E、WP-10/11 代表性回归、Ruff、Pyright、冷暖/并发性能、全仓 pytest，以及 controlled-live/fail-closed/rollback 记录。随后执行 `git diff --check`、归属/secret/private 审查、显式 staging、commit 与 push，并验证远端 HEAD。

《进度验收》在最终全仓门之前冻结已成立证据；全仓门和 Git 结果由对应不可变 quality receipt 与远端对象证明，不在测试后回写 tracked 文档制造自指源码漂移。
