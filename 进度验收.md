# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT
> 任务：完成投资请求同轮终局闭环、XSHG/XSHE/BJSE Universe 高冗余覆盖与 Serenity 方法层优化
> 更新日期：2026-09-10
> 执行：GPT-5.6 Sol / mcp-wjp；durable run `lr_mtv2kqdo_ede506756fd4`，并接手未完成 Serenity durable run `lr_mtv34toq_575b86f20b1f`
> 证据原则：只记录实际回执；live/recorded/secondary 权威等级不混用，Candidate/Seed 不冒充 BUY，测试不冒充真实市场数据。

## 1. 本次结论：软件实现

WP-12、WP-13、WP-14 的实现缺口已经收口并从 active manifest 移出。material investment request 现在具备机器可验证的同轮终局门：荐股、组合、买卖判断、持仓处置和完整公司研究只要 canonical REQUIRED capability 尚未完成，就保持同请求自动推进并阻断投资结论；只有完整 verified terminal 才允许 investor view。公共资料补证不得作为“下一步”交回用户，只有公共自动渠道真实耗尽且剩余材料为私人输入时才允许请求用户协助。

全市场链改为 denominator/numerator 分权。XSHG/XSHE/BJSE 分别以上交所、深交所、北交所 official master 为首选 denominator；EastMoney/Sina 继续作为结构化行情路径，并新增只拥有 `market.quote_batch` 权限的 Tencent 独立行情 family。Tencent 只能按官方 master 的精确 symbol set 补价格、成交额、换手率和流通市值，不能定义 Universe、不能进入证券身份正式事实链。官方 master 异常时 complete secondary master 只能维持 observation/research 连续性，即使 100% 也不得升级正式全市场权限。

Serenity 方法层已升级为 `research-skills-v4`：同一 BaseCase 可在既有 Specialist 预算内选择多个互补 Delta，`source_family/selection_group` 确定性去重；历史单 Delta frozen input 兼容。20/50/100/200 日均线由 canonical D1 + manifest/hash + frozen evidence 确定性编译，Fundamental/Valuation 只绑定既有 Phase 9 artifact；v5 upstream audit 与共享 `LocalAdaptationRelease` 解耦上游审计和本地适配，不建立第二套估值账本。

永久安全边界不变：`broker_execution_allowed=false`；真实账户不自动发单；actual/paper 分 lane；PIT、来源身份、对象 hash、财务完整性、Universe、Committee、Portfolio 和推荐门继续 fail closed。

## 2. 验证结果：发布前冻结证据

| 验证门 | 实际结果 |
|---|---|
| 投资终局/编排专项 | `95 passed`；缺 Committee/Portfolio 等必需能力时 `CONTINUE_AUTOMATICALLY`，完整 verified coverage 才 READY |
| Universe/Provider/韧性专项 | `73 passed`；官方主表、secondary 降级、Tencent fallback、tamper/partial/跨市场等负例均覆盖 |
| 最终合并专项（终局、展示、continuation、Universe、provider） | `310 passed`；随后 quote-only 权限收紧后的回归为 `284 passed`，Tencent 盘前活动代理补充测试 `5 passed` |
| Tencent live provider probe | `HEALTHY`；registry=`provider-registry-v8`，唯一能力与 checked capability 均为 `market.quote_batch` |
| 常规三市场 `research-seeds --live` | `READY / FULL / OFFICIAL_DENOMINATOR_RECONCILED`：BJSE 343/343=100%，XSHE 2901/2901=100%，XSHG 2316/2317=99.9568%；三市场 denominator 均 `PRIMARY_OFFICIAL`，原 99.5% 正式阈值未放宽 |
| 强制独立行情回退 live | 关闭普通 seed provider、仅 official master + Tencent quote 时：XSHG 2317/2317、XSHE 2901/2901、BJSE 343/343，均 100% |
| 真实标的同请求 smoke | 600519 current continuation 在公开财报/身份仍需自动补证时保持 `AUTO_RESOLUTION_REQUIRED`、`investment_conclusion_blocked=true`、`user_assistance_request_allowed=false`、`investor_view_allowed=false` |
| 最终冻结树全仓 pytest | `2123 passed / 18 skipped / 0 failed`，2141 项收集完成，exit 0；显式 live/real benchmark 因未 opt-in 按合同跳过 |
| 最终静态/文档门 | Ruff 全仓 `All checks passed`；Pyright `0 errors / 0 warnings`；文档治理 + Workflow + Serenity 核心 + 兼容长度审计 `36 passed`；`git diff --check` exit 0 |

专项 Review 期间额外修复了四处真实缺陷：official-covered 派生快照的 `available_to_system_at` 不能早于派生创建时刻；Provider registry 变更后升至 v8，避免旧探针状态伪装当前；Tencent 从初始的 identity+quote 权限收紧为 quote-only，并建立独立 `quote-batch` probe，防止进入证券身份事实路由；兼容入口 `audit_investor_answer()` 恢复使用 legacy quick-view 字符预算，避免 DEEP_RESEARCH 新增的大报告预算吞掉旧 `INVESTOR_ANSWER_TOO_LONG` 契约。公共展示本身仍取消 material deep research 的旧 900 字/4 理由压缩：DEEP_RESEARCH/PORTFOLIO_DECISION 使用完整决策级预算，保留多个去重后的风险与判断改变条件。

## 3. 发布门与仍需执行的冻结验证

当前 tracked 实现与文档已完成状态迁移，后续不得再改业务逻辑。发布前必须在同一冻结树执行文档合同、Ruff、Pyright、`git diff --check` 和全仓 pytest，并确认 source tree stable；随后审查 staged diff、secret/private/runtime 文件边界，完成独立 defect-first Review 后才允许 commit + push。此处不预写未来 commit SHA、远端 HEAD 或“已发布”事实。
