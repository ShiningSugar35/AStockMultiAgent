# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT / WP-23 ACCEPTED
> 任务：Recommendation Breadth, Entry Timing & Event Intelligence
> 更新日期：2026-09-14
> 执行：GPT-5.6 Sol / mcp-wjp
> durable run：`lr_mtzsl7jx_1e0706c6e5d9`
> 证据原则：只记录实际执行回执；失败尝试、Review 缺陷与修复链保留，不把中间红灯改写成 PASS。

## 1. 本次结论

WP-23 已完成。此次改造同时解决了“荐股研究预算过度集中于电子/通信/材料链”“好股票缺少正式低位/入场质量判断”“关键人事、工商、信用、司法与上下游企业事件捕捉不足”三类问题，并在所有者追加要求后补齐跨行业公共研究方法论。收尾主 Agent 反例发现并修复了企业事件权威抬升与同源伪独立问题；最终源码通过精确 2232 节点 L3 全量验证。

核心边界未改变：纯 market-score blind tranche 仍先冻结；Breadth 只增加研究机会、不替换 blind 成员；私人/博主/Serenity Skills 只做 edge，不是发现先验或 formal readiness 前置门；EntryQuality 只影响已正式 eligible 标的的建仓节奏/首笔权重，不能把 WATCH/REJECT 变成 BUY；企业情报最终仍回到现有 SourceAccessRouter → Snapshot/ObjectStore → Evidence → Research Team → Committee/Portfolio/Publication Gate，不新增第二事实库、第二投委会或 Skill 直达交易路径。

## 2. 行业广度与公共 Core Methodology

- 根因复核确认：历史 Existing Candidate priority 不会改变 reserved blind membership；基线 `expert_seed_count=0` 也证明 Serenity 行业偏好不是该次集中结果的直接成因。真实缺口是有限 top-N 研究预算只按成交额/流通市值/换手排名，在主题行情下会自然集中。
- 新增 bounded `BREADTH_CHALLENGER`：只从 blind cutoff 附近、仍满足市场质量门且属于 blind 未覆盖 broad domain 的标的中追加少量研究对象；不能替换 pure blind、不能因行业稀缺放宽公司质量、不能直接形成 Candidate/BUY/仓位权威。
- 行业映射优先复用 SSE/SZSE/BJSE official instrument master；公共板块 taxonomy 仅做增强并有不可变缓存。分类缺失时 breadth 显式降级，pure blind 始终继续。
- 真实 live smoke 中，20 个 pure blind 高度集中于工业/硬科技时，系统仍额外补出 6 个独立 breadth challenger，实际覆盖金融、资源、能源公用、运输、商贸分销、消费等非 blind 主导领域，证明研究机会扩展路径真实工作而非仅单测存在。
- 新增 `configs/industry_methodologies.yaml`、typed `IndustryMethodologyRegistry` 与 CLI `industry-research-methodologies` / `industry-research-methodology`。当前 22 个 internal archetype 全部拥有专项方法包，同时叠加 1 个 generic competitive core：`known_archetype_count=22`、`covered_archetype_count=22`、`methodology_count=23`、`source_count=10`、`uncovered_archetype_ids=[]`。
- 公共方法论来自 CFA/ACCA、Damodaran/NYU、FDIC、FDA、IEA、USDA、World Gold Council、Nareit 等专业/监管/行业标准来源，只提供 analysis questions、KPI、valuation lenses 与 red flags；永久保持 `fact_authority_allowed=false`、`recommendation_allowed=false`、`private_skill_required_for_analysis=false`、`specialist_draft_required=false`。A 股当前公司事实仍必须由本轮 admissible Evidence 支撑。
- 为避免“为了全行业补齐反而把模板套错”，Review 将银行/保险/券商、软件/半导体/通信、创新药/成熟制药、汽车/新能源、公用事业/煤炭、矿业/化工、机械/军工等拆成独立方法包，并增加 metric-leakage 反例测试。未来若新增 archetype 而专项包尚未同步，运行时使用 `ARCHETYPE_FALLBACK → GENERIC_COMPETITIVE_CORE`，不会因合同未对齐报错或制造 `NEEDS_INFO`。

## 3. EntryQuality：把“买在低位”变成可审计上下文

- 新增 typed `EntryQualitySnapshot` 与确定性 service/config，使用 canonical D1 历史及有完整 lineage 的当前价格锚点计算 20/60/120/250 日位置/收益、近期高点回撤、20/50/100/200DMA 距离与排列、实现波动、5/20 日量额活跃度，以及可选 benchmark 60 日相对强弱。
- 状态覆盖 `ATTRACTIVE_DISLOCATION / BASE_BUILDING / TREND_CONFIRMED / FALLING_KNIFE_RISK / EXTENDED / NEUTRAL / INSUFFICIENT_HISTORY`。短历史、未复权公司行动跳变、benchmark 缺失都会显式降级；不使用 RSI/MACD 单指标或“离高点越远越便宜”的硬规则。
- Full Research / valuation / ranking / investor projection / portfolio 已接入 EntryQuality。正式公司质量、估值、Bull/Bear、Committee 与 Publication Gate 仍决定能否推荐；EntryQuality 只缩放已通过正式资格的初始建仓权重。
- Review 中额外修复 benchmark `as_of`/session 对齐、无 lineage 当前价 override 自动回退 canonical close，以及 legacy `ValuationPack v1` IDs/hashes 独立排序兼容问题，避免未来数据、无来源价格或旧工件误配。

## 4. 企业、人事与经营事件情报

- Candidate/event taxonomy 增加管理层/董事会变动、法定代表人/工商变更、股权质押冻结、重大诉讼/执行、信用风险等早期研究信号；Full Research NewsEvent 增加 typed category、相关实体、人物和 relation scope。
- 新增 typed Enterprise Intelligence：observation 必须绑定 Evidence/Snapshot lineage；同事实多源冲突进入 `CONFLICTED + investigation_required`，不允许 last-write-wins。
- 上市公司内部事项优先交易所/CNINFO/监管正式披露；GSXT、信用中国、法院等正式域按 capability 纳入 authority policy，并修复 broad `gov.cn` 不能遮蔽更具体 authority domain 的匹配逻辑。
- QCC MCP/API 与 Tianyancha API 仅登记为 optional `SHADOW` / `SECONDARY_STRUCTURED` 能力；`enterprise.*` 的 transport 偏好为 MCP → API → authoritative Web，但正式来源的 officiality/formal 权重始终更高。无商业凭据时主研究自动回落官方源，不要求用户购买服务，也不因外部能力缺失终止工作。
- 同厂 QCC MCP/API 的 independence group 由 `source_id` 确定性归一，不能伪装成两家独立证据。
- 收尾 authority rework 后，有效事件权威受 canonical `EvidenceGrade` 上限约束：只有 DIRECT 且 canonical PRIMARY_OFFICIAL 才能在合法官方来源标签下投影为正式官方事件；SECONDARY 即使调用方误填 PRIMARY 也会压回二手；UNVERIFIED/CONFLICTED/INFERRED、COMMUNITY_LEAD、PRIVATE_PRIMARY 仅保留为调查证据，不能铸造成已确认企业事实。跨源确认同时要求不同 `source_id` 与不同 independence group。

## 5. 验证结果

| 验证门 | 实际结果 |
|---|---|
| WP-23 最终 targeted 功能回归 | `129 passed in 91.60s` |
| Public industry methodology / fallback / metric-leakage | 当前 22/22 archetype + 23 methods；专项回归通过 |
| Knowledge gap temporal-boundary 修复联合回归 | `25 passed` |
| Enterprise authority / source routing / recommendation rework | `69 passed in 30.89s` |
| 独立 defect-first Review r4 | PASS，task `81c8f42f-a437-4556-a4a2-5a3b45b352f1` |
| Ruff | `All checks passed!` |
| Pyright | `0 errors, 0 warnings, 0 informations` |
| Docs / product / governance pre-closeout | `34 passed` |
| `git diff --check` pre-closeout | PASS（仅 line-ending 提示，无 whitespace error） |
| Full-suite 第一次最终候选 | `1 failed, 2203 passed, 18 skipped`；唯一失败为 Knowledge gap cutoff 同毫秒跨 scope 误判 |
| Full-suite Knowledge 修复后 | `2205 passed, 18 skipped in 2837.77s (0:47:17)`，exit 0 |
| Final exact L3 after authority rework | `2232/2232` 节点精确覆盖；`2214 passed, 18 skipped, 0 failed`；两个 shard exit 0 |
| Final exact L3 integrity checks | `exact_collection_partition=true`、`exact_execution_coverage=true`、`no_duplicate_call=true`、`all_processes_exit_zero=true`、`source_fingerprint_unchanged=true`；828 个源码/配置/测试/Skill 文件无变化 |
| Closeout docs/governance | `34 passed in 8.83s`，exit 0 |

18 个 skip 均为仓库既有显式 opt-in acceptance/live 测试（真实 OCR benchmark、private-book visual run、candidate/provider/reference live probes），没有把失败改成 skip，也没有为清计划伪造 live 授权或样本。

## 6. 保留的红灯与修复链

第一次最终全仓 `edac0846-9399-474e-84ff-0ac9f4700100` 得到 `1 failed, 2203 passed, 18 skipped`。唯一失败为 `test_online_audit_excludes_retired_pending_and_gap_from_a_passing_scope`：保留的失败数据库显示 active answers/thoughts/articles scope 被错误标记 `GAP_CUTOFF_HISTORY_UNAVAILABLE`，但同一 cutoff millisecond 实际唯一 gap event 属于已退休的 `comments:answers:...` scope。

该失败单测 1/1、整个 coverage-audit 模块 18/18、相邻 Knowledge collection/comments/audit 顺序簇 37/37 曾独立通过，但这些重跑不能消除原失败。结合原版本 Git 差异及失败数据库，可确认旧边界检查没有按 author/content-type 排除无关事件，已退休评论与审计恰好处在同一毫秒时会污染有效范围。最终版本将 author/content-type/excluded comment-scope 条件直接下推到 SQLite boundary query，并让调用方传递一致范围。联合 gap-accounting + coverage-audit 25/25 通过，Ruff/Pyright 恢复全绿，随后全仓 `2205 passed, 18 skipped`。

Closeout 文档门第一次曾得到 `2 failed, 32 passed`，原因是无活动 WP 的计划页遗漏两个精确机器锚点：`不得伪造` 与 `broker_execution_allowed=false`。这不是业务代码失败；最终 closeout 重新保留这些治理边界并以文档门实际回执为准。

最终 authority rework 的第一批负向用例先得到 `8 failed, 9 passed`（task `c9f63c67-4be9-4696-81a3-693cc26c1e01`），证明权威抬升与同源伪独立边界确有缺陷；同源测试第一次又因 fixture 原始快照重复触发 FK，修正为不同原始文档后，task `b0f12f15-de4b-4f69-82fd-d037e87eb0cd` 真实复现 `cross_source_confirmed=True`，不把 fixture 问题冒充产品缺陷。

第一版 exact validation harness task `e873acf9-f4be-4377-a84e-36c8e50704c8` 被取消：unit shard 运行到 1218 个通过项后，`test_provider_resilience.py` 对 Python 全局 `time.monotonic` 的 monkeypatch 影响了同进程 observer，observer 自身触发 `StopIteration`。该任务既不是产品 PASS，也不是产品 FAIL。仅在 ignored runtime harness 中固定捕获真实 monotonic 后重启 task `20f055b7-3701-4042-a12d-f591e7c0f870`，最终 `2232/2232` 精确覆盖、`2214 passed / 18 skipped / 0 failed`，五项完整性检查全 true。

## 7. Defect-first Review 结论

Review 期间发现并修复的主要问题包括：EntryQuality benchmark 时点对齐、Enterprise resolution 可复现性/同厂独立组、legacy ValuationPack source binding、无 lineage 当前价 override、交易所行业粗分类误标、公共行业方法包 KPI 串行业、静态 methodology `created_at` 不确定、未来新增 archetype 硬失败、Knowledge gap cutoff 同毫秒 scope 污染，以及企业事件 caller-controlled source authority inflation / same-source fake independence。所有修复都优先采用降级/fallback、canonical registry/Evidence 重建或最小边界收紧，未新增可把公开缺口误判成用户 `NEEDS_INFO` 的硬合同。

最终未发现以下安全回归：breadth 改写 blind membership、Skill 数量变成推荐先验、技术面产生独立 BUY 权威、商业企业库覆盖正式披露、调用方标签抬高 canonical Evidence 权威、单一数据源伪造独立确认、商业 MCP/API 缺失阻断主研究、第二事实库/第二投委会、真实券商执行旁路。`broker_execution_allowed=false` 与正式 Publication Gate 均保持不变。

## 8. Closeout 状态

WP-23 已从《开发计划》与 `planning/work_packages_v1.yaml` 的 active work package 中移除，机器索引恢复 `work_packages: []`；当前无未完成开发任务。功能实现已提交为 `a398ff1d1d49c07016ae83dd697e92f768d541b1` 并成功推送到 `origin/main`。远端核对时 `v0.4.0` tag object 仍为 `3e4fc60542b346101f5748896a8871e083ccfcfb`，peeled release commit 仍为 `a511d64c46076ccaaf6dbc9eabd7056c66d96369`，未移动既有 release。docs-only closeout 治理门已实际通过；本页完成最终事实归档后仅需提交/push 该 docs-only closeout，并执行 durable final review/completion。

## 9. 验证边界

22/22 表示本地行业方法包的配置覆盖，不表示每个行业都具有同等深度、官方认证或已经验证的投资超额收益。公开方法来源支持分析框架，不证明本系统的阈值、入场权重或选股效果。境外监管/行业指标需按 A 股公司业务与会计口径选择适用项，不能把银行套入工业企业现金流模型，或把 REIT 的 AFFO 机械用于所有地产开发商。商业企业情报仅完成可选能力登记、来源路由和 typed evidence 处理；未宣称取得企查查/天眼查生产授权、稳定免费接口或全量事件覆盖。

## 10. 收尾主 Agent 反例与最终修复

独立辅助审查 r2 返回 PASS（回执 `6c17005c142278a778e4b7086eaf4b7700243c09d101070e4e23d9536c7a0e2e`），但主 Agent 随后实际构造 canonical SECONDARY Evidence + 调用方 PRIMARY_OFFICIAL_WEB 标签，并复现 OFFICIAL_FILING 投影。因此没有接受辅助结论作为最终验收；durable Review 1 正确记录 FAIL，s4/s5 重开。

最终修复仅扩展现有 `EnterpriseIntelligenceService`：有效官方等级受 canonical EvidenceGrade 上限约束；非 DIRECT、社区/私有线索留在原 Evidence 库供调查，不能生成已确认公共企业事实；多源确认同时需要不同 source_id 和 independence group；新解析身份含 authority policy 版本以避免旧解析冲突，旧记录投影亦重新检查独立来源。合法二手输入即使误填官方标签也自动降权继续研究，不制造标签对齐式 `NEEDS_INFO`。没有新增 Provider、事实库、投委会或交易权限。

受影响企业/路由/推荐回归 `69 passed in 30.89s`（task `4720677f-ef70-4f2c-92d3-ddf967cbc1b4`）；独立复核 r3 的 HTTP 503 未计作审查结论；缩小范围的 r4 实际 PASS（task `81c8f42f-a437-4556-a4a2-5a3b45b352f1`，回执 `2b3a2972051b47eddf86dcbcf64d431565ff2a5bf5cfca9c9400a1425edab545`）。最终 exact L3 task `20f055b7-3701-4042-a12d-f591e7c0f870` 精确收集并执行 2232/2232 节点，`2214 passed / 18 skipped / 0 failed`，两个 shard exit 0；执行节点无重复、收集/执行集合精确一致，828 个源码/配置/测试/Skill 文件指纹验证前后完全一致，fingerprint=`315e94a87bde8f41b45646b51885caac500ec6225726aba7bc596477a22879f8`。
