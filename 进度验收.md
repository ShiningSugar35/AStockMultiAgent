# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT / WP-25 RELAXED-PRIVACY L2 VERIFIED
> 任务：投资预期上下文 + request-aware 偏松隐私
> 更新日期：2026-09-15
> 执行：GPT-5.6 Sol / mcp-wjp
> 证据原则：只记录当前代码树实际回执；中间红灯与旧语义不改写成 PASS。

## 1. 本次结论

WP-25 的投资预期主链保持不变：material investment request 在 Full Research 前分别解析本金与目标年化，优先级为“本轮用户明确值 → 同账户最近可验证的已注册请求/持仓或荐股研究记录 → 版本化模型默认值”。模型 DEFAULT 不遮蔽更早用户明确值；其它账户、未来记录、坏对象和不可验证记录不冒充用户偏好。目标年化只作规划目标，不构成收益承诺，也不提高硬风险上限或真实券商权限。

本轮最终修正的是此前过严的公开隐私边界。现在采用**偏松、基于来源与本轮用户意图的最小隐私保护**：

- 荐股规划本金/投资预算、目标年化与目标盈利、推荐配置金额/建议股数、证券公开价格，以及发行人财务/股东公开数据可正常进入投资者报告；不能因为包含金额或股数就判定为隐私。
- 只有来自真实/模拟账户和既有持仓的个性化事实默认不主动输出：持仓本金/成本、账户资产/现金、当前持仓数量、实际持仓盈亏。
- 用户本轮明确提及、询问或要求查看对应账户/持仓类别时，系统会从已核实 canonical 私有记录中正常回答该类别；例如问“我的账户余额是多少”“我现在持有多少股”“这只持仓亏了多少”。只请求一个私人类别不会自动解锁其它类别。
- 发行人主语明确时，“公司账户余额”“公司持仓数量”“控股股东持有多少股”等公开事实不会被个人隐私审计误杀。
- 展示规则不回写账户事实、receipt、paper fill、估值工件或历史语义 hash；`broker_execution_allowed=false` 保持不变。

## 2. 投资预期与目标收益规划

- 本金与目标年化独立记录 CURRENT_USER / RECENT_HISTORY / DEFAULT 来源；当前只给一项时另一项可独立恢复。
- 已注册用户请求、冻结 request contract 与正式研究都可作为有界历史来源；账户隔离、时间截止、对象完整性与身份绑定保持原有门禁。
- 明确外币本金不静默当人民币；持仓市值不被猜成可投资本金；明确非法值不被默认值悄悄覆盖。
- 只有估值期限明确且可比较时才把目标年化折算到同一期限；未知/混合期限不制造目标达成结论。
- 情景盈亏按计划证券名义金额计算并扣除已估算建仓成本/滑点；目标收益条件价是独立研究条件，不改写基础估值或 execution authority。

## 3. 当前公开输出行为

普通荐股/投资组合报告可以自然展示模型或用户给定的规划本金、全年目标盈利、目标年化、情景盈亏、保留现金金额、建议首仓/目标股数以及每股条件价格。这些属于“方案本身”，不是账户隐私。

已有持仓/账户数据则默认使用比例或不主动展开绝对值；只有本轮用户主动问到某类数值时才显示该类已核实事实。例如普通“这个持仓要不要减仓”仍不主动报当前股数；“我现在持有多少股，这个持仓要不要减仓”则可以显示当前股数。

Developer Mode 与 Investor Mode 使用同一条用户意图例外：调试场景不会因为模式不同擅自泄漏未请求的持仓数据，也不会在用户明确询问自己账户数据时继续强制遮蔽。

## 4. Defect-first 修复链

本轮纠正了“本金/金额/股数一律不公开”的错误实现。返工后把隐私语义从词面匹配改为类别化、request-aware 判断，并恢复规划本金、目标盈利、方案股数等正常荐股信息。同时保留未请求真实账户/持仓事实的默认保护，并补充发行人主语豁免，避免“公司账户余额”“公司持仓数量”等公开事实被误判。

第一轮新隐私测试曾出现 2 个失败，原因只是断言要求小数尾随零原样输出（`783249.70`），而公共格式器正常输出 `783249.7`；测试改为数值等价后通过，没有为通过测试修改业务语义。Ruff 首次发现两处长行，拆行后通过。

## 5. 最新验证结果

| 验证门 | 当前实际结果 |
|---|---|
| 偏松隐私最小回归 | `283 passed in 31.05s` |
| 扩展 L2：投资预期 / Full Research / 编排 / 网关 / 展示 | `425 passed in 112.75s` |
| 发行人/个人边界专项 | `76 passed in 7.21s` |
| 最后展示网关增量回归 | `244 passed in 16.11s` |
| Ruff（本轮受影响源码与测试） | `All checks passed!` |
| Pyright（本轮受影响源码） | `0 errors, 0 warnings, 0 informations` |
| 文档治理 | `34 passed in 3.65s` |

以上测试集合存在重叠，不能机械相加。本任务仍按 **L2 targeted-first**：没有修改 PIT、Universe denominator、正式财务事实、交易账本、数据库迁移或真实执行权限，因此未机械运行全仓 pytest。

## 6. 性能与占用

- 隐私检查只扫描当前待展示文本与当前用户请求，不访问网络，不遍历账户历史，不增加第二份状态。
- 投资预期历史恢复仍使用固定 registry/object 上限；没有因为隐私修正增加历史查询或行情采集。
- 本轮测试使用项目内 `.ai-bridge/tmp/wp25-*`，终局只删除可再生 WP-25 临时目录，不触碰 `runtime/` 正式工件、`user_state/` 或其它会话文件。

## 7. Closeout 状态

业务语义与 L2 验证已经完成。终局只允许：清空活动 WP-25 计划、清理项目内 WP-25 临时目录、复跑文档合同、staged diff/Secret/private/runtime 审计、最终 defect-first Review、commit/push、HEAD==origin/main、clean worktree 与 durable review/complete。本页不写自身提交 SHA，避免自引用提交循环。
