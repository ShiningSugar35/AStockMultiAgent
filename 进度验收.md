# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT / WP-24 L3 VERIFIED
> 任务：自然投研表达、轻量持仓档案与主动跟踪
> 更新日期：2026-09-14
> 执行：GPT-5.6 Sol / mcp-wjp
> 证据原则：只记录实际执行回执；失败尝试、Review 缺陷与修复链保留，不把中间红灯或外部 reviewer 故障改写成 PASS。

## 1. 本次结论

WP-24 的实现与最终冻结代码树已完成 L3 验证。公共投资者回答从字段列表式模板调整为自然中文段落，继续由事实指纹、隐私/内部词审计与字符预算约束；文风问题不再被误判成资料不足，也不能绕过事实漂移、Secret、私人路径或内部实现泄漏门。只有用户明确提出开发/调试需求时才允许进入 Developer Mode。

持仓侧新增 Git-ignore 的轻量人类可读投影，用于会话恢复和后续增量复核；它不是第二套账本。实盘事实仍只来自 append-only external-account event，模拟盘事实仍只来自 paper ledger/fill。读取失败、账户身份暂缺、缓存损坏、旧观察或并发写入都不会被解释为清仓。`broker_execution_allowed=false` 保持不变，没有新增真实券商写入或模拟下单旁路。

## 2. 自然投研表达与公开安全边界

- `ResponseGateway` 改为自然段落式投资报告：主体、结论、判断把握、估值/赔率、决定性理由、风险、改变判断的条件、资料时点与引用保持在同一公开合同内；高阶术语只在确有必要时首次简释。
- presentation policy 增加机械“不是……而是…… / ……而不是……”、空泛套话、装饰性符号等中文文风检查。style-only finding 只有在事实等价已经核验且不存在其它安全 finding 时才允许继续公开发送；事实漂移、内部机器词、私人路径、Secret、超预算等仍会阻断。
- `NEEDS_INFO / BLOCKED / UNAVAILABLE / PENDING / READY` 等孤立机器状态有自然语言投影。机器状态本身不证明必须让用户补资料；只有公开恢复渠道真实耗尽且剩余缺口属于用户私人事实/文件时，才允许请求人工协助。
- 外部 Humanizer 项目仅用于规则侦察，没有 vendoring、没有新增运行时依赖，也没有使用“AI 检测器分数”驱动内容修改；数字、日期、方向、引用、风险与不确定性不进入机械同义词改写。

## 3. 轻量持仓、交易历史与首次快照

- `user_state/position_tracking/当前持仓.md`、`已结束交易.md`、`主动跟踪计划.md` 由 canonical preflight best-effort 更新；`.projection.json` / pending 只作为可恢复生成缓存，不拥有经济事实。
- 首次可核实观察价和首次研究快照一经记录即冻结。若首次记录时已经存在封存的 `RecommendationResearchReceipt`，只读投影当时可见的投资逻辑、基本面覆盖、行业周期、宏观、财务审计、治理及研究价格；后续研究不得倒填历史。
- canonical 交易历史可恢复真实买入/卖出或模拟 fill 的时间。费用完整时记录净盈亏/净收益；实盘费用缺失时只记录扣费前结果并明确净收益未知；转入/转出不伪造成买卖收益。
- 权威账户读取不完整时保留上次活动记录；只有完整 authoritative lane 确认持仓消失后才进入已结束交易。缓存损坏、缺文件、部分来源失败、较旧观察、写入竞争和中断恢复均有负向回归。
- 文档投影的项目根解析经过 defect-first 返工：外部配置的 state DB 不得把可选 Markdown 重定向到 `D:\AStockMultiAgent` 之外；生产 `runtime/state.sqlite` 解析回仓库根，测试隔离库仍留在仓库内隔离目录。

## 4. 待入场观察与主动任务

- 正式荐股 receipt 封存后，只有 formal recommendation 允许、候选仍 eligible 且 EntryQuality 为 `EXTENDED / FALLING_KNIFE_RISK / INSUFFICIENT_HISTORY` 的“时点未成熟”标的才自动进入 entry watch；历史显式移除、拒绝或退出会阻止自动重新加入，观察名单不具有 BUY 权威。
- Proactive monitor 以实盘、模拟盘、正式观察集合的并集生成计划；`max_subjects_per_run=50` 只限制单批大小，51 个及以上标的会继续分批，不会静默漏掉。
- 已创建并启用四个研究/通知任务窗口：盘前 09:10、早盘 10:30、盘中 14:00、收盘后 15:30（Asia/Shanghai，工作日规则）。任务每轮必须重新读取 canonical 持仓/观察集合；创建成功、启用成功与某轮实际读取成功是不同事实。截至本次验收这些 A 股任务尚无首轮成功运行回执，因此不宣称平台无人值守 MCP 执行已经得到实证。
- 平台任务只允许研究复核与必要通知，不提交真实订单、不写账户事实、不创建模拟订单、不执行 paper replay；连接器不可读时必须报告“无法核实”，不能猜持仓或把缺数据当空仓。

## 5. 验证结果

| 验证门 | 实际结果 |
|---|---|
| WP-24 最终 targeted 回归 | `306 passed in 331.49s` |
| 持仓路径根边界回归 | `25 passed`，外部 state path 不产生项目外写入 |
| Ruff 最终源码树 | `All checks passed!` |
| Pyright 最终源码树 | `0 errors, 0 warnings, 0 informations` |
| `git diff --check`（L3 冻结前） | PASS |
| 最终 L3 unit 分片 | `1705 passed in 1509.57s` |
| 最终 L3 integration + contract + property 分片 | `562 passed in 1845.11s` |
| 最终 L3 acceptance + live 分片 | `1 passed, 18 skipped in 8.00s`；skip 均为仓库既有显式 opt-in acceptance/live 门 |
| 完整测试集合收集 | `2286 tests collected in 9.61s`；与 `1705 + 562 + 19 = 2286` 精确一致 |

最终冻结代码树合计为 **2268 passed / 18 skipped / 0 failed**。18 个 skipped 分别要求真实 OCR benchmark、私有书视觉运行 ID、candidate live request 或显式外部 provider/reference live opt-in；没有把失败改成 skip，也没有为清计划伪造外网授权或私有样本。

## 6. Defect-first Review 与修复链

本轮早期独立持仓验收曾稳定复现 8 个失败，覆盖“读取失败误判清仓、估值/事件变化不可见、损坏缓存可能破坏归档、超过 50 个标的可能遗漏”等边界；随后逐项返工并扩大到首次观察价冻结、正式研究 PIT 快照、并发/中断恢复、真实交易 episode、费用未知语义及项目写入边界。

收尾主 Agent 又发现并修复了“外部 `ASTOCK_STATE_DB` 可能让可选持仓 Markdown 写到项目目录之外”的缺陷；修复后 targeted、Ruff、Pyright 与最终 L3 全绿。Gemini reviewer 多模型尝试均因服务端 HTTP 503 高负载未返回审查内容，OpenCode 独立 reviewer 也遇到自身 server error；这些调用不计作 PASS，也没有用空结果冒充独立结论。最终内容审查仍以当前 staged diff、实际测试回执和 durable final review 为准。

## 7. 安全与事实边界

- 未新增数据库迁移、第二账本、第二 Router/Evidence 或真实券商执行路径。
- `user_state/`、runtime 对象、账户私有事实和平台 task 运行工件均不进入 Git。
- 外部 Provider/连接器失败只产生未知覆盖或恢复路径，不得解释为“没有持仓”“没有风险”或“研究完成”。
- 投资建议仍受 Full Research、Committee/Publication Gate、组合风险和既有 paper confirmation 约束；entry watch 与主动通知本身均无交易权限。

## 8. Closeout 状态

当前机器计划已清空活动工作包；现行架构、Workflow、Skill 与 scouting note 已吸收本次稳定事实。最终提交前只允许执行文档合同、staged diff/Secret/private/runtime 审计、临时测试目录清理和 durable final review；这些收口动作不得再改变已经通过 L3 的业务代码语义。
