# AStockMultiAgent 进度验收

> 文档职责：这里只保留**最近一次任务**的有证据验收记录。历史阶段流水由 Git 历史承担；仍影响当前项目理解的稳定事实必须写入 `AGENTS.md`、`docs/architecture/`、`docs/workflows/`、README 或 canonical Skill。
> 当前任务：`v0.3.0` 剩余主线收口（R-01 / R-02 / E-02 集成、文档职责重构与正式发布）。
> 当前状态：`v0.3.0` 已正式发布；本文件为发布后 docs-only closeout 终态，只记录本次任务最终事实，不回填更早验收流水。

## 1. 本次任务范围

- 对齐 `runtime/worktrees/s7`、`s8`、`s12` 与最新 `main`，只吸收可验证成果，不直接合并落后的分支历史。
- 完成 R-01 官方主数据/宏观/BJSE 来源、R-02 ETF 产品深度与独立 paper execution、E-02 九项外部能力资格试点。
- 将旧《验收报告》压缩并更名为本文件；删除不再影响当前理解的历史流水，把长期边界迁入对应职能文档。
- 继续执行 `v0.3.0` L3 全仓门、独立 defect-first Review、release baseline、GitHub Release 与发布后 docs-only closeout。

## 2. 已成立的实现事实

### R-01｜官方来源补齐

- `s7` 三个提交已按原顺序集成到最新 `main`，未回退既有 R-03 / E-03。
- 沪深 `instrument.master` 已有上交所/深交所官方 denominator freeze 与对账；强证明只从真实 2026-09-02 freeze 的可得时间起生效，不回填更早历史。
- 国家统计局、人民银行、财政部、发改委保持 recorded-first，当前 `live_supported=false`；宏观 replay 只认真实 `FETCH_OBSERVED` PIT。
- 北交所财报与公司行动当前只准 exact-item 官方 Web 捕获，不声明 exhaustive enumeration，也不以“未找到”证明不存在。
- 集成后独立回归：**159 passed**。

### R-02｜ETF 产品事实与模拟执行

- ETF/FUND/INDEX 产品身份、benchmark、费率、AUM/份额、NAV/iNAV、市场价、溢折价、成分/权重、tracking error 与 liquidity 已进入既有 PIT / ObjectStore / portfolio 合同。
- VERIFIED 级产品事实缺关键字段时 fail closed；缺 NAV/iNAV 不制造伪溢折价。
- ETF 使用独立 `configs/etf_paper_trading_rules.yaml` 与 migration `0066`；STOCK 规则不会被继承。
- ETF execution 默认关闭；lot/tick/fee/settlement 按精确 instrument + effective date 冻结到订单 binding。只有确认订单后的 fill/replay fill 改变模拟持仓，真实券商执行继续永久不存在。
- 集成冲突只涉及 migration 测试：保留 R-03 持续监控自身 `0059` 依赖，并在 state migration 清单中同时保留 `0065` 与 `0066`。
- 集成后独立回归：**118 passed**。

### E-02｜九项外部能力资格试点

- 已固定并裁决 Arelle、Docling、Playwright MCP、AKShare、Crawl4AI、changedetection.io 与三个 Repo Skills。
- 当前九项候选全部为 `SHADOW`。`source-qualification-auditor` 的 recorded contract、observability、revocation 与 uninstall 回归成立，但此前所谓 controlled-live 只是合成观测/路由验证，没有冻结真实 Skill 任务输入、命令/输出 trace、不可变结果与延迟证据，因此已撤回其 `PRODUCTION_BACKUP` 声明。
- AKShare 没有为满足计划配额而强制升级：当前证据不足以证明 endpoint-specific 数据权利、PIT、provenance、recorded/live、SBOM 与故障边界，因此继续 `SHADOW`；其生产备用门已重分类为外部数据权利/授权义务并保留在《开发计划》。
- `s12` final status 之后出现的 BaoStock `0.8.9 → 0.9.3` + UTF-8 BOM 未提交漂移与 `pyproject.toml` 锁定版本冲突，已恢复到最终可信提交；未把未知漂移带入主线。
- Fresh-checkout 发现原资格报告直接 hash tracked JSON 原始 CRLF/LF 字节，导致跨 checkout report id 漂移。已改为对 validated evidence 做 canonical JSON freeze，并排除未显式提供的嵌套默认字段；九份报告重新冻结，新增 CRLF/LF 不变性回归。
- 修复后的 E-02 定向回归：**57 passed**。

## 3. 当前安全边界

- `broker_execution_allowed=false` 永久不变；系统没有真实券商自动下单接口。
- Recommendation Gate、PIT、官方性、完整性、人工确认和 append-only paper/account ledger 均未因本轮体验层、ETF 或外部备用能力放宽。
- 外部包 License、stars、下载量、Registry 上架或 demo 成功都不能替代 endpoint/data rights 与 M-06 资格门。
- 本轮没有建立第二套 Router、Evidence、Paper、用户状态或来源事实源。

## 4. 发布前验收状态

- R-01 integrated regression：`159 passed`。
- R-02 初始 integrated regression：`118 passed`；独立架构 Review 返修后，跨 instrument 卖出冻结、ETF milli-yuan 跨会话投影、E-02/root-doc/Skill 合同联合定向矩阵：`69 passed`。
- E-02 初始 integrated regression（含 fresh-checkout canonical evidence 修复）：`57 passed`；架构 Review 已把所有未经真实 controlled-live / endpoint data-rights 证明的候选降回 `SHADOW`，并由机器合同保证 AKShare 未满足生产备用门时必须继续保留外部资格义务。
- 最终 frozen tree：`uv run pytest -q` = **`1548 passed / 18 skipped / 0 failed`，1620.39s**。18 个 skip 均为既有显式 opt-in acceptance/private/live 测试。
- Ruff：`All checks passed!`；Pyright：`0 errors / 0 warnings / 0 informations`；`git diff --check`：PASS；只读 `state-integrity-audit`：`PASS / integrity_check=ok / read_only=true`。
- 受控 live smoke：SZSE `instrument.master` = `HEALTHY / PRIMARY_OFFICIAL`；SSE 本轮两次低频 probe 均为 `UNAVAILABLE / NETWORK`，按单层重试边界停止继续撞站。该结果记录为真实外部网络降级，不冒充解析/完整性失败；同日较早 SSE live probe 曾在相同 R-01 实现上 `HEALTHY`。
- release baseline `5567cab46195a5073a9bf5a4fc16acfe1ff35066` 已推送；annotated tag `v0.3.0`、非 Draft/非 Prerelease GitHub Release 与两个构建资产均已远端验证。wheel digest=`sha256:19ddb987ef5d73058b204f329525d5628d1a04c934ae6d4ec9f396f76bf1abd8`；sdist digest=`sha256:3a0a3efd2b84cecfa67114dabef1a2ad29f51f92d02770641b0f4ee52fa25109`，均与本地构建逐字节一致。

## 5. 发布终态

- `v0.3.0` release baseline：`5567cab46195a5073a9bf5a4fc16acfe1ff35066`；annotated tag `v0.3.0` 解引用精确指向该提交，tag 在发布后不得移动。
- GitHub Release：`AStockMultiAgent v0.3.0`，状态为非 Draft、非 Prerelease，发布于 `2026-09-02T14:50:30Z`；创建 Release 时远端 `main` 与 release baseline 一致。
- 构建资产：`astock_research-0.3.0-py3-none-any.whl`（1,303,295 bytes，SHA-256 `19ddb987ef5d73058b204f329525d5628d1a04c934ae6d4ec9f396f76bf1abd8`）；`astock_research-0.3.0.tar.gz`（2,060,224 bytes，SHA-256 `3a0a3efd2b84cecfa67114dabef1a2ad29f51f92d02770641b0f4ee52fa25109`）。GitHub asset digest 与本地构建一致。
- 本轮发布没有放宽 `broker_execution_allowed=false`、PIT、Universe/官方性/完整性、Recommendation Gate、人工确认或 append-only ledger。ETF paper execution 仍默认关闭，必须有精确 effective instrument rule + 既有确认/replay 链；E-02 九项候选均保持 `SHADOW`。
- AKShare 的“至少一个 endpoint 达到 `PRODUCTION_BACKUP`”仍因 endpoint-specific 生产数据权利和其他 M-06 外部证据不足而未满足，已作为**外部数据权利/授权义务**保留在《开发计划》，不伪造成已完成资格。
- 发布后仅允许 docs-only closeout 推进 `main`；closeout 自身提交 SHA 不写回本文件，避免形成自引用提交循环，最终 `origin/main`/tag/clean-worktree 关系由唯一 durable run 和 Git 终局核验记录。
