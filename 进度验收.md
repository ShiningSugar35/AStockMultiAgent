# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT / WP-22 ACCEPTED
> 任务：Current-Only Research Recovery Refactor
> 更新日期：2026-09-13
> 执行：GPT-5.6 Sol / mcp-wjp
> durable run：`lr_mtzhhvph_32d4e46b16a8`
> 证据原则：只记录实际执行回执；失败尝试与修复链保留，不把中间红灯改写成 PASS。

## 1. 本次结论

WP-22 已完成。生产荐股/模拟交易的 CURRENT 路径现在明确采用“**当前可获得信息持续向前研究**”语义：同一请求中的公开数据可以在 bounded acquisition/recovery 期间继续取得、修复与交叉验证，最终在本轮采集/恢复结束后冻结当前决策快照；用户提问时刻不再被当作 anti-lookahead 截止点，旧 reference release 也不再因为后来出现新 head 而使 CURRENT 研究失效。

显式 historical/backtest/replay 仍保留独立 PIT/source-availability/anti-lookahead 规则；这些历史规则不再反向约束 CURRENT。此次改造没有降低正式荐股门：Candidate 仍只是深研资格，最终 BUY/组合建议仍必须经过完整公司研究、Financial/Governance CriticalVeto、估值、独立 Bull/Bear/Reviewer、Committee、Portfolio/Risk、Mandatory Research DAG、`RecommendationResearchReceipt` 与 Publication Gate。

## 2. 实现结果

- Candidate Promotion / Verifier 从“wrapper 字段、snapshot 顺序、历史 PIT head 必须全等”改为 CURRENT 的语义一致性 + source lineage + object integrity 校验；显式 historical 模式继续使用严格历史边界。
- CURRENT `CandidateInputRelease` 的核心发现输入收敛为证券身份、交易日历、最低充分日线和数据质量。公司行动、公告、财务完整性改为 enrichment：缺失时保留 retryable recovery task，但不再把核心候选直接删除。
- 官方公司行动和公告标题命中改为研究信号，不再被错误解释成“发现事件所以缺信息”。公告正文和财务事实仍必须在后续 Deep Research 中闭环才能进入正式建议。
- 日线 `amount=None` 可在明确降级条件下使用 `close×volume` 作为 turnover proxy；仅当满足最低交易日、无重复/OHLC 错误等确定性条件时 PARTIAL 数据才能继续 Candidate discovery。
- CURRENT 不再因 `available_to_system_at > request.as_of` 或 legacy `NOT_PIT_SAFE` 标记而判“未来数据”；这些检查只在 `formal_historical=True` 时生效。
- `NEEDS_INFO` 被明确为内部自动恢复状态：canonical object 漂移先重读/重建，结构化 provider 缺口耗尽 allowlisted fallback，公开事实缺失或冲突再进入 bounded authoritative Web 多源补证并回灌既有 Evidence/ObjectStore。只有公共恢复真实耗尽或只剩私人事实时才允许请求用户输入。
- Current Research recovery budget 仍受 Schema `60..7200` 秒约束，但不再要求请求、Acquisition、Research Team 必须与默认 `1800` 秒精确相等；1800 只是默认策略值，不是身份合同。

## 3. 真实 live 回归

使用同一 20 Seed 冻结报告：
`ResearchSeedReport:research-seeds:750c4b41f97417750c678720367dbd2d25553c67ad44196a2c1d6f693962d9e8`

最终 Promotion：
`SeedPromotionReport:seed-promotion:e9de651d80b6b0478b8f4af5376550a0e6913fd88fbce1de9b7f3b2364a6aa90`

实际结果：

- selected：20；core promoted：19；blocked：1；reused：0。
- Candidate Scan：`SUCCEEDED`；scan id=`62278be3c2ffb369e2a2ae16b0fa8797c38a0c321037c202b02669a75f2ad42e`。
- 生成 15 个 `RESEARCH_READY` 深研候选。
- 11 个沪深标的缺 `FinancialIntegrity` enrichment 时仍保留为 promoted，并挂 `FINANCIAL_INTEGRITY_REQUIRED` retryable task；不再从 Candidate discovery 消失。
- 唯一核心阻断为北交所 `920045`：`DAILY_LOCAL_VERSIONED_REFERENCE_UNAVAILABLE`。这是最低发现所需日线真实不可用，因此保留为 retryable core-data block，没有通过猜值或 Web 临时行情强行放行。
- 改造前同一条 live lineage 只有 2 个 Seed promotion 通过，Candidate Scan 最终为 `NEEDS_INFO` / 0 records；本轮实测证明历史式包装门已从 CURRENT discovery 中解除。

审计结果：

- `research-seeds-promote-audit`：PASS，`finding_codes=[]`，同时 `recommendation_allowed=false`、`paper_ledger_write_allowed=false`、`broker_execution_allowed=false`。
- `candidate-audit`：PASS，`candidate_count=15`，`universe_member_count=15`，`failure_codes=[]`。

## 4. 验证结果

| 验证门 | 实际结果 |
|---|---|
| Candidate / Promotion / CLI final regression | `41 passed in 70.04s` |
| Current continuation / productization / capability / logging | `104 passed in 67.13s` |
| Full Research + Research Team + CURRENT continuation + real business E2E | `142 passed in 641.25s` |
| Docs / product / governance targeted | 最终 `38 passed in 3.86s` |
| Ruff | `All checks passed!` |
| Pyright | `0 errors, 0 warnings, 0 informations` |
| `git diff --check` | PASS |
| Full-suite final6 | `2160 passed, 18 skipped in 2642.25s (0:44:02)`，exit 0 |
| Tracked runtime/private audit | `git ls-files runtime user_state .ai-bridge .devbridge` 仅 `runtime/.gitkeep` |

### 保留的红灯与修复链

full-final5 曾得到 `1 failed, 2159 passed, 18 skipped in 2733.10s`。唯一失败是 `tests/unit/test_product_surface.py` 对 `AGENTS.md` 的精确安全句机器合同：新 CURRENT/historical 说明仍保留了安全含义，但改写了固定文本。修复仅恢复精确总括句“`AI 不得通过 prompt、proposal、Skill 或 fallback 绕过这些边界`”，没有修改运行时代码；随后相关文档/产品合同 `38/38` 通过，最终 full-final6 全绿。

## 5. Review 结论

Defect-first Review 未发现需要返工的业务缺陷：

- CURRENT 的 `CandidateInputRelease.as_of` 取本轮实际输入 `available_to_system_at` 的最大值，确认是 acquisition/recovery 后的当前快照，而不是用户提问时刻。
- `SeedPromotionStatus.PARTIAL` 仅表示仍有 enrichment recovery task；下游没有把该状态当作推荐发布权，也没有新的全局终止器。
- Candidate 的放宽只发生在 discovery/enrichment 职责边界；证券身份、最低日线样本、对象/来源完整性、重复/损坏/事实冲突检查仍保留。
- active Candidate/Orchestrator/Company Skills 已明确 CURRENT 与 historical 的隔离，没有发现“CURRENT 必须 PIT-safe / question-time cutoff”的旧规则残留。
- 没有建立第二套路由、事实源、Evidence store 或交易账本；自动 Web/LLM recovery 仍必须回到现有 Evidence/ObjectStore，并且不能直接产生 BUY。

## 6. Closeout 状态

WP-22 已从《开发计划》与 `planning/work_packages_v1.yaml` 的 active work package 中移除；当前机器索引 `work_packages: []`，无未完成开发任务。本轮只推进 `main`，不创建新 release，也不得移动既有 annotated tag `v0.4.0`；该 tag 继续固定在 release baseline `a511d64c46076ccaaf6dbc9eabd7056c66d96369`。
