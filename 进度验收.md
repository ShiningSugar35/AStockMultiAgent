# AStockMultiAgent 进度验收

> 状态：v0.4.0 RELEASED
> 主线：`full-research-recommendation-v1`
> LATEST VERIFIED SNAPSHOT
> 任务：Full-Research Recommendation P0 / E2E・Shadow・Release 已完成
> 更新日期：2026-09-13
> 执行：GPT-5.6 Sol / mcp-wjp；durable run `lr_mtw92jn9_aabe249465fb`
> 证据原则：只记录已实际取得的回执；测试不冒充 live 市场结果，upstream readiness 不冒充最终荐股权，失败尝试不从历史中删除。

## 1. 本次结论

Full-Research Recommendation P0 主线已经完成实现、缺陷修复、冻结树验证、release baseline、annotated tag、GitHub Release 与发布后归档。已完成工作按治理规则从 active manifest 移出；`planning/work_packages_v1.yaml` 当前无 active work package。

所有直接形成证券选择、买卖判断、价格、数量或仓位的请求统一归一为 `FULL_RESEARCH_RECOMMENDATION`。旧 `BUY_DECISION / HOLDING_DECISION / PORTFOLIO_DECISION / RECOMMENDATION` 不再是现行 RequestIntent、Planner、Executor、公共答案或发布路径；历史输入名称只允许在 request construction 时迁移到 Full Research。

最终公共投资建议的唯一权威是不可变 `RecommendationResearchReceipt + Publication Gate`。`FullResearchInputReadinessReport` 只证明上游研究输入完整，不具有正式荐股权。Mandatory Research DAG、PIT/source lineage、Financial/Governance CriticalVeto、多模型估值、因子/候选排名、组合/风险/执行、行情 freshness、独立 Challenger、receipt replay 与 tracking/re-evaluation 已绑定为同一闭环。

账户边界保持：`broker_execution_allowed=false`；actual/paper 分 lane；`EA_PROVISIONAL` 只允许冻结不完整持仓声明，真实 `EA_WRITE` 必须独立走 `ACCOUNT_FACT_WRITE`。系统允许零 BUY、少于目标数量和现金持有；无新鲜行情不生成即时成交数量；后续重评只追加 revision，不修改历史 receipt。

## 2. 本轮 defect-first 修复

1. **官方证据 authority 漂移**：Full Research source manifest 原先仅按 `document_id` 字符串猜 authority，导致已经 typed 为 `EvidenceGrade.PRIMARY_OFFICIAL` 的 CNINFO/正式披露被误降为媒体。现改为 typed evidence grade 决定 official authority；原稳定失败 scenario 36 随后通过。
2. **旧推荐权多中心**：Full-market `RecommendationReadiness*` 已改为 `FullResearchInputReadiness*`，旧上游 `formal_recommendation_allowed` 字段/CLI/checkpoint/文档语义删除。源码中的正式荐股许可只存在最终 `PublicationDecision`。
3. **持仓声明与真实账户写入混淆**：Full Research 只允许 `EA_PROVISIONAL` 作为显式 required 的临时持仓输入；`EA_WRITE` 仍 fail closed 并要求独立 `ACCOUNT_FACT_WRITE`。
4. **旧轻量设计残留**：RequestIntent、Planner、公共投影、Presentation Task、AGENTS、Research Team Workflow、总控/candidate/portfolio Skills 和 CURRENT 架构文档均收敛到 Full Research；未保留旧轻量荐股作为并行现行路径。
5. **配置状态漂移**：`full_research_recommendation.yaml` 已为 `ENABLED`，policy loader 会拒绝非 ENABLED 配置；无消费方的重复路由配置已删除，路由只保留 canonical request matcher。
6. **持仓 Full Research 终局动作丢失**：canonical dependency/语义路由共同冻结 `EXISTING_HOLDING`，`HOLDING_REVIEW` 成为最终 gate 必需依赖；持仓 snapshot 绑定 monitoring plan、preflight portfolio revision、artifact/hash 和 Evidence lineage，零新增 BUY 时仍保留 HOLD/ADD/TRIM/EXIT/REVIEW。
7. **持仓 ADD 可绕过候选准入**：receipt 交叉校验禁止任何 ineligible 标的保留 ADD；TRIM/EXIT/HOLD/REVIEW 仍可表达已有持仓处置。
8. **持仓路由关键词过宽**：裸 `持有` / `多少股` 不再把股东持股事实查询误路由为投资决策；“买多少股”等配置/下单语义仍强制 Full Research。

## 3. 最终验证结果

| 验证门 | 实际结果 |
|---|---|
| 全仓 pytest 最终冻结树 | `2153 passed, 18 skipped`，exit 0，`2694.14s (0:44:54)` |
| 旧 final3 失败回执 | `5 failed, 2148 passed, 18 skipped`；失败集中于 scheduled semantic 五族 coverage；该失败事实保留，不计为 PASS |
| final3 后针对性复验 | 单失败用例 `1/1`、完整文件 `8/8`、相关前缀 `65/65`、更宽前缀 `131/131` 通过；随后 final4 全仓通过 |
| 全仓 Ruff | `All checks passed` |
| 全仓 Pyright | `0 errors, 0 warnings, 0 informations` |
| 冻结树 Docs / Workflow / Skill governance | `35 passed` |
| 发布后 docs-only closeout 合同门 | 首轮 `33 passed, 2 failed`，两处仅缺少空 active manifest 的精确治理标记；补入 `NO OPEN DEVELOPMENT WORK PACKAGES` 后复验 `35 passed`，失败回执保留 |
| PIT temporal / non-interference | `PASS`，`finding_codes=[]` |
| Receipt / holding replay | `6 passed` |
| `git diff --check` | PASS |
| Changed-file secret scan | `secret_hits=0` |
| tracked runtime/private 审计 | `runtime/user_state/.ai-bridge/.devbridge` 中 Git 实际只跟踪 `runtime/.gitkeep` |
| defect-first Review | 核心 orchestration / schema / Publication Gate / routing / holding ADD admission / freshness / broker boundary 审阅后无新增真实缺陷 |
| 可再生临时物清理 | 精确删除本主线 pytest basetemp `45` 个目录与一次性 helper `21` 个文件；正式 PIT request、durable evidence 与用户态未删除 |
| `uv build` | exit 0；成功生成 v0.4.0 sdist + wheel |

性能审计未发现“为了速度应减少研究覆盖”的依据。已落地优化只减少重复扫描、死路径、无用对象与不必要复杂度；不可变 receipt、ObjectStore lineage、PIT 和 Publication Gate 不因性能优化而降级。已有 profile 证据中，Full Research 纯计算案例主要约 `0.01–0.02s`；持久化 receipt/replay/tracking 主要耗时来自必要审计 I/O；50 次五候选 `tracemalloc` 运行的 Python tracked peak 约 `1.457 MiB`。

## 4. v0.4.0 发布事实

- release baseline commit：`a511d64c46076ccaaf6dbc9eabd7056c66d96369`（`release: full research recommendation v0.4.0`）。
- baseline 已成功 push 到 `origin/main` 后再创建 tag。
- annotated tag：`v0.4.0`；`git rev-list -n 1 v0.4.0` 解引用为 `a511d64c46076ccaaf6dbc9eabd7056c66d96369`，tag 不因后续 docs-only closeout 移动。
- GitHub Release：`AStockMultiAgent v0.4.0`，非 draft、非 prerelease：`https://github.com/ShiningSugar35/AStockMultiAgent/releases/tag/v0.4.0`。
- Release asset：`astock_research-0.4.0-py3-none-any.whl`，size `1,538,708` bytes，SHA-256 `78f365b1aefcab831e1fc608b909e7f97b591923e31abcab3ddd6ee6a5d68784`；GitHub asset digest 与本地逐字一致。
- Release asset：`astock_research-0.4.0.tar.gz`，size `2,497,913` bytes，SHA-256 `efa275169ea059d4fca1dbe6cba678dc0eea6966617278401615af1d756239f6`；GitHub asset digest 与本地逐字一致。
- wheel METADATA：`Version: 0.4.0`。
- 包内容泄漏审计：wheel 无 `runtime/user_state/.ai-bridge/.devbridge/.env/secrets/cookies/browser-profile`；sdist 唯一 runtime 成员为仓库正式跟踪的空占位 `runtime/.gitkeep`。

## 5. 发布后归档规则

本次 Release 之后只允许 docs / machine-index closeout 推进 `main`；`v0.4.0` tag 永久固定在上述 release baseline。归档提交必须保持源码/配置/测试不变，并在 push 后再次核验：`origin/main` 指向 closeout commit、closeout commit 是 release baseline 的后代、`v0.4.0` 仍解引用到 release baseline、工作树 clean、无 secret/private/runtime 泄漏，最后通过 durable Review 与 complete。
