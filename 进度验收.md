# AStockMultiAgent 进度验收

> 文档职责：这里只保留**最近一次任务**的有证据验收记录。历史阶段流水由 Git / Release 历史承担；仍影响当前项目理解的稳定事实必须进入 `AGENTS.md`、`docs/architecture/`、`docs/workflows/`、README 或 canonical Skill。
> 当前任务：长期运行义务续推（Phase 7/8 prospective、Phase 11/12 Agent/Skill 生产证据、Continuous Monitor 长稳/SLO、外部资格门）。
> 当前状态：本轮可立即执行的软件与运行态工作已推进；Phase 7/8 的 12 个月/样本门、Phase 11/12 的真实 SkillUsage/Catalyst/KPI、自然投资监控场景及 AKShare/认证 taxonomy 外部资格仍是长期或外部依赖义务，不得伪装完成。

## 1. 本次任务范围

- 重新读取《开发计划》与现行架构，以真实 SQLite/运行工件核对长期门，而不是沿用上一轮“代码主线清零”结论后停工。
- 推进当前可执行的 Phase 7/8 前向评估、Phase 11/12 Agent/Skill 可观测报告、Continuous Monitor 常驻运行/SLO 和外部资格复核。
- 发现真实运行缺陷时继续按 defect-first 流程修复、回归和真实 Windows smoke；不为长期门制造 fixture、假样本或任意 watch target。
- 所有本轮测试/临时文件限定在 `D:\AStockMultiAgent\runtime\longrun\` 等项目内目录；正式 `runtime/state.sqlite` 仅通过既有命令/事务协议或只读审计访问。

## 2. Phase 7/8｜前向样本与准入

- 现行 study 继续复用 `shadow-study:1a45f7ee4b35d23b545bae881c751566ef5d91f60c65aa2b7b234a22089fc9de`，6 个 arm 完整，`shadow-audit=PASS`；未建立重复 study。
- 本轮重新生成 prospective all-trials 与 statistics plan：独立研究决策、independence unit、qualifying fold 与 regime 仍为 0；`INDEPENDENCE_SAMPLE_FLOOR_NOT_REACHED` / `PURGED_FOLD_COUNT_INSUFFICIENT` 按真实样本保留。
- 2026-09-03 当前时点 `shadow-evaluate` 已实际执行，结果为 `COLLECTING / NO_SHADOW_ASSIGNMENTS`，没有用 historical/fixture 填充前向观察。
- `phase8-status` 已形成当前 admission 对象：`NOT_ELIGIBLE_INSUFFICIENT_SAMPLE`；缺口保持 **100 个独立事件、12 个月、5 个 qualifying folds、3 个 qualifying regimes**，`user_admission_status=NOT_ADMITTED`。
- adaptive weights、online learning、主 paper ledger 写入和真实券商执行均继续关闭。

## 3. Phase 11/12｜Agent / Skill 生产证据

- 30 天 Agent observability 窗口已有 2 个真实任务且均完成；4 个 Research Run 平均 wall time 约 7.06 秒，p95 约 10.22 秒；selected/completed Skill slots 为 4/4。
- 当前 `routing_labeled_task_count=0`，production `SkillUsageEvent=0`；因此只能证明执行链有少量真实时长/选择样本，不能计算生产 Skill precision/recall 或效果提升。
- canonical 双源对齐样本 12 份，其中 6 份可双源评估、5 份通过；timestamp coverage=1.0；`agent-observability-audit=PASS`。
- `research-efficiency-report` 对所有可自适应 Skill 均保持 `INSUFFICIENT_PROSPECTIVE_EVIDENCE`；自动修改、自动退休、在线调权继续禁止，`research-production-audit=PASS`。
- 只读 SQLite 复核：`skill_usage_event_index=0`、`catalyst_registry_index=0`、`catalyst_monitor_index=0`。没有真实 Catalyst/KPI/SkillUsage 证据时不造数。

## 4. Continuous Monitor 与 Operations SLO

- 开工时 daemon 为 `STOPPED`，Watch Universe、持仓、开放模拟订单和外部账户均为空；本轮没有随意 enroll 股票来冒充 `ANALYZED/RECOMMENDED/PAPER_POSITION` 自然场景。
- 0-target `continuous-monitor-cycle --live` 成功，证明空载 live 闭环可运行；随后真实 Windows 常驻启动暴露缺陷：venv launcher PID 与实际 Python lease PID 不一致，旧 `continuous-monitor-start` 因硬比较 PID 且只等待 8 秒，会在 daemon 实际成功后误报 `START_FAILED`。
- 第一层修复：start 生成唯一 `owner_id`，通过隐藏参数传给 daemon，以 SQLite durable lease 的 `owner_id + RUNNING` 做启动握手，并把冷启动窗口扩到 30 秒；launcher PID 不再作为权威 daemon 身份。
- Review 随后发现第二个 P1 长稳风险：旧 daemon 在同步 `run_cycle` 期间不 heartbeat，若真实 live cycle 超过 180 秒 lease，第二实例可能误判 stale 并抢占 singleton。现已增加 active-cycle heartbeat worker，按 `heartbeat_seconds` 在 cycle 内续租；heartbeat 写库异常或 owner 丢失都会置 fail-closed 信号，生产 `run_cycle` 与各 source handler 在 source/target、网络返回和持久化边界检查 lease。进一步的并发 Review 指出单个 handler/paper replay 仍可能在两次 guard 之间产生副作用，因此 stale heartbeat 现在还必须满足“SQLite 中记录的真实 Python daemon PID 已不存活”才允许不同 owner 接管；旧进程仍活着、PID 权限不明或 PID 复用均保守拒绝 takeover，彻底避免旧 handler 尚未退出时出现第二 daemon 并发处理。
- 回归使用 2 秒 lease / 1 秒 heartbeat 人为阻塞 cycle 超过 lease，再发起第二 owner acquire，结果明确被拒；STOPPING 期间同 owner heartbeat 仍推进；生产 `run_cycle` 与真实 news handler 回归验证失租后不写成功游标/事件/任务、不跑下一 source；新增 stale-but-live-PID 及 active-without-heartbeat 回归验证即使 heartbeat 已超 lease或缺失，只要记录的 daemon PID 仍活着，第二 owner 仍无法接管，而 stale + dead PID 的 crash recovery 继续保留。最后补齐 event→task 崩溃恢复：若旧 daemon 在 `record_event()` 成功后、`ensure_task()` 前崩溃，下一轮对同一 deterministic event 即使得到 `inserted=false` 也会幂等补齐缺失任务；Market rule 的 `last_triggered_at` 同样按已存事件的稳定 `available_at` 对账，避免重试时间漂移。CLI wiring 同时覆盖“launcher PID != lease PID”仍应 `STARTED`。最终冻结代码已由独立 Windows smoke 返回 `STARTED`：daemon owner=`continuous-monitor:f857c4f857c54fdfb7f2ad66c5044d77`、PID=36368，`broker_execution_allowed=false`；紧接着第二次 start 返回 `ALREADY_RUNNING` 且 owner/PID 不变。
- 当前自然场景仍为 0 target/0 event/0 task，所以这些空载 heartbeat 只计基础设施长稳样本，不冒充“持仓/荐股后跨日监控”、崩溃接管、worker unavailable 或恢复时长证据。
- `operations-slo-report`：evidence freshness=`PASS`，Monitor failed/partial run=0、pending/failed task=0、recovery sample=0；但当前 DB operational snapshot 有 5 个 degraded provider 记录、16 个 OPEN source breaker，故保留 `PROVIDER_DEGRADATION_PRESENT`。

### 存储水位与生命周期

- SLO 同时报告 `STORAGE_WATERMARK_EXCEEDED`：清理前 `runtime/tmp≈13.54GB`。有界 `storage-lifecycle-plan` `908738090fae4acce7c448e98e25d4f3fecd9ce50e6a121884fadbf4c2c0bc20` 仅证明 14,971 个文件 / 152,481,566 bytes 满足过期候选，且 `scan_truncated=true`；10,186 个已引用对象受到保护。
- 该 plan 的安全 audit=`PASS`，唯一 finding=`SCAN_TRUNCATED`。并发执行通道随后按同一 exact plan 显式 confirm，实际删除恰好 14,971 个候选 / 152,481,566 bytes，`skipped_file_count=0`；未扩大到未审计目录。
- 清理后 SLO `runtime/tmp≈13.40GB`，仍明显超阈。容量归因显示主要大头是历史 pytest 树，例如 `pytest-r04-main-full-r3≈2.60GB`、`r2≈2.27GB`、`pytest-r04-unit-diagnose≈1.27GB`；它们跨历史任务/会话，本轮不继续猜测性批量删除。
- 重跑 `storage-lifecycle-audit` 还暴露 receipt 幂等缺陷：`content_hash` 排除 `created_at`，同语义 audit/run 有稳定 ID，但旧裸 `INSERT` 会撞 `UNIQUE`。现改为 deterministic semantic ID + `ON CONFLICT DO NOTHING` + 逐字段严格核对；相同 receipt 幂等复用，同 ID 内容漂移则 fail closed。
- 正式 `state.sqlite` 上再次重跑同一 plan audit 已 `PASS / exit 0`，不再出现 UNIQUE 冲突。

## 5. 外部资格与安全边界

- AKShare 1.18.94 当前仍 `SHADOW`，`active_report_id=null`；冻结资格证据仍缺 endpoint-specific data rights、ToS、PIT/provenance、SBOM/安全、recorded/live、故障/缓存/退出等生产备用门，未因“继续工作”而硬升 `PRODUCTION_BACKUP`。
- 九项 E-02 candidate 继续保持现行资格边界；GDELT 继续 `DISCOVERY_ONLY`。
- 行业 taxonomy 当前仍为 `INTERNAL_RESEARCH_ARCHETYPE`，`certified_external_taxonomy=false`、`certified_portfolio_taxonomy_claim_allowed=false`；没有可许可、PIT/provenance 完整 release 时不得冒充认证行业强约束。
- Recommendation Gate、PIT、官方性、完整性、人工确认、append-only ledger 与 `broker_execution_allowed=false` 全程未放宽；本轮没有建立第二套 Router、Evidence、Monitor、Paper、用户状态或 Agent 调度事实源。

## 6. 本轮验证

- Monitor / Storage / Repo-Skill / 根文档合同最终组合矩阵：**101 passed / 0 failed**；其中最终 Monitor singleton/lease/PID/handler fencing + event→task recovery 专项为 **20 passed / 0 failed**。
- Ruff（受影响 Python/测试）：`All checks passed!`。
- Pyright（受影响 Python/测试）：`0 errors / 0 warnings / 0 informations`。
- `state-integrity-audit`：`PASS / integrity_check=ok / read_only=true`。
- 真实运行回归：最终冻结代码的 `continuous-monitor-start --live --interval-seconds 60` 返回 `STARTED`；owner=`continuous-monitor:f857c4f857c54fdfb7f2ad66c5044d77` / PID=36368 随后第二次 start 返回 `ALREADY_RUNNING`，并在 02:24:11Z、02:25:11Z、02:26:11Z 连续完成 3 个新的 0-target live cycle，至 02:26:11Z 仍为 `RUNNING` 且 heartbeat 持续推进，无第二实例。
- 真实存储审计回归：已有 receipt 的同一 plan 再次 `storage-lifecycle-audit` 返回 `PASS / exit 0`。
- 最终 Git commit SHA / `origin/main` 关系不写回本文件，避免形成自引用提交循环；由 durable run 与仓库终态核验保存，提交前仍要求 `git diff --check` 与 defect-first Review 通过。

## 7. 发布终态

- 既有正式产品 release 仍为 `v0.3.0`，release baseline/tag 不移动；本轮长期义务续推发现并修复的是 `main` 上的运行维护缺陷，不回写或重打 `v0.3.0`。
- 本轮没有为了“减少剩余项”创建虚假 patch release；tracked 修复继续遵循“defect-first Review 通过后才提交”的风险门。若后续需要形成新的正式产品发行版，再独立执行 release baseline → annotated tag/GitHub Release → 资产校验 → docs-only closeout 全流程。
- 《开发计划》继续只保留真实未完成的长期时间/样本/授权/自然场景义务；本轮已完成的软件修复不作为完成清单回填其中。
