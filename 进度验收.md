# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT / WP-25 L2 VERIFIED
> 任务：投资预期上下文、目标收益规划与公开资金隐私
> 更新日期：2026-09-15
> 执行：GPT-5.6 Sol / mcp-wjp
> 证据原则：只记录实际执行回执；中间失败、Review 缺陷与返工链不改写成 PASS。

## 1. 本次结论

WP-25 已完成实现与最终 L2 冻结树验证。所有 material investment request 在 Full Research 前都会冻结本金与目标年化：每个字段独立按“本轮用户明确值 → 同账户最近可验证的已注册请求/持仓或荐股研究记录 → 版本化模型默认值”恢复。模型 DEFAULT 不会遮蔽更早的用户明确值；其它账户、未来记录、坏对象和不可验证记录不会被冒充为用户偏好。

目标年化是规划目标，不是收益承诺。它进入同期限目标收益路径、条件入场价、组合情景收益/下行与目标缺口复核，但不会提高单股/行业/流动性/风险上限，不会开启杠杆、做空或真实券商执行。`broker_execution_allowed=false` 保持不变。

## 2. 请求冻结与历史恢复

- `InvestorOrchestrationService` 在 capability 执行前冻结小型 Full Research request contract；同一 request id 重试复用已冻结投资预期，parent/child 决策不会被之后其它会话的新偏好漂移。
- 本金与目标年化分别记录 CURRENT_USER / RECENT_HISTORY / DEFAULT 来源；当前只给一项时，另一项可独立从最近用户历史恢复。
- 历史读取覆盖已注册用户请求、冻结 request contract 与正式推荐研究；账户隔离、时间截止、对象完整性和来源身份都经过验证。
- 读取为有界只读路径：最多检查配置限定的最近 registry 窗口、读取少量相关对象并限制单对象大小；缺库不会为了“查历史”创建数据库，不遍历全部 ObjectStore，也不触发额外行情/估值采集。
- 明确外币本金不会静默当成人民币；持仓市值不会被猜成可投资本金；明确无效本金/年化不会被默认值悄悄覆盖。

## 3. 目标收益、期限与盈亏规划

- 内部 contract 增加目标年化及逐字段来源；本金继续参与私有仓位、数量和盈亏计算。
- 目标期间使用可验证的公司研究决策期限。只有标的估值期限明确且可比较时才计算目标缺口；未知或混合期限标记为不可比较，不制造伪精确年化结论。
- 情景盈亏以实际证券名义金额计算，并扣除已估算建仓成本和滑点；卖出端未知费用不会伪装成已知净收益。
- 目标收益对应的条件价作为独立研究条件，不改写基础估值本身，也不放宽既有 Committee、Publication Gate、组合 allocator 或风险门。
- 高目标只会要求更有利的赔率或保留现金，不能成为提高仓位上限和风险暴露的理由。

## 4. 公开资金隐私边界

按所有者最终要求，账户规模只在内部参与计算，公开投资者报告不披露绝对持仓/本金数值。

- Investor Mode 不输出本金、账户资产/现金余额、配置金额、持有或建议股数、账户级绝对情景盈亏。
- 公开层保留目标年化、仓位/现金权重、收益率、下行比例、目标缺口百分点，以及证券公开价格、每股估值和条件入场价格。
- 结构化投影先避免生成敏感账户金额；`ResponseGateway` / public audit 继续对草稿、旧记录和 Developer diagnostics 做金额/股数泄漏检查，安全降级不回显被拒绝的数值，也不把隐私拦截谎称成“研究资料不足”。
- 该规则只影响公开投影，不修改内部 receipt、账户事实、paper fill、估值工件或历史语义 hash。

## 5. Defect-first Review 与返工链

独立续接 Review 曾在稳定树发现外币本金被误当人民币的 P1 缺陷，随后按原工作树修复并新增负向测试。并发审查还暴露并修复了：模型 DEFAULT 遮蔽更早用户值、同 request id 目标漂移、parent/child 未复用冻结目标、未知估值期限被强行比较、场景盈亏口径错误，以及公开本金/股数/金额盈亏泄漏风险。

旧不可变 receipt 兼容性继续保留：新增可选字段缺省时不进入旧序列化/hash，新 request contract 才冻结完整投资预期。早期失败的文档合同、Ruff/Pyright 与测试均保留为返工证据，最终 PASS 只认最新冻结树结果。

## 6. 最终验证结果

| 验证门 | 最新实际结果 |
|---|---|
| WP-25 最终整合 L2 定向回归 | `256 passed in 142.77s` |
| Ruff（受影响源码与测试） | `All checks passed!` |
| Pyright（受影响源码） | `0 errors, 0 warnings, 0 informations` |
| 独立 resume 验收 | `31 passed`，稳定树 |
| 独立公开资金隐私回归 | `246 passed` |

独立验收与最终 256 项存在重叠，不能把数字机械相加。最终定向集合已经覆盖投资预期解析、历史/账户/币种/冻结边界、期限与费用、Full Research、assembly/source binding、编排、公开回答以及资金隐私。

本任务保持 **L2**：未修改 PIT/Universe denominator、正式财务事实、账户/模拟账本、数据库迁移或真实券商权限，因此按 `AGENTS.md` 风险分级没有机械运行全仓 pytest。这样减少 CPU、内存、I/O 和测试时长，同时没有削弱本次实际影响面的负向与跨模块验证。

## 7. 性能与占用

- 历史恢复有固定 registry/object 上限，不进行全库扫描；显式当前值齐全时不会进行无意义历史读取。
- 目标计算复用现有 valuation/position 数据结构，复杂度随正式候选数线性增长；未新增行情抓取、后台 daemon、第三方运行时依赖或第二套事实源。
- 所有本任务测试/审查临时输出均限定在项目目录内；终局前只删除可明确归属于 WP-25 且可再生的临时目录/脚本，不触碰 `runtime/` 正式对象、`user_state/`、其它会话文件或账户材料。

## 8. Closeout 状态

现行 `AGENTS.md`、总控投研 Skill、Portfolio Skill、全市场荐股/组合 Workflow、投资请求架构和公共输出合同已吸收最终规则；活动工作包已从 `开发计划.md` 和 `planning/work_packages_v1.yaml` 清空。终局提交前只允许执行临时文件清理、文档合同、staged diff/Secret/private/runtime 审计和最终 defect-first Review，不再改变已通过 L2 的业务语义。本页不写入自身提交 SHA；最终 commit/push、远端一致性与 clean worktree 由 durable evidence 和最终回复记录，避免形成自引用提交循环。
