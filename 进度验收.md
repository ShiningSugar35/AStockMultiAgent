# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT
> 任务：文档治理、统一投资请求编排、市场状态总控、68 个业务问题验收设计与 ChatGPT / Codex 定时投研适配研究
> 更新日期：2026-09-07
> 结论边界：本次完成的是**仓库审查、文档治理和可实施开发/验收蓝图**；没有把拟议运行时、宏观 live 数据、市场状态 V2、Scheduled Research Adapter 或 68 个端到端场景伪装成已实现。

## 1. 本次结论

### 1.1 文档模式

继续使用 Markdown 是合理的，但 Markdown 只承担意图、架构、ADR、Workflow、Skill 方法与最近验收；Schema、配置、状态机、CLI、SQLite/Parquet/ObjectStore 和测试拥有机器执行权。

此前主要问题不是“文件数量多”，而是：

- README 混入完整命令目录、阶段状态和长篇架构解释，入口职责过重；
- 1944 行总体方案虽声明不是进度，但未冻结为历史，仍容易被 Agent 误读为当前能力；
- 原开发计划只有 11 行且显示“无未完成任务”，尚未承接本轮新增的统一编排、current macro 和市场状态工作；
- Skill 中已有的自然语言前置要求没有对应代码级 receipt 与覆盖硬门；
- 规划文字与当前机器事实的边界不够显式，重要架构取舍也缺少 ADR 状态和替代链。

本次已经建立文档事实层级、生命周期和反模式；根 README、开发计划和进度验收均按新职责收敛。

### 1.2 现有系统能力

只读审查确认，仓库不是单纯的提示词集合。当前已有：

- 301 个 `src/astock` Python 文件；
- 20 个 canonical Repo Skills；
- 22 个跨 Skill Workflow；
- ObjectStore、Parquet、SQLite、PIT、Provider resilience；
- 当前公司、基本面、行业、财务审计、治理、催化剂、独立多空、投委会；
- 三市场候选链、全市场研究、正式荐股门；
- 组合风险、构建、迁移、ETF/对冲；
- 外部真实账户 append-only 事件与多账户投影；
- 模拟交易、成交、T+1 与恢复；
- Continuous Monitor 和 Investor Mode/Response Gateway。

因此下一步不应重写第二套研究系统，而应补齐统一编排、current macro、市场状态风险覆盖层和业务场景 E2E。

### 1.3 关键缺口

| 缺口 | 当前事实 | 影响 |
|---|---|---|
| 统一请求前置门 | Skill 文档已有恢复要求，但没有“无 preflight receipt 就禁止个性化投资答复”的代码硬门 | 任一自然语言问题可能漏掉实际/模拟持仓或重大变化 |
| 账户统一视图 | 外部真实账户、本地兼容投影和 paper ledger 各自存在，尚无请求级分 lane 聚合合同 | 组合/持仓回答存在漏读或混淆风险 |
| preflight 性能 | `paper-status` 单次观察 66.838 秒；CLI 与 `portfolio_nav()` 重复构建 `status()`，每次又执行全库 `PRAGMA integrity_check` | 不能机械地在每问前全账本重算，需单请求快照复用与有界健康检查 |
| 能力链证明 | 没有机器可读的 required/conditional/prohibited 策略和覆盖回执 | 单模块可用不等于整条回答完整 |
| 历史研究标的 | Continuous Monitor 只覆盖显式纳入目标，没有所有提及/研究/推荐标的 registry | 问题 90 不能可靠覆盖跨会话全集 |
| current macro | NBS/PBOC/MOF/NDRC adapter 当前 recorded-first，`live=True` 明确未实现 | 不能声称当前宏观已专业闭环 |
| 市场状态 | `market-regime-v1` 位于 shadow 链，只用趋势、宽度、波动分位和回撤阈值 | 尚不能控制正式荐股数量、总仓位和单股风险 |
| regime 验证 | 单测只覆盖阈值/优先级 | 未证明状态稳定、转折识别或下游决策增量 |
| 不完整成交事实 | exact external event 不适合 month-only/date-only 和估算成本 | 不能按 K 线静默伪造真实成交 |
| public gateway | 已有 `ResponseGateway`，但尚未证明所有投资入口都只能从该网关退出 | 仍需绕过反例测试 |
| 定时语义研究 | 本地 Continuous Monitor 已能确定性采集、排队和回放，但未与 ChatGPT / Codex Scheduled 的唤醒、语义复核、幂等通知和 missed-run 恢复闭合 | 一问一答结束后，语义层持续跟踪仍依赖人工重新发问 |

## 2. 本次交付

### 2.1 文档治理

- `docs/README.md`
  - 定义十级事实源优先级；
  - 固定 README、AGENTS、ADR、Architecture、Workflow、Skill、计划、验收和历史职责；
  - 定义 `PROPOSED / ACCEPTED / CURRENT / SHADOW / SUPERSEDED / HISTORICAL` 生命周期；
  - 规定多 Agent 唯一写入、机器证据和文档反模式。
- `docs/adr/README.md`
  - 建立 ADR 编号、状态、模板与索引。
- `docs/adr/0001-documentation-as-code-with-machine-contracts.md`
  - 接受“保留 Markdown，但机器合同拥有执行权”的决策。
- `planning/work_packages_v1.yaml`
  - 将 11 个当前工作包的 ID、顺序、依赖、状态、优先级和唯一写入 lane 做成机器索引；
  - 由合同测试与 `开发计划.md` 保持一致，并明确 `.ai-bridge` 只作瞬时交接。
- `README.md`
  - 从 333 行收敛为 121 行；
  - 删除完整命令目录、阶段历史和重复架构论证；
  - 保留永久边界、能力现状、最小启动、事实源和导航。
- `开发计划.md`
  - 从原先“无未完成开发任务”的 11 行状态页，更新为本轮新增需求的 11 个待实施工作包；
  - 明确依赖、机器验收、回滚、启用顺序和多 Agent 唯一写入边界。

### 2.2 统一投资请求编排蓝图

`docs/architecture/investment-request-orchestration-v1.md` 新增：

- `InvestorRequestEnvelope`
- `InvestorSessionPreflightReceipt`
- lane-separated `UnifiedPortfolioContext`
- `ResearchSubjectRegistry`
- `CapabilityExecutionPlan / CapabilityCoverageReceipt`
- 实际/模拟/重大事件统一前置恢复，并只读返回 latest valid market-regime availability；纯账户事实写入不依赖 current regime
- revision/hash 驱动的增量快照与 request coalescing
- 空持仓用户回复静默规则
- read-only、真实账户写入、研究元数据和模拟订单 side-effect 隔离；既存已确认订单回放单列为 `PT_REPLAY`
- month-only/date-only、provisional position 与 estimated cost 安全语义
- 失败降级、可观测性、feature flag 和回滚

### 2.3 市场状态总控蓝图

`docs/architecture/market-regime-control-v1.md` 和 ADR-0002 新增：

- Economic / Market / Portfolio 三层状态；
- `HEALTHY_BULL / SPECULATIVE_BULL / NEUTRAL_RANGE / RISK_OFF_RANGE / TREND_BEAR / PANIC / TRANSITION / UNCLASSIFIED`；
- 趋势、宽度、尾部风险、流动性、估值、盈利扩散、宏观/信用/政策特征；
- 透明 scorecard 基线与 Markov/HMM challenger；
- 概率、置信度、覆盖、滞回、冲突和 OOD；
- PIT、first-release vintage、mixed-frequency 和修订敏感性；
- `CORE_LONG_HISTORY / ENRICHED_HISTORY` 分层，覆盖 2015 等长历史极端阶段并显式处理 A 股 point-in-time Universe、退市/停牌、板块/ST/涨跌停/注册制等制度断点；
- calibration/Brier/log loss 只用于事前冻结的 observable outcome 或独立标签；无标签 HMM hidden state 不伪称已校准；
- 总风险、新仓、单股、流动性、安全边际、推荐数量和分批节奏映射；
- 健康牛市只增加已通过硬门的风险预算/推荐供给；
- 投机性高波动牛市不按健康牛市放宽；
- 复用现有 `PortfolioIntentProfile` 作为用户绝对风险上限；状态标签本身不能强制已有持仓买卖；
- feature lineage 与重复计数审计，防止同一宏观/行业利好先抬估值、再无审计放大风险预算；
- 样本外、成本后、prospective shadow、feature flag 和回滚。

### 2.4 68 个业务问题验收设计

`docs/acceptance/business-question-capability-matrix-v1.md` 已按用户原始编号和顺序逐项覆盖 68 个问题 ID，并由合同测试锁定，避免把 3/38/46–48/54/62/73–74/93 等编号误改成连续补号；每项定义：

- required capability chain；
- side-effect class；
- 核心输出和失败边界；
- actual/paper/empty-holding/PIT/idempotency 断言；
- L0–L5 测试分层；
- 场景评分卡；
- GAP-01 至 GAP-15 的缺陷/不足登记。

矩阵是下一阶段验收设计，不是“68 个问题已经通过”。

### 2.5 ChatGPT / Codex 定时投研适配蓝图

`docs/architecture/scheduled-research-orchestration-v1.md` 新增：

- 本地 Continuous Monitor 继续承担确定性采集、游标、去重、持久任务和开放模拟订单回放；
- ChatGPT / Work / Codex Scheduled 只承担定时唤醒、重大增量语义复核和通知，不建立第二套行情、账户或研究事实源；
- 区分 Desktop 本地项目、Web 云任务和既有聊天上下文复用任务的文件权限与运行条件；
- 平台任务默认采用用户在原生界面创建、项目保存 opaque binding 的路径；未重新核实稳定官方 API 前不假设代码可直接创建；
- 以 `MINIMUM / ACTIONABLE / EXPLICIT_FULL` 约束披露，默认只向语义任务提供 allowlist/脱敏后的最小上下文，不上传整库 SQLite、完整交易历史或凭据；
- 提供每小时重大变化、收盘后摘要、周度研究清单和有限期催化剂跟踪模板；
- 设计 `ScheduledResearchPolicy`、`ScheduledTaskBinding`、`ScheduledResearchRunRequest/Receipt` 和 `MaterialChangeDigest`；
- 固定幂等 bucket、lease、watermark、有界 missed-run catch-up、无重大变化预算、通知和暂停/删除回滚；
- scheduled run 的真实账户事件、模拟订单/成交和真实券商执行经济写入恒为 0；
- `开发计划.md` 增加 WP-10，并将最终 controlled-live/activation 收口顺延为 WP-11。

该蓝图是 `PROPOSED`，平台侧任务尚未创建，适配代码尚未实现。

## 3. 验证结果

本节只记录本次实际执行的审查与验证。68 个自然语言问题的统一运行时尚未实现，因此不把场景矩阵登记成端到端通过，也不登记 market regime V2 准入。

- 仓库清点脚本确认：`src/astock` 有 301 个 Python 文件、20 个 canonical Repo Skills、22 个实际跨 Skill Workflow（`docs/workflows/` 共 23 个 Markdown，含索引）。
- 源码静态核验确认：`InvestorSessionPreflightReceipt`、`UnifiedPortfolioContext`、`CapabilityCoverageReceipt`、`ResearchSubjectRegistry`、`MarketRegimeSnapshotV2` 尚不存在；NBS/PBOC/MOF/NDRC 在 `live=True` 时明确拒绝；现有 regime 仍位于 shadow service；`ResponseGateway` 存在但尚未证明所有 public entry point 强制经过它。
- 只读 CLI 均成功退出；在全仓测试并发负载下的单次观察为：`external-account-list` 13.403 秒、`local-portfolio-status` 7.770 秒、`continuous-monitor-status` 9.655 秒、`paper-status` 66.838 秒。该组数字不是正式 p95；Continuous Monitor 状态与近期运行可读，未执行交易、订单或账户写入。
- 68 个用户原始场景已完成静态登记并由合同测试锁定为 68/68；但按新增共同硬门，当前可认证为“统一 preflight + 全相关能力覆盖 + 公共网关完整通过”的数量为 0/68。该结论是结构性未认证，不等于现有领域能力为零，也不是 68 份自然语言答案的质量评分。
- 文档治理、机器工作包索引、原始需求合同、E-02 外部资格边界和 Phase 2 根文档合同最终复跑：`37 passed in 13.37s`；
- 全仓静态检查：`uv run ruff check .` 通过；`uv run pyright` 通过；
- `git diff --check`：通过；仅提示若干既有工作区文件后续由 Git 触碰时会从 CRLF 规范化为 LF，无空白错误；
- 对本轮 14 份变更/新增 Markdown 执行本地链接核验：`missing=0`；
- 外部账户、本地持仓、模拟交易、持续监控、组合分析/决策、宏观权威源、shadow regime、展示网关和研究团队现状回归：`307 passed in 233.94s`；这证明既有能力基线未被文档重构破坏，不代表本轮拟议运行时已经实现；
- E-02 资格证据绑定的测试文件已恢复 canonical 内容，避免为迁就新计划而静默重签既有证据；对应单文件复核 `14 passed in 12.06s`；
- 并发编辑期间启动的全仓测试得到 `1561 passed, 18 skipped, 8 failed`，因运行过程中根文档和合同测试仍在变化，不作为终局证据。稳定后与本轮文档相关的 7 项旧快照失败均由上述 37 项定向测试覆盖消除；唯一独立的 BaoStock heartbeat 时序失败由两个独立进程复跑为 `1 passed in 3.62s` 与 `1 passed in 4.66s`，当前只能登记为重负载下的偶发观察，不能据成功复跑宣称根因已修复。
- 文档治理、市场状态和定时任务方法依据已对照官方或原始来源复核，包括 AWS ADR、NIST AI RMF、OpenAI Agents SDK/ChatGPT Scheduled/Work 与 Codex、Hamilton Markov regime、IMF Growth-at-Risk、Chicago Fed NFCI、NBS PMI 方法和 volatility-managed portfolio 研究。

本任务以 L0 文档/合同设计和只读架构核验为主；未在文档稳定后机械重跑耗时约 26 分钟的第二次全仓测试，最终证据由当前定向合同测试、307 项代表性底座回归、静态检查和 Git 差异审查共同组成。

## 4. 尚未实现或未准入

以下保持明确未完成：

- `InvestorSessionPreflightService` 和请求级 receipt；
- `UnifiedPortfolioContext` 增量缓存；
- machine-readable 68 场景 manifest；
- `CapabilityPolicyPlanner / Executor / CoverageReceipt`；
- `ResearchSubjectRegistry`；
- provisional position / estimated cost Schema；
- official current macro live capture；
- `MarketRegimeSnapshotV2`、probability/confidence/hysteresis；
- `RegimeDecisionPolicy` 与组合/荐股联动；
- 68 个 recorded E2E；
- representative controlled-live；
- regime prospective shadow 和正式启用；
- ChatGPT / Codex Scheduled Research Adapter、平台 binding、幂等运行与通知恢复；
- 对所有 public entry point 的 gateway 强制收口证明。

任何普通投资回答在这些能力实现前，都不能声称已经自动完成上述统一编排。

## 5. 下一步入口

下一会话应从 `开发计划.md` 的 WP-01 开始，优先完成机器合同与场景 manifest，再并行推进增量 preflight、subject registry、能力 planner 和 official current macro。不得先用提示词把回答“写得像完整”，再补运行时证据。
