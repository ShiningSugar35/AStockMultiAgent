# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT
> 任务：News Window Efficiency / WP-21
> 更新日期：2026-09-13
> 执行：GPT-5.6 Sol / mcp-wjp
> 证据原则：只记录本次任务已经实际取得的回执；历史 v0.4.0 发布事实仍由 Git tag / GitHub Release / durable run 保留，不在本文件重复累积。

## 1. 本次结论

Full Research 的强制新闻/事件回看窗口已经从 `7/30/90/180` 天收敛为 `7/30/90` 天。此次调整只减少每次当前荐股任务对 91–180 天旧新闻的重复检索与事件覆盖要求，不改变新闻类别、Evidence、PIT/source lineage、冲突解决、财报/公告/公司行动、基本面历史、行业研究、估值、CriticalVeto、Mandatory Research DAG 或 Publication Gate。

180 天前的重大事实没有从系统事实层删除：正式公告、财报、公司行动及长期公司研究仍可携带这些历史事实；变化仅是“180 天新闻面”不再作为每一轮 Full Research recommendation 的 mandatory current-event window。

## 2. 实现与合同迁移

- `configs/full_research_recommendation.yaml` 的 `news_windows_days` 由 `[7, 30, 90, 180]` 改为 `[7, 30, 90]`。
- `NewsEventCoverage` 的硬校验同步要求精确覆盖 `7/30/90`，避免配置与 Schema 漂移。
- `NewsEventResearchPack.recommendation_ready()` 默认窗口同步为 `(7, 30, 90)`；Research Team 继续直接读取同一 Full Research policy，不建立第二套窗口配置。
- 最终候选没有事件时的公共叙述由“四窗口事件研究”改为“三窗口事件研究”。
- 单测 fixture 与负例合同同步迁移；只有精确 `7/30/90` 满足窗口合同，旧 `7/30/90/180` 明确 fail closed。

## 3. 验证结果

| 验证门 | 实际结果 |
|---|---|
| Full Research + Research Team targeted regression | `54 passed in 48.49s`，exit 0 |
| 受影响 Ruff | `All checks passed!` |
| 受影响 Pyright | `0 errors, 0 warnings, 0 informations` |
| 旧四窗口负例 | 旧 `(7, 30, 90, 180)` coverage 明确被新 Schema 拒绝；专项合同 `1 passed in 3.43s` |
| Docs / Workflow / Skill governance | `35 passed in 3.59s`；验收记录更新后最终复验仍为 `35 passed` |
| `git diff --check` | PASS |
| 临时物清理 | 本任务 4 个 `runtime/longrun/pytest-news-window-*` basetemp 已删除 |
| 旧新闻窗口残留检查 | Full Research policy 无 `180`；`src/astock` 无 `7/30/90/180` 与“四窗口”公共合同残留 |

## 4. Review 与性能边界

Defect-first Review 未发现需要回退的缺陷。三窗口仍是正式推荐的硬门；新闻类别、冲突解决、Evidence/PIT 完整性和 Publication Gate 均保持原约束。

本次属于 L1 低风险策略合同调整。主要效率收益位于新闻源网络/I/O、91–180 天旧事件去重、分类和证据冻结；不通过削弱财务、治理、Universe、估值或正式发布门来换取速度。如果后续真实使用发现半年级旧事件对 current recommendation 有稳定增益，优先考虑低频历史事件摘要/缓存，而不是重新让每次请求强制扫描 180 天。
