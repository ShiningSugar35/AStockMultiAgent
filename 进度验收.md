# AStockMultiAgent 进度验收

> 状态：LATEST VERIFIED SNAPSHOT
> 任务：接管既有未提交工作，完成 WP-01→WP-11 修复、稳定树验收与发布收口
> 更新日期：2026-09-09
> 最终集成执行者：GPT-5.6 Sol / mcp-wjp；保留并接收此前并发 mcp-wjp 会话的已验证未提交成果；durable run `lr_mtqys4vy_81c26a9e8c93`
> 证据原则：只记录实际可读回执；不同测试批次不相加冒充业务覆盖。recorded/smoke/DEGRADED 不冒充 controlled-live/prospective；外部启用门不造样本。

## 1. 本次结论：软件实现

WP-01→WP-11 的工程缺口已完成收口，active manifest 已清空。统一请求 preflight、actual/paper 分 lane、跨会话研究标的、Capability Planner/Executor、官方 current macro、Market Regime 风险覆盖、Investor Answer Gateway、68 业务场景、三域定时研究、语义唯一 owner、通知 outbox/恢复、启用评估与 rollback 均已有机器合同和真实服务接缝。

本结论不改变永久安全边界：`broker_execution_allowed=false`；真实账户不自动交易；模拟账户只有既有准备、独立确认与机械回放可改变经济事实；PIT、来源、对象 hash、请求/实体/账户绑定和正式准入继续 fail closed。

## 2. 验证结果：最终实现快照上的稳定证据

文档状态迁移前的最终实现源码指纹为 `f4e74e9e7a18a87d30c38a8e2453d309691fe22e66ca64a8dfe43e9a6db5fc85`。以下运行均 `source_tree_stable=true`：

| 验证门 | 结果 | 不可变质量证据 |
|---|---|---|
| 68 业务问题真实领域 E2E | 70 passed / 419.81s；68 个原始业务 ID 全覆盖，另含辅助安全断言 | `.ai-bridge/quality-runs/20260909T093517Z-0487e2f3/` |
| WP-11 activation / source coverage / scheduled semantic | 42 passed / 243.55s | `.ai-bridge/quality-runs/20260909T093441Z-c90bc388/` |
| WP-10 source preparation + controlled classification + semantic single-owner | 8 passed / 66.95s | `.ai-bridge/quality-runs/20260909T093351Z-4f773ce5/` |
| Ruff 全仓 | All checks passed | `.ai-bridge/quality-runs/20260909T093426Z-ddfcc88d/` |
| Pyright 全仓 | 0 errors / 0 warnings / 0 informations | `.ai-bridge/quality-runs/20260909T093535Z-455f8584/` |
| 双规模 preflight 性能 | PASS；small warm p95 28.65ms / concurrent p95 109.41ms；scaled warm p95 41.43ms / concurrent p95 181.38ms；scaled cold p95 131.04ms | `.ai-bridge/quality-runs/20260909T094309Z-1b10987e/` |

性能基准为每规模 10 次冷请求、100 次暖请求、100 次四线程请求并含 revision 失效检查；全部远低于 2 秒单请求门，且核验缓存命中、账户隔离与经济表零意外变化。

## 3. Controlled-live 与启用边界

真实受控检查没有被包装成虚假全绿：

- NBS/PBOC official current macro fresh live 抓取可以形成 raw-first capture receipt；本轮受控运行 `CURRENT_MACRO=PASS`。
- scheduled source preparation 遇到 GDELT schema-less HTTP 200 body `{}` 以及后续 HTTP 429 时，系统将其视为来源失败/限流，不把 `{}` 推断成“没有新闻”。对应 bucket 保持 `DEGRADED/BLOCKED`，`economic_write_count=0`，`feature_activation_changed=false`，可在后续运行重试。
- recorded 已准备好的定时结果单独标为 `SCHEDULED_RESEARCH_RECORDED=PASS`；只有同一运行中存在匹配 live source preparation、认证 coverage 和 fresh final run 时才允许 `SCHEDULED_RESEARCH=PASS`。
- rollback drill 实际得到 `ROLLED_BACK`、rollback ledger write count=0、全部相关 feature flag=false、经济表不变、`broker_execution_allowed=false`。

`configs/investor_orchestration_activation_v1.yaml` 的 accepted evidence 仍不会因测试自动增长。真实 prospective shadow 数量/持续天数、current macro controlled-live、scheduled research controlled-live 与 owner approval 是**运行启用门**，不是尚未实现的软件工作包；未满足时继续 disabled/SHADOW，禁止伪造 100 条、20 天或批准记录。

## 4. 发布门与证据冻结规则

本文件在最终 tracked 文档状态迁移时冻结。随后必须在该最终树上重新执行文档治理、Ruff/Pyright、必要性能确认与**全仓 pytest**；只有 child exit=0 且 `source_tree_stable=true` 才允许 commit/push。之后还必须执行 `git diff --check`、staged diff、secret/private/运行时文件审查，并核对远端 HEAD。

最终全仓 quality receipt 与 Git commit/push 结果不再回写本文件：测试或提交完成后再次修改 tracked 文档会改变刚验收的源码指纹，形成自指失效。发布事实以 `.ai-bridge/quality-runs/<run_id>/result.json` 和 Git 对象/远端引用为准。
