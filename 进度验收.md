# AStockMultiAgent 进度验收

> 状态：v0.4.0 RELEASE CANDIDATE
> 主线：`full-research-recommendation-v1`
> LATEST VERIFIED SNAPSHOT
> 任务：Full-Research Recommendation P0 / WP-20 Release Review
> 更新日期：2026-09-12
> 执行：GPT-5.6 Sol / mcp-wjp；durable run `lr_mtw92jn9_aabe249465fb`
> 证据原则：只记录已实际取得的回执；测试不冒充 live 市场结果，upstream readiness 不冒充最终荐股权。

## 本次结论

WP-12→WP-19 已完成并从 active manifest 移出；当前只剩 WP-20 Release Review。所有直接形成证券选择、买卖判断、价格、数量或仓位的请求统一归一为 `FULL_RESEARCH_RECOMMENDATION`。旧 `BUY_DECISION / HOLDING_DECISION / PORTFOLIO_DECISION / RECOMMENDATION` 不再是现行 RequestIntent、Planner、Executor、公共答案或发布路径；历史输入名称只允许在 request construction 时迁移到 Full Research。

最终公共投资建议的唯一权威是不可变 `RecommendationResearchReceipt + Publication Gate`。`FullResearchInputReadinessReport` 只证明上游研究输入完整，不具有正式荐股权。Mandatory Research DAG、PIT/source lineage、Financial/Governance CriticalVeto、多模型估值、因子/候选排名、组合/风险/执行、行情 freshness、独立 Challenger、receipt replay 与 tracking/re-evaluation 已绑定为同一闭环。

账户边界保持：`broker_execution_allowed=false`；actual/paper 分 lane；`EA_PROVISIONAL` 只允许冻结不完整持仓声明，真实 `EA_WRITE` 必须独立走 `ACCOUNT_FACT_WRITE`。系统允许零 BUY、少于目标数量和现金持有；无新鲜行情不生成即时成交数量；后续重评只追加 revision，不修改历史 receipt。

## 2. 本轮 defect-first 修复

1. **官方证据 authority 漂移**：Full Research source manifest 原先仅按 `document_id` 字符串猜 authority，导致已经 typed 为 `EvidenceGrade.PRIMARY_OFFICIAL` 的 CNINFO/正式披露被误降为媒体，PIT critical source coverage 从 1.0 错降到 0.8。现改为 typed evidence grade 决定 official authority，document id 只区分 issuer/交易所类别；原稳定失败 scenario 36 随后 1/1 通过。
2. **旧推荐权多中心**：Full-market `RecommendationReadiness*` 已改为 `FullResearchInputReadiness*`，旧上游 `formal_recommendation_allowed` 字段/CLI/checkpoint/文档语义删除。源码中的正式荐股许可只存在最终 `PublicationDecision`。
3. **持仓声明与真实账户写入混淆**：Full Research 只允许 `EA_PROVISIONAL` 作为显式 required 的临时持仓输入；`EA_WRITE` 仍 fail closed 并要求独立 `ACCOUNT_FACT_WRITE`。scenario 13 与两条权限单测 3/3 通过。
4. **旧轻量设计残留**：RequestIntent、Planner、公共投影、Presentation Task、AGENTS、Research Team Workflow、总控/candidate/portfolio Skills 和 CURRENT 架构文档均收敛到 Full Research；未保留旧轻量荐股作为并行现行路径。
5. **配置状态漂移**：`full_research_recommendation.yaml` 已由 `IMPLEMENTED_DISABLED` 改为 `ENABLED`，policy loader 会拒绝非 ENABLED 配置；无消费方的重复 `intent_router` YAML 已删除，路由只保留 canonical request matcher。
6. **持仓 Full Research 终局动作丢失**：Planner/DomainContract 原先会要求并验证 `HOLDING_REVIEW`，但 final assembler/receipt/public projection 未消费该工件，导致“无新增 BUY、但现有持仓应 TRIM/EXIT”可能退化为“保留现金 + 空动作”。现由 canonical dependency/语义路由共同冻结 `EXISTING_HOLDING`，`HOLDING_REVIEW` 成为最终 gate 必需依赖；`HoldingReviewPack` 被投影为绑定 monitoring plan、preflight portfolio revision、artifact/hash 和 Evidence lineage 的不可变 holding snapshot，最终 receipt/公共答复在零新增 BUY 时仍保留 HOLD/ADD/TRIM/EXIT/REVIEW。专项功能回归 34/34、assembler lineage 回归 1/1、受影响完整回归 183/183 通过。
7. **持仓 ADD 可绕过候选准入**：上游 HoldingReview 可能建议 ADD，而 Full Research candidate 已因估值/财务/治理/证据被判 ineligible；现由 receipt 交叉校验禁止任何 ineligible 标的保留 ADD，TRIM/EXIT/HOLD/REVIEW 仍可表达已有持仓处置。负例回归通过。
8. **持仓路由关键词过宽**：裸 `持有` / `多少股` 会把“某股东持有多少股份”之类所有权事实查询误路由为投资决策。现改为组合语义：现有持仓信号必须与处置动作相连，具体股数必须与买入/配置/下单语义相连；“买多少股”仍强制 Full Research，股东持股事实保持非决策研究。正反路由回归通过。

## 验证结果

| 验证门 | 实际结果 |
|---|---|
| Full Research + orchestration + Research Team + Domain Contract + Closure + rendering current-code 核心回归 | `115 passed`，exit 0 |
| 业务场景 E2E | `70 passed`，exit 0；覆盖 recorded read/economic side-effect 场景，scenario 13/36 已纳入 |
| Full Research 专项 profile | `28 passed`；纯计算案例主要为约 `0.01–0.02s`，持久化 receipt/replay/tracking 约 `1.36s`，主要耗时来自必要审计 I/O |
| 内存 profile | 50 次、每次 5 候选，在 `tracemalloc` 开启时约 `167.746ms/次`；Python tracked current `0.530 MiB`、peak `1.457 MiB` |
| 当前全仓 Ruff 中间门 | `All checks passed`；最终冻结树仍需重跑 |
| 当前全仓 Pyright 中间门 | `0 errors / 0 warnings`；最终冻结树仍需重跑 |
| Portfolio 复杂度优化 | `build_portfolio` 已拆成 position/factor/constraint helper 并退出 C901 超限列表；未用 `noqa` 掩盖 |

性能审计没有发现“为了速度应减少研究覆盖”的依据。已落地的优化只删除重复扫描、死代码、无用对象和无效参数，并将 bounded shortlist/组合计算拆分复用；不可变 receipt、ObjectStore lineage 和 Publication Gate 不因性能优化而降级。

## 4. v0.4.0 冻结树待执行发布门

### 4.1 断网前 interrupt-safe checkpoint（2026-09-11）

当前 durable run=`lr_mtw92jn9_aabe249465fb`，step=`s5`，状态仍为 in-progress；没有 quality PASS review，也没有 release baseline commit/tag/Release，禁止视为已发布。

断网前已取得且可继续引用的事实：业务场景 E2E `70 passed`；PIT temporal/non-interference 实例审计 `PASS` 且 `finding_codes=[]`；receipt 持久化/离线 replay/防篡改/重评不可变性专项 `3 passed`；文档/Repo Skill 合同在修复 `RECOMMENDED` 跟踪事件契约后 `14 passed`；secret 扫描对当时 43 个 changed tracked files 为 `secret_hits=0`；Git 只跟踪 `runtime/.gitkeep`，`user_state/.ai-bridge` 未进入 Git；GitHub CLI 已鉴权，当前 release 序列最新 tag 为 `v0.3.0`，本轮 package version 已更新为 `0.4.0`。

复杂度 Review 已完成本轮新增热点收敛：`verify_receipt` 已拆为 identity/DAG-lineage/valuation/portfolio/publication helpers；`_full_research_decision` 已拆为 receipt/index、position reasons/risks/actions、portfolio summary helpers；`FullResearchReceiptAssembler.assemble` 的候选投影已拆为 candidate outputs/rejection/ranking helpers。最新 C901 仅剩历史 `inputs=12` 与 `_paper_status=13`，本轮新增热点已全部退出超限清单；对应 Pyright 为 `0 errors / 0 warnings`，Full Research + rendering + orchestration + domain-contract 专项为 `93 passed`。**全仓 Ruff/Pyright/pytest 仍需在此冻结树重新运行后才能作为最终发布证据。**

当前恢复工作固定从《开发计划》3.1 的顺序继续：直接在此冻结树重跑 L3 全门；全部通过后才允许 defect-first Review 与 v0.4.0 release baseline 发布。

以下项目尚未在本条记录时点宣称 PASS：

- 同一冻结树的 Ruff 全仓、Pyright 全仓；
- PIT temporal/non-interference、receipt replay/Publication bypass 显式门；
- 文档/Workflow/Skill 合同与 `git diff --check`；
- 全仓 pytest；
- secret/private/runtime/项目外输出审计；
- defect-first independent Review；
- release baseline commit/push、annotated `v0.4.0` tag、GitHub Release 与构建产物 SHA-256。

只有这些门真实完成后才能把本文件迁移为 `v0.4.0 RELEASED`。发布后仅允许 docs/machine-index closeout；若产生 docs-only commit，必须继续 push，但不得移动 `v0.4.0` tag。
