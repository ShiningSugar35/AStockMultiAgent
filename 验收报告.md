# AStockMultiAgent 当前验收报告

- Knowledge source anchor present: MR Dang, 黄彦臻, 派大星皮皮, 寒武纪的鳄鱼.

本报告只记录已实现且有可复核证据的当前事实；未完成项只写入《开发计划》。被替代的实现叙述不在
现行文档保留，由 Git 历史追溯。当前工作树包含并行任务和未提交改动，因此以下测试与运行结果均为
候选基线；只有对应提交、干净工作树和一致工件身份同时成立时，才转为正式基线。

## 当前能力

| 能力 | 当前状态 | 边界 |
|---|---|---|
| ObjectStore / Parquet / SQLite / migration | 已实现 | 原始对象不可覆盖；分析事实与元数据分离 |
| 行情、reference、财务来源 | 已实现基础链 | 免费源有覆盖和可用性边界；二级财务值只作线索 |
| 研究、专家、诊断、候选、委员会 | 程序已实现 | 只读冻结证据；缺证据返回缺口 |
| 模拟账本与 5m 回放 | 核心已实现 | 无公开自动下单能力，不连接券商执行 |
| 白名单知识采集 | 部分覆盖 | 当前范围为回答、文章、专栏正文和想法 |
| Paragraph → Argument 语义链 | 已实现并真实运行 | Paragraph 只作存储/辅助视图，AU 是最终判断单位 |
| 本地 BGE 三视图 | 已实现并固定模型资产 | 未校准低相似度不得自动删除 |
| 《价值投资功法》位图/图表 | 全量定位、OCR 和 AU 映射已完成 | 11 个视觉 AU READY，44 个仍需复核 |
| OpenCode/DeepSeek 交接 | 本地书批次已就绪 | 知乎须先冻结图片证据并重建受影响 AU |
| Phase 6 | 程序闭环完成 / recorded 验收通过 | 300750 受控 fixture 已到签名人工确认和单一模拟订单；不是当前公司研究 |
| Phase 7 | 程序已实现 / `COLLECTING` | 真实独立前向事件 0/100；当前本地 runtime 因 0044 checksum 漂移只读 |
| Phase 8 | `NOT_ENTERED_BY_DESIGN` | 自适应权重、在线学习和券商执行均关闭 |

## Phase 6 投资决策闭环

`TASK-PHASE6-FINAL-CLOSE` 已形成以下确定性程序链：

`ResearchRequest → EvidenceCollectionTask → EvidenceCollectionRun → EvidencePack →`
`FrozenEvidencePack → BaseCase → SpecialistRoute → Serenity Delta / 知乎专家 Delta →`
`ResearchMemo → Investment Committee → TradeProtocol → PaperExecutionRequest →`
`独立签名人工确认 → Paper Ledger`

- 委员会投资入口强制绑定 `BASE_CASE`、`SERENITY_DELTA`、`ZHIHU_EXPERT_DELTA` 和
  `FINANCIAL_INTEGRITY` 四个角色。每个角色引用已注册冻结工件及其精确对象哈希；委员会仍保持
  network、Browser、MCP、API 和 broker 全关闭。
- TradeProtocol 的公共结果固定为 `WATCH / REJECT / NEEDS_INFO / APPROVE_SIMULATION`。
  `WATCH`、`REJECT` 和 `NEEDS_INFO` 均为 `BLOCKED`；只有 `APPROVE_SIMULATION` 可以进入
  paper preparation。`broker_execution_allowed` 在 schema、SQLite 和服务层均固定为 `false`。
- 模拟执行不从委员会推导 BUY/SELL、数量或限价；调用者必须显式提供这些参数。prepare 阶段只写
  不可变请求，不写账本。人工确认须绑定 operation/request/account/type、有效期、nonce 和验签
  key；确认对象及公钥对象均冻结到 ObjectStore，审计可以重新验签。
- `account_id + idempotency_key` 绑定请求的协议、reference、时间窗、方向、数量和价格；重复请求和
  重复确认只复用原结果。若账本已提交而外层 checkpoint 中断，recovery 会根据已完成 operation
  和订单绑定对齐状态，不创建第二张订单。
- 旧版 TradeProtocol 保存在 legacy 索引，不会被 migration 静默解释为新授权；新协议身份带
  `paper-only-confirmed-v2` 合同版本。

300750 recorded case 的最终冻结身份为：

| 工件 | 身份 / 结果 |
|---|---|
| Phase 6 run | `dd9c81fd959bab5bd93ea7b784a8d2cf32702291cf96a329ec9a65d70d8d2377` |
| ResearchMemo | `research-memo:fb807504c03e9c4166c126df94f502f9def26d102fd3b4f0e6df5c2d24a708f4` |
| CommitteeDecision | `decision:73785b71129ff05b2fbece892721e7c3a0c3a3a8126f3bdb118cde4eee28f0ae` |
| TradeProtocol | `trade-protocol:138aa138879ec1b197c969b054418ad234bc70feaec2fedecc943f1af4162f6a` / `APPROVE_SIMULATION` |
| PaperExecutionRequest | `1b61d5460c78528f0a7670c04cbab1b9a9937a0105e0f80fcfb75389a9d902b5` |
| User confirmation | `cf8b46906dfdd0feb810b7b32bcf9c3ea5bccbae4850dcfb8ad049e8ed165ca9` |
| Paper order | `7246216b7e53454e96ecf0d95e053b82` |
| Phase 6 / execution audit | `PASS / PASS`，finding 均为空，SQLite `ok` |

端到端 acceptance 为 `1 passed in 19.44s`；Phase 6、committee、paper、migration 和开源适配
定向回归为 `63 passed`。完整链路日志位于
`docs/evidence/phase6_recorded_300750.log`。

上述 case 明确标记为 `RECORDED_ACCEPTANCE`，不是当前 300750 公司研究、投资建议或实盘授权。
recorded 知乎专家 Delta 只用于软件闭环；知乎图片证据冻结前，纯文本 v3 包继续保持
`PROVISIONAL_TEXT_VIEW`。通用 live paper classification 缺精确工件时继续 fail closed。

## Phase 7 真实前向影子研究

截至 `2026-07-27T07:34:01.694712Z`，由 SQLite/ObjectStore 事实生成的
`phase7_status.md` 报告：

| 项目 | 当前事实 |
|---|---:|
| 正式独立前向研究事件 | 0 / 100 |
| 距最低评估门槛 | 100 |
| 成熟最终期限未来事件 | 0 |
| 已存 assignment / observation | 0 / 0 |
| Skill / Committee / Research Quality | `COLLECTING` / `COLLECTING` / `COLLECTING` |

程序闭环已经实现，但没有伪造运行证据：

- 正式事件固定为 `ResearchMemo → ShadowDecision → Future Observation`。完整委员会臂必须携带
  `ResearchMemoArtifact`、`DecisionPack` 和 `TradeProtocol`，Decision 的冻结输入哈希必须精确
  包含对应 Memo 对象哈希。
- 正式 study 和 assignment 保存系统接收时间及前瞻资格；信号后超过策略登记窗口的事后 assignment
  被拒绝。旧数据和回填数据默认不具备前瞻资格。
- 正式观察只接受信号后真实采集的行情源快照。观察冻结 assignment 身份、标的/指数、未复权 5m
  范围、`SourceSnapshot`、逐条行情 observation 与系统可得时间；ObjectStore 内容哈希、SQLite
  索引和来源对象均可审计。价格、成交量、持有期最高/最低价及 MFE/MAE 必须与冻结 OHLCV 明细
  复算一致。
- 每个观察版本保存当时观点、信号/入场/估值时间、价格、净表现、观点是否仍有效，以及结构化失效
  原因。修订保留旧版本，不覆盖历史事实。
- 评估分别输出 Skill 表现、Committee 表现和 Research Quality。未达到 100 个真实独立事件时，
  三者由 schema 和服务层共同锁定为 `COLLECTING`。
- 同一 `ResearchMemo` 或 `ShadowDecision` 只能形成一个正式独立事件；服务校验与 SQLite 部分
  唯一索引共同阻止仅更换 event ID 的重复计数。
- 历史回放和回测只能显式标记为 retrospective，不能替代真实未来行情或增加正式计数。强化学习、
  动态/在线调权、自动修改 Skill、券商执行和自动实盘均关闭。

稳定操作入口包括 `shadow-study-create`、`shadow-assign`、`shadow-forward-market-freeze`、
`shadow-observation-record`、`shadow-evaluate`、`shadow-status`、`phase7-status-update` 和
`shadow-audit`。

当前本地 `runtime/state.sqlite` 的迁移完整性为 `BLOCKED_CHECKSUM_MISMATCH`：它在开发过程中执行过
一个早期 0044，随后并行 Phase 6 工作修改了同名迁移。系统没有改写 SQLite checksum，也没有跳过
门禁；`phase7-status-update` 以显式 `READ_ONLY` 模式生成上述真实计数，其他需要迁移的写入口继续
fail closed。全新临时数据库上的 0001→0046 链由测试覆盖；本地 runtime 对账仍列在《开发计划》。

## Phase 5 采集覆盖

最新冻结覆盖审计时间为 `2026-07-22T08:42:57Z`，冻结截点为
`2026-07-22T08:42:27Z`，状态 `PARTIAL`。

| 作者/类型 | 目录唯一项 | 详情已验证 |
|---|---:|---:|
| Mr. Dang 回答 / 文章 / 想法 | 118 / 16 / 0 | 118 / 16 / 0 |
| 黄彦臻回答 / 文章 / 想法 | 831 / 8 / 1 | 831 / 8 / 1 |
| 派大星皮皮回答 / 文章 / 想法 | 895 / 24 / 1,896 | 895 / 24 / 1,895 |
| 合计 | 3,789 | 3,788 |

开放 gap 9、待消费导入 0、对象缺失 0、Parquet 错配 0。寒武纪的鳄鱼本地导出状态为
`USER_CONFIRMED_COMPLETE_EXPORT`。专栏容器合同已实现；真实无凭据枚举快照尚未取得，状态
`BLOCKED_EXTERNAL_OBSERVATION`。

## 论证级语义链

- `ParagraphUnit` 保存来源、顺序、页码或 DOM locator、文本对象哈希、修辞角色、上下文依赖和
  merge action；它不是最终筛选或蒸馏单位。
- `ArgumentUnit` 只组合同一 SourceItem 内连续 Paragraph，将设问、回答、主张、解释、证据、
  案例、反方和结论组成可复核的完整论证，并保存 `ArgumentRelation`。
- active pipeline 为 `knowledge-semantic-funnel-three-view-v3`。局部上下文为前 1＋当前＋后 2；
  active Embedding 合同为 `PARAGRAPH_AUX_ARGUMENT_FINAL_V3`。
- 每个 Paragraph 生成 current 与 local-context 辅助向量；每个可判定 AU 生成完整 AU 向量；
  14 类方法原型只用于相似度参照。最终 `topic_relevance`、`methodological_completeness`、包选择
  和候选只能来自完整 AU。
- 14 类方法不设每类数量上限；达到探索保留阈值的类别全部保留。无 calibration manifest 时，
  低相似度只标记 `CALIBRATION_REQUIRED`，不得自动删除。
- DeepSeek 输入为 `semantic-deepseek-input-manifest-v3` 和
  `COMPLETE_ARGUMENT_UNIT_V2`。每行携带完整 AU、内部 Paragraph 角色/关系和来源；结果导入逐层
  复验 run、Embedding manifest、Parquet、packet hash、行数和证据引用。

三位作者的纯文本三视图运行已经完成，但图片仍未形成冻结证据，因此只作为
`PROVISIONAL_TEXT_VIEW`：

| 作者 | semantic run | 向量 / AU 分数 | 导出 / 结构留置 / 超长留置 |
|---|---|---:|---:|
| Mr. Dang | `knowledge-semantic-run:bfc6c8981f5293c0417b895d43f4ae25db9a3cef5b63495c8a4072153e8b10d7` | 15,172 / 278 | 43 / 235 / 68 |
| 黄彦臻 | `knowledge-semantic-run:5da9ae360128b7346797fc217aed2029b475688fed62e65ccb5cb094b86c68d9` | 8,100 / 628 | 136 / 492 / 2 |
| 派大星皮皮 | `knowledge-semantic-run:a93acd607ffaf01f0d9c7678ee1902971a8d4d7002b8102345b4b04a8898f434` | 53,930 / 4,426 | 1,126 / 3,300 / 35 |

合计 77,202 个向量、5,332 个 AU 分数和 1,305 个纯文本 packet。没有自动外发，没有 DeepSeek
结果、导入候选或人工批准。

## 《价值投资功法》视觉证据链

视觉状态机为：

`INPUT_FROZEN → LAYOUT_ENUMERATED → OCR_COMPLETED → CHARTS_CLASSIFIED →
SEMANTIC_MATERIALIZED → AUDITED`

- PDF 从 ObjectStore 的冻结原始对象读取。PyMuPDF 按 placement 枚举并保存页码、bbox、xref 和
  ordinal；xref 提取失败时使用 300 DPI bbox clip。每个 placement 都运行本地 RapidOCR。
- 原 PDF、提取图片和 OCR 文本先写 ObjectStore；SQLite 只保存 hash、locator、状态、版本和
  置信度。恢复会复验既有对象和 lineage，不覆盖原对象或重复执行已完成 OCR。
- visual run：
  `book-visual-run:e94590494783714a9f1743451ea91cbe778289ddd043c4db232c1f866bb66bf9`；
  coverage：
  `book-visual-coverage:73ba24ac531aaee10a2ed383fdaa83302bd7b500a0765692263ad007bf6922f2`；
  coverage object hash：
  `68de7444b58d66567456a05df829feb0111f9cb4c2766542b7035418094566e6`。
- 249 页中 57 页含图，共 74/74 placements。OCR 失败 0、低置信 0、无文字 2、重复内容 1；
  分类为 TABLE 35、CHART 14、TEXT_IMAGE 22、DECORATIVE 3、DIAGRAM 0、UNKNOWN 0。
- 71 个非装饰图全部具有 `ImageEvidence / ChartUnit → ParagraphUnit → ArgumentUnit` lineage，
  映射到 55 个 AU；`image_only_ready_candidate_count=0`。
- 视觉 Paragraph 固定 `standalone_distillable=false / MERGE_WITH_BOTH`。图表夹在论点和结论
  之间时，按 PDF 顺序将“论点＋OCR/图表证据＋结论”合并到同一 AU，不能从图片或 OCR 文本孤立
  生成 Skill。
- visual semantic run：
  `semantic-run:e0605aa01c8ddc0dffe69a81d88389d546e52b39aa89d19fc8a2012835e06ea5`；
  Embedding manifest：
  `semantic-embedding:7d9a87f5b6ce8757ddaf1a3147c122b3ca9ce238e06d319ff59dd9d48b6a52fe`；
  已写入 6,819 个三视图向量和 311 个 AU 分数。
- 55 个视觉相关 AU 中 11 个 READY、44 个 `NEEDS_REVIEW`。主要边界是跨页上下文、
  无方法关键词视觉复核和 1 个 OCR 无文字；原因可以重叠。
- READY batch：
  `semantic-llm-batch:af5d59de3bd985232b0c8c7da0a9e5fd34337bd510a4eeb5ef36690a0464911b`；
  packet 11 行、结构留置 300，没有自动外发。

## 本地模型与 OpenCode 边界

- 本地模型为 `BAAI/bge-small-zh-v1.5`，revision
  `7999e1d3359715c523056ef9478215996d62a620`，512 维，CPU 离线运行，禁止 remote code。
- bundle SHA-256 为
  `81d3cf5d98d31f7e79868f3667209279fff448b35490bffac3f4b2d5cc6f3563`。
  超长输入按 token 窗口覆盖全部内容，再按 token 权重聚合并 L2 归一化，不静默截断。
- 根目录 `OPENCODE_DEEPSEEK_PROMPT.md` 与 `OPENCODE_DEEPSEEK_TASK.md` 已冻结 AU 级筛选、视觉
  证据、输出 schema 和导入边界。本地书 11 个 READY AU 可执行；三位作者须先完成不可变图片
  snapshot、DOM locator、OCR、分类、上下文回填和受影响 AU 重建。
- OpenCode/DeepSeek 只写批次目录的 `deepseek-results.jsonl`；不能联网补证据、读取凭据、改数据库、
  自行批准候选或生成交易指令。

## 其他已验收边界

- Provider registry、免费 reference、财务来源和候选注册表均有 recorded 路径；live 必须显式开启。
  BaoStock 0.8.9 是 reference 主源，东方财富为备用/补充；原始响应先入 ObjectStore。
- 东财/Sina 财务数据均为 `SECONDARY_STRUCTURED`。只有官方原生 PDF 精确证明表名、合并口径、
  期间列、科目、值和单位的事实，才可进入财务审计。
- Serenity 固定两个上游 commit 和本地适配 hash，映射到 6 个 research-skills-v2 方法合同；
  未校准权重只报告，不进入交易裁决。
- 候选只表达研究优先级，不包含交易方向、目标价、订单或持仓。真实生产候选宇宙尚未生成。
- 默认模拟账户双重记账平衡；当前运行状态为 0 订单、0 成交、0 持仓。
- 系统没有自动实盘、券商订单接口或已批准的新 Skill。Phase 7 程序闭环可用，但真实前向证据仍为
  0/100，状态为 `COLLECTING`。

## 当前验证状态

- `knowledge-semantic-model-status` 为 `AVAILABLE`，revision、维度和 bundle hash 与本报告一致。
- 《价值投资功法》真实私有书 acceptance 在绑定上述 visual run 后通过；视觉覆盖为
  `COMPLETE / REVIEW_REQUIRED`。
- K5-D3/D4 冻结 Python 路径 Ruff 通过；全局 Pyright 为 `0 errors / 0 warnings`；
  `git diff --check` 通过。
- 当前全仓门禁不是正式干净基线：工作树含其他并行改动和未跟踪文件。正式验收仍须在最终范围
  固定后重新执行 `uv run pytest`、`uv run ruff check .`、`uv run pyright` 和
  `git diff --check`。
