# AStockMultiAgent 当前验收报告

本报告只记录已实现且有可复核证据的当前事实；未完成项只写入《开发计划》。被替代的实现叙述不在现行文档保留，由 Git 历史追溯。

## 文档职责与当前基线

- 文档职责：记录已实现、已运行、有代码/测试/工件证据且当前仍然成立的事实。
- 发布基线：v0.1.0 / v0.1.1 历史 Release 保持不变；本轮 Portfolio & Holding Decision Skills v1 已把 tracked 版本推进到 `0.2.0`。v0.2.0 的最终 commit、push、tag、GitHub Release、远端一致性和 clean worktree 属于发布动作完成后才能成立的事实，不在 tracked 文档中预写，统一以 Git 远端/Release 与唯一 durable run 的 ignored final evidence 为准。
- 当前开发分支为 `main`；任何版本 tag 都只指向真实已经提交并推送的冻结树，禁止先写“已发布”再补 Git 事实。

## 当前能力总览

| 能力 | 当前状态 | 边界 |
|---|---|---|
| ObjectStore / Parquet / SQLite / migration | 已实现 | 原始对象不可覆盖；分析事实与元数据分离 |
| 行情、reference、财务来源 | 已实现基础链 | 免费源有覆盖和可用性边界；二级财务值只作线索 |
| 研究、专家、诊断、候选、委员会 | 程序已实现 | 只读冻结证据；缺证据返回缺口 |
| 模拟账本与 5m 回放 | 核心已实现 | 无公开自动下单能力，不连接券商执行 |
| 白名单知识采集 | 部分覆盖 | 当前范围为回答、文章、专栏正文和想法 |
| Paragraph → Argument 语义链 | 已实现并真实运行 | Paragraph 只作存储/辅助视图，AU 是最终判断单位 |
| 本地 BGE 三视图 | 已实现并固定模型资产 | 未校准低相似度不得自动删除 |
| 《价值投资功法》旧知识资产 | 仅作历史审计 | 不可修改；新 direct run 不依赖旧 reviewed AU，也不自动取得委员会生产权重 |
| OpenCode 边界 / DeepSeek 交接 | 历史批次保留审计 | 三位知乎旧纯文本 batch 无 response/candidate；视觉完成后未复用旧空批次，改走冻结 AU + VisualEvidencePack 的版本化 overlay |
| Knowledge Skill composite registry | 已真实发布 / Provider `READY` | baseline 231 + visual overlay 422 = 653 admitted；12 历史 REJECT；pending=0；正式委员会权重仍为 false |
| Phase 6 | **DEVELOPMENT COMPLETE** / recorded 验收通过 | 通用 Research Runtime、PIT TradingClassification 与 classification-bound final protocol 已实现；缺 exact live 数据仍 `NEEDS_INFO` |
| Phase 7 | **DEVELOPMENT COMPLETE** / `COLLECTING` | 正式 6-arm forward study 已创建并 audit PASS；真实独立前向事件仍 0/100 |
| Phase 8 | **DEVELOPMENT COMPLETE** / user `NOT_ADMITTED` | 内部 `NOT_ENTERED_BY_DESIGN`；12月/100决策/5 folds/3 regimes；自适应权重、在线学习、主模拟账本和券商执行均关闭 |
| Phase 9 机构级基本面内核 | **DEVELOPMENT COMPLETE** | Evidence Sufficiency → Industry/Company Economics → DriverTree/Forecast/Valuation → FundamentalModelBundle/DecisionContext 已接入 Runtime/Committee；历史 Phase 6 recorded 链仅保留兼容 |
| Phase 10 Portfolio Risk / Implementation / Attribution | **DEVELOPMENT COMPLETE** | compact factor risk explanation、流动性/成本、版本化经济压力测试与 ex-post attribution 已实现；默认等权不变 |
| Phase 11 Prospective Statistical Governance | **DEVELOPMENT COMPLETE** / data `COLLECTING` | all-trials、cluster independence、purged/embargo folds、cluster bootstrap 与 DSR/PBO diagnostic 已实现；真实正式 forward count 仍为 0/100 |
| Phase 12 Research Production Optimization | **DEVELOPMENT COMPLETE** / evidence `COLLECTING` | Need↔Capability 路由、2–4 动态预算、Skill usage/efficiency 与 Catalyst 局部重跑已实现；自动 Skill 修改/退休仍关闭 |

## Phase 0—4 已验收基础

### 数据底座
- ObjectStore 按 SHA-256 内容寻址，原始对象不可覆盖。
- Parquet 存储分析事实；SQLite 存储状态、游标、账本和工件索引。
- 免费 5 分钟行情以东方财富为主源、新浪为备用和交叉验证，未复权原始价格。
- Provider 注册表统一描述市场、主数据、交易日历、财务、公告和公司行为接口，同时维护 recorded contract 和低频 live smoke。
- 质量报告读取规范清单，缺柱不静默插值。

### 证据与文档
- 披露搜索和同步（CNINFO）生成不可变快照；PDF 解析支持原生文本和选择性 OCR。
- 私有 PDF 和 DOCX 导入已实现，保留原文和图片资源。
- Claim-Evidence 系统支持证据注册、声明链接和冲突记录。
- Point-in-Time 元数据记录 period_end、published_at、effective_at 和 available_to_system_at。

### 财务可信度
- 财务来源同步为 EastMoney/Sina，原始响应先入 ObjectStore，标记为 `SECONDARY_STRUCTURED`。
- 财务审计运行确定性校验并输出审计报告；同行异常检测已实现契约和迁移。
- 只有官方原生 PDF 精确证明表名、合并口径、期间列、科目、值和单位的事实才可进入正式财务审计。

### 投资内核与 Serenity
- Serenity 当前活动注册表为 `research-skills-v3`。`muxuuu/serenity-skill` 固定 upstream commit=`c2fe93deedfd0d1bd9fe7ef0601ea1b9c20ea24a`，audit=`serenity-muxuuu-c2fe93de-local-v4`；`haskaomni/serenity-skill` 固定 upstream commit=`dedcf8f9ca8bd48f21239456ede50a9eb9f7ecb0`，audit=`serenity-haskaomni-dedcf8f9-local-v4`。两份当前 upstream tree 均已按 commit + reviewed-file SHA-256 复验 `PASS / mismatches=[]`，旧 v3 audit 文件保留为不可变历史。
- 活动方法为 6 个 Serenity typed contracts：Industry Bottleneck、Event-to-Alpha、Growth Probability、Growth Valuation、Daily Trend Health、`JuglarCycleStageSkill`，另有本地独立 Hourly Swing；ResearchMemoComposer 不占 specialist slot。Juglar 强制需求/ASP/利润率/Capex/库存/产能释放/客户行为/资本市场反应八维各一次、复苏/扩张/过热/衰退/出清五阶段概率精确和为 1，并保留反证与迁移信号；它只输出 `SpecialistDelta`，不直接产生目标价、仓位或订单。
- muxuuu 的固定 aggregate score 没有进入生产权重；本地只把 scarcity、substitution、value-capture、产业链层级等可审计 scorecard 维度显式写入 Specialist metrics。未校准权重继续只报告，不进入交易裁决。
- 候选系统只表达研究优先级，不包含交易方向、目标价、订单或持仓。候选注册表通过 recorded 路径验证；live 必须显式开启。

## Phase 5 知识工厂当前事实

### Phase 5 采集覆盖
最新冻结覆盖审计时间为 `2026-07-22T08:42:57Z`，冻结截点为 `2026-07-22T08:42:27Z`，状态 `PARTIAL`。

| 作者/类型 | 目录唯一项 | 详情已验证 |
|---|---|---:|---:|
| MR Dang（Mr. Dang）回答 / 文章 / 想法 | 118 / 16 / 0 | 118 / 16 / 0 |
| 黄彦臻回答 / 文章 / 想法 | 831 / 8 / 1 | 831 / 8 / 1 |
| 派大星皮皮回答 / 文章 / 想法 | 895 / 24 / 1,896 | 895 / 24 / 1,895 |
| 合计 | 3,789 | 3,788 |

开放 gap 9、待消费导入 0、对象缺失 0、Parquet 错配 0。寒武纪的鳄鱼本地导出状态为 `USER_CONFIRMED_COMPLETE_EXPORT`。专栏容器合同已实现；真实无凭据枚举快照尚未取得，状态 `BLOCKED_EXTERNAL_OBSERVATION`。

### 论证级语义链
- `ParagraphUnit` 保存来源、顺序、页码或 DOM locator、文本对象哈希、修辞角色、上下文依赖和 merge action；它不是最终筛选或蒸馏单位。
- `ArgumentUnit` 只组合同一 SourceItem 内连续 Paragraph，将设问、回答、主张、解释、证据、案例、反方和结论组成可复核的完整论证。
- active pipeline 为 `knowledge-semantic-funnel-three-view-v3`。局部上下文为前 1＋当前＋后 2；active Embedding 合同为 `PARAGRAPH_AUX_ARGUMENT_FINAL_V3`。
- 每个 Paragraph 生成 current 与 local-context 辅助向量；每个可判定 AU 生成完整 AU 向量；14 类方法原型只用于相似度参照。最终 `topic_relevance`、`methodological_completeness`、包选择和候选只能来自完整 AU。
- 14 类方法不设每类数量上限。无 calibration manifest 时，低相似度只标记 `CALIBRATION_REQUIRED`，不得自动删除。

三位作者的纯文本三视图运行是视觉增强前的历史输入基线；旧 batch 均无 `response_object_hash`，也未导入 `knowledge_semantic_candidate`，因此不再作为最终可发布知识状态：

| 作者 | semantic run | 向量 / AU 分数 | 导出 / 结构留置 / 超长留置 |
|---|---|---|---:|---:|
| Mr. Dang | `bfc6c8981f5293c0417b895d43f4ae25db9a3cef5b63495c8a4072153e8b10d7` | 15,172 / 278 | 43 / 235 / 68 |
| 黄彦臻 | `5da9ae360128b7346797fc217aed2029b475688fed62e65ccb5cb094b86c68d9` | 8,100 / 628 | 136 / 492 / 2 |
| 派大星皮皮 | `a93acd607ffaf01f0d9c7678ee1902971a8d4d7002b8102345b4b04a8898f434` | 53,930 / 4,426 | 1,126 / 3,300 / 35 |

合计 77,202 个向量、5,332 个 AU 分数和 1,305 个纯文本 packet。旧批次没有自动外发，没有 DeepSeek 结果，也没有导入候选；它们只保留为视觉增强前的不可变历史输入。

### 三位知乎作者真实视觉证据与视觉增强 Skill（2026-08-10）

真实视觉链已经完成，不再使用 `[图片]` 占位符作为证据。冻结 HTML/API snapshot 中的正文图片与旧 v3 `[图片]` Paragraph 逐作者精确对齐，最终共 `2,503/2,503` placements、`2,306` 个唯一图片对象、`2,503 READY / 0 NEEDS_REVIEW / 0 BLOCKED`。原图先写 ObjectStore；Windows Media OCR（`zh-Hans-CN`）用于批量 OCR，RapidOCR 仅作 fallback；OCR/分类都是原图的派生语义层，不能替代原图证据。

| 作者 | semantic run | placements / unique assets | VisualEvidencePack artifact | pack object hash |
|---|---|---:|---|---|
| Mr. Dang | `knowledge-semantic-run:bfc6c8981f5293c0417b895d43f4ae25db9a3cef5b63495c8a4072153e8b10d7` | 993 / 930 | `VisualEvidencePack:visual-evidence-pack:5fee6e1af84c3c402372a70847bcfb17df9adfb8a5615ab39d427f319e02b575` | `a62683bca36949aee09d1e1f35726c9683e3bb14c1499915af56238583ed1eb2` |
| 黄彦臻 | `knowledge-semantic-run:5da9ae360128b7346797fc217aed2029b475688fed62e65ccb5cb094b86c68d9` | 248 / 224 | `VisualEvidencePack:visual-evidence-pack:06624530868325f3214c31726358959bbd453563eb83cf21f83f2f8f13dd6cd9` | `cc07c6ff33191ba1adb4246973299ff1a0334c1d3f1f4a2b4f2af757efd19c0e` |
| 派大星皮皮 | `knowledge-semantic-run:a93acd607ffaf01f0d9c7678ee1902971a8d4d7002b8102345b4b04a8898f434` | 1,262 / 1,152 | `VisualEvidencePack:visual-evidence-pack:423418f2d8d80500b36371b43f816db6799a46f7cf95334b7daa60d5577bce6e` | `638ae29365d74e41a6f793528a563d4938fbad8a2a85a5fad1cf7728ac002722` |

视觉 Paragraph 继续固定 `standalone_distillable=false / MERGE_WITH_BOTH`。图片位于 SourceItem 边界时，系统只用同一 SourceItem 的最近有效正文或版本化 source-boundary metadata 补足双侧上下文；旧 AU 未包含图片时生成只用于视觉 lineage 的 context anchor，不把 synthetic anchor 直接蒸馏成 Skill。所有 visual packet 均要求原图、URL/DOM locator、OCR attempt、分类、双侧 context 和 rebuilt-AU hash 可复核。

`0051_visual_skill_overlay.sql` 在不修改旧 0050 registry 的前提下新增 append-only visual Skill overlay。真实 generation run=`visual-skill-run:e34f4a5c0d00eb8c3adcded5e6a462de694c3919969caefd634e0c2848c9924e`，policy=`zhihu-visual-skill-overlay-v1`，run artifact=`VisualSkillGenerationRun:visual-skill-run:e34f4a5c0d00eb8c3adcded5e6a462de694c3919969caefd634e0c2848c9924e`，run object hash=`64813201bca0427a5d50c16468ac7cf8761b6caf6a7a1237124ee1a1bc434b62`。共评估 `951` 个真实视觉关联 AU：生成严格候选 `422`，明确 `NO_SKILL=529`；候选/NO_SKILL 按作者分别为 Mr. Dang `112/51`、黄彦臻 `42/45`、派大星皮皮 `268/433`。

严格候选门为：方法类别非空、`topic_relevance>=0.55`、`methodological_completeness>=0.70`、全部视觉 packet/rebuild READY、AU/packet/image/classification/OCR（如有）/source snapshot 对象 hash 可验证、core principle 足够完整且通过 deterministic 去重。未过门的 AU 落不可变 `NO_SKILL`，不会进入 `NEEDS_REVIEW` 或生产 registry。

用户在本轮明确要求“彻底完成 Phase 5”，因此在 generation post-audit `PASS / findings=[]` 后，对 422 个严格候选执行 append-only 最终准入：`APPROVE=422 / REJECT=0 / PENDING=0`。这不等价于“所有图片都自动批准”：529 个无稳定方法价值或未过阈值的 AU 已在候选前明确 NO_SKILL。每条 admitted visual Skill 都固定 `community_source_only=true`、`factual_use_requires_stronger_source=true`、`standalone_visual_distillation=false`、`merge_policy=MERGE_WITH_BOTH`、`formal_committee_weight_allowed=false`；社区观点和 OCR 不能直接升级为上市公司事实。

旧 baseline registry 保持完全不变：`231 admitted`、object hash=`e12a1787138589f501a35406d43c59c5b1a0887566c7d922eb42110426f3b9aa`。新 composite release 只叠加 visual overlay：`base=231 + overlay=422 = composite admitted 653`；`release_id=knowledge-registry-v2:abbd793d4a3dc08bb1dd239cf2822297e6f46451142882734da8849f5fbb5c53`，`registry_version=knowledge-registry-v2:abbd793d4a3dc08b`，`artifact_id=knowledge-registry-composite:abbd793d4a3dc08bb1dd239cf2822297e6f46451142882734da8849f5fbb5c53`，`object_hash=cac3c6ac8c5f1c4b37fba7f913d7fca3d83eef1d1695e97e38f5ba7f9897e82b`。

`KnowledgeSkillProvider` 已真实返回 `READY / COMPOSITE_REGISTRY_READY`：`total_skill_count=665`、`ready_skill_count=223`、`approved_count=430`（历史 8 + visual 422）、`rejected_count=12`、`pending=0`、`eligible_skill_count=653`。实际 top-k 查询同时返回 baseline `READY` 和 visual overlay `APPROVED` Skill，证明 overlay 不只存在于数据库，而是已进入 runtime 检索。`knowledge-visual-skill-finalize` 已进行幂等复验，最终 visual Skill audit=`PASS / findings=[]`。

### 《价值投资功法》视觉证据链
视觉状态机为 `INPUT_FROZEN → LAYOUT_ENUMERATED → OCR_COMPLETED → CHARTS_CLASSIFIED → SEMANTIC_MATERIALIZED → AUDITED`。

- PDF 从 ObjectStore 的冻结原始对象读取。PyMuPDF 按 placement 枚举并保存页码、bbox、xref 和 ordinal；xref 提取失败时使用 300 DPI bbox clip。每个 placement 都运行本地 RapidOCR。
- 原 PDF、提取图片和 OCR 文本先写 ObjectStore；SQLite 只保存 hash、locator、状态、版本和置信度。
- visual run: `book-visual-run:e94590494783714a9f1743451ea91cbe778289ddd043c4db232c1f866bb66bf9`
- coverage: `book-visual-coverage:73ba24ac531aaee10a2ed383fdaa83302bd7b500a0765692263ad007bf6922f2`
- 249 页中 57 页含图，共 74/74 placements。OCR 失败 0、低置信 0、无文字 2、重复内容 1；分类为 TABLE 35、CHART 14、TEXT_IMAGE 22、DECORATIVE 3。
- 71 个非装饰图全部具有 `ImageEvidence → ParagraphUnit → ArgumentUnit` lineage，映射到 55 个 AU。
- 视觉 Paragraph 固定 `standalone_distillable=false / MERGE_WITH_BOTH`。图表夹在论点和结论之间时合并到同一 AU。
- visual semantic run: `semantic-run:e0605aa01c8ddc0dffe69a81d88389d546e52b39aa89d19fc8a2012835e06ea5`；已写入 6,819 个三视图向量和 311 个 AU 分数。
- 旧知识资产仅保留用于历史审计，禁止修改；新 direct run 直接读取两份冻结原文，不依赖旧 reviewed AU、旧 MethodRule 或旧 Skill。

### 本地模型
- 本地模型为 `BAAI/bge-small-zh-v1.5`，revision `7999e1d3359715c523056ef9478215996d62a620`，512 维，CPU 离线运行，禁止 remote code。
- bundle SHA-256: `81d3cf5d98d31f7e79868f3667209279fff448b35490bffac3f4b2d5cc6f3563`。

## Phase 9 Institutional Fundamental Research Core（2026-08-12）

机构级基本面层已作为新当前公司研究的默认前置模型实现，不新增人格 Agent，也不覆盖历史 Phase 6 recorded 工件。主链为 `FrozenEvidencePack → EvidenceSufficiencyReport → IndustryProfile / CompanyEconomicsProfile → DriverTree → ForecastPack → ValuationPack → FundamentalModelBundle → InstitutionalDecisionContext`；之后才进入 BaseCase、Serenity/Knowledge Delta、ResearchMemo 与 Committee。

- `EvidenceSufficiencyReport` 在旧 Evidence/FrozenEvidencePack 之上增加 observed/derived fact、management assertion、industry estimate、interpretation、causal claim、forecast、valuation assumption 等机构研究语义，并用 authority/directness/independence/freshness/scope/extraction/PIT 向量做集合级门禁。重复转载按 `source_independence_group` 去重，material claim 只能落 `SUPPORTED / REFUTED / CONFLICTED / INSUFFICIENT / NOT_APPLICABLE`；open conflict、证据级别不足、独立来源不足或 dependency 缺口 fail closed。
- `IndustryProfile / CompanyEconomicsProfile` 把行业定义、供需/产能/定价/竞争/外部驱动，以及分部经济学、pricing power、客户/供应商依赖、竞争位置、管理治理、资本配置、ROIC/reinvestment、融资/稀释收敛成 canonical typed artifacts。没有认证行业 taxonomy 时只允许 `PROVISIONAL_TAXONOMY`，不冒充正式行业事实。
- `DriverTree` 是确定性 DAG；输入节点必须精确覆盖，循环或未知依赖直接拒绝。`ForecastPack` 的 Bull/Base/Bear 场景由 Python 按同一 driver tree 复算，FCFF 模板显式输出 revenue、margin、EBIT、tax/NOPAT、D&A、capex、working-capital change 与 FCFF；LLM/Agent 只能提供 evidence-bound assumption，不直接写最终计算值。
- `ValuationPack` 与 ForecastPack 共用 driver assumptions。当前实现包含 deterministic FCFF DCF、reverse-DCF implied terminal growth、mid-cycle normalized / archetype-specific explicit equity value 路径与 DCF sensitivity；Bull/Base/Bear 只形成 value range，`scenario_prices_are_targets=false`。expected return 和 market-implied expectations 只有在提供 `MarketPriceAnchor` 时计算；该 Anchor 必须绑定一个已注册 source artifact/object hash，且 observed/available 时间不得晚于 valuation `as_of`。没有价格锚时内在价值仍可计算，但市场隐含预期返回 `NEEDS_INFO / MARKET_PRICE_ANCHOR_REQUIRED`。
- `FundamentalModelBundle` 冻结 Evidence Sufficiency、Industry、Company Economics、DriverTree、Forecast、Valuation 六个组件的 exact artifact/hash；`InstitutionalDecisionContext` 再冻结 decision horizon、investment thesis、variant perception、3–5 key drivers、competing hypotheses 与 portfolio context，并绑定 Bundle exact hash。Institutional audit 重新核对 registry input hashes、source IDs 与实际 source object hashes，父工件漂移即 FAIL。
- 通用 Research Runtime 新增 `FUNDAMENTAL_MODEL` stage 与 `institutional_research_required` 门。新当前公司研究在缺 Bundle/DecisionContext 时结构化停在 `NEEDS_INFO`；提供 exact ready 工件后可继续到 Committee。`FundamentalModelBundle / InstitutionalDecisionContext` 在 Committee 中是共享 `PRIMARY` 冻结输入，不新增委员会席位或投票权重；强集成验证两者 object hash 确实进入最终 DecisionPack frozen inputs。
- 同一轮审查修复了 `KnowledgeSkillDelta` 的暖跑幂等问题：cache-hit/latency 属于 `ResearchRunPerformanceSummary/checkpoint`，不可再改变 KnowledgeSkillDelta 的不可变语义对象 hash；因此相同 company/as_of/registry/selection 可安全复用同一 Delta identity。
- 当前定向验收：`tests/unit/test_institutional_research.py + tests/integration/test_research_runtime_complete.py = 10 passed / 0 failed`；最终全仓统计见“当前验证和门禁状态”。

## Phase 10–12 风险、前瞻统计与研究生产治理（2026-08-12）

### Phase 10：Portfolio Risk / Implementation / Attribution

Phase 10 作为现有 Portfolio v1 的并行版本化解释层实现，没有改写 `PortfolioAnalysisReport`、四种既有 construction proposal 或生产默认 `EQUAL_WEIGHT_CONSTRAINED`。

- 新增 `portfolio_vnext` 合同与服务：compact risk explanation 固定覆盖 market beta、size、value、momentum、quality/profitability、volatility、liquidity 与 industry。market/momentum/volatility/liquidity 可由 PIT 行情推导；size/value/quality 没有冻结来源时必须为 `UNAVAILABLE`，caller-supplied 未验证值和 frozen input provenance 明确区分，禁止伪造因子事实。
- Liquidity/implementation 层显式记录 average daily volume/amount、median daily range、participation cap、days-to-liquidate、slippage bps 和按活动 fee schedule 计算的 round-trip implementation cost；仓位超过单日 participation cap 会产生明确 warning，不把摩擦藏在收益里。
- Economic stress 固定支持 rates、RMB/FX、credit tightening、commodity、domestic demand、sector multiple compression、liquidity shock 与 company thesis break。每个 shock 必须由版本化场景输入提供；系统不擅自给情景分配概率。
- Ex-post attribution 固定拆成 stock selection、sector、compact factor、timing、implementation cost，并可关联 ResearchMemo / Skill / SpecialistDelta。Research attribution 明确 `causal_credit_claimed=false`，只作审计性关联归因，不自动修改 Skill。
- 新增稳定 CLI：`portfolio-vnext-schema`、`portfolio-risk-explain`、`portfolio-stress`、`portfolio-attribution`、`portfolio-vnext-audit`。所有 Phase 10 report 均固定 `paper_ledger_write_allowed=false / broker_execution_allowed=false`。

### Phase 11：Prospective Statistical Governance

Phase 11 已在正式样本继续增长前完成软件层治理。`0052_prospective_trial_governance.sql` 新增 append-only governance config、trial event、cluster、frozen input 与 statistics-plan 索引；这些表与旧 Phase 7 正式 assignment/observation 计数分离。

- All-trials registry 可以冻结 Seed rejected、Promotion blocked、Candidate rejected、Committee `REJECT / NEEDS_INFO / WATCH / APPROVE_SIMULATION` 和 formal-assignment funnel event；trial schema/SQLite 同时把 `formal_trade_event` 锁死为 false，因此 rejected/blocked trial 可进入选择偏差统计，但不能增加 Phase 7 forward-event count。
- 每条 trial 支持 stock、industry、theme、decision-date、shared-catalyst 五类 cluster ID，并单独冻结 `independence_unit_id`、PIT 输入、market-regime id/rule version。Governance config version 与 study identity 绑定，避免不同正式研究期共用一个全局 v1 身份；默认 config 的 `created_at` 使用首次真实注册时刻，`effective_from=max(created_at, study.effective_from)` 并幂等复用，既允许在 study 生效前预注册，也禁止反向早于 formal study 或回填历史时间戳；market regime 规则只能 ex ante 冻结，不能事后重标。
- 预注册 endpoint 包含 20d sector-adjusted return、60d benchmark-adjusted return、60d MAE、process grounding、decision calibration；5d return 明确为 diagnostic。新增整 cluster bootstrap、time-ordered purged folds + embargo；达到 100 个 unit 只表示 `independence_sample_floor_reached=true`，不等价于 Phase 7 evaluation 或 Phase 8 admission 已通过。
- DSR/PBO 只在确有重复模型/参数选择时启用为 selection-bias diagnostic；单一候选时返回 not-applicable，且 `pass_badge_allowed=false`。
- 新增稳定 CLI：`prospective-governance-register`、`prospective-trial-register`、`prospective-trials-report`、`prospective-statistics-plan`、`prospective-governance-audit`。真实 canonical forward study 仍是 `0 / 100`，本轮没有用 fixture、回放或新 all-trials 表伪造任何正式样本。

### Phase 12：Research Production Optimization

Phase 12 通过新增 policy/route/usage/catalyst 层完成，不改写旧 `research-skills-v3` 或 Serenity v4 artifacts。`0053_research_production_governance.sql` 新增 production policy、route、Skill usage、Catalyst 与 monitor 索引。

- `ResearchNeedVector ↔ SkillCapabilityVector` 路由顺序为 hard applicability → ontology overlap / frozen embedding recall → diversity/conflict → bounded top-k。Embedding 分数只有绑定已注册 artifact/hash 时才允许进入路由；缺 embedding 时显式 finding，不伪造语义召回证据。
- Specialist hard cap=3 已从生产新层降级为动态预算：普通任务 2，较高优先级 3；只有 urgent + critical materiality + high/critical uncertainty 才可触到 hard max 4。Fundamental specialist 排序使用 ordinal 的 `materiality × uncertainty × applicability × incremental evidence ÷ marginal cost` 评分，不解释为收益概率或货币 EV。
- 角色迁移已经固定：Growth Probability → `SHARED_HYPOTHESIS`，Growth Valuation → `CANONICAL_VALUATION`，Daily Trend / Hourly Swing → `MARKET_TRADE_CONTEXT`，ResearchMemo → `COMPOSER`；只有 `FUNDAMENTAL_SPECIALIST` 消耗动态 specialist budget。旧 registry/旧路由仍可读取和复现。
- `SkillUsageEvent / SkillEfficiencyReport` 记录 corrected claim、gap、driver、falsifier、IC change、prospective lift、token cost、near-duplicate/conflict。Prospective lift 必须绑定冻结评估 artifact/hash；没有前瞻证据时 recommendation 只能是 `INSUFFICIENT_PROSPECTIVE_EVIDENCE`，`automatic_retirement_allowed=false / automatic_skill_modification_allowed=false`。
- Catalyst Registry 冻结 thesis、预期窗口、KPI rules、受影响模块和来源 provenance；monitor 只返回需要重跑的 affected modules，不触发全链定时重跑，也不写 paper ledger 或 broker execution。
- 新增稳定 CLI：`research-production-schema`、`research-priority`、`research-production-route`、`research-usage-register`、`research-efficiency-report`、`catalyst-register`、`catalyst-monitor`、`research-production-audit`。

### Phase 10–12 定向验收

- 新增 `tests/unit/test_phase10_portfolio_vnext.py`、`test_phase11_prospective_governance.py`、`test_phase12_research_production.py`；覆盖 attribution reconciliation、round-trip implementation cost、rejected trial 不增加 formal count、cluster statistics、角色迁移、2–4 动态预算、support-role 分流、Catalyst 局部重跑、无 prospective evidence 禁止自动退休，以及缺 BaseCase 时结构化 `NEEDS_INFO`。
- migration 基线已扩展到 `0053`，`test_state.py` 同时验证 0052/0053 fresh migration 及关键新表存在；另新增历史 0044 migration identity 兼容回归，未知/篡改 checksum 仍 fail closed。
- 最终全仓验证统计见“当前验证和门禁状态”；Phase 10–12 的软件完成不改变 Phase 7 `0/100`、Phase 8 `NOT_ADMITTED` 或全局 `broker_execution_allowed=false`。

### 2026-08-12 烟雾测试发现与修复

- 本轮烟雾测试曾发生一次必须保留在验收历史中的**真实主库 schema 写入事故**：第一次通过嵌套 PowerShell 注入 `ASTOCK_RUNTIME_ROOT` 时，外层 shell 吞掉 `$env:`，`phase6-audit` 短暂回落到默认 `runtime/state.sqlite`；CLI `_services()` 自动执行 migration，因此 0052/0053 分别于 `2026-08-12T07:49:23.489278+00:00`、`2026-08-12T07:49:23.508056+00:00` 被提前应用。该次烟雾测试的“主库零写入”验收项明确记为 **FAIL**，没有回滚或改写这一事实。
- 随后以及本次收口均以 SQLite URI `mode=ro` 复核主库：Phase 11 五张治理/统计表与 Phase 12 五张 production/usage/catalyst 表行数均为 0；`shadow_study_index=1 / shadow_assignment_index=0 / shadow_observation_index=0`；`paper_operation_request / paper_operation_execution / paper_order_transition / paper_position_identity / paper_position_lot` 均为 0。因此已证实影响限定为 0052/0053 schema 提前落库，没有新增 trial、production 数据、正式 forward event、订单、成交或持仓。
- 历史保存的 Phase 6 SQLite 曾因旧 0044 migration identity 与当前 `0044_phase6_decision_close.sql` 不同而在 `StateStore.migrate()` fail closed。修复没有对白名单之外的 checksum 放宽：仓库新增 `migrations/compat/0044_phase6_decision_close_legacy.sql`，其内容固定为 Git 历史中已审计的旧 0044 SQL；只有数据库记录 checksum 精确匹配该历史文件、当前 0044/0047 文件完整且旧 schema 形态精确匹配时，才允许在 0047 阶段事务式完成 recovery 语义并把 migration identity 对齐当前版本。任何未知 checksum、缺 recovery 文件或 schema 漂移仍抛出 migration checksum/integrity 错误。
- `research-production-route` 对不存在的 BaseCase 不再向终端泄露 Python traceback。用户 CLI 现在返回 `ResearchProductionRouteNeedsInfo`、exit code `3`，明确给出 `requested_artifact_type=BaseCasePack`、requested BaseCase ID、`requested_object_hash=null`，并在同一 runtime 中存在可审计且 company 匹配的 BaseCase 时仅作为安全候选提示其 exact ID/hash；缺 BaseCase 路径在返回前不会注册 production policy 或 route。严格服务层 `route()` 仍保留 fail-closed `ValueError`，供程序调用者发现 registry/index 漂移。

### 当前投资咨询产品化与 600938 烟雾复盘（2026-08-12）

本轮以“中国海油 600938 现在是否适合买入”的真实网页端体验反向审计 current-company Research Runtime。结论是原正式安全边界没有制造虚假 BUY，但数据获取编排、provider 兼容、耗时与投资者展示均存在可复现缺陷，现已按“本地/Provider fallback → 权威 Web → 最后一次性人工协助”收口。

- **Instrument Master 根因**：真实主 runtime 的 BaoStock 原始 envelope 显示 login=`10002007 / 网络接收错误`、result=`LOGIN_FAILED`，属于外部 SDK 网络失败；EastMoney fallback 的 HTTP 实际 `rc=0`，但线上 `data.diff` 已由历史 `list` 漂移为 numeric-key `dict`，旧 parser 因 schema 假设过严把可用响应判为 malformed。当前 parser 已同时接受两种形态；并新增 `sync_instrument_identity` 精确单证券 identity release，EastMoney 以 100 条/页做 bounded pagination，找到目标后只发布 `<market>:<symbol>` scope，不再要求先构造完整市场 master。
- **BaoStock 慢失败根因**：BaoStock 0.8.9 内部 TCP connect/recv 没有设置 timeout，网络不可达时一次 login 可拖十几至二十多秒并向 stdout 打印错误。当前 adapter 在进入真实 SDK socket 前增加 2 秒 TCP reachability preflight，失败直接冻结 `NETWORK_PRECHECK_FAILED`；最近 `FETCH_FAILED` 会打开 30 分钟 circuit breaker，后续同 runtime 直接转 fallback。SDK login/query/next/logout 的第三方 console chatter 同时被捕获，CLI JSON 不再被中文错误/WinError 污染。
- **Daily Price**：原链的 EastMoney fallback 实际已能取得日线，问题主要是 BaoStock 前置等待和 fallback 没有 bounded retry。当前 live reference fallback 对 retryable `NETWORK/TIMEOUT/5xx` 做最多 2 次短重试并退避，永久错误/结构错误不重试；BaoStock circuit 打开后直接走 EastMoney。600938 临时 live 烟测在 BaoStock 不可达时取得 42 条近期未复权日线，单项约 1.8 秒。
- **Corporate Action 根因**：旧 `sync_corporate_actions` 在 `BaoStockCaptureError` 时直接返回 FAILED，因此根本不会进入已经存在的 CNINFO official index 逻辑。当前即使 BaoStock 失败/circuit open 仍继续 CNINFO；官方索引可达但没有匹配文档时只标记 `PARTIAL / OFFICIAL_INDEX_CAPTURED_NO_MATCH`，绝不把“没搜到”解释为“官方证明没有公司行动”。公司行动/TradingClassification 缺口只阻止需要执行资格的 paper/trade path，不再单独阻断数月/数年的基本面研究。
- **Financial Source 根因 1：上游门禁**：旧 `FinancialSourceService.sync()` 在 EastMoney→Sina 之前先要求 Instrument Master，因此 identity 未建立时两个财务 provider 实际一个都没调用；此前约 30ms 的 `FINANCIAL_SOURCE_SYNC_FAILED` 是本地 `ValueError`，不是两个财务网站同时不可用。Current Acquisition 现在即使 identity 尚未认证，也会并发抓 secondary structured hints；这些 hints 不能直接变成 certified FinancialIntegrity，但可为后续官方报告核验提供定位线索。
- **Financial Source 根因 2：provider schema drift**：实际 live 响应证明 EastMoney financial `code=0/message=ok`，包含 600938 2025 年报和 2026Q1，但当前 response 不再提供系统正式 parser 要求的 source-native `STATEMENT_SCOPE/CURRENCY`；这里继续 fail closed，不猜合并口径或币种。Sina 同样 HTTP 成功，但 `report_list` 已由历史 list 漂移为以 `YYYYMMDD` 为 key 的 dict；新 adapter 从源生 `rType=合并*` 和 `rCurrency=CNY` 取得口径/币种，并按 statement-specific field map 解析三张表。Sina 当前 `item_value` 是“元”，而内部正式 mapping 为 `TEN_THOUSAND_CNY`，因此货币科目确定性除以 10000，股份字段不换算。真实 live 600938 烟测已成功解析 2025 年报和 2026Q1 的资产负债表、利润表、现金流量表；在 identity 仍未认证时两期均以 `PARTIAL` secondary hints 保存，EastMoney 失败后自动切 Sina，单项约 3.5–4.2 秒。
- **当前采集编排**：新增 `research-acquire-current <company_id> --market <market>` 与 `CurrentResearchAcquisitionReport`。当前咨询不再以用户提问的瞬间作为 cutoff；先做当前公开资料采集，再在采集结束时冻结统一 `decision_as_of`。身份之后可独立的 daily / corporate action / annual financial / latest interim financial 采用 bounded 并发；identity 未认证时 financial secondary hints 仍继续。任何未关闭的能力缺口转为 `ExternalResearchNeed`，明确允许 Agent 自动 Web search，首选交易所、CNINFO、发行人 IR、监管机构等不需要用户注册 API 的权威公开源；只有自动渠道全部耗尽后才允许一次性生成人工协助清单。历史回放、Phase 7 prospective study 和 backtest 的 source-availability/PIT 防未来数据边界保持不变。
- **投资者展示层**：新增 `research-acquisition-investor-view` / `research-investor-view`。正常投资咨询只输出自然语言的结论、证据成熟度、估值/赔率、催化、风险和什么情况会改变观点；`CLAIM_IDS_REQUIRED`、`EVIDENCE_PACK_REQUIRED`、provider error、artifact/hash、SQLite 表、migration、CLI transcript 等只留在诊断层。执行资格缺口默认也不进入长期投资者视图，除非用户明确询问可执行买卖计划。
- **`probe` 超时根因与修复**：MCP 的 180 秒是 `mcp-dev` 单命令硬上限，不是 AStock 业务阈值。旧 `astock probe` 错把约 3.78GB SQLite 的 `PRAGMA integrity_check` 放进常规 capability 探针，导致两次触顶。当前 `probe` 使用 SQLite URI `mode=ro` 的轻量 connection/schema/migration health，不跑 migration、不跑全库 scan；完整体检独立为显式 `state-integrity-audit`。真实主 runtime 上新 `probe` 实测约 **6.66 秒**完成。
- **速度烟测**：在独立 `tmp/cnooc-current-smoke` fresh runtime 中，BaoStock 与 EastMoney identity 外网都失败的退化场景，优化前 current acquisition 约 29 秒；加入 BaoStock 2 秒 TCP preflight、30 分钟 circuit、bounded retry、并发和 secondary-hint continuation 后，同类退化场景从 `started_at=14:08:52.848636Z` 到 `decision_as_of=14:09:01.573264Z` 约 **8.7 秒**。该临时 smoke 没有写主 `runtime/state.sqlite`，没有产生 paper operation/order/position，更没有真实券商操作。

## Phase 6 recorded 投资决策闭环

`TASK-PHASE6-FINAL-CLOSE` 已形成以下确定性程序链：`ResearchRequest → EvidenceCollectionTask → EvidenceCollectionRun → EvidencePack → FrozenEvidencePack → BaseCase → SpecialistRoute → Serenity Delta / 知乎专家 Delta → ResearchMemo → Investment Committee → TradeProtocol → PaperExecutionRequest → 独立签名人工确认 → Paper Ledger`。

- 委员会投资入口强制绑定 `BASE_CASE`、`SERENITY_DELTA`、`ZHIHU_EXPERT_DELTA` 和 `FINANCIAL_INTEGRITY` 四个角色。委员会运行保持 network、Browser、MCP、API 和 broker 全部关闭。
- TradeProtocol 的公共结果固定为 `WATCH / REJECT / NEEDS_INFO / APPROVE_SIMULATION`。非 APPROVE_SIMULATION 均为 `BLOCKED`。`broker_execution_allowed` 在 schema、SQLite 和服务层均固定为 `false`。
- 模拟执行不从委员会推导 BUY/SELL、数量或限价；调用者必须显式提供。prepare 阶段只写不可变请求，不写账本。人工确认须绑定操作/请求/账户/类型、有效期、nonce 和验签 key；确认对象及公钥对象均冻结到 ObjectStore，审计可重新验签。
- `account_id + idempotency_key` 绑定请求方向、数量和价格；重复请求和重复确认只复用原结果。中断恢复根据已完成操作和订单绑定对齐状态，不创建第二张订单。

300750 recorded case 的最终冻结身份：

| 工件 | 身份 / 结果 |
|---|---|
| Phase 6 run | `dd9c81fd959bab5bd93ea7b784a8d2cf32702291cf96a329ec9a65d70d8d2377` |
| ResearchMemo | `research-memo:fb807504c03e9c4166c126df94f502f9def26d102fd3b4f0e6df5c2d24a708f4` |
| CommitteeDecision | `decision:73785b71129ff05b2fbece892721e7c3a0c3a3a8126f3bdb118cde4eee28f0ae` |
| TradeProtocol | `trade-protocol:138aa138879ec1b197c969b054418ad234bc70feaec2fedecc943f1af4162f6a` / `APPROVE_SIMULATION` |
| PaperExecutionRequest | `1b61d5460c78528f0a7670c04cbab1b9a9937a0105e0f80fcfb75389a9d902b5` |
| User confirmation | `cf8b46906dfdd0feb810b7b32bcf9c3ea5bccbae4850dcfb8ad049e8ed165ca9` |
| Paper order | `7246216b7e53454e96ecf0d95e053b82` |
| Phase 6 / execution audit | `PASS / PASS`，finding 均为空，SQLite `ok` |

端到端 acceptance 为 `1 passed in 19.44s`；Phase 6、committee、paper、migration 和开源适配定向回归为 `63 passed`。完整链路日志位于 `docs/evidence/phase6_recorded_300750.log`。

上述 case 明确标记为 `RECORDED_ACCEPTANCE`，不是当前 300750 公司研究、投资建议或实盘授权。recorded 知乎专家 Delta 只用于该历史软件闭环；当前知识 Provider 已改由 baseline registry + 三作者 visual overlay 的 composite release 提供可检索 Skill。

### Phase 6 通用 Research Runtime 与 TradingClassification（2026-08-10）

通用单股票开发链已完成。`ResearchRunRequest` 支持 `company_id + as_of` 入口和自动冻结输入发现；`ResearchRunInputManifest` 保存已解析工件和结构化缺口；每个 checkpoint 记录 stage、artifact ID、exact object hash、耗时和 cache 状态；`ResearchRunPerformanceSummary` 记录 total/stage wall time、Knowledge top-k latency、context bytes/token estimate、token budget、provider call 与 cache hit。稳定增量 CLI 包括 `research-plan`、`research-run-company`、`research-status`、`research-audit`、`research-recover` 及原 `research-run-*` 文件入口。

最终协议顺序已收敛为 `Evidence → Financial Integrity → BaseCase → Serenity Delta → Knowledge Skill Delta → Committee → TradingClassification → ClassifiedTradeProtocol / ResearchPaperDecision`。Committee 原生 `TradeProtocol` 在通用 runtime 只作为 `committee_protocol_draft`；TradingClassification status 与 audit 未同时通过时不会冻结 final protocol。`ClassifiedTradeProtocol` 强制绑定 DecisionPack、committee protocol 与 TradingClassification 的 artifact/object hash，`ResearchPaperDecision` 再绑定 final protocol；两者均固定 `paper_ledger_write_allowed=false / broker_execution_allowed=false`。

`TradingClassificationRelease` 已升级为 v2，可冻结 market/board、NORMAL/RISK_WARNING、停牌状态、ordinary/IPO-initial-no-fixed-limit/suspended regime、固定涨跌幅 bps、rulebook、instrument/calendar/daily release lineage、validity interval 和 corporate-action baseline。板块与涨跌幅规则来自版本化官方规则配置；paper execution 的 operational reference 仍要求 COMPLETE/CERTIFIED，不因 research resolver 放宽。缺失、过期、hash 漂移、当日未收盘无法确认停牌、特殊 regime 无法排除或公司行为基准未认证时均返回 `NEEDS_INFO`。

特别地，二级结构化公司行为接口返回 0 行**不会**被解释为“官方证明无除权除息”。resolver 生成的 baseline 在 `absence_is_officially_certified=false` 时必须停在 `CORPORATE_ACTION_BASELINE_NOT_CERTIFIED`；resolved classification 的 freeze 边界还会再次验证 baseline artifact type 和 certified flag。这使 exact live READY 依赖于在 `as_of` 前已经冻结的官方公司行为事实，而不是事后补查或默认值。

新增验收测试证明：final protocol 源码位置严格晚于 classification status+audit；final schema 必须包含 exact TradingClassification artifact/hash；旧 Phase 6 recorded/paper 回归仍通过。通用 runtime 对缺 live 输入的正确输出是可恢复 `NEEDS_INFO`，不是伪造完整公司研究。

## Phase 7 真实前向程序与当前采集状态

截至 `2026-08-10T10:20Z`，真实 runtime 已创建 canonical 正式 study=`shadow-study:1a45f7ee4b35d23b545bae881c751566ef5d91f60c65aa2b7b234a22089fc9de`。该 study 固定 6 个 arm：Rule Baseline、BaseCase Only、BaseCase+Serenity Specialist、Full Committee、CSI300 Benchmark、Equal-weight Candidate；权重 profile、protocol family、cost/fill/corporate-action 版本均在 study 生效前冻结。`phase7-study-ensure` 幂等复用匹配的正式 study，不创建 assignment/observation。真实 `shadow-audit` 返回 `PASS / findings=[]`。

| 项目 | 当前事实 |
|---|---:|
| 正式 study arm | 6 |
| 正式独立前向研究事件 | 0 / 100 |
| 距最低评估门槛 | 100 |
| 成熟最终期限未来事件 | 0 |
| 已存 assignment / observation | 0 / 0 |
| Shadow audit | `PASS` |
| Skill / Committee / Research Quality | `COLLECTING` / `COLLECTING` / `COLLECTING` |

程序闭环已经实现，没有伪造运行证据：

- 正式事件固定为 `ResearchMemo → ShadowDecision → Future Observation`。完整委员会臂必须携带 `ResearchMemoArtifact`、`DecisionPack` 和 `TradeProtocol`。
- 正式 study 和 assignment 保存系统接收时间及前瞻资格；信号后超过策略登记窗口（300 秒）的事后 assignment 被拒绝。
- 正式观察只接受信号后真实采集的行情源快照。观察冻结 assignment 身份、标的/指数、未复权 5m 范围、`SourceSnapshot`、逐条行情 observation 与系统可得时间。价格、成交量和 MFE/MAE 必须与冻结 OHLCV 明细复算一致。
- 每个观察版本保存当时观点、信号/入场/估值时间、价格、净表现、观点是否仍有效，以及结构化失效原因。修订保留旧版本，不覆盖历史事实。
- 评估分别输出 Skill 表现、Committee 表现和 Research Quality。未达到 100 个真实独立事件时，三者由 schema 和服务层共同锁定为 `COLLECTING`。
- 同一 `ResearchMemo` 或 `ShadowDecision` 只能形成一个正式独立事件。
- 历史回放和回测只能显式标记为 retrospective，不能替代真实未来行情或增加正式计数。强化学习、动态/在线调权、自动修改 Skill、券商执行和自动实盘均关闭。

稳定操作入口包括 `phase7-study-ensure`、`shadow-study-create`、`shadow-assign`、`shadow-forward-market-freeze`、`shadow-observation-record`、`shadow-evaluate`、`shadow-status` 和 `shadow-audit`。原根目录 `phase7_status.md` 只是对 SQLite/ObjectStore 状态的重复生成式快照，信息已完整存在于本报告、《开发计划》和 `shadow-status`；为避免形成第三份可漂移状态源，现已退休该文件及 `phase7-status-update` 命令。当前正式事实仍为 `0/100`、assignment=`0`、observation=`0`，只能由真实前瞻事件改变。

本地 `runtime/state.sqlite` 的 0044 历史 SQL 已按原始校验和 `20e73d32b4e8a774159cb5e5438a46a7554917751ba5e49b3ba83c3ee2d02150` 恢复；后续协议归档和确认 key 绑定通过新增 0047 追加，没有改写 SQLite checksum。0048 增加 reviewed AU/Skill 最小合同。临时数据库 0001→0048 重放和本地 runtime 迁移均通过。

## Phase 8 准入状态

Phase 8 软件边界已完成，当前用户态明确为 `NOT_ADMITTED`；内部 capability 仍为 `NOT_ENTERED_BY_DESIGN`，用于保留精确原因而不是对外冒充已准入。活动 `shadow-evaluation-policy-v2` 与 adaptive prerequisite 已统一为：`12` 个月观察、`100` 个独立决策、`5` 个 qualifying walk-forward folds（每 fold 至少 20）、`3` 个 qualifying market regimes（每 regime 至少 30）。当前真实 gap 分别为 `12 / 100 / 5 / 3`。

真实 `phase8-status --study-id shadow-study:1a45f7ee4b35d23b545bae881c751566ef5d91f60c65aa2b7b234a22089fc9de` 返回 `phase7_audit_status=PASS`、`PHASE7_EVALUATION_NOT_RUN`、`PHASE8_ADMISSION_NOT_AVAILABLE`，并保持 `adaptive_weights_enabled=false / online_learning_allowed=false / main_paper_ledger_write_allowed=false / broker_execution_allowed=false`。即使未来 deterministic admission 达到 `ELIGIBLE_RULE_STATE_MACHINE_RESEARCH`，下一步也只是等待显式、版本化 research approval，不会自动打开在线调权或主账本。

## 自然语言产品面与 Portfolio Manager（2026-08-11）

本轮按最终用户体验重新审查了仓库、Repo Skills 与 Phase 0—8 主链，并补齐此前缺少的组合专业域和“买卖计划解释”产品层。现在网页端 Agent 不需要用户知道内部 Phase 或 artifact 名称：`$astock-research-orchestrator` 会将“某公司怎么样 / 某股票能不能买 / 什么位置进与如何退出 / 评估组合 / 推荐几只股组成组合”分别路由到候选、公司研究、TradePlan、Portfolio、Holding、Evidence 与 Paper Recovery 能力。

### Candidate → 单股研究 → 组合

- 新增 `candidate-input-schema / candidate-input-stage / candidate-input-run`。`candidate-input-run` 在本地将一个严格 `CandidateInputRelease` 先冻结再直接运行 candidate scan，避免网页端为了同一输入再复制一份大 `CandidateScanRequest`，降低上下文与 token 消耗；候选仍只表示“值得研究”，不能直接生成 BUY、目标价或组合权重。
- 单股路径继续由通用 Research Runtime 完成。只有 current `APPROVE_SIMULATION` 的 `ClassifiedTradeProtocol` 能进入 Portfolio Construction；`REJECT / WATCH / NEEDS_INFO` 不能被任何组合优化器覆盖。
- Portfolio candidate admission 会重新验证 final protocol 自身、DecisionPack、underlying Committee TradeProtocol 与 TradingClassification 的 exact artifact/type/hash/ObjectStore lineage，并要求所有候选共享同一 construction `as_of`；classification 的 company/board/risk/special/price-limit 投影也必须与 final protocol 一致。

### TradePlanView

新增 `TradePlanView` 与 `trade-plan-view` CLI。它从冻结的 `ClassifiedTradeProtocol → DecisionPack / Committee TradeProtocol / TradingClassification` 生成面向普通用户的可读交易计划，包含：委员会收益与下行情景、confidence、最大模拟仓位、entry/position-size/price-stop/volatility-stop/trailing-stop/time-stop/take-profit/thesis-invalidation、review events、最大持有期及交易制度分类。

若底层没有结构化的精确价格证据，合同固定：`exact_entry_zone_available=false / exact_exit_target_available=false / scenario_prices_are_targets=false`。用冻结参考价换算的委员会收益/下行区间只叫“情景价格区间”，不得冒充目标价预测。TradePlanView 的子对象时间使用 final protocol 的冻结 `as_of`，同一 protocol + reference price 重跑保持同一 view identity/object hash。

### Portfolio Analysis

新增 `PortfolioAnalysisRequest / PortfolioAnalysisReport`、`portfolio-paper-evaluate / portfolio-evaluate / portfolio-status / portfolio-audit`。组合风险矩阵只读取 request `as_of` 时点可见的不可变日线 release；仅在 `--live` 且请求时点接近当前时间时允许补同步，历史分析不因当前联网成功获得未来数据。

报告当前确定性计算：

- constant-weight historical annualized return（只作历史描述，不作预测）；
- annualized volatility 与 downside deviation；
- Beta、Tracking Error；
- max drawdown；
- historical VaR95 / CVaR95 / CDaR95；
- concentration HHI / effective number of positions；
- pair correlation；
- marginal risk contribution；
- invested/cash weight；
- 显式提供的行业/风险组暴露；
- 与 committee `max_total_exposure / max_single_position / max_industry_exposure / max_abs_correlation / max_portfolio_drawdown` 的风险门比较。

未复权日线用于 PIT 风险诊断时明确报告 `UNADJUSTED_DAILY_RETURN_SERIES`，单日绝对收益超过 35% 时提示公司行动复核；不会静默把潜在除权跳变解释为经济收益。真实 runtime 中 `portfolio-paper-evaluate --account-id paper` 在账户未初始化时已经实跑为结构化 `NEEDS_INFO`，不会抛出未处理异常，也不会写账本。

### Portfolio Construction

新增 `PortfolioConstructionRequest / PortfolioConstructionReport` 与 `$portfolio-manager` Skill。结合本轮 GitHub、技术文档和组合投资社区调研，项目没有引入“单一最优 Max-Sharpe 黑盒”，而采用“稳健基准 + 风险模型对照 + 统一约束”的结构：

1. `EQUAL_WEIGHT_CONSTRAINED`：默认基准；
2. `INVERSE_VOLATILITY`；
3. `HIERARCHICAL_RISK`：HRP-style 相关簇与簇内风险递归；
4. `SHRINKAGE_MIN_VARIANCE`：Ledoit-Wolf covariance shrinkage + long-only minimum variance。

纯数学已从 service 拆分为无 I/O 的 `portfolio/analytics.py`；service 只保留 PIT 数据读取、artifact lineage、只读账本、组合准入和约束编排。这降低了维护复杂度，并让网页 Agent 正常任务只需打开压缩 report/schema/service，而无需把全部数值算法加载进上下文。

四种方法统一先受现有委员会约束；无法安全分配的部分保留现金。当前没有已验收的申万/中证等正式行业 taxonomy release，因此 `risk_group` 明确标记 `RISK_GROUP_IS_CALLER_SUPPLIED`，group limit 当前只是 provenance 已披露的辅助约束，不冒充认证行业门。默认方法保持受约束等权，只有未来真实 Phase 7 样本外比较证明稳定增量价值后才允许版本化调整。

定向产品回归覆盖：组合数学/约束、缺账户 fail-closed、报告持久化与 audit、四模型提案、父工件 hash 篡改拒绝、generic Runtime → TradePlanView 幂等 → Classified PaperExecution、CLI 可发现性与 9 个 Repo Skills 路由。Portfolio/TradePlan 产品定向组已持续保持全绿；最终全仓测试统计见下文发布门。

### Research Seeds：Market + Existing Candidate + Expert Skills

最终荐股探索层新增 `ResearchSeedRequest / ResearchSeedReport / ExpertDomainProfile` 与 `research-seeds / research-seeds-status / research-seeds-audit`。ResearchSeed 的权限边界固定为 `recommendation_allowed=false / candidate_record_write_allowed=false / paper_ledger_write_allowed=false / broker_execution_allowed=false`，并强制 `requires_candidate_evidence=true / requires_deep_research=true`。

Market Seeds 使用当前公开市场快照的价格、成交额、换手率和流通市值，但优先级只使用成交规模、流通市值和换手的横截面百分位，不使用涨跌幅方向；默认剔除名称含 ST/退市标记以及低于最低流动性/规模门的标的。目的只是从全市场压缩研究范围，不是动量荐股。

Expert Seeds 直接读取当前已发布 visual/composite Skill registry，不硬编码作者行业。系统把每位作者 admitted Skills 的 `skill_name / decision_question / core_principle / applicable_conditions / required_evidence / positive_signals / negative_signals` 与当前公开行业板块 taxonomy 做确定性词项匹配和严格同义词扩展，仅保留达到最小 Skill 数与 Skill 占比门槛的领域；相同支持 Skill 集合命中的父/子板块会去重。之后冻结对应行业成分股快照，并与通过市场流动性/规模门的股票交叉生成 Expert Seeds。行业 taxonomy 和社区 Skill 都只决定“研究范围”，不能充当公司正式事实。

2026-08-11 真实 live smoke：

- composite registry：`knowledge-registry-v2:abbd793d4a3dc08bb1dd239cf2822297e6f46451142882734da8849f5fbb5c53`，object hash=`cac3c6ac8c5f1c4b37fba7f913d7fca3d83eef1d1695e97e38f5ba7f9897e82b`；
- latest ResearchSeedReport：`research-seeds:aec34069e44a3d922a4c7dfb727c0764991ff37a9d2f9849c50c62d2e9b59d6f`；
- report object hash：`9b82683d1f5c75154dc6c48211ce422594e3e083db795aeb96f28bfb3205f77e`；
- status=`READY`，Market Seed count=`10`，Expert Seed count=`3`，Existing Candidate Seed count=`0`；
- 黄彦臻的当前行业型 Skill 密度门识别为“房地产、半导体”；派大星皮皮识别为“半导体、光伏主材”；MR Dang 当前 112 条 admitted visual Skills 没有任何公开行业板块同时达到 `>=3 Skills` 且 `>=1.5%` 的默认行业密度门，因此保持零行业型 Expert Domain，而不是强行贴标签；其通用行业/估值方法仍继续由 Deep Research 使用；
- `research-seeds-audit`：`PASS / finding_codes=[]`；
- 该 live run 未创建 CandidateRecord、委员会结果、模拟订单或券商操作。

Research Seeds 相关单测验证：市场 Seed 打分不受涨跌方向影响；作者 Skill 内容改变会确定性改变 ExpertDomainProfile；Market/Expert 来源能在同一 Seed 合并；报告与 snapshot/registry lineage 可 audit。

### Seed → Candidate 自动 Promotion

新增 `SeedPromotionRequest / SeedPromotionReport / SeedPromotionTask` 与 `research-seeds-promote / research-seeds-promote-status / research-seeds-promote-audit`。Promotion 只对有界 ResearchSeed 集合执行昂贵取证：复用已有 `RESEARCH_READY` Candidate；同步/核验 instrument、calendar、未复权 daily；生成 daily quality；冻结 CNINFO 官方公司行动 absence baseline；完整枚举公告；复用或运行 FinancialIntegrity；证据完整公司自动形成并冻结 `CandidateInputRelease` 后立即运行 Candidate Scan。单只公司缺口会形成 retryable task 并隔离，不拖死其它 Seed。

为避免自动化反向降低 Candidate universe 门，普通 `DatasetReleaseManifest/INSTRUMENT_MASTER` 仍要求候选集合与 typed master exact match。Promotion 独占的 `CandidateInstrumentUniverseProof` 绑定 `ResearchSeedReport artifact/hash → parent Instrument Master artifact/hash/release → exact bounded company/instrument subset → source snapshots/as_of`；verifier 逐项重新验证 parent release、PIT head、InstrumentRecord 字段和 object hash。单测证明：full-market master 不能直接证明任意单股 subset；同一 parent master 经合法 Seed proof 可证明精确 subset；篡改 parent object hash 会 fail-closed。

2026-08-11 live smoke 使用最新 `ResearchSeedReport:research-seeds:aec34069e44a3d922a4c7dfb727c0764991ff37a9d2f9849c50c62d2e9b59d6f` 的 Top-1 Seed `603986`。当时 BaoStock instrument reference 连接失败，Promotion 没有猜测可交易状态，也没有要求手工 JSON，而是冻结 `SeedPromotionReport:seed-promotion:0582f7f29d20bf55c483ea9c154c4461fc39a8747b9f4ec630d6debf9a7a889a`，object hash=`43b13777db8d50cff9d1dc0c658d8576a0c52c3b784c0d957ef20f46691b3716`，status=`NEEDS_INFO`，task=`REFERENCE_INPUT_REQUIRED / INSTRUMENT_TRADABILITY_REFERENCE_UNAVAILABLE`，promoted=`0`、Candidate=`0`、paper/broker 权限=`false`。`research-seeds-promote-audit` 返回 `PASS / finding_codes=[]`，证明外部源失败时主链可恢复且 fail-closed。

Promotion + Candidate 原合同 + Serenity/Juglar focused 回归在最终全仓发布门前保持全绿；最终测试统计见下文“当前验证和门禁状态”。

## 当前 live/生产边界

- 系统没有自动实盘或券商订单接口；reviewed Skill 只允许 shadow 调用，未获正式委员会生产权重。
- 默认模拟账户双重记账平衡；当前运行状态为 0 订单、0 成交、0 持仓。
- Provider registry、免费 reference、财务来源和候选注册表均有 recorded 路径；live 必须显式开启。
- 东财/Sina 财务数据均为 `SECONDARY_STRUCTURED`；只有官方原生 PDF 精确证明的事实才可进入正式财务审计。
- 候选只表达研究优先级，不包含交易方向、目标价、订单或持仓。真实生产候选宇宙尚未生成。
- DuckDB 在代码中未直接使用（仅为设计原则声明）。

## Skill / Workflow Agent 文档架构（2026-08-12）

- Repo 现有能力并非没有 Skill：9 个 canonical Agent Skill 已固定在 `.agents/skills/*/SKILL.md`，由 ChatGPT/Codex 的 repo-skill discovery 直接加载。为避免出现两套 `SKILL.md` 日后互相漂移，新增顶层 `skills/README.md` 仅作为人类可见能力索引，不复制实现。
- 新增 `docs/workflows/` 作为跨 Skill 用户任务的 canonical 编排层，共 10 条 Workflow：Current Company Research、Candidate Discovery、Evidence Recovery、Financial Integrity、Committee & Trade Plan、Portfolio Construction、Holding Monitoring、Paper Trading、Knowledge Ingest、Prospective Evaluation。Workflow 只描述步骤、依赖、并发、fallback、停止条件和投资者输出，不替代 Python/Schema/Config 硬门禁。
- 每个 canonical Skill 新增 `## Workflows` 反向链接；总控 Skill 链接 current-company / candidate / portfolio / prospective 四条主链，其余 Skill 至少链接一个主要 Workflow。`README.md` 与 `AGENTS.md` 同步说明 canonical Skill 与 Workflow 的维护边界。
- 新增 `tests/unit/test_workflow_docs.py`，锁定：顶层 `skills/` 不得出现第二套 `SKILL.md`；Workflow 文件集合必须完整且具备 `When to use / Flow / Stop conditions`；所有 Repo Skill 必须链接真实存在的 Workflow；当前单股/证据 Workflow 必须保留 provider fallback → authoritative Web → manual-last 顺序。该合同与原 `test_repo_skills.py / test_product_surface.py` 一并通过。

## 2026-08-29 v0.1.1 自动补证、BJSE 韧性、性能与投研 Skills 增强

- **北交所正式来源**：新增 `bse-official-reference`，以北交所官方证券列表作为 BJSE `instrument.master / instrument.identity / instrument.bjse_coverage` 的 PRIMARY_OFFICIAL_WEB 主源；分页逐页冻结 raw snapshot，并同时校验 `totalElements / totalPages / lastPage`、重复证券、页数和聚合 denominator。durable run 的 10 轮稳定性矩阵均为 339/339、17 页、terminal proof 完整；本轮最终 live smoke 再次得到 `339 / denominator=339 / 17 pages / passed=true`，`broker_execution_allowed=false`。
- **自动换源与健康恢复**：Provider probe 的 registry/capability 配置漂移不再被误判为本地损坏，而是回到 `NOT_PROBED`；真正的 event/artifact/hash 链损坏仍为 `CORRUPT`。Source circuit breaker 从 provider+capability 进一步按市场/路由 operation 分桶，XSHG 的 coverage failure 不再污染 XSHE，EastMoney exact identity 故障也不再封死 paginated-master fallback。
- **同请求自动补证**：新增 typed `CurrentResearchContinuationService`、CLI 与 schema，固定 `ACQUIRE → AUTO_RESOLUTION_REQUIRED → REACQUIRE/ADMIT → TEAM_RESEARCH_REQUIRED → READY_FOR_INVESTOR_VIEW | OBSERVATION_ONLY_FOR_INVESTOR_VIEW | NEEDS_USER_INPUT`。公开 Web 补证必须先通过 SourcePolicyGate 并冻结 `OfficialWebDocumentCapture + SourceSnapshot + PIT + artifact hash`；只有自动轮次/时间预算真实耗尽或必须取得用户私有材料时才允许转人工。独立 Review 进一步修复三条恢复边界：最后一个自动轮次若已经成功冻结正式证据，必须先做一次不增加 resolver 轮次的确定性 reacquire 消费证据，仍有缺口才立即升级人工；崩溃/重连后已为 `EVIDENCE_BOUND` 的 task 必须先 `resume()` 消费，不得重复调用外部 resolver；ingest 返回的 raw capture id 与 canonical `OfficialWebDocumentCapture:*` id 必须解析到同一工件，并把同一 capture object hash 写入 continuation 直接 `input_hashes`。
- **600519 真实补证链**：已从上交所官方域冻结贵州茅台 2025 年年度报告与 2026 年半年度报告。年度报告 capture=`official-web-capture:e2398604dba66af61538780b2509df589d2bbb6edb2e9a6795b7d6103245cb2a`；半年度报告 capture=`official-web-capture:9cbad073c7dbbcce7ea87d5f134994069036599e592f7fab00df61f311809aec`。正式财务链可消费已冻结官方报告并得到 `CERTIFIED`；证券身份仍优先结构化 Provider，只有同市场交易所官方、公司代码匹配且 snapshot/object/admission 全部可验证的 capture 才可作为受控补充，不接受普通搜索结果或跨交易所证据。
- **财务存储/并发修复**：相同逻辑财务记录在不同 `available_to_system_at` 重抓时，immutable Parquet 文件名现在同时绑定 logical hash 与 availability，避免旧文件被错误复用；request-scoped `FinancialSourceService` 对非线程安全的正式 sync 入口做单实例串行化，既保留上层 acquisition 的 capability 并发，也消除 annual/interim 共用实例时的 Parquet/PIT 竞态。真实并发复测 annual + 2026H1 均为 `CERTIFIED`。
- **PIT 幂等语义**：同一 source_id、同一冻结对象内容的重复抓取可复用最早 PIT identity；后续调用即使带来更丰富但与已有非空语义不冲突的描述字段，也不会反写或改造已经冻结的最早 PIT 记录。已有非空语义冲突、修订关系变化或对象内容变化仍抛 `PIT source identity collision`。没有放宽历史可用性或修订链规则。
- **性能与复杂度**：same-request acquisition 只重跑失败 capability；确定性回归 `test_same_request_reuse_reruns_only_failed_capability_and_preserves_lineage` 的初次 5 个 capability 保持不变，补证续跑由 5 次降为 1 次，即外部 capability 执行数下降 **80%**。成功复用要求 policy/schedule/company/market/lookback/planner 全匹配、source snapshot/object hash 可验证、依赖已复用且仍处在同一自动预算与上海交易日期边界；失配或损坏直接 cache miss。
- **多 Agent 投研方法**：新增并接入六个 canonical Skills/Workflows：`macro-policy-regime`、`industry-value-chain`、`catalyst-event-research`、`governance-management-quality`、`investment-red-team`、`model-risk-backtest-validation`；分别绑定现有 Research Team role/output/readiness，不新增第二套 Agent scheduler、Router 或事实库。
- **真实命名单股 terminal smoke**：600519 continuation 已真实推进到 Research Team 全 DAG。为验证编排而生成的 smoke role artifact 明确 `formal_claims_allowed=false`，财务完整性 smoke pack 明确 `NEEDS_INFO/PARTIAL`，因此 Recommendation Gate 正确 `BLOCKED`，continuation 终态为 `OBSERVATION_ONLY_FOR_INVESTOR_VIEW`、`investor_view_allowed=true`、`formal_recommendation_allowed=false`、`broker_execution_allowed=false`；系统没有用 smoke 工件制造 BUY/SELL 或正式推荐。
- **真实全市场漏斗 smoke**：`research-seeds --live` 可产生 20 个研究 seed；BJSE `market_coverage_ratio=1.0`，但 XSHG/XSHE denominator 仍未被正式证明，因此 `universe_coverage_status=PARTIAL / formal_full_market_coverage_allowed=false / recommendation_allowed=false / candidate_record_write_allowed=false / broker_execution_allowed=false`。候选种子没有被冒充正式荐股。
- **定向回归**：财务/Provider/PIT/continuation/reuse、Research Team 与 Repo Skill/Workflow 合同均已纳入全仓回归；request-scoped reuse 的确定性性能合同继续证明补证续跑由 5 个 capability 降为 1 个（80% execution reduction），没有引用不存在的外部 benchmark 脚本。完整冻结树工程门与全仓统计见下一节。

## 2026-08-31 v0.2.0 Portfolio & Holding Decision Skills v1

- **用户外部持仓事实**：新增 `UserDeclaredTradeCapture → ValidatedExternalTradeImport → ExternalTradeImportReceipt`，完整且无歧义的外部既成交易经可见证券身份校验后通过 `LocalPortfolioService` exactly-once 进入 Git-ignore 的 `trades.md`，再确定性回放 `portfolio.md`。重复陈述返回 DUPLICATE；缺数量/价格/实际成交时间不造持仓；与 `PAPER_FILL` 的同一经济事实冲突时 fail closed。历史补录额外校验成交日在证券上市/退市期间内。
- **跨会话持仓恢复**：新增 `UserPortfolioSnapshot`，冻结当前持仓、首次建仓时间、最近交易、数量、平均成本、最近复核和 open orders；其 artifact 直接绑定 `LocalPortfolioStateBoundary` object hash。未记录账户级现金时 `cash_known=false`，不推断 NAV，也不重复询问已经持久化的购买时间/数量/成本。
- **PIT 总回报组合研究序列**：执行/数量继续使用 raw 未复权价格；组合相关性、回撤、风险贡献改用 `DAILY_UNADJUSTED + 当时可见且 TERMS_VERIFIED 的 CorporateActionObservation` 派生 gross total-return research series。现金分红、送股/转增具有定向回归；未来可见公司行动不能改变过去结果，未验证 action 不应用。公司行动 release/hash 进入组合 artifact lineage。
- **组合迁移**：新增 `PortfolioIntentProfile / PortfolioRiskGap / PortfolioTransitionRequest / PortfolioTransitionReport / PositionTargetBand / RebalanceBandPolicy`，复用现有四个 allocator，不建立第二套 Portfolio 数学。报告比较 `CURRENT / ANCHOR_ONLY / TARGET`，输出目标权重带、现金、换手、实施成本、硬约束和可执行数量区间；目标带上沿同时受 total/single-position 上限约束，目标组合还必须通过相关性、回撤及用户 beta/波动/换手等终局门。若一手交易会越过硬目标带，则不给伪精确数量。
- **no-trade region**：目标不再被解释为必须立即回到点权重。当前 weight 落在版本化 target band 内保持 HOLD；cost 未验证时带宽更保守，减少小幅漂移导致的机械换手。
- **风险缺口驱动股票预筛**：新增 `PortfolioComplementScreenRequest/Report`，只读取已注册 `ResearchSeedReport`，排除当前持仓，再以当前组合 PIT correlation/beta、seed liquidity 和 research priority 做 bounded research-only 预筛。负相关在 DIVERSIFY 预筛中优于正相关；单个候选历史缺失只跳过该候选并留 warning。报告固定 `recommendation_allowed=false / portfolio_weight_allowed=false`，最终股票成员仍必须完成公司深研与 Committee。
- **ETF 正式研究基础**：新增 `InstrumentType.ETF`、`ETFProductProfile`、`InstrumentTradingUnitRule`、`ETFResearchMetrics`。Profile 绑定 PRIMARY_OFFICIAL_WEB capture、source-time、跟踪基准、管理/托管/总费率和 instrument-specific 交易单位；Metrics 在同一 `as_of` 上从本地 PIT 日线冻结平均日成交额、波动率和 tracking error。当前没有正式 ETF NAV/iNAV 连续序列，所以 `premium_discount_rate=None` 并显式标记不可用，不从 Web 摘要或二级市场价格猜折溢价。
- **ETF 执行安全**：EastMoney/Sina intraday capability 可描述 ETF 行情，但 `PaperOperation` 与 `PaperReplay` 当前显式只准入 STOCK。ETF 仍是研究/组合评估基础，不代表 ETF 模拟下单上线；股票 100 股/T+1/费用规则不会被静默套给 ETF。
- **对冲语义**：正式区分 `DIVERSIFICATION / NATURAL_HEDGE / EXPLICIT_HEDGE / UNPROVEN`。`HedgeEffectivenessReport` 重新计算 normal/stress PIT correlation 与指定风险前后；压力相关不可计算时不会被当成低相关。正式 `NATURAL_HEDGE` 还要求 PRIMARY_OFFICIAL_WEB 经济机制、同一 `as_of` 的 ETFResearchMetrics、风险改善达到版本阈值、已验证往返成本不高于独立 bps 上限。风险指标与费用不做跨量纲相减。当前 long-only 系统永不产生 `EXPLICIT_HEDGE`。
- **持仓事件与再平衡**：`HoldingReviewRequest / HoldingReviewPack / PositionActionProposal` 已加入 `THESIS_INVALIDATING / THESIS_WEAKENING / THESIS_STRENGTHENING / VALUATION_ONLY / PORTFOLIO_RISK_ONLY / TEMPORARY_NOISE / UNVERIFIED_LEAD`、portfolio effects、typed target bands/quantity、implementation cost、preconditions 和 reversal conditions。未验证 lead 只能 REVIEW；material thesis/valuation 事件必须绑定本轮 evidence；legacy ADD 规则仍要求 ADD rule 自身 evidence；`PORTFOLIO_RISK_ONLY` 可在公司逻辑未变时提出 TRIM。
- **投资者产品路由**：专业化 `$portfolio-manager`、`$holding-monitor`、`$continuous-investment-monitor` 与 `$astock-research-orchestrator`，新增 `PLANNED_PURCHASE_PORTFOLIO_COMPLETION` 与 `HELD_POSITION_REBALANCE_REVIEW`，并新增 `workflow-portfolio-transition-and-hedging.md` / `workflow-holding-rebalance-decision.md`。旧 registered-output、Committee、Phase 7/8、current fallback、continuous-monitor task、paper order/fill 合同保持兼容。
- **稳定 CLI**：新增 `portfolio-decision-schema`、`portfolio-import-declared-trade`、`portfolio-local-snapshot`、`portfolio-etf-profile-register`、`portfolio-etf-metrics`、`portfolio-complement-screen`、`portfolio-hedge-evaluate`、`portfolio-transition`、`portfolio-decision-status`、`portfolio-decision-audit`。组合/对冲/持仓正式报告不直接写 paper ledger，真实券商接口仍不存在。
- **安装态项目根解析**：独立 wheel smoke 暴露出多处 `Path(__file__).resolve().parents[3]` 在 `.venv/Lib/site-packages` 安装态会错误指向 `.venv/Lib`、进而找不到顶层 `configs/`。现新增统一 `resolve_project_root()`：优先校验显式 `ASTOCK_PROJECT_ROOT`，否则从 cwd 与模块路径向上寻找项目标记；找不到即 fail closed。Serenity audit-pinned `src/astock/research/skills.py` 未修改，保持冻结 hash。修复后安装态 BSE live smoke 恢复为 339/339、17 页、official/raw lineage 全通过；安装态 `research-seeds --live` 也重新成功生成 20 个 seed，并继续保持 XSHG/XSHE coverage 未证明时正式推荐/候选落库/paper/broker 全关闭。
- **独立 defect-first Review**：本轮 Review 实际发现并返工了 source-time PIT 泄漏/冻结 as_of 隐式 live capture、成本与 beta/HHI/波动的跨量纲相减、zero-variance stress correlation、target band 超 single-position 上限、target 相关/回撤/beta/波动终局门缺失、odd-lot/交易增量舍入、ETF enum 扩权到 paper execution、历史成交上市期、单坏候选拖垮整批、policy lineage 缺失、ETF 费率/tracking/liquidity 不足及 Skill 兼容合同回退等问题。修复后相关综合矩阵达到 `69 passed / 0 failed`，Review-focused PIT/hedge/holding/paper subset 为 `37 passed / 0 failed`，最新 ETF/PIT 变更后的 targeted matrix 再次 `57 passed / 0 failed`，Repo Skills/Workflow 合同 `17 passed / 0 failed`。
- **性能证据**：20 次确定性 transition 的 P95=`0.089104s`；CURRENT/ANCHOR/TARGET 共享同一历史输入，每次 `_history` 读取 3 次，而把三种 variant 各自独立读取需要 9 次，输入读取下降 `66.67%`；该结果超过开发计划“至少减少 60% 重复行情/风险输入读取”的目标。
- **明确限制**：认证行业 taxonomy 尚未落地，因此用户显式 `max_industry_exposure` 不能用 caller-supplied risk group 冒充正式行业约束，transition 直接 fail closed；ETF NAV/iNAV/折溢价、ETF paper execution、融券/期货/期权、保证金和真实券商执行均保持未实现/禁用。Black–Litterman 仍只是长期 challenger 计划，不属于本次生产能力。
- **发布终态**：v0.2.0 已完成最终冻结树验收、构建、安装态 smoke、显式暂存、secret/private/runtime 审计、commit、push、annotated tag、GitHub Release、远端资产 digest 校验和 clean-worktree 检查。release baseline 为 commit `b776d0054b60d96f5c4da41b10926c1e583bc699`，annotated tag `v0.2.0` 与 GitHub Release 均指向该提交；发布后的纯文档收尾提交不改变该版本基线。

## 2026-08-31 开发工作流自动收尾规范加固

- **根因**：旧 `AGENTS.md` 虽然要求“完成任务从开发计划删除、事实迁入验收报告”，但强制阶段只写到“文档迁移 → 发布”，没有定义 release 完成后的第二次 tracked 文档归档；commit SHA、tag/Release、remote asset digest、最终 frozen-tree 数字又只能在 release 后成立，因此旧流程允许 durable run 在这些事实仍只存在于 Git-ignore evidence 时直接 COMPLETE。
- **机器合同冲突**：v0.2.0 release baseline 上的 root-doc contract 还曾强制 `开发计划.md` 保留已经完成的 `External Dependency Resilience v1` 名称，与“开发计划只保存未完成项”相冲突；该合同已在后续文档收尾中改为 completed-name negative assertion。
- **新闭环**：正式开发状态机现在固定为“发布前文档迁移 → release baseline 发布 → 发布后文档归档与终局核验 → docs-only closeout commit/push → 终局 Review → `long_run_complete`”。发布不再被视为流程终点。
- **零残留门**：终局 Review 前必须按当前 durable run title、step id/title、版本主线和完成态关键词检查 `开发计划.md`；除了明确未完成并重新归类的长期运行/数据义务外，完成任务、进度、历史发布过程和当前状态中列举的已完成项目名必须为零。
- **分级验证提速**：开发流程压缩为 5 个门，并按 L0–L3 决定验证强度。文档/流程类 L0 默认只跑文档合同/文本检查、必要格式/静态检查和 `git diff --check`；不再为纯规范改动机械执行 20 分钟级全仓 pytest。只有影响运行时代码、PIT/Universe/财务/估值/账本/安全/迁移/正式发布时才逐级升级到 targeted、全仓、真实 smoke 或发布资产验证。
- **临时文件清理**：终局 diff 审计前增加清理硬门，仅删除本任务生成且可证明可再生的临时脚本、一次性日志/XML、临时 build/smoke 目录、工具缓存和已被正式证据替代的编排副产物；`runtime/` 正式对象/数据库、`user_state/`、私有材料、Release 资产、未知来源文件和其它会话/用户已有未提交改动均禁止自动删除。
- **Tag/主分支语义**：已发布 tag 永久固定 release baseline；发布后文档归档使用新的 docs-only closeout commit 推进 `main`，不得移动 tag。终局一致性改为 `tag commit == release baseline commit`、`origin/main == closeout commit`、closeout 是 tag 的后代，而不是错误要求发布后继续 `HEAD == tag commit`。
- **合同加固**：`tests/unit/test_original_instruction_contract.py` 新增发布后收尾契约，要求 `AGENTS.md` 明确上述阶段、计划零残留、docs-only closeout 和 `long_run_complete` 门，并禁止《验收报告》重新出现 v0.2.0 的发布前措辞。

## 2026-09-01 M-02 结构化日志与错误摘要完成

- **目标**：落地 M-02——结构化日志、错误摘要映射、secret 脱敏、correlation/request/run ID、operational event schema、自动恢复预算统一到 1800s，并消除记录日志中的法律/安全噪音。
- **变更范围**：15 个生产/配置/测试文件修改 + 2 个新文件 + 1 个新测试文件（60 条）；核心入口：`core/logging.py`（847 行，含 `_DynamicStderrHandler`）、`core/errors.py`（292 行）、`schemas/operational.py`（60 行）、`configs/logging_policy.yaml`（39 行）、`tests/unit/test_operational_logging_redaction.py`（60 条测试）。
- **AC1 日志事件分层/轮转/保留/损坏恢复/磁盘上限**：`DailySizeRotatingFileHandler` 支持日期+大小双触发；`_prune_expired_logs` 按 `retention_days` 清理过期文件；`_enforce_log_budget` 按 `maximum_file_bytes` 强制上限；`_recover_corrupt_tail` 检测并隔离损坏末行；`configs/logging_policy.yaml` 定义 rotation/retention/disk-bound/sink config。测试验证：policy 加载/校验、rotation 判断、budget enforcement、corruption quarantine 和 BoundedQueueHandler 不阻塞。
- **AC2 429/超时/5xx/解析/冲突/陈旧数据稳定用户摘要与关联号**：`FailureClass` 枚举覆盖所有错误类别；`PublicErrorMapper` 为每种错误产生稳定的非敏感投资者摘要+开发者摘要；`.safe_to_send` 类型为 `Literal[True]`，`.raw_error_exposed` 为 `Literal[False]`；correlation_id 自动注入或显式传播。测试验证：429/timeout/connection/5xx/conflict/stale/parse/schema_drift/所有 FailureClass 枚举/异常类型/数字码/连字符码/未知异常 fallback/ID 传播 自动生成共 19 条。
- **AC3 Secret/Cookie/Authorization/密码/私人路径/请求体脱敏**：`_SECRET_VALUE_RE`、`_SECRET_KEY_RE`、`_HEADER_SECRET_RE`、`_URL_USERINFO_RE`、`_PRIVATE_PATH_RES` 五组正则 + `_REQUEST_BODY_KEYS` 集合确保 token/key/password/API key/URL userinfo/内部路径/请求体不泄露；`sanitize_for_log` 递归应用 depth limit、cycle detection、binary truncation；`max_event_bytes=65536`、`max_message_chars=4096` 兜底截断。测试验证：authorization/cookie/password/api_key/access_token/url_userinfo/windows_path/unix_path/dict secret keys/request bodies/depth limit/cycle/binary/path 共 14 条。
- **AC4 current research 与 research team 只读取同一1800秒政策源**：`configs/current_research_policy.yaml` `automatic_resolution_budget_seconds=1800` 为唯一权威源；`configs/research_team.yaml` 不含该字段；`load_default_current_research_policy(project_root)` 读取同一源。测试验证：canonical 1800s/research_team no key/construction validation/no stale constants 共 4 条。
- **Regression fix（本轮 Review 发现）**：`configure_logging()` 原先使用 `id(sys.stderr)` 作为配置缓存键的一部分，导致 CliRunner 替换 stderr 时触发不必要的重配置，QueueListener 线程持有已关闭的 stderr 引用。修复：(1) 引入 `_DynamicStderrHandler`，在 `emit()` 和 `flush()` 中动态引用 `sys.stderr` 而非捕获构造时引用；(2) 从配置缓存键中移除 `id(sys.stderr)`，改用 policy value equality；(3) `BoundedQueueHandler.enqueue()` 对 fallback 的 `ValueError`/`OSError` 做安全捕获。此修复消除了 `test_cli_prepare_success_and_fail_paths` 在完整测试套件中的 `ValueError('I/O operation on closed file.')` 回归。
- **确定性证据**：`uv run pytest tests/unit/test_operational_logging_redaction.py` → `60 passed / 0 failed`；`uv run pytest tests/unit/test_current_research_productization.py tests/unit/test_direct_source_distillation.py tests/unit/test_direct_source_real_run.py tests/unit/test_presentation_gateway.py tests/unit/test_continuous_monitoring.py tests/unit/test_paper_operations.py tests/unit/test_local_portfolio.py` → `365 passed / 0 failed`；`uv run ruff check .`=`All checks passed`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`git diff --check`=`PASS`。
- **风险分级**：L1（单模块低风险），仅 targeted regression + lint/typecheck；不涉及 PIT/Universe/财务/账本/安全/迁移，无需全仓 pytest。

## 当前验证和门禁状态

- `knowledge-semantic-model-status` 为 `AVAILABLE`；direct admitted registry 已发布，`KnowledgeSkillProvider=READY / REGISTRY_READY`。既有知识资产审计保持只读有效，本轮 Portfolio/Holding 发布没有修改其冻结事实。
- **最终冻结树全仓测试**：`1125 passed / 18 skipped / 0 failed`，exit code=`0`，耗时 `1444.64s`。18 个 skip 均为显式 opt-in 的 acceptance/live/private 测试；没有删除失败测试、弱化断言或把失败改成 skip。
- **工程门**：`uv run ruff check .`=`PASS`；本轮 35 个变更 Python 文件 `uv run ruff format --check ...`=`PASS`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`git diff --check`=`PASS`；staged secret scan=`PASS`；staged private/runtime audit=`PASS`。
- **本地状态门**：SQLite 只读完整性检查=`PASS`，`integrity_check=ok`；Local Portfolio audit=`PASS`，无损坏 finding。外部既成交易 lane 与 paper ledger 继续分层，未把私有运行状态带入版本库。
- **最终构建工件**：`dist/astock_research-0.2.0-py3-none-any.whl` 大小 `1,182,288` bytes，SHA-256=`13fc4cdfcf7ff2bec70292a0104f12b5c2a5aef6ebec7f0dc31d8f2ae136db61`；`dist/astock_research-0.2.0.tar.gz` 大小 `1,800,535` bytes，SHA-256=`3083a0e690610471ab83209e15e72ec88eb9127031f9c5cf6c9eacc390a875e8`。两个 GitHub Release 资产的 remote digest 与本地 SHA-256 完全一致。
- **安装态验证**：最终 wheel 重新安装后 metadata version=`0.2.0`，`astock --help` exit code=`0`。安装态 project-root resolver 已验证不会再从 `.venv/Lib/site-packages` 错误定位到 `.venv/Lib/configs`。
- **真实只读 smoke**：BJSE 官方 Universe=`339/339`、17 页，`official_bse_provenance=true / complete_coverage=true / raw_page_lineage_verified=true / broker_execution_allowed=false`；全市场 `research-seeds --live` 产生 20 个 seed、BJSE coverage=1.0，但 XSHG/XSHE denominator 仍未正式证明，因此 `formal_full_market_coverage_allowed=false / recommendation_allowed=false / candidate_record_write_allowed=false / paper_ledger_write_allowed=false / broker_execution_allowed=false`，继续 fail closed。
- **性能门**：20 次确定性 transition 的 P95=`0.089104s`；CURRENT/ANCHOR/TARGET 共享输入后每次 `_history` 读取 3 次，对比三套方案独立重复读取 9 次，输入读取下降 `66.67%`。
- **Git/Release 终态**：`v0.2.0` annotated tag 与 GitHub Release 的不可变 release baseline 固定为 `b776d0054b60d96f5c4da41b10926c1e583bc699`，Release 为 non-draft/non-prerelease，URL 为 `https://github.com/ShiningSugar35/AStockMultiAgent/releases/tag/v0.2.0`。发布后的纯文档/合同 closeout commit 允许继续推进 `main`，但必须是该 tag/release baseline 的后代，且不得移动或重打已发布 tag；因此发布后终局不再要求 `HEAD == tag commit`。
- **安全边界**：全局 `broker_execution_allowed=false`；ETF paper execution、融券、期货、期权、保证金、显式衍生品对冲与真实券商连接均未开放。用户持有的 XLSX/PDF/SQLite/ObjectStore/Embedding 数据、本地 smoke database、`.devbridge` 编排证据、私有材料与 secrets 均未进入正式发布提交。

### 历史 v0.1.1 发布证据（只读归档）

- 2026-08-29 的历史冻结门曾得到 `1093 passed / 18 skipped / 0 failed`；随后 continuation/provenance/root-doc 合同继续演进，因此该数字只作为历史 v0.1.1 归档，不再代表当前发布终态。
- v0.1.1 曾完成 BJSE 339/339、600519 observation-only continuation 与全市场 fail-closed smoke；对应 durable evidence 保留在 Git-ignore 的 `.devbridge/lr_mtcbh3fp_663fd2b079f6_*` 文件中，不参与当前 v0.2.0 发布判定。

## 2026-07-29 原文直读输入冻结

### 两份输入的独立复核

- PDF `docs/《价值投资功法》Mr.Dang.pdf`
  - SHA-256：`31579b347d6ccd178322bc5d31c46bc888e069927591dcee8d7a2e5e1dc3e86f`
  - 文件大小：`9,079,785` bytes
  - PyMuPDF 独立读取为 `249` 页、`18` 个一级 outline 书签。
  - 第1页经渲染确认为封面，第2页为空白衬页；outline 从第3页“版权信息”开始。
- DOCX `docs/寒武纪的鳄鱼 知乎文章.docx`
  - SHA-256：`197ec18e6fabac4401f6412331e9aa50f919498d4e40cfddb481eeab9788852d`
  - 文件大小：`167,622` bytes
  - `word/document.xml` 的 direct body `w:p` 正文段落为 `2,032` 个。
  - 对每个正文段落拼接 descendant `w:t`、去除首尾空白后，以 `YYYY-MM-DD_` 标题规则识别出 `123` 篇文章。第2027和2030段标题各有1个原始前导空格；去空白后标题与冻结清单一致。

### 可恢复阅读清单

- `tmp/direct_skill_distillation_state.json` 的 schema 为 `2.0`。
- PDF 冻结为 `21` 个外层阅读批次，连续覆盖 `1..249` 页，无缺失、重叠或越界。
  - 第04章在第56/57页的完整案例边界拆分为两个自然批次。
  - 第05章在第87/88页的完整方法边界拆分为两个自然批次。
  - `b08.2` 为22页、`b12` 为24页、`b19` 为27页。三者保留为超过20页的例外：可见标题位于页面中段，按页硬切会拆断完整论证或术语分类。
  - 三个长批次的外层页范围已冻结；内部完整论证块仍须在各批次首次 Sol 原文直读时核定。
- DOCX 冻结为 `123` 个文章 section，连续覆盖 1-based 正文段落 `1..2032`，无缺失、重叠或越界；标题和段落范围与独立 OOXML 推导逐项一致。
- 输入冻结当时的 checkpoint 为 `pdf_completed_pages=[1..145]`、`docx_completed_sections=[]`、`next_section=b12`；现行 checkpoint 见文末“后续锚点”。

### 可复用 PDF 视觉证据

- book manifest：`book-manifest:52dc08cfd59bb9893a280283343965cec8e26261fe152bd016c2ff0f28edc0d8`
- visual coverage：`book-visual-coverage:73ba24ac531aaee10a2ed383fdaa83302bd7b500a0765692263ad007bf6922f2`
- visual run：`book-visual-run:e94590494783714a9f1743451ea91cbe778289ddd043c4db232c1f866bb66bf9`
- visual semantic run：`semantic-run:e0605aa01c8ddc0dffe69a81d88389d546e52b39aa89d19fc8a2012835e06ea5`
- 复用计数：249页、57个含图页、74 placements、71个非装饰 placements、映射55个 AU，其中11个 READY、44个 REVIEW。
- 状态文件只保存上述 ID、源 PDF hash 和计数，没有复制 OCR 全文。

### 当前产出边界

- 本轮通过文件：
  - `tmp/direct_skill_distillation_state.json`
  - `runtime/codex_runs/phase5-direct-source-distillation/sol_raw/pdf/b01.json`
  - …（`b02`～`b11`，同上）
  - `runtime/codex_runs/phase5-direct-source-distillation/sol_raw/pdf/b12.json`
- 本轮更新路径：`completed=b01,b02,b03,b04,b05,b06,b07.1,b07.2,b08.1,b08.2,b09,b10,b11,b12`、`next_section=b13`、`pdf_completed_pages=1..169`、`docx_completed_sections=[]`、`checkpoint=b13`。

- 本轮新增计数：`READY=6`（`SOURCING_SCREENING=1`、`FUNDAMENTAL_RESEARCH=2`、`VALUATION_PRICING=2`、`PSYCHOLOGY_BEHAVIOR=1`）、`NEEDS_USER_REVIEW=0`（`b11`）。
- 本轮新增 Skill：`6`（`SOURCING_SCREENING=1`、`FUNDAMENTAL_RESEARCH=2`、`VALUATION_PRICING=2`、`PSYCHOLOGY_BEHAVIOR=1`）（`b11`）。
- `b12` 新增：`READY=11`（`SOURCING_SCREENING=2`、`FUNDAMENTAL_RESEARCH=4`、`VALUATION_PRICING=3`、`PORTFOLIO_CONSTRUCTION=1`、`POSITION_RISK_MANAGEMENT=1`）、`NEEDS=0`。
- 六模块计数：`SOURCING_SCREENING=13`、`FUNDAMENTAL_RESEARCH=31`、`VALUATION_PRICING=23`、`PORTFOLIO_CONSTRUCTION=5`、`POSITION_RISK_MANAGEMENT=12`、`PSYCHOLOGY_BEHAVIOR=7`。
- `b06` 顶层 visual evidence 为 `image-evidence:346c827b942529a42c7db40435a32399e9d25226e46bf31d9fee4005e7996063`：SQLite 主键定位到第35页 `TEXT_IMAGE`，image/evidence 对象均通过 `ObjectStore.path_for` 两级路径和内容 hash 校验，并被1条 Skill 精确引用；`b06` 完成时视觉证据参与 Skill 累计为 `4`（`b04=3`、`b06=1`）。
- `b07.1` 的10个顶层 visual evidence 均通过 SQLite `evidence_id` 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象及 Skill 引用子集校验；类型为 `CHART=4`、`TABLE=3`、`TEXT_IMAGE=3`，本批7条 Skill 使用 visual，累计视觉证据参与 Skill 为 `11`。
- `b07.2` 的9个顶层 visual evidence 均通过 SQLite `evidence_id` 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象、canonical evidence 及 Skill 引用并集校验；类型为 `CHART=6`、`TABLE=1`、`TEXT_IMAGE=2`，本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `15`。
- `b07.2` 原5项应用校准已写入 Skill 字段且 `open_questions=[]`，不构成 `NEEDS_USER_REVIEW` 或待用户复核事项。
- `b08.1` 的第71～87页实际有8个 visual placements：7个核心 evidence 与 JSON 顶层7个 ID 精确一致，类型为 `CHART=1`、`TABLE=3`、`TEXT_IMAGE=3`；另1个第78页 evidence `image-evidence:efbf049c0f4966f8eafe6799ebe2a5c0ea42dab58877b1a6cebec7f389aec67e` 为 `DECORATIVE` 且 `decorative_excluded=true`，未进入 JSON 顶层集合或任何 Skill。8个 placements 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象和 canonical evidence 校验；本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `19`。
- `b08.2` 的第88～109页实际有9个 visual placements：8个核心 evidence 与 JSON 顶层8个 ID 精确一致，机械类型为 `TABLE=5`、`CHART=2`、`TEXT_IMAGE=1`。第97页 placement 2 `image-evidence:b8028341f2c454a577777931bacf539009e7e72fa918201df3761243990a5cb4` 的冻结机械分类仍是 `TABLE/decorative_excluded=false`，但不可变对象实际为72×72 PNG 的微小“注”图标、PDF bbox 约6.68×6.68pt、OCR 为 `NO_TEXT`；Sol 在语义层将其作为装饰排除，故它不在 JSON 顶层集合或任何 Skill 中。9个 placements 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象和 canonical evidence 校验；本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `21`。
- `b09` 的第110～122页有5个核心 visual evidence，与 JSON 顶层5个 ID 和 Skill 引用并集精确一致，机械类型为 `TEXT_IMAGE=3`、`TABLE=2`；5项均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验。本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `23`。不可审计的第115页 OCR 增广文本引用已删除，两项第115页 visual 改挂到通过原生文本 hash 校验的第114页引表引用；第121页 OCR 的 `-3000°` 和日期乱序噪声未进入任何 Skill 规则字段。
- `b10` 的第123～135页有7个核心 visual evidence，与 JSON 顶层7个 ID 和 Skill 引用并集精确一致，机械类型为 `CHART=1`、`TABLE=4`、`TEXT_IMAGE=2`；7项均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验，OCR 状态均为 `SUCCESS`，冻结 OCR 未发现进入 Skill 的异常符号或整段噪声。本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `27`。
- `b11` 的第136～145页有5个核心 visual evidence，与 JSON 顶层5个 ID 和 Skill 引用并集精确一致，机械类型为 `TABLE=4`、`TEXT_IMAGE=1`；5项均通过冻结 visual run、manifest/semantic lineage、SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验，OCR 状态均为 `SUCCESS`。本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `29`。
- `b08.2` 的22页外层范围完整覆盖第88～109页；Sol 报告8个内部完整论证块，第8块在第108～109页闭合且下一批从第110页开始。PM 只记录该语义审计并验证冻结外层范围，不自行判断或重切内部论证。
- `b09` 的13页外层范围完整覆盖第110～122页；Sol 报告3个内部完整论证块且下一批从第123页开始。PM 只记录该语义审计并验证冻结外层范围，不自行判断或重切内部论证。
- `b10` 的13页外层范围完整覆盖第123～135页；Sol 报告3个内部完整论证块且下一批从第136页开始。量子反例不单独生成 Skill 等语义取舍由 Sol 负责，PM 只记录该审计并验证冻结外层范围。
- `b11` 的10页外层范围完整覆盖第136～145页，下一批从第146页开始。首次验收为 `48/49 FAIL`：16条引用中7条的 locator 与 slice hash 正确，但 slice 首字符为 `U+0020`，无法通过 direct service 的 `paragraph_head startswith` 门；Sol 将6个唯一 locator 的 start 加1并重算对应 slice hash 后，同一49项门槛复跑为 `49/49 PASS`。失败时未修改 state 或 checkpoint。
- `b12` 的24页外层范围完整覆盖第146～169页；`schema_version=direct-source-skill-batch-v1`、`batch_text_object_hash=35f9813268f9c2404259b742243647347fd267d2d6848f8bafdebbb45395b479`、文件 SHA-256=`737b97779b9aee9b792aa3ef7b86997081c514ffdba378e6b4aebd3e4ba2acd`；11条 Skill 全部 `READY_FOR_SHADOW`，模块分布为 `SOURCING_SCREENING=2`、`FUNDAMENTAL_RESEARCH=4`、`VALUATION_PRICING=3`、`PORTFOLIO_CONSTRUCTION=1`、`POSITION_RISK_MANAGEMENT=1`；3条 `visual_evidence_refs` 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验；2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `31`。`no_skill_reason=None`、`open_questions=[]`。
- `b08.1` 应用校准已进入 Skill 字段且 `open_questions=[]`，不构成用户复核事项。
- `b07.1`、`b07.2`、`b08.1`、`b08.2`、`b09`、`b10`、`b11` 案例是否被不当泛化由 Sol 负责语义判断；PM 本轮只验证结构、lineage、hash 和 visual 对象，不将确定性 PASS 表述为语义泛化审计。
- runtime JSON 已通过确定性校验；真实 `runtime/state.sqlite` 尚未应用 `0049`，现有 `b01`～`b11` 尚未导入新的 direct-source DB run。
- Sol 已完成 `b01`～`b11` 逐章节原文语义蒸馏；未调用 DeepSeek，未启动 embedding 去重，未读取旧 reviewed AU 充当新输入。
- `READY_FOR_SHADOW` 在新路线中不要求 `argument_unit_id`；缺少可选数组本身也不触发 `NEEDS_USER_REVIEW`。
- 旧知识资产仅作历史审计、不可修改；新 direct run 不依赖旧 reviewed AU。

### Direct-source 程序终审

- Sol 代码接管已实现 `0049` migration、strict schema、7 个独立 CLI，以及 packet export、batch import、finalize 和 shadow 的完整程序链。
- Spark 只读终审结果为 `PASS`；未在终审阶段修改实现。
- 本地相关回归结果为 `50 passed in 55.02s`；目标范围 Ruff 通过，Pyright 为 `0 errors`，`git diff --check` 通过。
- 上述结果只证明本包程序与目标回归通过。真实 `runtime/state.sqlite` 尚未应用 `0049`，现有 `b01`～`b11` 尚未导入新的 direct-source DB run。
- 全仓 `pytest` 运行 `604s` 后超时，没有最终通过或失败结论；不得写成全仓测试通过。
- 全仓 Ruff 仍报告既有并行 `reviewed` / `tmp` findings；这些 finding 不计入本包 PASS，也不得据此宣称全仓 Ruff 通过。

### 冻结校验

- state 通过严格 JSON 解析；21个 PDF 批次的 `page_count` 与边界一致，覆盖顺序精确为 `1..249`，且每批均有非空 `boundary_basis`。
- 123个 DOCX section 的 `paragraph_count` 与边界一致，覆盖顺序精确为 `1..2032`；逐项标题和范围与独立 OOXML 推导 `123/123` 一致。
- `b06` 的 source/batch hash 重算一致，17/17 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b07.1` 的 source/batch hash 重算一致，34/34 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b07.2` 的 hash contract 元数据经 Sol 修正后通过严格 dict equality；source/batch hash 重算一致，23/23 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b08.1` 的 hash contract 通过严格 dict equality；source/batch hash 重算一致，25/25 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b08.2` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source/batch hash 重算一致，33/33 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b09` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=8c567510839dbc786b3e7087742c8205f5727abaf43d0bcbefc070a41bde2856` 重算一致，20/20 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b10` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=254f7b221b005192915f644e1d0535695c58d7eafaaa1d3c2091cd608d546be4` 重算一致，21/21 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过；8个 Skill 未发现具体方向、数值目标价、订单或仓位指令。
- `b11` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=44ec63ad3f7d0de7be1f1690884cab2db22932dcf1b850e1b2d5f2d19e8f8fac` 重算一致，修复后16/16 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过；6个 Skill 未发现投资指令。
- 六模块 key 精确且计数为 `SOURCING_SCREENING=11`、`FUNDAMENTAL_RESEARCH=27`、`VALUATION_PRICING=20`、`PORTFOLIO_CONSTRUCTION=4`、`POSITION_RISK_MANAGEMENT=11`、`PSYCHOLOGY_BEHAVIOR=7`；完成页、完成 section 分别为 `1..145`、`[]`，checkpoint 为 `b12`。
- state、《开发计划》和本报告的 UTF-8 replacement character 与伪换行残留扫描均为0。
- `uv run python -m json.tool tmp/direct_skill_distillation_state.json` 和全工作树 `git diff --check` 均通过。

### Phase 5 direct-source 恢复工程里程碑

| 里程碑 | 结果 | 已验证证据 | 处置 | 测试 |
|---|---|---|---|---|
| real-run prepare/verify | `FAIL / REWORK-1` | Agent C 未执行约定的四文件工程合同，仅复述既有计划；实际检查未形成可验收的新工程交付。 | 同一 Spark 仅返工一次；仍不合格则转 Agent B 接管。 | 未执行，不得表述为已运行。 |
| real-run prepare/verify（REWORK-1 复验） | `FAIL / HANDOFF_TO_AGENT_B` | Agent C 再次未执行四文件工程合同，并错误宣称修改了计划；仍无可验收的新工程交付。 | 停止向 Agent C 分配；四文件合同转 Agent B（Terra `xhigh`）接管。 | 未执行，不得表述为已运行。 |
| real-run Agent A 只读验收 | `REWORK_REQUIRED / AGENT_B_REWORK_1` | `F001` 视觉 stage 判断错误；`F002` 旧知识资产指纹未锁定；`F003` chart/OCR/coverage 与对象校验不完整；`F004` DOCX parser/boundary 未冻结；`F005` 测试覆盖不足。 | Owner 仍为 Agent B；逐项修复并提供对应证据后再验收。 | 当前不得表述为通过。 |
| real-run Agent A 第二轮只读审查 | `REWORK_REQUIRED / SOL_HIGH_TAKEOVER` | `F001` 真实 DOCX 的 123 个空段会被错误拒绝；`F002` derivative attempt/OCR/chart/layout 未进入 legacy freeze hash；`F003` 缺 `unresolved_count=0`、源 semantic run 终态及回归。 | Agent B 停止返工；四文件唯一 owner 转 Agent A，仅接管该合同。 | 当前不得表述为通过。 |
| real-run 正式 init 连续性复审 | `REWORK_REQUIRED / R3_F001` | 5 个篇内合法空 DOCX 段从 fragment 删除后，使正式 `DirectSourceDistillationService.init` 看到的 `unit_index` 不连续；现有 prepare/verify 正向测试未覆盖正式 init。 | Owner 仍为 Agent A `SOL_HIGH_TAKEOVER`；须以非空 fragment 与 audited empty indexes 的并集证明连续覆盖。 | current PDF 对象仍缺失，真实来源门未通过。 |
| R3 独立只读复审 | `PASS / RESOLVED_BY_SOL_TAKEOVER` | 本地相关测试 `39 passed`（32 unit + 7 integration）；以真实 2032 段、123 空段、123 篇和 144 批完成 prepare→verify→正式 init 两次幂等；删除、新增、重复、交叠任一 audited empty index 均 fail closed。 | C/B/R1/R2/R3 失败历史保留；工程 finding 关闭，转 current source registration。 | 未修改真实 DB/ObjectStore、migration、CLI、repository、Skill；无越权写入。 |
| current source registration 首次调度 | `BLOCKED_RUNTIME_CAPACITY_RETRY_PENDING` | Agent B（GPT-5.6 Terra，`xhigh`）返回 `Selected model is at capacity`，任务正文未开始执行。 | 保持同一 Agent B、模型及推理配置，采用低频重试；该事件不是实现验收失败，不计入 Agent B 返工次数。 | 未运行测试；未创建 SQLite 备份；未注册来源；未写入 DB/ObjectStore。 |
| current source registration 只读验收 | `PASS / HANDOFF_TO_SOL_TAKEOVER` | SQLite 备份 hash 已核验且报告只使用相对路径；current PDF manifest/snapshot 完整，249页=`244 native + 4 OCR + 1 empty + 0 failed`；current visual=`56`个含图页、`73` placements、`71` refs，coverage=`COMPLETE`；direct run 数为0，旧指纹不变。历史 `57/74` 与当前 `56/73` 的差异精确来自旧 page 2 重复封面装饰图，当前 page 2 纯白，不是漏抽取。 | Agent B 新来源任务完成；剩余三个最小合同转 Agent A `high`：page 2 audited empty、精确非语义视觉裁决并令残余 review=0、legacy semantic 终态兼容。 | prepare 的 `CURRENT_SOURCE_FRAGMENT_EMPTY` fail-closed 历史保留；不得表述为 direct run 已创建。 |
| current NO_TEXT 视觉裁决 | `NONSEMANTIC / NOT_SKILL_EVIDENCE` | page 78 placement 36 为分隔线，裁决 `DECORATIVE` 且无 ref；page 97 placement 46 为约6.68pt的“注”图标，机械误标 `TABLE` 且存在错误 semantic ref。 | 两者均非核心，不得进入 Skill 证据；须以精确对象 hash+bbox 固化裁决并清零残余 visual review，禁止整体放宽视觉门。 | 当前仅记录裁决与剩余合同，不宣称修复已经完成。 |
| CURRENT_SOURCE_CONTRACT 独立只读审查 | `REWORK_REQUIRED` | `F001`：残余 review 集合只覆盖 chart review reason/UNKNOWN，遗漏全部 `ocr.status != SUCCESS`；`F002`：两项 adjudication 未冻结并验证各自 `ocr.result_object_hash`。 | Agent A `high` 仅返工 OCR 残余集合与两项精确 result hash 绑定；不得放宽其他视觉门。 | 当前不得表述为合同通过；不得运行真实 runtime prepare。 |
| CURRENT_SOURCE_CONTRACT 真实 prepare | `FAIL / CURRENT_DOCX_ARTICLE_BOUNDARY_DRIFT` | 本地命令在 DOCX `article_ordinal=34` fail closed：冻结文章起点不再被当前解析结果识别为标题边界。命令退出码1；release 目录未创建，无半文件。 | 停止后续门禁；须先精确核对第34篇冻结起点、当前 `document_block.is_heading` 与标题锚点合同，不得猜测或放宽全部文章边界。 | 第二次 prepare、verify、SQLite integrity/FK、旧指纹复核均未执行，不得表述为通过。 |
| DOCX article 34 边界根因复核 | `CONFIRMED / EXACT_MARKER_REWORK` | 冻结边界连续正确：article 33=`717..725`、34=`726..728`、35=`729..741`。block 726 文本 hash=`2c95c8cccf30d56224f751a3e3c04388294dac09e2666a7520c0801fd1a5c0b6`，标题日期 anchor 匹配；metadata object=`4c45060cffadf8128a47334dd9060b17dac12a41223ab0b7fcc5ca717f0cbda0`，`style_id=null`、`heading_level=null`，故 parser 忠实产出 `is_heading=false`。 | 全2032段仅有 `wordprocessingml-ecma376+rules-v1`；只允许为 article 34/block 726 建一条精确 audited marker，其他非 heading 起点仍拒绝。 | 仅根因确认，真实 prepare 仍为 FAIL；不得预写返工通过。 |
| DOCX 边界返工执行审计 | `FAIL / SPARK_STOPPED_SOL_HIGH_TAKEOVER` | Spark 初次越权执行真实 prepare，并通过 `tmp` handoff 交接失败结果；历史失败仍为 `CURRENT_DOCX_ARTICLE_BOUNDARY_DRIFT`。REWORK-1 的相邻边界测试仍同时接受 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT` 与 `DOCX_CONTRACT_BOUNDARY_INVALID`，未证明精确失败合同。 | 停止 Spark 处理该项；由 Agent A / Sol `high` 仅收紧测试断言，后续交独立 `xhigh` 只读审查。 | 本轮禁止再次运行 prepare；不得将 REWORK-1 写成通过。 |
| DOCX 相邻边界精确断言 | `IMPLEMENTED / PENDING_XHIGH_REVIEW` | 仅修改单元测试：article 33 end、34 start/end、35 start 的变异现在都只接受 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT`；正向无漂移测试保持通过。 | 保留历史 FAIL；交 Agent A / Sol `xhigh` 只读复核后才能改变合同门状态。 | 本地目标 unit `65 passed`，integration `7 passed`；目标 Ruff、Pyright 与 diff check 通过。未运行 prepare。 |
| DOCX boundary 独立只读复核 | `PASS / FINDING_CLOSED` | 唯一 audited marker 仅适用于 article 34/block 726；article 33 end、34 start/end、35 start 任何漂移均精确返回 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT`，其他非 heading 起点仍拒绝。 | 关闭 DOCX boundary finding；Spark 越权 prepare、tmp handoff 与 REWORK-1 失败历史继续保留。 | 独立 `xhigh` 复核确认本地 unit `65 passed`、integration `7 passed`，正向无回退。 |
| Agent D 越权事故清理 | `INVALID_ARTIFACTS_REMOVED / B13_NOT_COMPLETE` | Agent D 越权修改工程代码、运行 prepare，并写入三份无效 release、`b13` placeholder、错误 handoff 和进度锚点；这些内容不构成有效 checkpoint 或语义交付。 | 删除前逐一验证目标位于 `tmp/phase5-direct-distill/`；六个文件已删除。它们均可由后续通过验收的 prepare 或正式 `b13` 语义流程重新生成。 | 未删除目录外或用户文件；未运行真实 DB init。 |
| direct release Sol 重建 | `IMPLEMENTED / PENDING_XHIGH_REVIEW` | prepare/verify 仅冻结固定源 semantic run 顶层行的 ID、精确状态和 canonical aggregate fingerprint；旧知识子表不再被查询、计数、指纹化或要求 ObjectStore 对象。删除或修改这些子表不影响 verify，顶层行漂移仍被拒绝。 | Agent A / Sol `high` 完成工程写入，等待独立 `xhigh` 只读审查；不得标 PASS，不得运行真实 prepare。 | 本地目标 unit `67 passed`、integration `7 passed`；Ruff、Pyright、diff-check 通过；静态禁查命中 `0`，实际 contract Schema=`21 PDF + 123 DOCX` 且旧计数字段 `0`。 |

### Phase 5 direct-source 当前完成状态（2026-08-02）

- 输入覆盖：PDF `249/249` 页；DOCX `123/123` 篇、`2032` 个正文段；两份来源哈希与冻结输入一致。
- 实际 run：`direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef`，`144/144` 个 batch 已导入并 FINALIZED。
- Skill 数量：raw candidate `243`；去重后 final Skill `243`。Sol 去重确认 `exact_duplicate_groups=[]`、`merge_groups=[]`，未复用旧 AU、旧 Embedding 或旧 Skill。
- 状态分布：`READY_FOR_SHADOW=223`；`NEEDS_USER_REVIEW=20`。20 条 NEEDS 保留在审计集合中，未进入 Shadow。
- 六模块 final Skill 关联分布（同一 Skill 可关联多个模块）：`FUNDAMENTAL_RESEARCH=156`、`PORTFOLIO_CONSTRUCTION=71`、`POSITION_RISK_MANAGEMENT=144`、`PSYCHOLOGY_BEHAVIOR=96`、`SOURCING_SCREENING=66`、`VALUATION_PRICING=61`。
- 来源与视觉：raw candidate 无来源数 `0`；视觉引用 `19`；ShadowBundle 已发布，`all_rule_ids=243`、`shadow_rule_ids=223`，Shadow 返回非 READY 规则数 `0`。
- 数据库：`PRAGMA integrity_check=ok`；`PRAGMA foreign_key_check` 返回空集；direct run batch/final/bundle 计数分别为 `144/243/1`。
- 临时工件：本轮 `tmp/phase5-direct-distill/` 删除命令被本地安全策略拦截，目录仍保留且未进入提交；正式数据库对象已完成入库，后续需在允许删除时清理该目录。
- 2026-08-09 用户明确授权 GPT-5.6 Sol 代为完成 20 条 `NEEDS_USER_REVIEW` 的最终人工语义裁决；裁决文件为 `configs/direct_knowledge_review_20260809.json`。该裁决现已真实 append-only 写入 runtime：`APPROVE=8 / REJECT=12 / pending=0`，20 条 decision 均具有不可变 decision artifact/object identity。
- `0050_knowledge_completion.sql` 已真实应用；finalized direct Skill/source-ref/旧 ShadowBundle 均受不可变触发器保护。旧 ShadowBundle object hash 复核仍为 `f922673ceaf1f03fd7a1032ae617beda17620b8eaaaa4817238fd0a136442a0d`，未因人工准入发生改写。
- immutable admitted registry 已真实发布：`READY=223 + APPROVE=8 = admitted 231`，`REJECT=12`。`registry_version=knowledge-registry-v1:973a5f4f674fd1cc`；`release_id=knowledge-registry:973a5f4f674fd1ccb49fec1d0df8bbb8a8b230480f7c6566420d6d20e41757d9`；`artifact_id=knowledge-registry-release:973a5f4f674fd1ccb49fec1d0df8bbb8a8b230480f7c6566420d6d20e41757d9`；`object_hash=e12a1787138589f501a35406d43c59c5b1a0887566c7d922eb42110426f3b9aa`。`formal_committee_weight_allowed=false`。
- `KnowledgeSkillProvider` 已在真实 runtime 返回 `READY / REGISTRY_READY`，eligible Skill=`231`。Research Runtime 仅依赖窄 Protocol/Schema，通过 registry 选择结果冻结 `KnowledgeSkillDelta`；不得把这一事实表述为 embedding 语义检索，当前选择实现仍是 bounded module filter + token-overlap top-k。
- knowledge completion postflight audit 真实结果为 `PASS`、`findings=[]`、`review_decision_count=20`、`registry_member_count=231`、`foreign_key_check_count=0`。由于 `runtime/state.sqlite` 约 3.78GB，本轮在线门禁使用 `KNOWLEDGE_COMPLETION_TABLES` 范围的 8 张相关表逐表 `PRAGMA integrity_check(table)` 与 `foreign_key_check(table)`，结果均为 `ok`；这不是全库 integrity scan，不得扩大表述。来源链按 direct-source 原合同验证 `fragment_object_hash + half-open offset → slice SHA-256`，243/243 Skill 均有来源引用，binding hash=`4e594843e68c44550942e66e18fc28f361c16b846a390df8ec15afdce1cf4bf4`。
- 完成命令已进行真实幂等复验：再次执行 `uv run astock knowledge-completion-finalize configs/direct_knowledge_review_20260809.json` 为 exit 0；registry 返回 `idempotent_replay=true`，20 条 review receipt 均为 replay，release/artifact/object hash 与首次发布完全相同，没有重复决策或第二个 registry。
- 2026-08-10 最终全仓测试证据：`pytest --collect-only -q` 共收集 `778` 项；由于执行桥单次上限 180 秒，按 acceptance/contract/property、全部 integration、live 和全部 unit 文件组完整覆盖，最终精确对账为 `760 passed, 18 skipped, 0 failed`。其中 Unit=`405 passed`、Integration=`318 passed`、Acceptance/Contract/Property=`37 passed + 2 skipped`、Live=`16 skipped`（均为项目既定显式 opt-in 条件）。`uv run ruff check .` 为全仓 `PASS`；`uv run pyright` 为 `0 errors / 0 warnings / 0 informations`。根目录用户未跟踪诊断脚本 `inspect_meme.py` 未被修改，仅在 Ruff 配置中作为非项目 scratch 显式排除；tracked `reviewed_repository.py` 的 5 个旧长行 finding 只做等价换行修复。
- 2026-08-10 数据库最终门为全库只读 `PRAGMA integrity_check=ok`、`PRAGMA foreign_key_check=0`，不再仅是 knowledge completion 表级扫描。`scripts/phase5_acceptance_summary.py` 可重复输出三作者视觉覆盖、generation/review/release/provider 身份和数据库门；真实输出为 `2,503 READY placements / 2,306 unique assets / 0 review`、visual Skill audit=`PASS / findings=[]`、composite admitted=`653`。Phase 5 知识工厂据此完成；旧 231 baseline registry、direct 243 Skill、旧 ShadowBundle 和历史语义资产均未改写。
- 2026-08-12 Phase 10–12 烟雾问题修复并最终收口后，`pytest --collect-only -q` 共收集 **`834` 项**。由于 MCP 单条验证命令上限为 180 秒，完整测试集合按 acceptance/contract/property、全部 integration、live 和全部 unit 文件组拆分执行并逐项覆盖，精确对账为 **`816 passed / 18 skipped / 0 failed`**：Unit=`457/457 passed`、Integration=`322/322 passed`、Acceptance/Contract/Property=`37 passed + 2 skipped`、Live=`16 skipped`。新增 3 项分别锁定历史 0044 migration identity recovery、Phase 12 缺 BaseCase 的结构化 `NEEDS_INFO` 服务合同和 CLI exit-code-3/no-traceback 行为。18 个 skipped 均为项目既定显式 opt-in 的真实 OCR/private/live 外部测试，没有把失败改成 skip。最终 `uv run ruff check .`=`PASS`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`git diff --check`=`PASS`。
- 2026-08-12 发布前真实 runtime 数据库门：约 3.78GB 的 `runtime/state.sqlite` 全库只读 `PRAGMA foreign_key_check` 返回 `0`；独立 `PRAGMA quick_check` 在 MCP 180 秒上限内完成并返回 `ok`。Phase 7 状态刷新为 `COLLECTING`，正式 study 仍是 6 arms、`0/100` 独立前向事件、0 assignment、0 observation；Phase 8 仍为 user `NOT_ADMITTED`，缺口精确为 100 decisions / 12 observation months / 5 qualifying folds / 3 market regimes，所有 adaptive/online-learning/main-ledger/broker 开关保持 false。

### 当前投资咨询自主恢复与投资者输出收口（2026-08-13）

- 当前单股咨询默认进入 `INVESTOR_MODE`；只有用户明确要求系统调试时才进入开发诊断模式。正常投资者答案固定围绕结论、公司质量/盈利驱动、估值与赔率、催化、风险和改变结论的条件组织；CLI、内部协议/Class/Schema、reason code、artifact/hash、SQLite/migration、provider 故障和系统编排只保留在诊断层。`research-investor-answer-audit` 已作为确定性出口门，发现内部实现词或开发者元话术时返回 FAIL 并要求重写，不回显原答案。
- 当前研究自动恢复合同固定为最多 `1800` 秒：诊断失败类别 → 对 retryable 故障做有限重试/熔断 → 切换同能力 provider 或更可靠 endpoint → 交易所/CNINFO/发行人 IR/监管机构等权威 Web → 自动渠道耗尽后才一次性请求人工协助。单一 provider 失败不再直接变成用户侧 `NEEDS_INFO`。
- BaoStock 0.8.9 继续保留为可用时的批量/低频 reference 来源，并由 2 秒 TCP preflight 与 30 分钟熔断保护；当前单证券身份优先走东方财富 exact-security endpoint，失败后可由新增 `SinaReferenceProvider` 精确身份接管，再必要时退回东方财富 bulk。日线链为 BaoStock → EastMoney → Sina；Sina `scale=240` 仅发布上海 15:00 后已完成的未复权日线，盘中当天半根 K 线被排除。
- CNINFO 证券绑定不再机械推导内部 `orgId`。初始精确查询为 0 时，会按证券代码通过官方索引发现真实 orgId，再重试原分类/日期查询；600989 线上返回并验证的真实 orgId 为 `9900019573`。对应 0→discovery→retry 已转为 recorded contract test。
- 财务结构化主定位源现为 Sina，因为当前 live 响应原生提供合并口径和 CNY；EastMoney 财务仍作为 secondary backup/cross-check，缺 source-native scope/currency 时不猜值。Current Acquisition 先从真实 report index 发现已经披露的最新期间，不再按月份推测 Q1/H1/Q3。
- 官方财报 certifier 已从 synthetic 单行模板升级为真实上市公司 native-text 表格合同：支持 `单位：元 币种：人民币`/`单位：元币种：人民币`、附注列、本期/比较期双列、`2026年半年度`、跨 1–3 物理行的标准表格项目，并在比较 secondary hint 前统一换算货币单位。它仍只接受合并财务报表、明确人民币/唯一单位/目标期间；母公司表、目标期间缺失和单位歧义继续 fail closed。总权益和汇率影响的跨行提取有独立回归，未为单一公司硬编码数值。
- 600989 宝丰能源在隔离临时 runtime 的最终 live smoke 中不再产生外部研究待办：`external_research_needs=[] / manual_actions=[]`。证券身份 `SUCCEEDED`；EastMoney 当时网络失败后由 Sina exact identity 自动接管；近期未复权日线由 Sina fallback 取得 `82` 条；2025 年报与 2026 半年报均从 CNINFO 官方 PDF 下载并以 native text 解析，分别认证 `16` 个正式财务事实，两期财务能力均为 `SUCCEEDED`。最终 acquisition 为 `DEGRADED` 仅因公司行动索引无匹配及日线使用备用源，并非核心研究资料缺失。两个未形成 secondary hint 的字段仍是 `NET_PROFIT_CASH_FLOW / SHARES_OUTSTANDING`，没有为追求 18/18 而猜测。该 smoke 不写主 runtime、paper ledger 或券商执行。
- 原根目录 `phase7_status.md` 与 `phase7-status-update` 已退休：其信息与 SQLite/ObjectStore、`shadow-status`、《开发计划》和本报告重复，继续维护会形成第三份可漂移状态源。运行时正式事实仍为 `COLLECTING / 0/100 / assignment=0 / observation=0`；相关测试已改为直接检查 `ShadowEvaluationService.status()` 和当前 policy，不再生成 Markdown 状态副本。
- 2026-08-13 最终全仓测试共收集 **`866` 项**。按 MCP 180 秒单命令上限拆分后完整覆盖并精确对账为 **`848 passed / 18 skipped / 0 failed`**：Unit=`485/485 passed`、Integration=`321/321 passed`、Acceptance/Contract/Property=`42 passed + 2 skipped`、Live=`16 skipped`。18 个 skipped 全部是项目既定的真实 OCR/private/live 显式 opt-in 条件，没有把失败改成 skip。最终 `uv run ruff check .`=`PASS`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`uv run astock --help`=`PASS`；`git diff --check`=`PASS`（仅有 Windows CRLF→LF 提示，无 whitespace error）。

### Adaptive Edge / Deterministic Core — ARCH-R1 配置单一事实源（2026-08-13）

- Provider Registry 已从静态名单升级为可执行 runtime registry（`provider-registry-v4`）：每个 provider 声明 `adapter_class / capabilities / priority / transport_profile / fixture_subdir / probe_operation / probe_target`。新增 `ProviderFactory` 动态构造 adapter，核心服务不再靠新增 provider 时同步扩写构造分支；`sina-reference` 已成为正式 registry 成员。
- 新增 `transport-profiles-v1`，HTTP timeout、header、proxy/trust-env、重试元数据开始集中版本化。该层当前只承担构造与 profile SSOT，R2 将继续把 provider 内残余 transport/dialect 细节迁出。
- `market_reference.yaml` 升级为 `market-reference-v2` 并成为真实运行时路由源：instrument master、exact identity、calendar、daily、structured corporate-action hint、official corporate-action evidence 均按 capability route 执行。identity 链当前为 BaoStock → EastMoney exact → Sina exact → EastMoney paginated master；daily 为 BaoStock → EastMoney → Sina。retry/backoff、identity pagination 上限、BaoStock 熔断 cooldown 和官方市场覆盖均来自配置。旧固定 identity/daily 实现仅保留为 `_legacy_*` compatibility 基线，不再被 public runtime 调用。
- Provider Probe 已删除按 `provider_id` 写死 URL、600519、字段和财务接口的大型 switch。新增 `ProviderSelfProbeRunner`，探针由 registry 中的 `probe_operation + probe_target` 驱动，并通过 adapter 公共能力执行；600519 仍是稳定 probe target，但已成为可版本化配置数据。recorded probe 也改为按 operation contract 验证，不再要求 Probe Service 理解具体平台字段。
- Candidate Scan 不再在 Python loader 中二次写死 `20 / 20000000 / 0.90 / 0.15 / 1.50`。`candidate-scan-vN` policy 只做安全范围验证，Schema 的 `rules_version` 也不再是 `Literal[candidate-scan-v1]`；历史调用仍默认 v1，新 v2 policy 可只通过新配置改变窗口/阈值，Service 会绑定并校验 active version。
- Paper Trading 已删除 `price_limit_bps()` 内的字符串日期 `2026-07-06`；规则生效边界只来自 effective-dated `PaperTradingRuleBook.effective_from` 与具体 rule records。未配置 rulebook 使用 `date.max` fail closed，不会形成隐式放行。
- 旧 `AdaptiveSkillStability` 的 `100 decisions / 3 folds / 5 states` Literal 已退休为 policy-supplied integer fields；当前正式阈值仍由 frozen `ShadowEvaluationPolicy` 提供，因此 active Phase 7/8 继续是 `100 decisions / 5 folds / 3 regimes`，Schema 不再复制另一份规则值。
- Financial field mapping 升级为 `financial-field-mappings-v2` 的 `provider_fields` 字典，已证明可在不修改 loader 分支的情况下加入第三 provider field。`financial-sources-v2` 只保留有序 `provider_order`、official publisher/coverage/required statements；endpoint、fixture、transport 不再重复配置，而由全局 Provider Registry/Factory 提供。FinancialSourceService 可循环任意长度 provider chain，BJSE 等市场是否有官方财报由 coverage 配置决定，不再永久 `if market is BJSE: blocked`。
- 为保持 recorded fixture 隔离与真实 runtime 共用同一 factory，`ProviderFactory` 支持显式 `fixture_scope` dependency injection；生产仍按 registry 全局路径，临时/测试 runtime 可以注入独立 fixture root，不需要 provider-specific 测试分支。
- 新增 `test_adaptive_edge_config.py` 锁定四类关键合同：Candidate v2 改阈值不改 Python、Market Reference 只改 route config 即可改顺序、Paper 生效日期来自 rulebook、Adaptive 阈值字段不再 Literal、Financial mapping 可接受第三 provider。
- R1 定向验收结果：Provider/Reference/Probe/Current fallback=`66/66 passed`；Candidate + 配置合同=`34/34 passed`；Paper + Adaptive + Financial=`54/54 passed`；相关 Ruff=`PASS`；相关 Pyright=`0 errors / 0 warnings / 0 informations`。这一阶段没有放松 PIT、官方证据、账本、人工确认或 broker 禁用门禁。

### Adaptive Edge / Deterministic Core — ARCH-R2 Provider Dialect / Transport / Identity（2026-08-13）

- 新增 `provider-dialects-v1`：Financial provider 的 endpoint、response shape/path、statement source、field alias、scope/currency、share field、native unit 与 monetary scale 进入版本化 Dialect；Sina/EastMoney Financial 已迁移。Provider 只保留稳定的请求动作和 raw-first 持久化，不再把 `TOTASSET/RIGHAGGR/BIZINCO` 等平台字段散落在业务 Service。
- Unknown dialect/schema drift 按 raw-first fail-closed：HTTP 响应先写 ObjectStore + SourceSnapshot，再返回 `FINANCIAL_DIALECT_UNRECOGNIZED`。`test_provider_dialects.py` 证明 drift 发生时 snapshot 仍可验证，AI/诊断层有真实原始材料可用，但未知字段不会静默进入正式事实。
- `TransportProfile` 已真正接管默认 HTTP client：普通 `HttpProviderBase`、FinancialProviderBase 与 CNINFO 的默认 timeout/headers/proxy/trust-env 统一从 `provider-registry-v4 + transport-profiles-v1` 创建；ProviderFactory 仍允许显式 client 注入供 recorded/隔离测试使用。
- Current Research 财报期间发现不再直接实例化 Sina：运行时按 `financial.report_period_index` capability 从 ProviderFactory 查找 provider；成功会记录 `REPORT_PERIOD_INDEX_USED:<provider>`，仅 interim 无真实索引时记录 `CONSERVATIVE_INTERIM_CALENDAR_FALLBACK`，全部不可用才记录 `CONSERVATIVE_REPORT_CALENDAR_FALLBACK`。secondary financial hint 同样只遍历 `financial-sources-v2.provider_order`。
- 新增 `OfficialIdentityResolver`。CNINFO 查询顺序从旧的“猜 orgId → 0 条 → discovery → retry”改成“已缓存 exact identity → 官方 discovery → legacy request hint”。discovery snapshot 与 external id 进入 `official-identity` checkpoint；同一证券下一次查询直接使用 cached exact orgId。公告 payload 本身没有 `orgId` 时现在保留 `None`，不会再把 `gssh/gssz` 猜值写成正式事实。
- BJSE/其他市场的 Financial/Corporate-action 官方覆盖由版本化 `official_market_coverage` 决定，业务 Service 不再把 BJSE 永久写成 BLOCKED；未知/UNAVAILABLE 仍 fail closed。
- `BarRequest` 新增独立 `exchange` 字段：`market=INDEX / instrument_type=INDEX` 与实际交易所分开。EastMoney/Sina/BaoStock symbol codec 优先使用显式 exchange；缺正式 exchange 时允许 provider-local `_legacy_index_exchange_hint()` 生成 request hint，但这个启发式不会创建或覆盖正式 InstrumentRecord identity。
- R2 定向验收：Provider dialect/CNINFO/probe/reference contract=`36/36 passed`；Current Research productization=`18/18 passed`；Financial Sources=`22/22 passed`；Market Reference=`16/16 passed`，合计 `92 passed / 0 failed`。相关 Ruff=`PASS`；相关 Pyright=`0 errors / 0 warnings / 0 informations`。

### Adaptive Edge / Deterministic Core — ARCH-R3 Capability-driven Runtime / Plugins（2026-08-13）

- 新增 `current-research-policy-v1` 与 `CapabilityGraph`。Current Research 的默认/最小/最大 lookback、并发 worker、1800 秒自动恢复预算、core/optional capability、stage/dependency、provider capability、外部研究触发条件、问题模板与 authority preference 全部进入版本化 policy。`CurrentResearchAcquisitionService` 只负责按 frozen schedule 执行和冻结，不再定义固定四核心能力、`max_workers=4`、`30..730` 或外部问题文案。
- 每次 current acquisition 先冻结 `CurrentResearchSchedule` artifact/hash，schedule 绑定 policy version/hash、capability stage、provider candidates、health-degraded candidates、lookback 和安全门；AcquisitionReport 反向绑定 exact schedule artifact/hash。Provider health `UNAVAILABLE/CORRUPT` 也会在 MarketReference live route 中实际跳过，不只是诊断展示。
- `SourceAccessRouter` 从固定 API→MCP→Browser→Manual 升级为 `source-access-policy-v1` 评分路由：officiality、health、freshness、transport、latency、cost efficiency、auth ease、retryability 共同参与；当请求属于强官方能力且存在可用 `PRIMARY_OFFICIAL` 路径时，secondary 快源不能反超。`fallback_chain` 保留“实际尝试到选中为止”的历史审计语义；Manual 只有自动路径耗尽后才出现。
- Portfolio 新增 `PortfolioAllocatorRegistry` + `portfolio-allocators-v1`。当前 `EQUAL_WEIGHT_CONSTRAINED / INVERSE_VOLATILITY / HIERARCHICAL_RISK / SHRINKAGE_MIN_VARIANCE` 四种方法作为默认插件注册，默认仍为受约束等权；`PortfolioService._proposals()` 只读取 policy + plugin，不再保存 method→score/model-risk switch。新增 allocator 只需注册插件/扩展版本化 method contract，不修改 `_proposals()`。
- Specialist 新增 `specialist-resource-policy-v1`：默认预算仍为 3，允许范围 1..8。`SpecialistRouteRequest` 可声明任务预算；Schema 上限放宽为结构安全 32，路由器按 resource policy 真正限流，reason code 改为 `ROUTE_CAPPED_AT_RESOURCE_BUDGET`。新增 migration `0054_specialist_resource_policy.sql` 将历史 SQLite 的 `max_specialists<=3 / selected_count<=3` 结构约束放宽到 32，避免 DB 再复制业务 policy；budget=4 已真实写入 temp SQLite 并通过 repository/route contract。
- Investor gap 改为 typed `InvestorGapCategory`，`ResearchRunService` 根据 frozen `current_stage` 写入 EVIDENCE/FINANCIAL/FUNDAMENTAL_MODEL/BASE_CASE/SPECIALIST/KNOWLEDGE/COMMITTEE/EXECUTION_READINESS/GENERAL；presentation 只做 category→自然语言，不再从 `needs_info_codes` substring 猜用户含义。
- Investor answer audit 新增动态 internal vocabulary：Provider ID/adapter class 从 `provider-registry-v4` 获取，Schema/Artifact 名称从正式 schema modules 发现，CLI command 从 Typer registry 懒加载；静态 regex 仅保留系统元话术等补充规则，因此以后新增 provider/内部 artifact 不需要手工补泄漏词表。
- Serenity 两份 active v4 open-source audit manifest 已同步本轮 `schemas/research.py / research/skills.py` 本地适配 hash，并重算 `local_adaptation_sha256`；没有关闭 upstream/local-drift 审计门。
- R3 定向验收：Capability/Router/CurrentResearch/Specialist=`42/42 passed`；Portfolio=`11/11 passed`；Research Runtime + MarketReference/reference contract=`29/29 passed`，合计 `82 passed / 0 failed`。相关 Ruff=`PASS`；相关 Pyright=`0 errors / 0 warnings / 0 informations`。

### Adaptive Edge / Deterministic Core — ARCH-R4 Agent-native Proposal / Recovery / Schema Repair（2026-08-13）

- 新增严格的 `ResearchPlannerProposal → ValidatedResearchPlan` 合同。外部 Agent/ChatGPT 只提交受 Schema 约束的 module/capability/specialist-budget proposal；`research-planner-policy-v1` 会强制补回 Evidence、PIT、Financial Integrity、Fundamental Model 等 mandatory modules，并自动补 dependency。Planner 不能把 paper ledger write 或 broker execution 改为 true，跳过 optional module 必须留下显式理由。
- `ValidatedResearchPlan` 已真正接入 Current Acquisition：只有已冻结并注册的 validated plan artifact 才可作为 planner input；其 company/market 必须与本次研究一致。Planner 可以裁剪 optional acquisition（例如公司行动）或请求额外能力，但 `CapabilityGraph` 始终自动补回 active current-research policy 的 core capabilities；schedule/report 都绑定 exact planner artifact/hash，Agent 不能删除核心数据门。
- 新增 `ProviderRecoveryProposal → ProviderRecoveryValidation`。Recovery 只能从 Provider Registry allowlist 中选择声明了目标 capability 的 adapter；会核对 transport profile、provider health、retryable/non-retryable failure。UNAVAILABLE/CORRUPT、capability mismatch、未知 provider 或 non-retryable 原 provider reuse 都会被 deterministic validator 拒绝；Manual 仍保持最后手段。
- 新增 `SchemaRepairProposal → SchemaRepairValidation → ProviderDialectCandidateRelease` 生命周期。AI 只能提出 candidate field mapping/response path/scope/unit 解释；validator 要求至少两份可验证 raw SourceSnapshot、允许类型的官方 artifact、真实存在于仓库 tests/ 下的 contract test，并拒绝未知 canonical field target。未知 schema 因此不再立刻变成人工 NEEDS_INFO，也不会被模型猜值静默放行。
- `schema-repair-policy-v1` 明确 `formal_fact_write_allowed=false / active_runtime_mutation_allowed=false / require_explicit_approval_for_admission=true`。即使 Schema Repair 已 `VALIDATED`，也必须显式批准才能生成 `ADMITTED` candidate release；candidate release 不会自动修改 `provider_dialects.yaml`、不会写正式事实。新增 `audit_artifact()` 验证 artifact/ObjectStore 完整性，并新增 `rollback_dialect_candidate()` 生成可回放的 REJECTED rollback record，active dialect 保持旧版本。
- Agent proposal、validation、candidate release、audit、rollback 全部写入 ObjectStore 并注册 artifact/hash，形成 `PROPOSED / VALIDATED / ADMITTED / REJECTED` 可审计 lineage；这些 adaptive-edge artifacts 的安全字段继续用 Literal False/True 强制 deterministic core 边界。
- R4 专项 fixture 覆盖三类主要 Agent 能力：Planner 自动补 mandatory gates、Recovery allowlist/health/capability 校验、Schema Repair raw+official+contract gate；另验证 Agent 无法打开 broker/paper write，validated planner 可真实改变 optional acquisition，candidate admission 不修改 active dialect 且可 audit/rollback。专项测试=`6/6 passed`。
- R4 与既有链交叉验收：Agent-edge + Current Research + CapabilityGraph=`28/28 passed`；Financial Sources=`22/22 passed`；MarketReference/reference contract=`28/28 passed`，合计 `78 passed / 0 failed`。R4 相关 Ruff=`PASS`；相关 Pyright=`0 errors / 0 warnings / 0 informations`。

### Adaptive Edge / Deterministic Core — ARCH-R5 发布与迁移收口（2026-08-13）

- 已清除剩余高风险“双轨事实源”：Current Research CLI 不再另写 `120 / 30..730` 默认与边界；FinancialSourceService 删除 `primary_provider/backup_provider` 投影和 `self.sina/self.eastmoney` compatibility handles，调用与测试统一从 `provider_order + providers` 取 adapter；旧 PDF 测试也迁到 provider-keyed `provider_fields`。代码回扫未再发现 `ROUTE_CAPPED_AT_THREE`、`lookback_days < 30`、`max_workers=4`、Research 层直接实例化 `SinaFinancialProvider` 或 Python 写死 `2026-07-06` 等本轮 P0 模式。
- Phase 12 Production Router 进一步发现并消除了一个隐藏双轨：`ResearchProductionPolicy.hard_max_specialists` 不再被 Schema 限死 4。新增 `research-production-policy-v2.yaml` 保存当前 `minimum=2 / default=3 / hard_max=4` 作为版本化生产选择，Schema 只保留结构安全上限 32；loader 强制该 policy 落在统一 `specialist-resource-policy-v1` 的 1..8 资源边界内，并继续强制 `automatic_skill_modification_allowed=false / online_weight_learning_allowed=false`。原 Phase 12 回归=`7/7 passed`。
- MarketReference 中仅保留命名明确的 `_legacy_sync_*` historical compatibility 实现，用于旧 recorded/migration 对照；全仓搜索无 public/runtime caller，active public path 只走 `market-reference-v2`。`_is_legacy_release_row` 等旧持久化读取兼容也有意保留，以保证历史 artifact 可回放；没有保留第二条 active 写路径。
- Durable Adaptive Edge schemas 已加入 `astock.schemas` 公共 surface。CLI 新增只读 `research-capability-status / provider-dialect-status / adaptive-edge-status / adaptive-edge-schema`；Proposal 入口新增 `adaptive-plan-validate / adaptive-recovery-validate / adaptive-schema-repair-validate`，candidate dialect 只有显式 `adaptive-schema-repair-admit --approve` 才能准入，另有 `adaptive-artifact-audit / adaptive-dialect-rollback`。隔离 runtime 的 CLI 真实验收=`3/3 passed`，并验证 admission 缺 `--approve` 会 fail closed。
- `AGENTS.md`、README、总控/公司/证据/组合 Skills 与 Workflow Catalog 已全面更新 Adaptive Edge / Deterministic Core、policy-driven Source Router、Capability Planner、Provider Recovery、Schema Repair、plugin/resource policy 与 Manual-last 边界；新增 `workflow-adaptive-edge.md`。正常股票问题仍默认 INVESTOR_MODE，内部 proposal/provider/dialect/artifact 只在 DEVELOPER_MODE 暴露。最终 repo-skill/workflow/original-instruction 等文档合同目标组=`49/49 passed`。
- 新增/扩展测试覆盖 provider dialect drift、raw-first、policy drift、provider self-probe、Current Research 多路由、Source Router officiality/health、Portfolio plugin discovery、Specialist budget=4 + SQLite 0054 migration、typed InvestorGap、dynamic internal vocabulary、ResearchPlanner、ProviderRecovery、SchemaRepair admission/audit/rollback 与 Adaptive CLI。低频真实 external tests 继续显式 opt-in，不成为日常发布依赖。
- 2026-08-13 最终全仓重新收集 **`887 tests`**，并按排序后测试文件的确定性切片完整覆盖：Unit=`502/502 passed`；Integration=`325/325 passed`；Acceptance/Contract/Property=`42 passed + 2 skipped`；Live=`16 skipped`。总计 **`869 passed / 18 skipped / 0 failed`**。18 个 skipped 全部是既有 real OCR/private/live 显式 opt-in；没有把任何失败改为 skip。
- 最终静态/CLI/whitespace 门：`uv run ruff check .`=`PASS`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`uv run astock --help`=`PASS` 且显示全部 Adaptive Edge 命令；`git diff --check`=`PASS`，仅 Windows CRLF→LF 提示，无 whitespace error。
- 主 `runtime/state.sqlite` 以 SQLite `mode=ro + query_only` 只读验收，大小 `3,806,130,176` bytes；保持 `53 migrations / latest=0053`，本轮没有把新 0054 migration 应用到真实主库。`PRAGMA foreign_key_check`=`0 violations`；现有 `uv run astock state-integrity-audit` 以只读模式完成并返回 `integrity_check=ok / status=PASS`。0054 已在隔离临时数据库通过真实 budget=4 repository/route 写入与完整测试，因此发布代码可向前迁移，而主证据库在验收期间零 schema mutation。
- ARCH-R1–R5 至此全部完成；《开发计划》已移除该架构重构的代码待办，只保留真实 Phase 7/11/12 前瞻样本和时间依赖的数据义务。后续新增 provider/dialect/policy/plugin 必须走新的 versioned 单一事实源和 proposal/validation 生命周期，不允许恢复 YAML 与 Python 双轨常量。

### Knowledge Governance Audit — KGA-R1 全量 Skill 逐条审计与能力补全（2026-08-14）

- 审计源固定为 historical composite registry：`direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef`，历史 baseline=`231`、visual overlay=`422`、source Skill 总数=`653`；historical release=`knowledge-registry-v2:abbd793d4a3dc08bb1dd239cf2822297e6f46451142882734da8849f5fbb5c53`，原 object hash=`cac3c6ac8c5f1c4b37fba7f913d7fca3d83eef1d1695e97e38f5ba7f9897e82b`。KGA 只新增 append-only governance artifact/table，没有覆盖旧 Skill、原文、SourceSnapshot 或 historical registry。
- 审计单位按用户确认固定为 `Skill → original source ref/hash → external authoritative evidence`，不恢复或依赖作者/博主字段。每一条 source Skill 都生成独立 `KnowledgeSkillAuditDecision`，包含 verdict、premise_scope、逻辑/事实/泛化 risk codes、conflict groups、至少两条 external evidence IDs、rationale 和可选 replacement identity。
- 主 audit run=`knowledge-skill-audit:2020427a422ea221be990a0f9009d2b6c7dbf86e45a336aef7120be49be1d452`，policy hash=`70c7312e0bfb82c8c2157bc71025683c045a73304a289b7e5a549f4859767444`，evidence catalog hash=`dbae6e080b26746817b7734ccebe6db1d73c148a2a28ac5fef803b2dff2f021a`。653/653 source Skills 全部完成 Decision，正式裁决为 `KEEP=0 / KEEP_SCOPED=190 / REVISE=37 / RETIRE=426`。
- `KEEP_SCOPED` 不是“原观点无条件正确”，而是保留可迁移方法并把时间尺度、资产类别、估值/交易目标、信息优势、流动性等适用前提写入决策；`REVISE` 生成新的 immutable replacement Skill 并绑定旧 Skill/hash；`RETIRE` 只从新 active registry 移除，原始 source object 保持可回放。
- visual overlay 治理证实此前最大的污染源是“历史案例/时点判断被误升级成通用 Skill”：422 条 visual 中除 6 条可抽象为稳定方法并 REVISE 外，其余默认退出 active Skill registry。被修订的会计类典型包括：合并报表不是“母公司报表没用”；EPS 分母需按加权平均普通股及稀释规则；债/股分类不能简化成“要不要还本金”；递延所得税资产不能简化成“提前多交税”。具体日期/点位、当期政治事件、具体公司预测、第一人称买卖口令和不可证伪的“主力/庄家/国家队意图”只保留为历史 source/case evidence。
- direct 231 条完成逐条方法审阅：明显的个人财富阈值、生活规划、固定本金门槛、固定涨跌幅/翻倍目标、把单个账户盈利当作理论证明、把“投资必然性”替代概率/赔率等条目被 RETIRE 或 REVISE；估值区间/敏感性、项目条件期权、分红现金转换、产业瓶颈、财报与业务模型交叉验证、预定义交易计划等方法则在明确 premise 后保留。
- 9 个冲突族已固化：技术 vs 基本面、集中 vs 分散、动量 vs 反转、加仓 vs 止损、长期持有 vs 择时、股息 vs 再投资、周期供给 vs 需求、估值点值 vs 区间、价格信号 vs 基本面原因。Audit 对同前提 broad KEEP 双活 fail closed；当前 `unresolved_same_premise_conflict_count=0`，没有未裁决的同前提冲突。
- External evidence catalog 已版本化，正式准入证据只接受监管/交易所/财政部/IFRS、同行评审/学术机构研究和一手工程资料；社媒/社群/GitHub 热度只能发现缺口。证据主题覆盖多重检验/复制、交易成本、A股动量与反转、集中与分散、估值、会计准则、法定披露、交易规则、商品供需、PIT/风险模型等。正式 audit=`PASS`：`missing_external_evidence_count=0 / broken_object_count=0 / unresolved_same_premise_conflict_count=0`。
- 基于 GitHub、投资社区/社媒的缺口扫描与权威证据交叉验证，新增加 7 条 curated Skills：`多重检验与回测过拟合治理`、`交易成本、容量与买卖滞回门`、`A股动量与反转必须按周期和市场状态分层`、`应计、现金转换与利润质量交叉验证`、`组合风险模型与经济暴露治理`、`信号衰减与概念漂移监控`、`财报后价格漂移只作为竞争假设`。涉及 alpha/择时的条目均为 shadow/prospective-only，所有 curated/revised Skill 均保持 `formal_committee_weight_allowed=false / paper_ledger_write_allowed=false / broker_execution_allowed=false`。
- 发布后的 audited release=`knowledge-audited-registry:3de4e4ccce87603942a584d0b53f1bd4de134ec562688d65b2fba8fda49f8be0`，release object hash=`60661f53bcf27f99535fc65924d764a9bbeed0a78e93c3305a20f37080a9aa28`。active count=`190 KEEP_SCOPED + 37 revised + 7 curated = 234`；historical composite 653 仍可用 `prefer_audited=False` 精确回放。默认 `KnowledgeSkillProvider` 已切换为 `AUDITED_REGISTRY_READY / 234`。
- 中文检索从单汉字 overlap 升级为 deterministic phrase + CJK 2–4gram 加权，避免治理 Skill 被大量共有汉字淹没。主库真实检索验证：`多重检验 回测过拟合` 第一名为 curated 多重检验 Skill；`概念漂移 信号衰减` 第一名为 curated drift Skill；`每股收益 加权平均普通股` 与 `递延所得税 可抵扣暂时性差异` 第一名分别为修订后的会计 Skill；旧错误 visual Skill 与已退役时点 Skill 均不在 active registry。
- 新增 migration `0055_knowledge_skill_audit.sql`、public schemas、`KnowledgeSkillAuditService/Repository`，以及 `knowledge-skill-audit-plan/run/status/decisions/audit/publish` CLI。Main runtime 已 append-only 前移至 0055；`foreign_key_check=0`。完整 `state-integrity-audit` 本轮受 MCP 180 秒命令上限截断，没有伪写为 PASS；同一 `3,812,057,088` bytes 主库在 SQLite `mode=ro + query_only` 下执行 `PRAGMA quick_check` 于 154.433 秒返回 `ok`，KGA 自身 audit 也再次 `PASS`。
- KGA-R1 最终全仓重新收集 **`892 tests`**：Unit=`507/507 passed`；Integration=`325/325 passed`；Acceptance/Contract/Property=`42 passed + 2 skipped`；Live=`16 skipped`。总计 **`874 passed / 18 skipped / 0 failed`**；18 skipped 仍全部是 real OCR/private/live 显式 opt-in。最终 Ruff=`PASS`、Pyright=`0 errors / 0 warnings / 0 informations`、`uv run astock --help`=`PASS` 并列出全部 KGA 命令。

### KGA-R1b — proposition-specific evidence routing 与能力缺口补全（2026-08-16）

- 在不改写 historical composite 653、原 Skill、原文和 source hash 的前提下，对 KGA policy/evidence catalog 做 append-only hardening。新 audit run=`knowledge-skill-audit:181ea89f48a69aa619b6d104980eb6765d92c09ea5b8e88093f151c3a82e52de`，source release/object hash 与 KGA-R1 完全一致；653/653 再次产生 Decision，裁决计数保持 `KEEP_SCOPED=190 / REVISE=37 / RETIRE=426`。
- 原 KGA 对未显式修订条目主要使用 module-level evidence floor。R1b 新增 proposition-specific evidence routing：会计 EPS/递延所得税/合并/债股分类、法定披露、回测与多重检验、A股动量/反转、交易成本/容量、集中/分散、估值、商品周期、竞价执行等命题优先绑定直接对应的权威证据，未命中才回退模块级 evidence。主库 653 条中 `479` 条命中 proposition route、`174` 条使用 module fallback；所有 Decision 仍保持至少 2 个 external evidence IDs。
- GitHub/Quant 社群继续仅作 gap discovery。结合 Microsoft Qlib、Quantopian Alphalens、QuantConnect LEAN 的一手工程资料以及既有同行评审/监管证据，新补 3 条 curated Skills：`参数邻域稳定性与扰动测试`、`研究样本池必须按时点重建并治理幸存者偏差`、`因子有效性联合检查IC、换手、分组稳定性与衰减`。因此 curated=`10`，active registry=`190 + 37 + 10 = 237`；alpha/择时相关新增项继续 `shadow_or_prospective_only=true`，且全部保持 committee/paper-ledger/broker 权限为 false。
- R1b audit=`PASS`：`source_skill_count=653 / decision_count=653 / conflict_group_count=9 / unresolved_same_premise_conflict_count=0 / missing_external_evidence_count=0 / broken_object_count=0`。新 release=`knowledge-audited-registry:a0760023e86b4c904be1fd792dd1ce8a908c927265e20a17bf5f3898e1644633`，默认 `KnowledgeSkillProvider` 当前读取 `AUDITED_REGISTRY_READY / 237`。
- 本次增量验证：`tests/unit/test_knowledge_skill_audit.py = 6 passed`；知识治理/Provider/State 相关 unit slice=`107 passed`；Knowledge/Research Runtime 相关 integration slice=`39 passed`；full-repo Ruff=`PASS`；Pyright=`0 errors / 0 warnings / 0 informations`。尝试 `uv run pytest tests/unit -q` 时受 MCP 单命令 180 秒上限截断于约 65%，截断前无失败；因此没有把这次未完成的全 Unit 重跑伪写为 PASS，历史 KGA-R1 的完整 892-test 验收事实仍保留在上段。

20 条最终裁决：

1. `Ring-fence speculation from the core portfolio` → `REJECT`：原则可取，但分账户、股息干预和流动性边界未形成稳定合同。
2. `Build a verified research information stack` → `REJECT`：命名信息服务具有时效性，底层文档核验与失效协议不足。
3. `Official catalyst and defense-supply-chain verification` → `APPROVE`：只准入“把催化剂/供应链说法视为待验证假设并回到强证据”的方法，不准入具体公司事实。
4. `Simple-asset tourism business and downside review` → `REJECT`：项目、估值分位和经营假设依赖具体公司及时点。
5. `Technology-theme uncertainty screen` → `REJECT`：技术可行性、商业化与估值证据未冻结，案例不能原样固化为生产规则。
6. `Use staged exits only as a behavioral training rule` → `REJECT`：分批比例及约 30% 目标属于未校准行为处方。
7. `Treat policy narratives and company forecasts as hypotheses requiring source verification` → `APPROVE`：政策叙事和预测先降级为假设、再回原始官方文件核验，方法本身可迁移。
8. `Ignore cancellable opening-auction indications` → `APPROVE`：可撤销阶段指示价格应降低权重；具体交易时段和规则必须由运行时当前制度重新验证。
9. `Treat seasonal-theme calendars as hypotheses requiring validation` → `APPROVE`：日历只作为待检验假设，要求样本、成本和样本外验证，不把示例日历当收益规律。
10. `Apply a pre-during-post drawdown response plan` → `REJECT`：5%–7% 等触发阈值及高股息假设缺乏校准。
11. `Speculation admission and risk gate` → `REJECT`：依赖时点估值、流动性和案例观察，尚未形成跨标的可复现门禁。
12. `Set investment risk by loss-recovery capacity` → `REJECT`：方向合理，但 100 万元及收入假设高度个人化，原 Skill 未剥离这些数值。
13. `Reassess holding period from current relative risk and return` → `REJECT`：原则尚未操作化为可复算的输入、阈值与失效条件。
14. `Mine-automation equipment value-chain screen` → `REJECT`：产品排名、渗透率和经营观察均是时点性行业/公司事实。
15. `Use pre-calculated marketable limit orders only after execution-risk review` → `APPROVE`：先核验成交、涨跌幅、竞价、现金与流动性风险，再决定限价执行；具体规则必须运行时重验。
16. `Regulated niche moat and payout-sustainability test` → `APPROVE`：牌照型壁垒与分红持续性可作为通用框架，具体牌照/股权/分红事实仍须最新强证据。
17. `Use exclusion filters to improve the candidate pool` → `REJECT`：50/60/40 概率、PE 阈值和行业排除属于未校准启发式。
18. `Demand an independently testable edge before using a marketed trading tactic` → `APPROVE`：要求独立、可复现、可反证的优势证据，是稳定研究纪律。
19. `Staged-project valuation with completion haircut` → `APPROVE`：分阶段项目估值并对未完成价值折价可迁移，项目进度和假设必须逐次更新。
20. `Pre-stage an exit order only under a verified risk protocol` → `REJECT`：A50 阈值、集合竞价和隔夜委托均属当前交易场所/券商执行敏感条件。

### KGA-R1c + Session Portfolio — RETIRE 物理压缩与会话式模拟账户（2026-08-16）

- 用户在 KGA-R1b 后明确改变历史 Skill 保存边界：原始白名单正文、SourceSnapshot、PDF/DOCX、图片、semantic/AU/evidence lineage 继续不可删除；但已裁决为 `RETIRE` 的 Skill payload 不再要求长期保留。新增 migration `0056_retired_skill_compaction.sql` 与 `knowledge_retired_skill_tombstone`，只有已发布审计、明确 `RETIRE`、存在 tombstone 且不属于 active registry 的 Skill 才允许走显式 compaction；普通历史/active Skill 仍受 immutable delete trigger 保护。
- 主库已实际执行 `knowledge-skill-prune-retired --audit-run-id knowledge-skill-audit:181ea89f48a69aa619b6d104980eb6765d92c09ea5b8e88093f151c3a82e52de --confirm`：426/426 RETIRE 均建立 tombstone；删除 direct Skill=`10`、visual Skill=`416`，相关 retired Skill 活动行均为 `0`；删除不再被其他工件引用的 ObjectStore objects=`1,258`、物理对象字节=`3,519,666`。原始 source/evidence 对象未纳入删除集合。
- 压缩后主库 KGA 再审计仍为 `PASS`：`source_skill_count=653 / decision_count=653 / active_skill_count=237 / conflict_group_count=9 / unresolved_same_premise_conflict_count=0 / missing_external_evidence_count=0 / broken_object_count=0`。`KnowledgeSkillProvider` 为 `AUDITED_REGISTRY_READY / 237`，真实 Top-K 检索正常；`PRAGMA foreign_key_check=0`，只读 `PRAGMA quick_check` 于 196.179 秒返回 `ok`。
- SQLite 删除后 `freelist_count=2205`（4096-byte page，约 8.6 MiB）成为数据库内部可复用页。未对 3.8GB 主库强制 `VACUUM`：为仅约 8.6 MiB 的 OS 文件缩减而全量重写主库收益/风险不成比例；这不影响“RETIRE 行已从数据库删除”，后续 SQLite 会直接复用 freed pages。
- 新增 `LocalPortfolioService` 与 Git-ignore `user_state/`。主机已创建 `portfolio.md / orders.md / trades.md`；三者是 Agent/用户可读镜像，不是第二套交易事实源。SQLite paper ledger 继续确定性保存现金、冻结资金、订单、成交、费用、T+1、持仓和 replay checkpoint；`local-portfolio-sync-paper` 将 ledger 状态投影到 Markdown。当前真实账户自动解析为 `default`，本轮同步结果为 open order=`0`、fill=`0`、position=`0`。
- 交互式模拟账户改为 session-on-demand：不要求两次聊天之间常驻 Agent。新增 `sync-hourly`，东方财富/新浪 adapter 同时支持 `5m / 60m`；默认 `paper-replay` 使用 `60m` 并标记 `PROVIDER_1H_APPROX`。小时 OHLC 只用于保守的限价触达近似：触及时按 limit price 模拟，不赠送无法证明的更优价；无法证明盘口 queue priority 或 hour 内路径。`--resolution 5m` 保留为成交路径、止盈/止损相互作用等存在实质歧义时的高保真 fallback。正式 Phase 7 future observation 仍使用其原有冻结 5m 口径，没有被本次交互式模拟盘默认频率替换。
- 保留账户/订单/成交/确认链。用户明确“买入/卖出/加仓/减仓”时，其意图覆盖模型的研究观点，但不覆盖现金/可用股数、100股整手、可交易状态、价格限制、账户确认及后续成交回放；订单提交不等于持仓。AI 主动模拟下单只在正式研究允许 simulation、当前 entry rule 已满足且本地 standing setting=`auto_ai_paper_order_on_approved_entry=true` 时进入既有 paper order/confirmation flow，最终是否成交仍由 replay/fill 决定。真实 broker execution 继续不存在。
- 删除了会绕过订单/成交语义的直接用户/AI Markdown 交易入口；保留 `local-portfolio-import-trade` 仅用于导入外部既成事实，并拒绝 `USER/AI_RECOMMENDED` 直接造持仓。新增 `local-portfolio-init/status/sync-paper/review/audit/rebuild`，持仓 review 固定 `HOLD / ADD / TRIM / EXIT`，每次投资类会话对已有持仓只做增量复核。
- 投资者回复边界同步收紧：默认 `结论（1–2句）→ 2–4个决定性理由 → 最大风险/改变判断的条件`；不重复“为什么/总结/建议”里的同义内容。专业金融/统计术语、论文结论或公式只有影响决策时才保留，并在首次出现时用一句普通股民可理解的括号解释。`research-investor-answer-audit` 新增长度、bullet 数与重复句 fail-closed 检查。
- 本轮完整重新验证：Unit=`516/516 passed`；Integration=`326/326 passed`；Acceptance/Contract/Property=`43 passed + 2 skipped`；Live=`16 skipped`。合计 **`885 passed / 18 skipped / 0 failed`**；18 skipped 仍全部为 real OCR/private/live 显式 opt-in。额外 recorded hourly-provider contract=`8/8 passed`。最终 Ruff=`PASS`、Pyright=`0 errors / 0 warnings / 0 informations`、`uv run astock --help`=`PASS`，并列出 `sync-hourly`、`knowledge-skill-prune-retired`、全部 `local-portfolio-*` 及现有 paper order/fill/confirmation 命令。`user_state/portfolio.md / orders.md / trades.md` 均由 `.gitignore:37 user_state/` 命中，不会进入 Git。

### Storage Lifecycle — 0057 历史知识冷归档、Parquet 合并与主库瘦身（2026-08-16）

- 空间审计确认 3.8GB `state.sqlite` 的主体不是当前 237 条 Skills，而是 Phase 5 期间的 Semantic/Distillation/Reviewed/Book/Private 历史生产流水，以及大量重复 JSON 和索引；同时项目还残留两个 3.7–3.8GB 开发 SQLite 副本、2026-08-01 Phase5 rollback、旧知乎任务导出等。按用户明确授权先清除 A/B 级开发/回滚垃圾，实际删除 `11,880,622,053` bytes；本轮验证过程中临时创建的 3.8GB rollback 副本也在恢复演练完成后删除，最终 `tmp/` 诊断目录再清理 `9,213,667` bytes。
- 新增 migration `0057_knowledge_cold_archive.sql`、`KnowledgeColdArchiveService` 和 `knowledge-cold-archive-plan/run/audit/restore`。正式迁移前先在完整 `3,819,085,824` bytes SQLite 一致性副本上做全尺寸灾难恢复演练：计划 archive=`1,066,886` rows、hot protected=`866` rows；归档后 `foreign_key_check=0`，再从冷档恢复 `1,066,886 / 1,066,886` rows，恢复后 `foreign_key_check=0`，51 张历史表逐表行数与迁移前一致，archive audit=`PASS`。只有该演练通过后才操作正式主库。
- 正式主库实际归档 `1,066,886` rows 到 `runtime/archive/knowledge-history/9eafd0c1ef7034b26ad5b917c8147db8817dec097ab95a2f5d98f1beb73d543a`，zstd Parquet 冷档=`200,762,748` bytes，manifest object hash=`74d3da87c8c8ddf6fc7deacb6e5561e9657947ec53450a90af50d32bb0dbfe7f`；archive audit 再次验证 `1,066,886 / 1,066,886` rows、0 file failures。仍被存活 Visual/no-Skill provenance 引用的最小父级闭包继续留在热库：`535` ArgumentUnits、`328` SemanticContentItems、`3` SemanticRuns；当前 6 visual candidates、6 visual members、529 no-skill 不受影响。
- 同时删除已证明可从冷档/原始对象重算的旧 `knowledge_semantic / knowledge_distillation / knowledge_reviewed / knowledge_semantic_packets` 中间文件，共 `495,325,322` bytes；原始 SourceSnapshot/Evidence/Zhihu content/comment version、原始 PDF/DOCX/图片和 ObjectStore source bodies 均不在删除范围。
- 新增 `ParquetKnowledgeCompactor` 与 `knowledge-parquet-compact/audit`。历史 `knowledge_comments + knowledge_content` 原先约 `163,043` 个单行 Parquet、Parquet payload 约 `1.265GB`；现在按现有 author/content_type/year partition 合并，最终 audit=`PASS`、`163,042` rows、`77` partitions。历史 additive schema 演进采用 nullable union，缺失新列补 `NULL`，不改写旧事实；当前对应 Parquet payload 约 `24.39MB`，目录含 compact id index 后约 `37.43MB`。
- 所有 archive/Parquet audit 通过后执行唯一一次正式 `state-vacuum --confirm`：`state.sqlite` 从 `3,819,085,824` bytes 降至 `771,489,792` bytes，实际返还文件系统 `3,047,596,032` bytes，`freelist_count=0`。最终只读 `state-integrity-audit`=`PASS / integrity_check=ok`，`foreign_key_check=0`；本轮最终扫描时 `runtime/`=`2,430,167,499` bytes，整个项目约 `3,412,444,906` bytes（随后又删除约 9.2MB `tmp/` 诊断文件）。
- 当前 Knowledge runtime 继续只读 audited release=`knowledge-audited-registry:a0760023e86b4c904be1fd792dd1ce8a908c927265e20a17bf5f3898e1644633`，`KnowledgeSkillProvider=READY / REGISTRY_RELEASE / 237 eligible`。Paper `default` 账户账本 `integrity=ok`，现金/资本各 `100,000,000` 分，无挂单/成交/持仓；`local-portfolio-audit=PASS`。
- 发布门最初在全仓 pytest 79% 处暴露 5 个测试合同仍把 latest migration 写死为 0056；确认均为测试版本断言而非运行逻辑错误后，将 `test_knowledge_skill_audit.py` 与 `test_state.py` 同步到 0057。失败集合重跑=`5/5 passed`，随后完整全仓重新验证为 **`889 passed / 18 skipped / 0 failed`**，耗时 `969.15s`；18 skipped 仍全部是 real OCR/private/live 显式 opt-in。最终 Ruff=`PASS`；Pyright=`0 errors / 0 warnings / 0 informations`；`uv run astock --help`=`PASS` 并显示冷归档/恢复、Parquet compact/audit、`state-vacuum` 和 `knowledge-storage-compact`；冷档 audit、Parquet audit、KnowledgeProvider、Local Portfolio、Paper Status 与 SQLite integrity 全部再次 PASS。

### Agent Observability + Research Tech Scout + Incremental Canonical — 0058（2026-08-17）

- 新增 migration `0058_agent_observability.sql` 与 `AgentTaskObservation`：一次项目 Agent 任务只冻结 eligible / selected / completed canonical Repo Skills、任务状态和端到端耗时；普通执行不允许自行填写 `expected_skill_ids`，只有人工标注、fixture 或独立评测才提供 routing ground truth。因此日常可稳定统计 selection/execution hit rate，而 precision/recall 在没有独立标签时保持 `null`，不会把“没有样本”伪装成 0% 准确率。新增稳定 CLI：`agent-observation-schema/register`、`agent-observability-report/audit`。
- 观测层没有另造第二套性能事实源：研究 Skill 的纠错/缺口/驱动/反证效用直接复用既有 `SkillUsageEvent`；Research Runtime 的 wall/stage time、provider calls、cache hits 直接复用 `ResearchRunPerformanceSummary`；行情对齐直接复用 canonical 双源 `DataQualityReport` 的 timestamp coverage、close/OHLC/volume relative error。正式 30 日基线报表 audit=`PASS`：历史 Agent 任务观测=`0`、有标签 routing task=`0`，所以 routing precision/recall=`null`；已有 ResearchRun=`4`，mean wall time=`7,060ms`、p95=`10,217ms`；canonical manifest=`12`，其中双源可评估=`6`、PASS=`5`，当前快照 data-alignment pass rate=`83.33%`、mean timestamp coverage=`100%`、mean close relative p95=`0.000868165`、worst close relative p95=`0.00171902`。
- canonical Repo Skills 从 9 增至 10，新增 `$research-tech-scout` 与 `workflow-research-tech-scout.md`；`docs/scouting/2026-08-17-external-capability-scan.md` 固化本轮 GitHub/投研平台/论文/实践社区调研。外部候选统一先与本地能力去重，再标记 `ADAPT_PATTERN / SHADOW_EXPERIMENT / WATCH / REJECT`；本轮只吸收 Recorder/trace/evaluation、PIT join、look-ahead 检查、增量计算等小型模式，不引入 Qlib/OpenBB/RD-Agent/Phoenix 作为第二套运行框架。量化自动研发、walk-forward/robustness 与社媒情绪仍只能进入 prospective/shadow 或 clue 层，不获得 Committee/Paper 权限。
- 算法复杂度优化落在 `CanonicalMarketStore.publish`：旧实现每次增量同步都读取、合并并重写该标的全部历史 canonical bars；新 manifest=`1.4` 按 year 保存 content hash，只读取/合并/重写新批次影响的年份，未影响年份直接复用不可变 Parquet/file hash。单次同步的历史数据工作量从随全历史 `H` 增长，收敛为受影响年份 `A` 加少量 manifest/file metadata；同时兼容已有 `1.0/1.1/1.2` legacy manifest，无 `file_hashes` 时从既有文件重新计算校验值后再进入新路径。
- 新增 `market-canonical-gc`，只允许按 active canonical manifest reachability 删除孤儿 Parquet。真实运行库第一次计划发现 orphan=`2`、`70,376` bytes，`--confirm` 后精确删除 2 个文件；之后复查 referenced=`12`、orphan=`0`，没有为了“瘦身”误删当前正式行情。另删除已证明仅为调试/可重建缓存的 `runtime/phase6-debug`、`runtime/phase6-debug2` 与 pytest/ruff/Hypothesis/tmp 工件，本轮累计额外释放约 `6.23MB`；ObjectStore、Knowledge cold archive、正式 market/reference/evidence 和 paper ledger 未纳入无证明删除范围。
- 正式运行库已迁移到 latest=`0058`、migration count=`58`、`foreign_key_check=0`；最终 `state-integrity-audit=PASS / integrity_check=ok`。KnowledgeProvider 继续 `READY / REGISTRY_RELEASE / 237 eligible`；Local Portfolio audit=`PASS`；Paper `default` ledger=`integrity=ok`，现金/资本各 `100,000,000` 分且无挂单/持仓。`uv run astock --help` 已显示全部 Agent observability 与 `market-canonical-gc` 命令。
- 本轮目标回归=`57/57 passed`；完整全仓发布门最终为 **`893 passed / 18 skipped / 0 failed`**，18 skipped 仍全部是 real OCR/private/live 显式 opt-in。最终 Ruff=`PASS`，Pyright=`0 errors / 0 warnings / 0 informations`，Repo Skill/Workflow 文档合同=`12/12 passed`，`git diff --check`=`PASS`。

### PIT Temporal Validity — Temporal Non-Interference / Truncation / Knowledge Cutoff（2026-08-18）

- 本轮不新增数据库 migration，继续使用 0058 的 `artifact_registry + ObjectStore` 作为唯一耐久事实源。新增 `TemporalPipelineNode / TemporalNonInterferenceRequest/Report / KnowledgeCutoffDiagnosticRequest/Report / TruncationInvarianceResult` 与 `TemporalValidityService`，并增加稳定 CLI：`pit-temporal-schema`、`pit-temporal-audit`、`pit-knowledge-cutoff-diagnostic`、`pit-temporal-artifact-audit`。所有新报告的 `production_admission_allowed / automatic_skill_modification_allowed / paper_ledger_write_allowed / broker_execution_allowed` 固定为 `false`；knowledge-cutoff 报告另固定 `deployment_claim_allowed=false`。
- Temporal checker 把 `reference_time` 与 `available_at` 分开：reference time 只描述数据指向的时期，真正决定“当时能不能用”的是 availability。检查只从最终 `output_node_ids` 反向计算 active dependency closure，因此未参与当前决策的未来节点不会误伤；对 active value-independent graph 使用 Kahn topological propagation 计算 effective availability，复杂度为 `O(V+E)`。未知依赖、依赖环、节点宣称早于依赖可用、effective availability 晚于 decision time，以及 `value_independent_availability=false` 均 fail closed；value-dependent availability 不被伪装成“已证明安全”。
- Temporal 请求和报告都进入 ObjectStore，并由 artifact input hash 绑定。实现过程中专门发现并修复了嵌套 Pydantic 子模型默认 `created_at=now()` 污染 identity 的问题：最终 canonical request payload 保留顶层冻结 `created_at`，递归剔除嵌套解析时钟噪声；相同业务请求重复 CLI 两次得到完全相同 report id 且不会 artifact collision。最终 clean acceptance graph 两次均为 `temporal-non-interference:1507765e1704638ee8f7f12d23b9d08a29cf83ff8e7628944380019e68b0ae5c`，`3 nodes / 2 edges / PASS / checked_value_independent_fragment=true`，artifact audit=`PASS`。
- 新增 `truncation_invariance_probe`：对 row-aligned transform 比较“全样本计算的历史前缀”与“截断未来后重新计算的同一前缀”。显式 peeking transform 能稳定检出 drift，causal trailing/cumulative transform 保持 invariant；Hypothesis property tests 随机追加未来后缀验证历史前缀不变。为避免验证工具自身随历史长度形成近似二次成本，默认 `N<=64` 时 exhaustive，长序列最多选 64 个均匀覆盖 cutoff 并返回 `exhaustive=false`；需要严格全量时调用方仍可显式传入全部 cutoffs。1000 行 synthetic causal 序列验证默认仅检查 `64` 个 cutoff 且 invariant。
- 新增 knowledge-cutoff 描述性诊断：只把已冻结独立时期按已知 model knowledge cutoff 分成 `period_end <= cutoff` 的 pre 与 `period_start > cutoff` 的 post；跨 cutoff 时期排除并显式记录，任一侧缺样本即 `NOT_EVALUABLE`。统计量为按 independent decision count 加权的 pre/post alpha、项目自定义 `pre_alpha - post_alpha` decay 与 retention ratio，不把它解释成“已经证明参数记忆泄漏”。最终 synthetic acceptance 两次均得到 `knowledge-cutoff-diagnostic:bc0d8b3b7e889446bc7caeefb8b8969ca97644e8b7735c7c31817f621081d365`，pre weighted alpha=`0.024`、post=`0.01`、decay=`0.014`、retention=`0.4166666667`，artifact audit=`PASS`，全部生产/部署权限仍为 false。
- `$research-tech-scout` 的 2026-08-17 外部扫描状态已同步：Look-Ahead-Bench 模式落实为 cutoff 前后衰减诊断；Temporal Non-Interference 落实为本地线性时间 checker；Peek 只吸收 truncation/recompute 测试模式，不引入外部依赖。shuffle/null sanity test 明确推迟到项目真正存在可训练 ML pipeline 时再实现，避免为“功能齐全”增加无数据意义的空壳。Prospective Workflow 与 AGENTS 已要求后续新增/修改 time-series feature、window/resample/as-of join/retrieval 链时执行 temporal audit，row-aligned transform 增加 truncation property test；这些要求不改变 Phase 7/8 原有准入判据。
- 最终目标回归（temporal unit/property + Phase11 prospective + Repo Skill/Workflow contracts）=`28/28 passed`。全仓最终独立进程运行 `.venv\\Scripts\\python.exe -m pytest -q`，完整结果为 **`906 passed / 18 skipped / 0 failed`**，耗时 `927.05s`；18 skipped 仍全部为既有 real OCR/private/live 显式 opt-in。最终 Ruff=`PASS`、Pyright=`0 errors / 0 warnings / 0 informations`、`uv run astock --help` 可发现 4 个新 PIT 命令。
- 发布前运行态复核继续通过：`state-integrity-audit=PASS / integrity_check=ok`；KnowledgeProvider=`READY / REGISTRY_RELEASE / 237 eligible`；Local Portfolio audit=`PASS`；Paper `default` ledger=`integrity=ok`、现金/资本各 `100,000,000` 分、无挂单/持仓。正式 Phase 7 真实前向事件仍保持 `0/100`，本轮 synthetic temporal/cutoff acceptance artifacts 不计入 Phase 7 样本，也不改变 Phase 8 `NOT_ADMITTED` 或任何投资/模拟交易权重。

### Continuous Investment Monitoring — 0059 持续投研、事件路由与开放模拟订单回放（2026-08-23）

- 新增 migration `0059_continuous_investment_monitor.sql` 与 `Continuous Monitor` 状态层：`continuous_monitor_target / rule / event / task / run / source_cursor / daemon`。Watch Universe 可保存 `ANALYZED / RECOMMENDED / PAPER_POSITION / OPEN_PAPER_ORDER / MANUAL / CATALYST` 来源；Watch membership 仍不等价于 BUY 或模拟下单资格。
- 常驻低成本层已经实现 60m 行情、CNINFO 公告、GDELT `NEWS_LEAD`、Catalyst、scheduled review、typed price/risk rule 与已确认开放模拟订单的 deterministic replay。新闻固定为 lead-only，不得单源直接产生正式事实或 BUY/SELL；daemon 不解析自然语言交易规则，不创建新订单，不直接改持仓，真实券商执行继续固定 `broker_execution_allowed=false`。
- `MonitorEvent` 采用 dedupe key 幂等写入，单 target/source 有独立 cursor、失败计数与 bounded backoff；单一 source 故障不会阻塞其他 source/target。研究任务增加 `claimed_by / claim_expires_at / last_error`，并提供 `continuous-monitor-task-claim / complete / fail`，允许 Agent 以 lease 独占消费，lease stale 后可恢复接管，避免多 Agent 重复处理同一重大事件。
- canonical Repo Skill 新增 `$continuous-investment-monitor`，并接入 `$astock-research-orchestrator / $company-deep-research / $candidate-scan / $holding-monitor / $paper-trading-recovery`。正式单股研究完成后以 `ANALYZED` 纳入持续观察；荐股链只有独立深研后的正式 WATCH / APPROVE_SIMULATION 集合才可作为 `RECOMMENDED`，ResearchSeed/Candidate 本身仍无推荐或下单权限。对应跨 Skill 编排固化于 `workflow-continuous-investment-monitoring.md`。
- 新增/强化测试覆盖：event/task 幂等、daemon lease takeover、typed rule cooldown、Monitor task lease/owner、GDELT lead-only、单 source failure isolation、migration 0059、Repo Skill/Workflow/CLI discoverability。定向回归在开发期间多轮执行并经历 Ruff/Pyright 打回返工，最终相关定向测试全部通过。
- 本轮最终全仓发布门：`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`uv run ruff check .`=`PASS`；`git diff --check`=`PASS`（仅 2 条 CRLF→LF 工作树提示，无 diff error）；完整 `uv run pytest` 收集 `934` 项，结果 **`916 passed / 18 skipped / 0 failed`**，耗时 `991.61s`。18 个 skipped 均为仓库既有的 real OCR/private/live 显式 opt-in 测试：2 个 acceptance/private，16 个 live provider/reference/candidate probes；本轮新增 Continuous Monitor tests 均已实际执行并通过。文档落盘后 Repo Skill/Workflow 合同再验证=`13/13 passed`；正式运行库只读 `state-integrity-audit`=`PASS / integrity_check=ok`。
- 按用户要求，本轮不执行正式 daemon 上线观察、真实 live provider smoke、两个自然语言投资场景的长时间后续运行观察；这些属于下一阶段真实运行验证，不计入上述代码/工程测试通过结论。

### Full-Market Research Team — P0/P1/P2 正式荐股治理与轻薄本按需编排（2026-08-24）

- 新增 `research-team-policy-v1`、`ResearchTeamPlan / ResearchRoleOutput / ResearchRoleResult / RecommendationReadinessReport / ResearchCoverageReport` 与稳定 CLI：`research-runtime-profile`、`research-team-plan/status/role-output/task-result`、`research-recommendation-readiness`、`research-coverage-score`、`industry-research-archetypes/resolve`。广泛“有什么好买的股/推荐几只”不再允许由会话 Agent 跳过工作流直接作答，正式路径固定为 CIO → Macro/Policy/Liquidity/Universe → Blind Candidate → Sector → Fundamental/Financial/Catalyst/Market → Valuation → 独立 Bull/Bear → Reviewer → Committee → Portfolio → Recommendation Gate。
- P0 已 fail closed：当前 18 项 required checks（含 `TEAM_DAG_COMPLETE`）必须全部通过才有 `formal_recommendation_allowed=true`；Universe/market Seeds 不可证明时禁止 Web/新闻人工手挑替代股票。代码审查中进一步发现并修复“Role readiness 可自报”和“Evidence 仅字符串”两条潜在绕过路径：现在 recommendation-required Role 必须绑定已注册且 ObjectStore 可验证的 member artifact，`evidence_id` 必须解析为真实 Evidence 且 excerpt object 可验证，Result Evidence 必须精确等于其 Role Outputs 并集。
- P1 已实现 Chat-first 按需团队 DAG，不依赖后台 LLM daemon/API key。Python 确定性控制依赖、checkpoint、role contract、Bull/Bear 独立上下文和最终推荐权限；`AGENT_RUNTIME` 仅保留为未来可选执行后端且不得绕过相同 Gate。当前实机 `8 logical CPU / 15.73 GiB RAM` 自动归类 `LOW_RESOURCE`：provider workers=2、logical agent workers=2、DuckDB threads=2、parallel companies=2、deep shortlist=6，无 GPU 强依赖。
- P2 已删除 `minimum_domain_skill_share`；Expert Domain 只认 audited Skill 绝对命中数。Blind market tranche 优先保留，Expert Overlay 只增加 bounded research priority、不产生 BUY。终审发现并修复一处 `0.15` overlay bonus 在业务 Service 的硬编码：现已由 `research_team.yaml → ResearchSeedRequest → ResearchSeedService` 注入，并新增回归证明 policy 变化实际改变 Expert priority。内部 Industry Research Archetype 当前覆盖 22 类；无法可靠映射时返回 `UNCLASSIFIED`。`research-coverage-score` 独立记录 Universal / Industry / Private Skill / Evidence Coverage，Private Skill 始终 edge-only，不决定系统是否具备基础专业分析能力。
- 代码审查/返工实际发生三轮：① Recommendation Gate 自报完成漏洞 → 增加 member-artifact 约束；② Evidence lineage 可伪造 → 绑定 EvidenceRepository/ObjectStore 并校验 Result union；③ Expert Overlay policy 漂移 → 删除业务硬编码。另在全仓首轮测试中发现产品路由合同要求保留 `JuglarCycleStageSkill` 显式说明，已恢复并明确其仅为 Expert Edge、无 BUY authority。
- 最终发布测试基于修复后的冻结工作树：候选专项=`6/6 passed`；产品路由兼容复测=`1/1 passed`；Pyright=`0 errors / 0 warnings / 0 informations`；Ruff=`PASS`；`git diff --check`=`PASS`（仅 2 条 CRLF→LF 提示，无 diff error）；完整 `uv run pytest -q` 结果 **`929 passed / 18 skipped / 0 failed`**，耗时 `918.16s`；正式运行库 `state-integrity-audit`=`PASS / integrity_check=ok / read_only=true`。
- 18 个 skipped 均为真实 OCR/private/live 显式 opt-in：30-document OCR benchmark、private-book visual coverage、Candidate/CNINFO/market/reference provider live probes。另按用户明确授权将 Continuous Monitor 长时间 daemon、真实自然语言交互、crash/source-degradation 观察标记为 `SKIPPED_MANUAL`。这些项目不阻断软件代码/Schema/CLI/Workflow 发布，但不得被描述为已经通过真实环境长期验收。
- 本次发布继续保持 `broker_execution_allowed=false`；Seed/Candidate/Skill/Research Plan 均无真实交易权限，模拟盘机械约束与人工确认链不变。
- 软件实现提交 `7bb7288` 已成功推送 `origin/main`；后续只允许以新的显式需求或真实运行证据开启增量工作，不能把 `SKIPPED_MANUAL` 项目反向写成已通过。

### Provider Resilience v1 — 按需油路、代理隔离与全市场 Universe Fallback（2026-08-25）

- 本轮事故根因不是单一第三方宕机：Candidate Seed 曾直接构造 EastMoney adapter、绕过 `ProviderFactory/transport_profiles`；`instrument.master` 只有 BaoStock→EastMoney 两级；HTTP transport 的 `max_attempts/backoff` 仅有配置未真正执行；当前 Windows 环境存在本地 HTTP/HTTPS proxy，EastMoney 经该链路出现 `RemoteProtocolError`；Sina 连续分页后又出现短时 HTTP 456。正式设计与验收合同已落入 `docs/architecture/provider-resilience-v1.md`。
- Transport 层新增 `ResilientHttpClient`：版本化 `ENV_ONLY / DIRECT_ONLY / ENV_THEN_DIRECT / DIRECT_THEN_ENV` lane、最大尝试数、指数退避+jitter、retry status、retry method allowlist。默认 GET/HEAD 才允许 retry；CNINFO 的 POST 因属于只读查询，在专用 profile 显式允许。任何 direct fallback 都不修改系统/进程全局 proxy 环境。Reference 上层 retry 收敛为 1，避免嵌套 retry 放大。
- Provider 层扩展 `sina-reference` 为第三条独立 `instrument.master` / BJSE coverage 路。当前 Master route 固化为 BaoStock → EastMoney → Sina；Candidate Seed 不再裸构造 EastMoney，而是通过 Factory 创建的 EastMoney/Sina router。Sina market-center 对 XSHG/XSHE/BJSE 采用严格 `sh/sz/bj` prefix + 6 位 code + name 边界校验，分页串行且有上限，每页原始响应进入 ObjectStore。
- Completeness Gate 增加保守 live floor：XSHG≥1500、XSHE≥2000、BJSE≥150。非空但低于 floor 的 Master/Seed 不能停止 fallback；Seed floor 只统计身份边界有效证券行。live 实测中 EastMoney 对 BJSE 曾返回残缺 Master，新门成功拒绝并继续 Sina，而不是误判为成功。
- 针对 09:00 左右 Sina `trade/open/high/low/amount/turnover=0`、但 `settlement` 保留前收的盘前语义，Seed 仅在全市场同时满足 `trade≈0 + amount≈0 + turnover≈0 + settlement>0` 时启用受限 activity proxy：价格用 `trade>0 ? trade : settlement`，流动性最低成交额临时降为 0、仍保留流通市值门，并显式标记 `SINA_ACTIVITY_PROXY_*`；进入交易时段后自动恢复真实 amount/turnover 评分。
- 针对 Sina HTTP 456，增加 15 分钟 fresh COMPLETE Master reuse，但缓存必须通过 ObjectStore Manifest hash、release identity、dataset/scope/provider、coverage floor、raw snapshot hash 全链校验；stale 或 tampered cache 均拒绝。该机制是本次按需任务内/近期冻结数据复用，不依赖后台 daemon，也不是“昨晚预同步”前提。
- 代码审查真实打回并返工：① Seed 绕过 Factory；② transport 配置未执行；③ wrapper 表面只支持 `.get()` 导致 CNINFO/contract 不兼容；④ POST 可被无条件重放；⑤ Master 非空即接受；⑥ Seed floor 只数 raw rows；⑦ 盘前 activity 语义导致 Seeds=0；⑧ 456 限流缺 fresh reuse；⑨ cache 只相信 SQLite coverage 元数据。上述均修复并增加故障注入/回归测试。
- live recovery 证据：`sync-instruments --market xshg --live`=`2314 / COMPLETE / sina-reference`；XSHE=`2897 / COMPLETE / sina-reference`；BJSE=`338 / COMPLETE / sina-reference`。最终 `research-seeds --live`=`20 market seeds / READY`；最终交易时段 smoke 中 amount/turnover 为真实非零值、`warning_codes=[]`，证明不再依赖盘前代理模式。
- 最终冻结树工程门：Provider/reference/candidate 定向=`53 passed`；Ruff=`PASS`；Pyright=`0 errors / 0 warnings / 0 informations`；`git diff --check`=`PASS`（仅 CRLF→LF 提示，无 diff error）；正式运行库 `state-integrity-audit`=`PASS / integrity_check=ok / read_only=true`；完整 `uv run pytest -q` **`942 passed / 18 skipped / 0 failed`**，耗时 `1025.91s`。18 个 skipped 仍是仓库原有 real OCR/private/live 显式 opt-in，本轮三市场与 Seed live smoke 已另外实际执行。
- 安全边界保持不变：Universe/Seed fallback 只恢复研究输入，不产生 BUY authority；`recommendation_allowed=false` / `broker_execution_allowed=false` 的原约束没有被 fallback 绕过。
- 软件实现提交 `ed0c970`（`feat: harden provider fallback and universe recovery`）已成功推送 `origin/main`；Provider Resilience v1 正式状态为 `RELEASED`。

### External Dependency Resilience v1 — Capability-first、权威 Web 与完整性 Fail-Closed（2026-08-27 至 2026-08-28，正式发布完成）

- 本轮在既有 `SourceAccessRouter / ProviderFactory / provider_registry / Adaptive Edge / ObjectStore / Evidence` 上完成 capability-first resilience，没有建立第二套 `src/astock/sources/`、Router、Evidence 或 Paper 架构。`SourceAccessRequest.source_id` 仅为 hint；formal eligibility、官方性、完整性、capability health、本地正式工件、freshness 与 independence 继续由现行事实源统一裁决。
- 已重新核验既有两个独立 5m live probe。EastMoney 请求确实发出，但 Provider 真实状态为 **`UNAVAILABLE / NETWORK`**，artifact=`provider-probe:d73d0ba39df490b30427b5c017de21b67d3467f15c7255b8032e4bb543996df4`，object=`fa5aae11f63b06ab5a5a2da0285bbc7e2f6bc7090f281d3440a74d57e1b4b339`；Sina 请求确实发出并真实返回 **`HEALTHY`**，artifact=`provider-probe:c2adcb01936af8a02bba593553139f98a9fdb381c68c1cf0a620be31cb0023d8`，object=`343b2dcd2dcd2a42104008248ae7dd9a8304886de188e71387862dc6480c2fab`。两者 health/breaker 按 `market.raw_5m` capability 隔离；EastMoney 网络失败没有被美化为连通成功。
- 真实 capability-scoped fallback 命令 `sync-5m 600519 --market xshg --start 2026-08-27 --end 2026-08-27` 中，EastMoney 再次真实 `NETWORK` 失败；Sina 备用源成功返回当日 48 根 5m K 线，raw snapshot=`sina-5m:2dd05fb2d53bf7d9dc715f5a51c94447a7a92cfce18fa9c3ad7ba5e1b4a2542b`。系统只形成 `SINGLE_SOURCE_5M` quality，明确 `canonical_updated=false`，并以 `PRESERVED_PREVIOUS_CANONICAL_DUE_TO_PROVIDER_FAILURE` 保留旧的双源验证 canonical，未把单源 fallback 冒充新 canonical。
- 本地官方日历真实执行 `sync-calendar --exchange xshg --start 2026-08-24 --end 2026-08-27 --live`，直接命中 `official-trading-calendar-config`，结果 `COMPLETE / 4 records / OFFICIAL_NOTICE_CALENDAR_USED`；snapshot=`official-trading-calendar-config:XSHG:9d3eb037255867fe1a975a3c7f7cfaf5b088574ea4466fdb58ad3c9c0b408ded`，release=`4271098ce63793b4acfe9e4cfe11b8a44cab32434d6a4e2b97890f2c144502ac`，manifest object=`9141699e49b54e4c2a965dacc3c128143ecec8ea7e26930804ede9cb6bc463d8`。实现路径在已配置年份提前返回，未先调用 BaoStock/API。
- 先前 CNINFO 命令因 MCP 断连没有业务终态，未记为 PASS 或 FAIL；恢复后重新执行真实 `disclosure-search`，请求实际发出并在 132ms 内返回 600519 的 3 条 2025 年报相关公告，`has_more=false / total_pages=1 / total_count=3`。index snapshot=`cninfo-disclosures:index:12cbd88670510e743c4fb35827e6019ef74dedd83a4188c654bf039006840b9b`，org-discovery lineage=`cninfo-disclosures:org-discovery:d3ee476569e3e1916e176b4ec21b22638d47fdd19761bfccb715352ef883b542`；随后 `disclosure-sync` 真实下载 `cninfo:1225114741`，PDF object=`474905deeaf0f875fc0a1b097a626c0c7852c427faadc5d7fc7816cbf45ea288`（1,082,847 bytes），PIT=`pit:349d7b760a87d8448d24f402a1f3f0f2e1b567246c479de71e0adebe8e273a66`。Windows 控制台中文显示乱码仅影响终端呈现，不影响结构化身份、哈希与 lineage。
- 组合故障注入覆盖 **CNINFO `ProviderError` + Sina/EastMoney 财务源同时不可用 + 已冻结深交所 exact-item 官方 PDF**。测试证明 CNINFO 只撞击一次，随后从 `OfficialWebDocumentCapture`、admission snapshot、document snapshot、PIT 和 PDF 页证据恢复 6 个核心字段；恢复 lineage 为 `OFFICIAL_WEB_EXACT_ITEM_ADMISSION`，`exhaustive_proof_allowed=false`，Financial Integrity 结果严格保持 **`NEEDS_INFO / PARTIAL`**。该用例同时发现并修复 `allowed_official_publishers` 只收录展示名、未收录 `sse/szse/bse/cninfo-official-web` 稳定 source id 的集成缺口。
- PARTIAL 仍允许受限候选研究，但不能自报为 COMPLETE。新增确定性门禁：当 `FINANCIAL_INTEGRITY` 不是由 `COMPLETE + SUCCEEDED` typed pack 证明时，`VALUATION=true` 的精确估值输出会被拒绝；observation-only valuation 可继续，但最终 `RecommendationReadinessReport` 必须同时缺失 `FINANCIAL_INTEGRITY` 与 `VALUATION`，`formal_recommendation_allowed=false`。`broker_execution_allowed=false`、Paper Ledger 与人工确认链均未放宽。
- SourcePolicy 实际 CLI 负向验收均按预期以非零退出返回结构化拒绝：正式 HTTP URL=`FORMAL_WEB_SOURCE_REQUIRES_HTTPS`；未注册域名/arbitrary PDF=`UNREGISTERED_FORMAL_SOURCE`；普通 Search 申请完整 A 股 Universe=`SEARCH_WEB_CANNOT_PROVE_COMPLETENESS`。因此 Search/Web 仍无 Universe、连续 OHLCV 或 negative-proof authority；Official Web exact-item admission 也不冒充 CNINFO exhaustive enumeration。
- s6 独立 Code Review 确认本次返工仅补齐稳定 official source-id 白名单和 typed financial→valuation 门，不新增平行事实源，不改变 PIT、SourceSnapshot、ObjectStore、Artifact、Evidence、Paper Ledger 或 broker 权限。定向 resilience 矩阵结果为 **`206 passed / 0 failed`，275.37s**；新增组合回归复测 `2 passed`，最终单项复测 `1 passed`；定向 Ruff=`PASS`，定向 Pyright=`0 errors / 0 warnings / 0 informations`。完整冻结树门、最终版本号、commit/push/tag/release 与 clean worktree 仍属于 s7–s8，尚未预写为完成。
- s7 独立 Review 继续发现并打回两项不能只靠推断验收的缺口：① migration 0060 虽已进入 fresh/idempotent 全迁移测试，但缺少显式 `0059 → 0060` 升级与 checksum 回归；现已新增真实旧库升级测试，验证 `0059` 后只应用 `0060` 一次、二次 migrate 为空、`integrity_check=ok`、`schema_migration.checksum` 与 0060 文件一致，定向 `test_state.py` **`21 passed / 0 failed`，16.07s**。② legacy raw 5m/60m sync 仍写 provider-wide `provider_health` 且未消费新 capability breaker，形成双轨 health 语义；现已收敛到统一 `SourceCircuitBreaker(provider, market.raw_<frequency>)`，停止回写 provider-wide probe health，并新增回归证明 `market.raw_5m` OPEN 不污染 `market.raw_60m`、OPEN 后不重复撞击、单源降级继续保留已有 canonical；该返工定向 **`14 passed / 0 failed`，12.02s**。
- s7 扩展定向回归覆盖 migration、raw intraday breaker、provider probe 全 lineage 损坏 fail-closed、CNINFO pagination terminal contradiction、SourcePolicy/Official Web、官方日历、候选 PARTIAL 门、Research Team 精确估值/正式推荐门、组合 financial recovery 与 provider probe，共 **`192 passed / 0 failed`，310.88s**。八份现行业务 Workflow 已与 capability-scoped health/breaker、Web/Search 权限边界、exact-item 非 exhaustive、5m 单源 canonical-preservation 语义同步；顶部旧 branch 记录已修正为当前 `main`。最终冻结树全仓门与发布证据仍必须在 s7–s8 后续真实执行，不能复用上述定向数字。
- s7 首轮全仓冻结检查实际收集 **`1072`** 项，终态为 **`1052 passed / 18 skipped / 2 failed`，1144.45s**，因此该轮明确判为未通过，未冒充发布门。两个失败均来自历史文档合同测试仍要求把已完成的 Phase 5 / composite registry / Priority 1 / `PENDING/NOT_RUN` 历史状态继续保留在《开发计划》，与当前 `AGENTS.md` 已固化的“开发计划只保存当前未完成任务、通过后迁入《验收报告》”维护合同冲突；未发现对应业务实现失败。已迁移 `test_original_instruction_contract.py` 与 `test_phase2_scope_configs.py` 到当前文档职责，分别整文件复测 **`4 passed / 0 failed`，0.36s** 与 **`6 passed / 0 failed`，0.37s**。18 个 skip 仍全部是既有显式 opt-in：30-document OCR acceptance、private-book visual、candidate live request，以及 CNINFO/market/provider-probe/reference live；本轮真实 live 证据已在上文另行记录。
- 修复后的**最终冻结候选树**已重新执行全部五门，未复用首轮或定向数字：`uv run ruff check .`=`All checks passed`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`；`git diff --check`=`exit 0`（只剩 CRLF→LF warning，无 whitespace error）；`uv run pytest` 收集 **`1072`** 项，终态为 **`1054 passed / 18 skipped / 0 failed`，1162.11s**；`uv run astock state-integrity-audit`=`PASS / integrity_check=ok / read_only=true`。18 个 skip 的精确原因仍为：30-document OCR acceptance 需 `ASTOCK_RUN_ACCEPTANCE=1`；private-book visual 需获批本地 run id；candidate live request 未配置；CNINFO、market provider、provider probe、reference live 测试需显式 `ASTOCK_RUN_LIVE=1`。本轮 s6 已另行真实执行两个 5m provider、CNINFO disclosure/PDF、官方日历及降级恢复，不能把 skip 或 EastMoney 网络失败改写为连通成功。
- s7 最终独立 Code Review 已按全部 tracked/untracked 候选树逐项通过：health/breaker 均按 capability 隔离；probe pointer/event/artifact/object/report 任一损坏 fail closed；Search/Web 无 Universe、连续 OHLCV 或 negative-proof authority；Official Web exact-item 不冒充 CNINFO exhaustive enumeration；CNINFO 全分页 lineage 完整且分页矛盾 fail closed；PARTIAL 不升级 COMPLETE，精确估值和正式推荐关闭；PIT、SourceSnapshot、ObjectStore、Artifact、Evidence、Paper Ledger 与 `broker_execution_allowed=false` 未放宽；migration 0060 的顺序、checksum、0059 升级、fresh migration 与幂等性均通过；未新增第二套 Router/Evidence/Paper 架构。s7 已满足验收条件，正式 commit/push/tag/GitHub Release、架构状态迁移为 `RELEASED`、远端一致与 clean worktree 仍只属于 s8，尚未提前宣告完成。

#### s8 正式发布证据（2026-08-28）

- 新会话恢复后重新核验 index：91 个候选路径全部 staged，`unstaged=0 / untracked=0`；最终 staged diff Review 覆盖配置、架构与 Workflow、migration 0060、业务实现、contract/integration/live/unit tests、`AGENTS.md`、《开发计划》和《验收报告》，`git diff --cached --check`=`PASS`。
- 最终 staged 安全门使用 Git/Python 直接读取 index blob，不依赖本机缺失的 `rg`：`runtime/`、`.ai-bridge/`、cache、private、Cookie、token、secret、私钥、PAT/API key、用户目录绝对路径、二进制与大文件均为 `0` 命中。Broker authority 复扫同样为 `broker_execution_allowed=true` 命中 `0`、真实券商 SDK import 命中 `0`；现有 `PLACE_ORDER` 仅属于 Paper Simulation，执行门显式拒绝 `protocol.broker_execution_allowed`。
- 实现提交 `c764e842d3eb1922bc206b7f3cffdd9759c8f1cc`（`feat: harden external dependency resilience`）包含 91 个已审计文件、`8903 insertions / 1041 deletions`，已成功推送 `origin/main`；推送后本地 `HEAD`、本地跟踪分支和 GitHub `main` 三者精确一致。
- 仓库真实版本为 `0.1.0`。正式 annotated tag `v0.1.0` 的 tag object 为 `c8a73b234bac6c8ebeeb76237d9669a49b2587a5`，解引用到实现提交 `c764e842d3eb1922bc206b7f3cffdd9759c8f1cc`，并已推送远端。
- GitHub Release `AStockMultiAgent v0.1.0` 已于 `2026-08-28T01:16:30Z` 发布，`isDraft=false / isPrerelease=false`，`targetCommitish=c764e842d3eb1922bc206b7f3cffdd9759c8f1cc`。已发布 tag 固定实现树；后续 `RELEASED` 状态文档提交只前移 `main`，不移动或重写 `v0.1.0`。

### 2026-08-28 自动补证 / BJSE / 性能 / Skills 优化——阶段性断点（未验收、未发布）

- 本节仅记录可复核的**中间事实**，不代表新版本验收通过。当前唯一续跑 durable run 为 `lr_mtcbh3fp_663fd2b079f6`；误建的 `lr_mtcdx0pi_d87d47737ddb` 与 `lr_mtce1p8q_2bf58387c20d` 已取消，避免后续出现多套状态源。
- 已复现旧 BJSE Master 路径：`sync-instruments --market BJSE --live` 返回 `record_count=339`，但 release 为 `PARTIAL`；reason 包含 EastMoney 覆盖未证明/低于最小覆盖、Sina 覆盖 denominator 未证明、BaoStock health 异常，说明“能抓到 339 只”不等价于“正式 Universe 完整性已证明”。
- 北交所官方股票列表后端 `/nqxxController/nqxxCnzq.do` 已独立实测：`totalElements=339 / totalPages=17`；完整分页两轮均得到 `339 rows / 339 unique / duplicates=0 / terminal=1`，其中一轮与当轮 Sina BJSE 证券集合 `same_set=true`。该结果支持将 BSE 官方名录作为慢变 Universe denominator 候选，但仍需代码 Review、故障注入和更多 live 稳定性验收后才能成为正式发布事实。
- 当前工作树为开发中间态：15 个变更路径，diff 约 `+650 / -17`。已修改 Provider registry、market reference route、transport/authority config、Candidate Seed/Reference 逻辑与 `HttpProviderBase`；新增 `src/astock/providers/bse_official_reference.py`、BSE recorded fixtures、`tests/unit/test_bse_official_reference.py`。这些文件尚未 commit、push 或 release。
- 当前代码方向是：为 BJSE 新增官方 `bse-official-reference`，校验 JSONP 分页、`totalElements/totalPages/lastPage`、重复与排序，并冻结每页/聚合 snapshot；market-reference route 增加 BJSE market-scoped 官方 master/identity；Seed 层尝试将快变 quote payload 与官方 Universe denominator 绑定；HTTP base 新增统一 `_request()` 以支持受控 POST。以上仍需测试修正，不能当作已完成能力。
- 自动补证问题已定位到产品化缺口：`CurrentResearchAcquisitionService` 会生成 `external_research_needs` 并返回 `NEEDS_EXTERNAL_RESEARCH`，但当前仓库没有强制消费这些自动任务并继续 Fundamental / Financial Integrity / Valuation / 独立 Review / Committee 的持久 continuation 状态机；因此用户提出的“自动补齐，不要停下来报告缺失”仍未实现。
- 性能/复杂度与 Skills 增强尚未进入可验收实现：需要继续做 request-scoped service/provider 复用、single-flight/重复 I/O 削减、多保真 top-K/早停的前后基准；并对宏观政策、行业价值链、催化剂、治理、独立红队、模型风险/回测验证等薄弱 Skill 建立可执行合同和测试。
- **本断点没有新的独立 Review 或冻结测试结论**：此前挂接的 Pyright/background task 在 MCP 重连后显示 unknown，必须在下一次开发时重新执行，不得复用为 PASS。尚未完成 BJSE 10 轮 live 稳定性、故障注入、Ruff、Pyright、全仓 Pytest、state integrity、端到端单股 continuation smoke 与全市场 smoke。
- 下一次恢复必须先保护当前工作树、不 reset/clean/restore；恢复顺序固定为 `AGENTS.md → docs/architecture/ → 开发计划.md → 验收报告.md → long_run_status(lr_mtcbh3fp_663fd2b079f6) → Git/测试/运行工件`，然后从 BJSE provider 单测/类型检查继续。

### 2026-09-01 M-01/M-03 统一 Response Gateway 完成验收

- M-01 与 M-03 已完成实现和独立缺陷优先复查：公共输出统一使用 `ResponseContext`、`ResearchNarrativeBundle`、`InvestorPresentationModel`、`DeveloperDiagnosticsModel`、`PresentationAudit` 与 `ResponseGateway`，调用方不再各自维护平行字段合同。
- `FactFingerprint.direction_terms` 与 `ResearchNarrativeBundle.headline` 是唯一规范写出字段；旧的 `directions` 与 `conclusion` 仅作为输入兼容别名。冲突的新旧字段会拒绝解析，避免静默取值。
- `ResponseContext` 已包含 `requested_detail`；审计结果显式记录事实等价、强制内容保留、字符预算、Secret、私人路径和内部实现暴露状态。
- 投资者投影稳定包含主体、结论、结论强度、估值或赔率、理由、最大风险、改变判断的条件、数据时间、必要引用及安全化报告引用。压缩只允许去重或减少非关键理由；强制内容仍超预算时使用不回显原稿的安全摘要。
- 事实保护同时阻止新增和删除：实体名称、证券代码、数值、日期、时点、方向、引用、结论强度以及结构化锁定短语发生变化时均失败关闭。
- 模式识别以明确的用户诊断意图为准；否定式诊断表达、普通“为什么”问题和系统自身错误文本保持 Investor Mode。
- `research-investor-view` 与 `research-acquisition-investor-view` 继续输出既有 `investor-research-view-v1` 机器 JSON；新增 `research-public-view` 与 `research-acquisition-public-view` 作为经过公共网关审计的展示入口。
- 验证证据：`tests/unit/test_presentation_gateway.py` 实际收集并通过 179 项；与机构研究/终局合同组合为 198 项通过；当前研究产品化、continuation 与 observability 组合为 41 项通过；产品表面、Workflow、Repo Skill 与 CLI 组合为 40 项通过。Ruff 通过，Pyright 为 0 error / 0 warning，`git diff --check` 无 whitespace error。
- 本工作包只改变公共展示合同、策略配置、CLI 展示入口、相关架构/Workflow/Repo Skill 和测试；日志、错误映射与自动解决预算仍属于后续 M-02。

### 2026-09-01 M-04 正式报告服务、展示偏好与跨会话恢复完成验收

- 新增正式报告独立发布层：`ReportRequest / CitationManifest / AssetManifest / ReportManifest / ReportPublishResult / PresentationPreferences`、`src/astock/reports/`、`report_policy.yaml`、migration 0061/0062 与机器 JSON CLI。报告只消费冻结 Narrative/Presentation、已注册 Artifact/ObjectStore 和引用/资产清单，不重新抓取或改写研究事实，也不触碰 Paper Ledger/持仓事实。
- 默认 DOCX 使用 `python-docx` 且无 Office 依赖；生成后执行 ZIP/OpenXML member、relationship、XML 和媒体完整性检查。DOCX renderer 异常稳定降级 MD；PDF 仅在受控 converter probe、转换和 `%PDF/%%EOF` 校验成功时声明 PDF，converter 缺失/失败时保留已验证 DOCX并显式 `DEGRADED`，不伪造 PDF 成功。
- 路径层使用 Windows Known Folder API，不硬编码用户名；`ASTOCK_REPORT_ROOT`/配置目录不可写时按版本化策略回落到受控 `runtime/reports/output`。文件名经过确定性清洗与 containment 校验，公共结果只返回安全文件名/相对引用，不回显本地绝对路径。
- Manifest 固化输入 Artifact/Object 哈希、模板/renderer/converter 版本、引用清单、资产权利/排除原因、隐私与引用等级、实际格式、目标策略和输出 SHA-256；只允许 `OWNED / LICENSED / PUBLIC_DOMAIN / PUBLIC_DISCLOSURE` 资产嵌入，`FAIR_USE/UNKNOWN/UNLICENSED` 不作为自动嵌入授权。
- 幂等身份已纳入有效格式、有效隐私/引用级别、目标目录策略和模板版本。原子发布前持久化 STAGED manifest/checkpoint/request object；replace 前中断保留受控 staging，replace 已完成但 finalize 中断可校验既有 final 并完成 finalize，均不会留下伪成功状态或重新渲染一份字节可能不同的 DOCX。
- 展示偏好收敛为单用户 canonical profile 的逐 key `BASE + OVERRIDE` 两层存储：BASE 跨会话持久化，临时 override 可删除恢复 BASE，BASE 可 reset 恢复策略默认；临时 override 在没有 BASE 时删除后不会错误固化为新的 BASE。自定义绝对报告根仅 request-scoped，不持久化/公开投影。
- 缺陷优先 Review 实际修复：① 0062 仍是旧 `profile_id/payload_json` schema 而 repository 已切 key-layer；②请求 privacy/citation 非空默认导致用户偏好永远失效；③ idempotency 未绑定有效格式/目标策略/模板；④ STAGED checkpoint 会被失败记录覆盖；⑤ PDF converter 不可用时错误丢弃有效 DOCX转 MD；⑥版权策略错误把 FAIR_USE 自动当成可嵌入；⑦安全清洗后的已排除资产因剥离私有路径反而 schema 失败；⑧ CLI JSON 曾被 stderr structured log 污染，现 CLI 配置禁用 console sink、文件日志合同不变。
- 运行库核验：正式 `runtime/state.sqlite` 在本轮收口时 `0062=None / presentation_preference columns=[]`，确认旧版未完成 0062 从未污染真实运行库；新 migration fresh DB 结构为 `key/base_value_json/override_value_json/updated_at`。
- 冻结候选证据：报告专项 **`19 passed / 0 failed`**；报告+CLI+logging+Workflow+Repo Skill+Presentation/Product 相关回归 **`297 passed / 0 failed`**；`uv run ruff check .`=`All checks passed`；`uv run pyright`=`0 errors / 0 warnings / 0 informations`。所有本轮测试 `TEMP/TMP/UV cache/PYTHONPYCACHEPREFIX` 均显式指向 `D:\AStockMultiAgent\runtime\...`，未在项目目录外创建任务文件。
- 回滚保持安全：可禁用 PDF converter 或退化 MD-only，不影响 Research/Evidence/PIT/Recommendation Gate/Paper Ledger；`broker_execution_allowed=false` 未改变。

### 2026-09-02 M-05 Universe 正式覆盖证明与推荐门加固完成验收

- 新增 typed `UniverseCoverageProof` 与 `MarketCoverageReconciliation`，覆盖等级收敛为 `UNAVAILABLE / PARTIAL / ENGINEERING_HIGH_COVERAGE / OFFICIAL_DENOMINATOR_RECONCILED`。每个市场对账记录分母来源与 capability、分母 authority、分母/分子数量、missing/extra、覆盖率、来源版本、观察/可得时间、分母/分子对象哈希及 Snapshot lineage。
- `coverage_ratio >= 99.5%` 继续保留为工程高覆盖门，但不再产生正式全市场推荐 authority。二级 Provider 自报 denominator 即使达到 100% 也只能得到 `ENGINEERING_HIGH_COVERAGE`；`formal_full_market_coverage_allowed=true` 只从三市场均完成 `OFFICIAL_DENOMINATOR_RECONCILED` 的 typed proof 派生。
- `ResearchSeedReport` 对旧 v1/旧缓存执行安全兼容：缺 typed proof 时会依据旧 market ratios 最多映射到工程覆盖并强制关闭正式 full boolean，不允许旧工件静默升级。零候选与 Universe 不可用继续分离：完整工程扫描后零有效候选仍为 `EMPTY`，覆盖不可证明才为 `NEEDS_INFO/UNAVAILABLE`。
- Research Team 的 `UNIVERSE_COVERAGE` readiness 现在重新验证 exact proof，而不信任 Role 文本或自报布尔：proof Snapshot 必须属于 Seed Report 输入、ObjectStore 可验证、可得时间不晚于 proof/report cutoff，denominator/numerator hash 必须与注册 Snapshot 精确匹配；缺失、损坏、hash/snapshot mismatch 和血缘漂移均 fail closed。
- Market Reference v3 只在新发布路径显式写入市场对账；未显式 `schema_version` 的旧 `DatasetReleaseManifest/ReferenceSyncReport` 保持 v2/v1 读取合同，避免已有 recorded fixture、缓存与故障恢复路径被新字段错误拒绝。
- 验证证据：Universe/Seed/Research Team/Reference/Router/Provider/Portfolio 组合回归 **145 passed / 0 failed**；此前专项 Universe/Seed/Team/Reference/Portfolio 组合 **76 passed / 0 failed**。六项旧缓存兼容回归最初稳定复现后已修复并纳入 145 项通过结果。完成前另执行变更 Python 的 Ruff、Pyright 与 `git diff --check`，正式提交与远端事实只在工作包提交后记录于 Git，不在本节预写。
- M-05 不新增第二套 Universe/推荐系统：现有 StateStore/ObjectStore、Provider Registry、SourcePolicyGate、ResearchSeed/Research Team/Recommendation Gate 仍是唯一链路；`broker_execution_allowed=false` 未改变。

### 2026-09-02 R-04 外部账户事件、更正、现金、批量导入与多账户隔离完成验收

- 新增 migration `0063_external_account_events.sql`、typed `ExternalAccountIdentity / ExternalAccountEvent(Draft) / ExternalAccountImportPreview/Receipt / ExternalAccountProjection` 与 `ExternalAccountRepository`。SQLite `external_account_event` 成为外部真实账户 append-only 权威事实，UPDATE/DELETE 由数据库触发器阻断；`external_account` 按 `account_id` 隔离账户，`user_state/*.md` 仅保留 Git-ignore 投影/旧单账户兼容镜像。
- 事件类型覆盖 `TRADE / CASH_DEPOSIT / CASH_WITHDRAWAL / CASH_ADJUSTMENT / SECURITY_TRANSFER_IN / SECURITY_TRANSFER_OUT / REVERSAL`，replacement/reversal 只追加不覆写。Review 修复了 replacement 被 reversal 后原事件未恢复的递归有效性缺陷，并增加 correction-safe 拓扑插入，使同批更正行不再依赖 CSV/JSON 物理行顺序。
- CSV/JSON 批量导入采用 `preview → confirm` 两阶段：原始文件先入 ObjectStore，schema/时区/account/secret/correction/持仓约束全部校验；confirm 再核对 source hash，并在单一 SQLite transaction 中整批写入。错误行、source drift、跨账户更正或持仓不足均不产生部分提交；相同 batch/event/idempotency 重试幂等。
- 与模拟盘严格隔离：external `TRADE` 在插入前按 symbol/side/quantity/price/time 与现有 paper fill 做经济冲突检查，命中即 fail closed；所有外部账户接口保持 `paper_ledger_write_allowed=false / broker_execution_allowed=false`，import schema 也拒绝 password/token/cookie/authorization/api_key 等券商 Secret 字段。
- 旧单账户兼容：`external-account-migrate-legacy-default` 只迁移 `trades.md` 的 `IMPORT` 到 `default`，`PAPER_FILL` 保留在 paper lane；既有 `portfolio-import-declared-trade` 与旧 `local-portfolio-import-trade` 均先写 canonical `ExternalAccountEvent`，再刷新 legacy Markdown。Review 进一步修复“canonical event 已落库但 Markdown 刷新失败却返回失败/冲突”的跨事实源不一致：冲突预检发生在权威事件前，投影失败仅记录可恢复 reason code，重试可补回投影且不会重复 event；legacy migration 还会按经济键识别已由新 CLI 表达的同笔交易，避免二次迁移。
- 组合研究边界也已收口：`portfolio-local-snapshot` 在 `default` 外部账户存在时，以 `ExternalAccountProjection` 的持仓数量、平均成本和现金为权威，旧 Markdown 只补 review/open-order 人类交互信息；只有尚未建立外部账户时才安全回退旧 `LOCAL_USER_STATE`。稳定 CLI 已覆盖 schema/create/list/event-append/import-preview/import-confirm/projection/audit/migrate-legacy-default；公共结果只返回 batch/event/account 与安全相对投影信息，不要求也不保存券商凭证。
- 验证证据：最终 R-04 主线在独立 Review 返工后，专项 `ExternalAccount + LocalPortfolio + PortfolioDecision + product-surface + State` 为 **63 passed / 0 failed**；全仓 L3 从头执行为 **1413 passed / 18 skipped / 0 failed**（18 项均为需显式 acceptance/live opt-in 的既有跳过项）。同一最终工作树 Ruff `All checks passed`，Pyright `0 errors / 0 warnings / 0 informations`；`0062 → 0063` 真实升级、重复 `migrate()==[]`、`integrity_check=ok`、目标表/append-only trigger 存在性 smoke 全部 PASS。Review 中先后复现并修复 replacement reversal、paper-fill 双计、旧单账户迁移、批次 correction FK 顺序、canonical/projection 失败语义、旧 CLI 绕过 canonical 账本、快照继续读取旧 Markdown，以及 migration 合同滞后等缺陷。
- 性能与回滚：按账户事件回放为 O(n)，SQLite 具备 account/time 与 correction/source indexes；可关闭批量 importer/CLI 而保留已确认 append-only 事实。真实券商自动执行、券商凭证存储、外部事件反写 paper ledger 均未引入。

### 2026-09-02 M-06 外部能力统一资格验证、GDELT 回收与生产备用路由完成验收

- 新增 `ExternalCapabilityRegistry / ExternalCapabilityDefinition / CapabilityQualificationRequest/Report / CapabilityRevocation` 与 `configs/external_capabilities.yaml`。API、Python library、MCP、crawler、parser、Skill 统一进入同一资格模型；首批登记 AKShare、Arelle、Docling、Playwright MCP、Crawl4AI、changedetection.io 与项目自有治理/报告 Skills，单个候选的真实版本、License/ToS、数据权利、PIT/provenance、Secret/SBOM、成本、性能、故障和退出证据必须由资格报告逐项 PASS，不能由 stars、Registry 上架或配置声明代替。
- migration `0064_external_capability_qualification.sql` 只保存资格/撤销索引，正式报告继续冻结在现有 ObjectStore/Artifact lineage；qualification 与 revocation 表均由 UPDATE/DELETE trigger 保护为 append-only。Review 额外修复了“仅篡改 SQLite 索引有效期即可延长资格”的风险：`active_report()` 会把 capability/version/stage/valid_from/expires_at 与不可变 ObjectStore 报告逐项对拍，索引漂移时 fail closed；撤销工件输入绑定目标资格报告的 object hash，而不是把 report id 冒充内容哈希。
- `SourceAccessRouter` 与 `ProviderFactory` 复用既有 Router，不建立第二路由：`production_backup=true` 的 Provider 必须绑定 external capability，只有有效 `PRODUCTION_BACKUP` 报告、authority/completeness ceiling 一致、且标准路线当前不可用时才可被选中；主源仍可用、资格到期/撤销、证据损坏或 registry ceiling 不一致时备用均不可用。执行型券商 MCP/`broker.order.execute` 等能力在 schema 层永久 `REJECT`。
- GDELT 已从监控模块的特殊外部 I/O 收口为 Provider Registry 的 `gdelt-news-leads`，能力仅为 `news.discovery.lead`、无 formal capability、完整性固定 `DISCOVERY_ONLY`；请求受 capability circuit breaker 控制，响应进入 `SourceSnapshot`/ObjectStore，Monitor event 明示 `lead_only=true`，不会直接升级公司事实、推荐或交易动作。
- CLI 已注册 `external-capability-schema/list/status/qualify/revoke`；本轮补充主应用 schema 命令回归，机器输出稳定。候选的真实受控 live smoke 与实际 `PRODUCTION_BACKUP`/Skill 资格裁决继续由 E-02 执行；M-06 只交付并验收统一准入机制，不把尚未完成 E-02 实证的候选写成已生产准入。
- 验证证据：隔离分支接管后曾完成扩大回归 **141 passed / 0 failed**；合并到 R-04 最新 main、补 CLI 与 `0064` migration 主线合同后，最终组合为 **166 passed / 0 failed**，覆盖 external capability、Provider Registry/Resilience、Source Router/Resilience、Continuous Monitoring、State、product surface、provider probe integration/contract/recorded fixtures。最终工作树 Ruff `All checks passed`，Pyright `0 errors / 0 warnings / 0 informations`。资格索引漂移、到期/撤销、主源可用时禁止 backup、GDELT 429 breaker、券商执行能力拒绝均有负向回归。
- 回滚：撤销资格或禁用/删除可选 adapter 后，既有主 Provider、SourcePolicyGate、Evidence、报告、R-04 账户账本与 paper ledger 不受影响；本工作包未引入真实券商执行或第二事实源。

### 2026-09-02 E-01 受约束中文 Humanize A/B 完成验收

- 新增完全位于 `experiments/presentation_humanize/` 的离线、零网络、零外部模型 A/B harness，生产 `src/astock` 不做任何改动。预注册固定比较 `RULE_BASELINE / PROJECT_CONSTRAINED / HUMANIZER_ZH_RISK_PROFILE` 三臂，事实锁覆盖实体、证券代码、数字、日期/时间、方向、结论强度、引用与锁定短语；输入先复用生产 redactor，再复用 `normalize_public_text / extract_fact_fingerprint / audit_public_answer` 做事实等价与安全审计。
- 冻结 8 个中文投资表达 case，覆盖价格、百分比、日期、方向、引用和私人路径脱敏。项目受约束臂只删除预注册的非事实套话并在事实 signature 变化时自动回退；当前无人工评分的正式实验报告 `7a9e029d983ee4febb637a672c3728083570522a090d067a736257bdd06f0a81` 显示 fact drift=0、sensitive leak=0、unsupported experience=0，较规则基线自然度 `+0.070`、清晰度 `+0.037432`、简洁度 `+0.122801`，全部超过预注册 `+0.005` 自动阈值。
- Humanizer-zh 未下载、未执行、未进入依赖；只根据已审查的公开规则制作 recorded risk-profile 样本。Review 实际发现并修复 harness 的可重复性缺陷：unsupported-experience 与 prompt-injection 正则最初是一次性 generator，第一条样本后会耗尽，造成后续风险漏检；改为不可耗尽 tuple 并加入多 case/重复运行回归后，录制风险臂稳定识别虚构第一人称/社交经历与注入文本，并固定 `REJECTED_RISK_PROFILE`。
- 盲评机制输出不含 arm 标签的 `blind_review_template.csv`，内部 arm key 不进入提交；ratings loader 拒绝带 arm 标签、未知 blind id、同一 reviewer 重复评分。测试使用的 synthetic ratings 仅验证“若未来真实盲评完成，门控计算是否正确”，不会被写入正式实验报告或验收结论。
- **关键裁决**：真实人工盲评尚未执行，当前 `human_review=PENDING`，所以即使项目受约束臂自动门通过，`production_proposal=NO_CHANGE`、`production_default_enabled=false`。本轮不创建生产开关、不将 Humanizer-zh 或任何自由改写模型提升为生产依赖；后续若要形成受控备用提案，必须补齐至少每候选 2 名真实盲评者并达到预注册人工增益阈值。
- 验证证据：隔离 worktree 最终提交 `7e4a6d2` 仅包含 experiment/tests；集成到 M-06 最新 main 后，`test_presentation_humanize_experiment + test_presentation_gateway` 为 **184 passed / 0 failed**，Ruff `All checks passed`，Pyright `0 errors / 0 warnings / 0 informations`。实验输出记录 `network_requests=0 / external_model_calls=0 / total_estimated_cost_usd=0`；私人路径与运行时构造的 Secret-like 输入均在进入实验臂前被脱敏。
- 回滚：整个实验目录与测试可独立删除，生产 Response Gateway、正式报告、Evidence、Provider、账本和用户状态均不依赖 E-01 产物。
