# AStockMultiAgent 当前验收报告

## 报告口径

验收报告只记录当前已经实现且有可复核证据的事实。本报告随
`codex/phase5-stabilization` 的稳定化提交固化；Git 身份以包含本文件的提交为准。提交前结果为
候选基线，提交后且工作树无额外改动时为正式基线。

## 当前能力矩阵

| 能力 | 当前状态 | 已验证边界 |
|---|---|---|
| ObjectStore / Parquet / SQLite / migration | 已实现 | 原始对象不可覆盖；分析事实和元数据分离 |
| 东方财富/Sina 未复权 5m | 部分可用 | 免费历史有限；Sina 最新 1023 根；北交所偏差门保留 |
| 巨潮公告与 PDF | 已实现 | 其他交易所入口尚未统一接入 |
| 财务完整性审计 | 已实现 | 当前依赖准备好的结构化输入；Provider 自动链未完成 |
| Phase 4 研究/专家/诊断/持仓生命周期 | 程序已实现 | 输入仍需冻结装配 |
| Phase 6 委员会 | 已实现 | 断网、只读冻结工件；不能写账本 |
| 模拟账本与 5m 回放 | 核心已实现 | 公开下单/撤单/结算操作层未完成 |
| Phase 7 影子评测 | 程序已实现 | 真实前向观察为 0 |
| Phase 8 | 按设计未进入 | 自适应权重、在线学习、券商执行均关闭 |
| 知乎回答/文章/想法采集 | 部分覆盖 | 评论永久退出生产；1 条详情缺口及专栏枚举证据待处理 |
| Paragraph → Argument 新链 | 已实现并真实运行 | 三个完整 AU 批次已导出；DeepSeek 外部执行和人工批准尚未发生 |
| 本地 BGE 三视图 | 三位作者全量完成 | 77,202 个向量、5,332 个 AU 评分；未校准阈值不自动删除 |
| Serenity | 来源审计与 6 个方法合同 v2 已验收 | 固定上游 commit、本地适配哈希、严格 PIT/证据/降级合同 |
| Provider 注册与探针 | 已实现 | 5 个免费 Provider；默认 recorded，live 必须显式开启 |
| 免费 reference 数据 | 已实现 | 主数据、日历、未复权日线、公司行动线索；无账本写入 |

## 已验收的基础与研究能力

- `uv run astock probe` 可报告 Python、SQLite、行情 Provider、委员会、Codex 工件、影子评测和
  Phase 8 禁用边界。
- 近期探针结果：Python 3.12.10，SQLite integrity `ok`；东方财富/Sina 为 `PARTIAL`；QMT 为
  `UNAVAILABLE`；OpenAI-compatible 为 `OPTIONAL_DISABLED`。
- 默认模拟账户可初始化为 100 万元，双重记账平衡；当前真实运行状态为 0 订单、0 成交、0 持仓。
- Phase 7 当前 study/arm/assignment/observation 均为 0，状态 `NOT_RUN`。
- Phase 8 状态 `NOT_ENTERED_BY_DESIGN`，仍缺 12 个月、100 个独立决定、5 个合格折和 3 个市场
  状态；没有任何自动准入或实盘能力。

## Phase 5 采集与覆盖事实

最新覆盖审计时间为 `2026-07-22T08:42:57Z`，冻结截点为 `08:42:27Z`，状态 `PARTIAL`。

| 作者/类型 | 目录唯一项 | 详情已验证 |
|---|---:|---:|
| MR Dang 回答 / 文章 / 想法 | 118 / 16 / 0 | 118 / 16 / 0 |
| 黄彦臻回答 / 文章 / 想法 | 831 / 8 / 1 | 831 / 8 / 1 |
| 派大星皮皮回答 / 文章 / 想法 | 895 / 24 / 1,896 | 895 / 24 / 1,895 |
| 合计 | 3,789 | 3,788 |

该审计有开放 gap 9、待消费导入 0、对象缺失 0、Parquet 错配 0。寒武纪的鳄鱼本地导出为
`USER_CONFIRMED_COMPLETE_EXPORT`。生产采集配置中评论、嵌套回复和作者参与链均为 false。

## 新论证级知识链

### 已实现合同

- migrations `0035_knowledge_semantic_argument.sql` 与
  `0036_semantic_candidate_provenance.sql` 均为 additive migration；空库安装和迁移幂等测试通过；
  0036 将离线批次/响应哈希绑定到候选 provenance。
- `ParagraphUnit` 保存稳定定位、文本对象哈希、16 种修辞角色、依赖、合并动作、
  `standalone_distillable`、主题相关度和方法完整度，不在 SQLite 保存正文。
- `ArgumentUnit` 只组合同一内容项内连续段落，关系支持问答、主张—证据、主张—解释、案例、
  反方、结论和延续。
- 自包含单段可形成 AU；依赖上下文的孤段保持 `NEEDS_REVIEW`。
- 关键词以完整回答/文章/想法为筛选单位；完全不命中只派生排除，原始对象仍可验证。
- 生产服务只选择每个内容 ID 的最新 `DETAIL_VERIFIED` 版本，避免较晚列表快照遮住已验证正文。
- 生产输入固定为回答、文章和想法，评论只存在于 synthetic golden fixture。

### Golden 验证

`tests/fixtures/knowledge/argument_golden_cases.yaml` 包含 25 个跨段/边界案例和 2 个纯负例，覆盖：

- 设问—回答、多个问题共用回答、反问；
- 故事到规则、主张—案例、主张—证据、隔段证据；
- 反方、结论、过渡、指代、风险；
- 两个相近主题的边界、图片阻隔；
- synthetic 评论追问链；
- 营销与闲聊整篇排除。

Golden 定向结果为 `28 passed`；语义持久化/三视图/离线包定向结果为 `5 passed`；migration/state
定向结果为 `13 passed`，合并语义复审门禁为 `48 passed`。这些是本工作树定向结果，不替代最终
全仓门禁。

### 真实论证层运行

| 作者 | semantic run ID 后缀 | 详情篇目 | ParagraphUnit | ArgumentUnit | 整篇关键词候选 / 排除 |
|---|---|---:|---:|---:|---:|
| MR Dang | `8c318191…cb925` | 134 | 7,667 | 297 | 122 / 12 |
| 黄彦臻 | `4bac2f5b…a1bff` | 840 | 6,135 | 633 | 395 / 445 |
| 派大星皮皮 | `90ad5c7d…c2ef` | 2,814 | 28,318 | 4,436 | 1,293 / 1,521 |
| 合计 | — | 3,788 | 42,120 | 5,366 | 1,810 / 1,978 |

以上运行均已推进到 `DEEPSEEK_PACKET_READY`，`comments_included=false`。MR Dang 的 AU 中
43 READY、235 NEEDS_REVIEW、19 DERIVED_EXCLUDED；黄彦臻为 136/492/5；派大星皮皮为
1,126/3,300/10。`READY + standalone_distillable` 才会进入外部包，结构未闭合 AU 不因相似度高而
外发。

## 本地语义模型与真实运行

已安装并校验官方 `BAAI/bge-small-zh-v1.5` 固定 revision
`7999e1d3359715c523056ef9478215996d62a620`：

- 维度 512；本地 CPU；模型运行时禁止联网和 remote code。
- 本地资产 bundle SHA-256：
  `81d3cf5d98d31f7e79868f3667209279fff448b35490bffac3f4b2d5cc6f3563`。
- 三种必需视图为当前 Paragraph、前 1＋当前＋后 2、完整 ArgumentUnit；另有 14 类方法原型。
- 超长输入按 token 窗口覆盖全部内容，再按 token 权重聚合并 L2 归一化，不静默截断。
- 向量和评分进入两个 Parquet；SQLite 只保存 manifest 与哈希。

当前真实 Embedding 结果：

| 作者 | embedding manifest 后缀 | 向量数 | AU 分数 | 状态 |
|---|---|---:|---:|---|
| MR Dang | `18cc8d3d…0e3a` | 15,172 | 278 | `DEEPSEEK_PACKET_READY` |
| 黄彦臻 | `b1180936…5472c` | 8,100 | 628 | `DEEPSEEK_PACKET_READY` |
| 派大星皮皮 | `c43862fc…60f76` | 53,930 | 4,426 | `DEEPSEEK_PACKET_READY` |
| 合计 | — | 77,202 | 5,332 | — |

三位作者最高相似度范围分别为 `0.431492～0.715618`、`0.346099～0.710184` 和
`0.389977～0.767599`；0.3/0.4 对本域几乎没有筛除力，不能直接用来永久排除。当前
`automatic_exclusion_enabled=false`；真实域校准前只保留排序与审核带。

## OpenCode/DeepSeek 边界

根目录已提供 `OPENCODE_DEEPSEEK_PROMPT.md` 和 `OPENCODE_DEEPSEEK_TASK.md`。离线包只导出完整
ArgumentUnit，并携带内部段落、修辞角色、关系、来源快照、输入哈希、主题相关度和独立的方法
完整度。输出 schema 要求每个 AU 恰好一条 JSONL，模型不能自评、自批或写交易指令。

真实批次已经冻结但尚未外发：MR Dang batch `0c4e3f45…28c3e`（43 条）、黄彦臻
`40031660…8f51`（136 条）、派大星皮皮 `b9729bf2…268d`（1,126 条），合计 1,305 条。另有
4,027 个结构未闭合 AU 留在本地审核边界，其中 105 个同时触发超长审核。result stage/import 的整批原子拒绝、
哈希/引用/作者校验、幂等和候选来源绑定已通过 recorded integration；外部 DeepSeek 结果和真实
SkillCandidate 尚未产生。

## Serenity 整合事实

当前 `configs/research_skills.yaml` 为 `research-skills-v2`，固定两个 Serenity commit 并将上游方法
映射到 6 个已启用的 v2 本地合同：IndustryBottleneck、EventToAlpha、GrowthProbability、
GrowthValuation、DailyTrendHealth 和 ResearchMemoComposer。注册表共 7 个合同，其中
`HourlySwing` 为纯本地合同。v2 冻结产业层级/候选宇宙、事件到 Alpha 的验证链、H0～H5 贝叶斯
更新、TAM/PEG 单位、20/50/100/200DMA 健康和 Bull/Base/Bear memo；缺证据时确定性降级。
未经 A 股历史校准的固定权重和阈值只报告，不进入交易裁决；v1 历史工件不被改写。

两个 v2 `OpenSourceAuditManifest` 机器可校验：固定 commit、MIT LICENSE 哈希、每个来源
各 11 个关键文件 SHA-256、精确上游文件到本地合同的映射、本地 patch set/hash 和不可变升级策略。
`open-source-audit-status` 静态验证 PASS；两个临时固定 commit checkout 通过
`open-source-audit-verify` 全量重算，均为 11/11 文件匹配。两个 manifest 还绑定 19 个本地适配文件
的逐文件哈希和 bundle `f1d50b3af485b4eea4d125b7f35b4e4c0a77183fe88f1bb0f201232147bedc61`，因此
不能再用“只冻结上游、未证明本地整合”冒充闭环。正常研究运行不联网、不 vendor 上游源码。

SER-3 的用户路由仍未验收：候选宇宙尚未由 P5X-4 冻结，因此 v2 方法不会自动进入委员会或模拟
交易。

## Provider 注册与免费 reference 数据底座

### P5X-1：Provider Registry

- `provider-list`、`provider-probe`、`provider-status` 已实现；registry 为
  `provider-registry-v2`。
- 已登记东方财富 5m、Sina 5m、巨潮披露、BaoStock reference 和东方财富 reference 五个免费
  Provider，记录能力、transport、officiality、timeout、recorded fixture 和 live 支持边界。
- 默认探针只读取 recorded fixture；真实网络必须显式选择 live。原始响应、探针报告、ObjectStore、
  artifact registry、不可变 probe event 和 latest health 指针形成闭环。
- migration `0037_provider_probe_history.sql` 保存不可变事件历史；latest 由
  `(completed_at, probe_id)` 确定，损坏报告 fail closed，CLI 不泄漏响应正文或私密路径。
- P5X-1 收口定向结果为 `70 passed / 3 live skipped`；三个原有 live Provider 的显式 smoke 为
  `3 passed`。

`financial.structured`、`corporate_actions.ledger_ready` 和 `model.inference` 仍是明确 capability gap，
没有叙述性伪装为已连接。

### P5X-2：免费 reference foundation

- `sync-instruments`、`sync-calendar`、`sync-daily`、`sync-corporate-actions`、
  `reference-status` 和 `reference-audit` 已实现。
- BaoStock `0.8.9` 是主数据、交易日历、未复权日线和公司行动结构化线索主源；东方财富 direct
  HTTP 是日线/主数据备用及北交所覆盖。AKShare 未引入，Tushare/QMT 不是硬依赖。
- 原始 envelope 先写 ObjectStore；observation/canonical facts 写 Parquet；manifest 写
  ObjectStore；release/head/checkpoint 写 SQLite。Parquet 的文件哈希、schema、行数和从实际
  `record_json` 重算的逻辑哈希在推进 head 前全部复验。
- 日线固定上海交易日 15:00 可得边界、`NONE` 复权、`SHARE/CNY` 单位；东方财富 lot 明确乘 100
  并有成交额/成交量量级门。相同事实但新 raw provenance 会形成新不可变 release。
- 公司行动结构化线索必须一对一匹配巨潮官方文档；歧义不链接，`TERMS_VERIFIED` 前永不允许写
  ledger。北交所在缺官方证据时明确 `OFFICIAL_EVIDENCE_UNAVAILABLE`。
- BaoStock SDK 全临界区由 fenced lease 与 heartbeat 串行化；阻塞、丢租约、login/query/logout
  异常均 fail closed 并保留不完整 raw envelope。
- migration `0038` 保持已应用校验和
  `cf261bc90ac3c4b86efc5ce3a87c2fc2333baf283548a06c8717f43031fed813`；`0039` 以增量迁移移除
  旧 content 唯一约束、增加完整性字段和 provider lease。旧 v1 release 只显示
  `UNVERIFIED_LEGACY`，不能作为 PIT 事实消费，但验证 lineage 后可作为 previous head 前滚 v2。
- 根编排最终 reference 验证为 `40 passed / 3 live skipped`。东方财富 reference 的沪深/北交所
  显式 live smoke 曾为 `2 passed`；BaoStock live 两次遇到外部 SDK TCP `WinError 10057`，未重试、
  未伪报可用。

## 最近门禁证据与边界

- 本轮为规避 Windows 单进程输出超时，按互斥目录分片执行完整测试集合：unit+contract
  `178 passed`，integration 前后两组 `116 + 136 passed`，property `7 passed`，合计
  `437 passed`。
- `tests/live` 为 `13 skipped`，原因均为未设置显式 `ASTOCK_RUN_LIVE=1`；acceptance 为
  `1 skipped`，原因是未设置真实 30 文档 OCR benchmark 开关。总计 `14 skipped`，没有静默跳过
  recorded、unit、contract、integration 或 property 测试。
- `uv run ruff check .` PASS；`uv run pyright` 为 `0 errors / 0 warnings`；
  `git diff --check` PASS。
- 委员会 v2 memo 夹具已改为读取真实 `composer_version`，服务端严格版本校验未放宽；定向结果
  `5 passed`。
- 旧段落级链最近产生过 648 条观点摘录和 54 个 Skill 候选，全部 `PENDING/NOT_RUN`；它们只作
  legacy 审计，不是新系统成功指标，也不进入新 AU 级候选链。
- 系统没有自动实盘、没有券商订单接口、没有已批准的新 Skill、没有 Phase 7 真实前向证据。
