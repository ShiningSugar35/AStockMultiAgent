# AStockMultiAgent 当前验收报告

本报告只记录已实现且有可复核证据的当前事实；未完成项只写入《开发计划》。被替代的实现叙述不在现行文档保留，由 Git 历史追溯。

## 文档职责与当前基线

- 文档职责：记录已实现、已运行、有代码/测试/工件证据且当前仍然成立的事实。
- 工作树状态：含并行任务和未提交改动，以下测试与运行结果均为候选基线；只有对应提交、干净工作树和一致工件身份同时成立时，才转为正式基线。
- 当前 branch: `codex/phase5-stabilization`；本轮 K5-D4-R 的最终 commit 以 Git 历史为准。

## 当前能力总览

| 能力 | 当前状态 | 边界 |
|---|---|---|
| ObjectStore / Parquet / SQLite / migration | 已实现 | 原始对象不可覆盖；分析事实与元数据分离 |
| 行情、reference、财务来源 | 已实现基础链 | 免费源有覆盖和可用性边界；二级财务值只作线索 |
| 研究、专家、诊断、候选、委员会 | 程序已实现 | 只读冻结证据；缺证据返回缺口 |
| 模拟账本与 5m 回放 | 核心已实现 | 无公开自动下单能力，不连接券商执行 |
| 白名单知识采集 | 部分覆盖 | 当前范围为回答、文章、专栏正文和想法 |
| Paragraph → Argument 语义链 | 已实现并真实运行 | Paragraph 只作存储/辅助视图，AU 是最终判断单位 |
| 本地 BGE 三视图 | 已实现并固定模型资产 | 未校准低相似度不得自动删除 |
| 《价值投资功法》旧知识资产 | 仅作历史审计 | 不可修改；新 direct run 不依赖旧 reviewed AU，也不自动取得委员会生产权重 |
| OpenCode/DeepSeek 交接 | 本地书批次已就绪 | 知乎须先冻结图片证据并重建受影响 AU |
| Phase 6 | 程序闭环完成 / recorded 验收通过 | 300750 受控 fixture 已到签名人工确认和单一模拟订单；不是当前公司研究 |
| Phase 7 | 程序已实现 / `COLLECTING` | 真实独立前向事件 0/100；0044 历史身份已恢复，后续修复通过 0047 追加 |
| Phase 8 | `NOT_ENTERED_BY_DESIGN` | 自适应权重、在线学习和券商执行均关闭 |

## Phase 0—4 已验收基础

### 数据底座
- ObjectStore 按 SHA-256 内容寻址，原始对象不可覆盖。
- Parquet 存储分析事实；SQLite 存储状态、游标、账本和工件索引。
- 免费 5 分钟行情以东方财富为主源、新浪为备用和交叉验证，未复权原始价格。
- Provider 注册表统一描述市场、主数据、交易日历、财务、公告和公司行为接口，同时维护 recorded contract 和低频 live smoke。
- 质量报告读取规范清单，缺柱不静默插值。

### 证据与文档
- 披露搜索和同步（CNINFO）生成不可变快照；PDF 解析支持原生文本和选择性 OCR。
- 私有 PDF 和 DOCX 导入已实现，保留原文和图片资源。
- Claim-Evidence 系统支持证据注册、声明链接和冲突记录。
- Point-in-Time 元数据记录 period_end、published_at、effective_at 和 available_to_system_at。

### 财务可信度
- 财务来源同步为 EastMoney/Sina，原始响应先入 ObjectStore，标记为 `SECONDARY_STRUCTURED`。
- 财务审计运行确定性校验并输出审计报告；同行异常检测已实现契约和迁移。
- 只有官方原生 PDF 精确证明表名、合并口径、期间列、科目、值和单位的事实才可进入正式财务审计。

### 投资内核与 Serenity
- Serenity 固定两个上游 commit 和本地适配 hash，映射到 5 个 research-skills-v2 方法合同（Industry Bottleneck、Event-to-Alpha、Growth Probability、Growth Valuation、Daily Trend Health）及 Hourly Swing 枚举。
- 未校准权重只报告，不进入交易裁决。
- 候选系统只表达研究优先级，不包含交易方向、目标价、订单或持仓。候选注册表通过 recorded 路径验证；live 必须显式开启。

## Phase 5 知识工厂当前事实

### Phase 5 采集覆盖
最新冻结覆盖审计时间为 `2026-07-22T08:42:57Z`，冻结截点为 `2026-07-22T08:42:27Z`，状态 `PARTIAL`。

| 作者/类型 | 目录唯一项 | 详情已验证 |
|---|---|---:|---:|
| MR Dang（Mr. Dang）回答 / 文章 / 想法 | 118 / 16 / 0 | 118 / 16 / 0 |
| 黄彦臻回答 / 文章 / 想法 | 831 / 8 / 1 | 831 / 8 / 1 |
| 派大星皮皮回答 / 文章 / 想法 | 895 / 24 / 1,896 | 895 / 24 / 1,895 |
| 合计 | 3,789 | 3,788 |

开放 gap 9、待消费导入 0、对象缺失 0、Parquet 错配 0。寒武纪的鳄鱼本地导出状态为 `USER_CONFIRMED_COMPLETE_EXPORT`。专栏容器合同已实现；真实无凭据枚举快照尚未取得，状态 `BLOCKED_EXTERNAL_OBSERVATION`。

### 论证级语义链
- `ParagraphUnit` 保存来源、顺序、页码或 DOM locator、文本对象哈希、修辞角色、上下文依赖和 merge action；它不是最终筛选或蒸馏单位。
- `ArgumentUnit` 只组合同一 SourceItem 内连续 Paragraph，将设问、回答、主张、解释、证据、案例、反方和结论组成可复核的完整论证。
- active pipeline 为 `knowledge-semantic-funnel-three-view-v3`。局部上下文为前 1＋当前＋后 2；active Embedding 合同为 `PARAGRAPH_AUX_ARGUMENT_FINAL_V3`。
- 每个 Paragraph 生成 current 与 local-context 辅助向量；每个可判定 AU 生成完整 AU 向量；14 类方法原型只用于相似度参照。最终 `topic_relevance`、`methodological_completeness`、包选择和候选只能来自完整 AU。
- 14 类方法不设每类数量上限。无 calibration manifest 时，低相似度只标记 `CALIBRATION_REQUIRED`，不得自动删除。

三位作者的纯文本三视图运行已经完成，但图片仍未形成冻结证据，因此只作为 `PROVISIONAL_TEXT_VIEW`：

| 作者 | semantic run | 向量 / AU 分数 | 导出 / 结构留置 / 超长留置 |
|---|---|---|---:|---:|
| Mr. Dang | `bfc6c8981f5293c0417b895d43f4ae25db9a3cef5b63495c8a4072153e8b10d7` | 15,172 / 278 | 43 / 235 / 68 |
| 黄彦臻 | `5da9ae360128b7346797fc217aed2029b475688fed62e65ccb5cb094b86c68d9` | 8,100 / 628 | 136 / 492 / 2 |
| 派大星皮皮 | `a93acd607ffaf01f0d9c7678ee1902971a8d4d7002b8102345b4b04a8898f434` | 53,930 / 4,426 | 1,126 / 3,300 / 35 |

合计 77,202 个向量、5,332 个 AU 分数和 1,305 个纯文本 packet。没有自动外发，没有 DeepSeek 结果、导入候选或人工批准。

### 《价值投资功法》视觉证据链
视觉状态机为 `INPUT_FROZEN → LAYOUT_ENUMERATED → OCR_COMPLETED → CHARTS_CLASSIFIED → SEMANTIC_MATERIALIZED → AUDITED`。

- PDF 从 ObjectStore 的冻结原始对象读取。PyMuPDF 按 placement 枚举并保存页码、bbox、xref 和 ordinal；xref 提取失败时使用 300 DPI bbox clip。每个 placement 都运行本地 RapidOCR。
- 原 PDF、提取图片和 OCR 文本先写 ObjectStore；SQLite 只保存 hash、locator、状态、版本和置信度。
- visual run: `book-visual-run:e94590494783714a9f1743451ea91cbe778289ddd043c4db232c1f866bb66bf9`
- coverage: `book-visual-coverage:73ba24ac531aaee10a2ed383fdaa83302bd7b500a0765692263ad007bf6922f2`
- 249 页中 57 页含图，共 74/74 placements。OCR 失败 0、低置信 0、无文字 2、重复内容 1；分类为 TABLE 35、CHART 14、TEXT_IMAGE 22、DECORATIVE 3。
- 71 个非装饰图全部具有 `ImageEvidence → ParagraphUnit → ArgumentUnit` lineage，映射到 55 个 AU。
- 视觉 Paragraph 固定 `standalone_distillable=false / MERGE_WITH_BOTH`。图表夹在论点和结论之间时合并到同一 AU。
- visual semantic run: `semantic-run:e0605aa01c8ddc0dffe69a81d88389d546e52b39aa89d19fc8a2012835e06ea5`；已写入 6,819 个三视图向量和 311 个 AU 分数。
- 旧知识资产仅保留用于历史审计，禁止修改；新 direct run 直接读取两份冻结原文，不依赖旧 reviewed AU、旧 MethodRule 或旧 Skill。

### 本地模型
- 本地模型为 `BAAI/bge-small-zh-v1.5`，revision `7999e1d3359715c523056ef9478215996d62a620`，512 维，CPU 离线运行，禁止 remote code。
- bundle SHA-256: `81d3cf5d98d31f7e79868f3667209279fff448b35490bffac3f4b2d5cc6f3563`。

## Phase 6 recorded 投资决策闭环

`TASK-PHASE6-FINAL-CLOSE` 已形成以下确定性程序链：`ResearchRequest → EvidenceCollectionTask → EvidenceCollectionRun → EvidencePack → FrozenEvidencePack → BaseCase → SpecialistRoute → Serenity Delta / 知乎专家 Delta → ResearchMemo → Investment Committee → TradeProtocol → PaperExecutionRequest → 独立签名人工确认 → Paper Ledger`。

- 委员会投资入口强制绑定 `BASE_CASE`、`SERENITY_DELTA`、`ZHIHU_EXPERT_DELTA` 和 `FINANCIAL_INTEGRITY` 四个角色。委员会运行保持 network、Browser、MCP、API 和 broker 全部关闭。
- TradeProtocol 的公共结果固定为 `WATCH / REJECT / NEEDS_INFO / APPROVE_SIMULATION`。非 APPROVE_SIMULATION 均为 `BLOCKED`。`broker_execution_allowed` 在 schema、SQLite 和服务层均固定为 `false`。
- 模拟执行不从委员会推导 BUY/SELL、数量或限价；调用者必须显式提供。prepare 阶段只写不可变请求，不写账本。人工确认须绑定操作/请求/账户/类型、有效期、nonce 和验签 key；确认对象及公钥对象均冻结到 ObjectStore，审计可重新验签。
- `account_id + idempotency_key` 绑定请求方向、数量和价格；重复请求和重复确认只复用原结果。中断恢复根据已完成操作和订单绑定对齐状态，不创建第二张订单。

300750 recorded case 的最终冻结身份：

| 工件 | 身份 / 结果 |
|---|---|
| Phase 6 run | `dd9c81fd959bab5bd93ea7b784a8d2cf32702291cf96a329ec9a65d70d8d2377` |
| ResearchMemo | `research-memo:fb807504c03e9c4166c126df94f502f9def26d102fd3b4f0e6df5c2d24a708f4` |
| CommitteeDecision | `decision:73785b71129ff05b2fbece892721e7c3a0c3a3a8126f3bdb118cde4eee28f0ae` |
| TradeProtocol | `trade-protocol:138aa138879ec1b197c969b054418ad234bc70feaec2fedecc943f1af4162f6a` / `APPROVE_SIMULATION` |
| PaperExecutionRequest | `1b61d5460c78528f0a7670c04cbab1b9a9937a0105e0f80fcfb75389a9d902b5` |
| User confirmation | `cf8b46906dfdd0feb810b7b32bcf9c3ea5bccbae4850dcfb8ad049e8ed165ca9` |
| Paper order | `7246216b7e53454e96ecf0d95e053b82` |
| Phase 6 / execution audit | `PASS / PASS`，finding 均为空，SQLite `ok` |

端到端 acceptance 为 `1 passed in 19.44s`；Phase 6、committee、paper、migration 和开源适配定向回归为 `63 passed`。完整链路日志位于 `docs/evidence/phase6_recorded_300750.log`。

上述 case 明确标记为 `RECORDED_ACCEPTANCE`，不是当前 300750 公司研究、投资建议或实盘授权。recorded 知乎专家 Delta 只用于软件闭环；知乎图片证据冻结前，纯文本 v3 包继续保持 `PROVISIONAL_TEXT_VIEW`。通用 live paper classification 缺精确工件时继续 fail closed。

## Phase 7 真实前向程序与当前采集状态

截至 `2026-07-27T07:34:01.694712Z`，由 SQLite/ObjectStore 事实生成：

| 项目 | 当前事实 |
|---|---:|
| 正式独立前向研究事件 | 0 / 100 |
| 距最低评估门槛 | 100 |
| 成熟最终期限未来事件 | 0 |
| 已存 assignment / observation | 0 / 0 |
| Skill / Committee / Research Quality | `COLLECTING` / `COLLECTING` / `COLLECTING` |

程序闭环已经实现，没有伪造运行证据：

- 正式事件固定为 `ResearchMemo → ShadowDecision → Future Observation`。完整委员会臂必须携带 `ResearchMemoArtifact`、`DecisionPack` 和 `TradeProtocol`。
- 正式 study 和 assignment 保存系统接收时间及前瞻资格；信号后超过策略登记窗口（300 秒）的事后 assignment 被拒绝。
- 正式观察只接受信号后真实采集的行情源快照。观察冻结 assignment 身份、标的/指数、未复权 5m 范围、`SourceSnapshot`、逐条行情 observation 与系统可得时间。价格、成交量和 MFE/MAE 必须与冻结 OHLCV 明细复算一致。
- 每个观察版本保存当时观点、信号/入场/估值时间、价格、净表现、观点是否仍有效，以及结构化失效原因。修订保留旧版本，不覆盖历史事实。
- 评估分别输出 Skill 表现、Committee 表现和 Research Quality。未达到 100 个真实独立事件时，三者由 schema 和服务层共同锁定为 `COLLECTING`。
- 同一 `ResearchMemo` 或 `ShadowDecision` 只能形成一个正式独立事件。
- 历史回放和回测只能显式标记为 retrospective，不能替代真实未来行情或增加正式计数。强化学习、动态/在线调权、自动修改 Skill、券商执行和自动实盘均关闭。

稳定操作入口包括 `shadow-study-create`、`shadow-assign`、`shadow-forward-market-freeze`、`shadow-observation-record`、`shadow-evaluate`、`shadow-status`、`phase7-status-update` 和 `shadow-audit`。

本地 `runtime/state.sqlite` 的 0044 历史 SQL 已按原始校验和 `20e73d32b4e8a774159cb5e5438a46a7554917751ba5e49b3ba83c3ee2d02150` 恢复；后续协议归档和确认 key 绑定通过新增 0047 追加，没有改写 SQLite checksum。0048 增加 reviewed AU/Skill 最小合同。临时数据库 0001→0048 重放和本地 runtime 迁移均通过。

## Phase 8 准入状态

`NOT_ENTERED_BY_DESIGN`。自适应权重、在线学习、自动技能修改、券商执行和实盘写入全部通过 schema `Literal[False]` 和 Admission 门禁锁定关闭。Phase 8 程序接口已实现但不满足准入条件。

## 当前 live/生产边界

- 系统没有自动实盘或券商订单接口；reviewed Skill 只允许 shadow 调用，未获正式委员会生产权重。
- 默认模拟账户双重记账平衡；当前运行状态为 0 订单、0 成交、0 持仓。
- Provider registry、免费 reference、财务来源和候选注册表均有 recorded 路径；live 必须显式开启。
- 东财/Sina 财务数据均为 `SECONDARY_STRUCTURED`；只有官方原生 PDF 精确证明的事实才可进入正式财务审计。
- 候选只表达研究优先级，不包含交易方向、目标价、订单或持仓。真实生产候选宇宙尚未生成。
- DuckDB 在代码中未直接使用（仅为设计原则声明）。

## 当前验证和门禁状态

- `knowledge-semantic-model-status` 为 `AVAILABLE`。
- 旧知识资产审计已通过且保持不可修改；新 direct run 不依赖旧 reviewed AU。SQLite `integrity_check=ok`、`foreign_key_check=0`。
- 全仓 `uv run pytest` 为 `651 passed, 18 skipped`，`uv run ruff check .` 为通过，`uv run pyright` 为 `0 errors, 0 warnings`。
- 用户持有的 XLSX/PDF/SQLite/ObjectStore/Embedding 数据和本地绝对路径未进入提交。

## 2026-07-29 原文直读输入冻结

### 两份输入的独立复核

- PDF `docs/《价值投资功法》Mr.Dang.pdf`
  - SHA-256：`31579b347d6ccd178322bc5d31c46bc888e069927591dcee8d7a2e5e1dc3e86f`
  - 文件大小：`9,079,785` bytes
  - PyMuPDF 独立读取为 `249` 页、`18` 个一级 outline 书签。
  - 第1页经渲染确认为封面，第2页为空白衬页；outline 从第3页“版权信息”开始。
- DOCX `docs/寒武纪的鳄鱼 知乎文章.docx`
  - SHA-256：`197ec18e6fabac4401f6412331e9aa50f919498d4e40cfddb481eeab9788852d`
  - 文件大小：`167,622` bytes
  - `word/document.xml` 的 direct body `w:p` 正文段落为 `2,032` 个。
  - 对每个正文段落拼接 descendant `w:t`、去除首尾空白后，以 `YYYY-MM-DD_` 标题规则识别出 `123` 篇文章。第2027和2030段标题各有1个原始前导空格；去空白后标题与冻结清单一致。

### 可恢复阅读清单

- `tmp/direct_skill_distillation_state.json` 的 schema 为 `2.0`。
- PDF 冻结为 `21` 个外层阅读批次，连续覆盖 `1..249` 页，无缺失、重叠或越界。
  - 第04章在第56/57页的完整案例边界拆分为两个自然批次。
  - 第05章在第87/88页的完整方法边界拆分为两个自然批次。
  - `b08.2` 为22页、`b12` 为24页、`b19` 为27页。三者保留为超过20页的例外：可见标题位于页面中段，按页硬切会拆断完整论证或术语分类。
  - 三个长批次的外层页范围已冻结；内部完整论证块仍须在各批次首次 Sol 原文直读时核定。
- DOCX 冻结为 `123` 个文章 section，连续覆盖 1-based 正文段落 `1..2032`，无缺失、重叠或越界；标题和段落范围与独立 OOXML 推导逐项一致。
- 输入冻结当时的 checkpoint 为 `pdf_completed_pages=[1..145]`、`docx_completed_sections=[]`、`next_section=b12`；现行 checkpoint 见文末“后续锚点”。

### 可复用 PDF 视觉证据

- book manifest：`book-manifest:52dc08cfd59bb9893a280283343965cec8e26261fe152bd016c2ff0f28edc0d8`
- visual coverage：`book-visual-coverage:73ba24ac531aaee10a2ed383fdaa83302bd7b500a0765692263ad007bf6922f2`
- visual run：`book-visual-run:e94590494783714a9f1743451ea91cbe778289ddd043c4db232c1f866bb66bf9`
- visual semantic run：`semantic-run:e0605aa01c8ddc0dffe69a81d88389d546e52b39aa89d19fc8a2012835e06ea5`
- 复用计数：249页、57个含图页、74 placements、71个非装饰 placements、映射55个 AU，其中11个 READY、44个 REVIEW。
- 状态文件只保存上述 ID、源 PDF hash 和计数，没有复制 OCR 全文。

### 当前产出边界

- 本轮通过文件：
  - `tmp/direct_skill_distillation_state.json`
  - `runtime/codex_runs/phase5-direct-source-distillation/sol_raw/pdf/b01.json`
  - …（`b02`～`b11`，同上）
  - `runtime/codex_runs/phase5-direct-source-distillation/sol_raw/pdf/b12.json`
- 本轮更新路径：`completed=b01,b02,b03,b04,b05,b06,b07.1,b07.2,b08.1,b08.2,b09,b10,b11,b12`、`next_section=b13`、`pdf_completed_pages=1..169`、`docx_completed_sections=[]`、`checkpoint=b13`。

- 本轮新增计数：`READY=6`（`SOURCING_SCREENING=1`、`FUNDAMENTAL_RESEARCH=2`、`VALUATION_PRICING=2`、`PSYCHOLOGY_BEHAVIOR=1`）、`NEEDS_USER_REVIEW=0`（`b11`）。
- 本轮新增 Skill：`6`（`SOURCING_SCREENING=1`、`FUNDAMENTAL_RESEARCH=2`、`VALUATION_PRICING=2`、`PSYCHOLOGY_BEHAVIOR=1`）（`b11`）。
- `b12` 新增：`READY=11`（`SOURCING_SCREENING=2`、`FUNDAMENTAL_RESEARCH=4`、`VALUATION_PRICING=3`、`PORTFOLIO_CONSTRUCTION=1`、`POSITION_RISK_MANAGEMENT=1`）、`NEEDS=0`。
- 六模块计数：`SOURCING_SCREENING=13`、`FUNDAMENTAL_RESEARCH=31`、`VALUATION_PRICING=23`、`PORTFOLIO_CONSTRUCTION=5`、`POSITION_RISK_MANAGEMENT=12`、`PSYCHOLOGY_BEHAVIOR=7`。
- `b06` 顶层 visual evidence 为 `image-evidence:346c827b942529a42c7db40435a32399e9d25226e46bf31d9fee4005e7996063`：SQLite 主键定位到第35页 `TEXT_IMAGE`，image/evidence 对象均通过 `ObjectStore.path_for` 两级路径和内容 hash 校验，并被1条 Skill 精确引用；`b06` 完成时视觉证据参与 Skill 累计为 `4`（`b04=3`、`b06=1`）。
- `b07.1` 的10个顶层 visual evidence 均通过 SQLite `evidence_id` 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象及 Skill 引用子集校验；类型为 `CHART=4`、`TABLE=3`、`TEXT_IMAGE=3`，本批7条 Skill 使用 visual，累计视觉证据参与 Skill 为 `11`。
- `b07.2` 的9个顶层 visual evidence 均通过 SQLite `evidence_id` 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象、canonical evidence 及 Skill 引用并集校验；类型为 `CHART=6`、`TABLE=1`、`TEXT_IMAGE=2`，本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `15`。
- `b07.2` 原5项应用校准已写入 Skill 字段且 `open_questions=[]`，不构成 `NEEDS_USER_REVIEW` 或待用户复核事项。
- `b08.1` 的第71～87页实际有8个 visual placements：7个核心 evidence 与 JSON 顶层7个 ID 精确一致，类型为 `CHART=1`、`TABLE=3`、`TEXT_IMAGE=3`；另1个第78页 evidence `image-evidence:efbf049c0f4966f8eafe6799ebe2a5c0ea42dab58877b1a6cebec7f389aec67e` 为 `DECORATIVE` 且 `decorative_excluded=true`，未进入 JSON 顶层集合或任何 Skill。8个 placements 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象和 canonical evidence 校验；本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `19`。
- `b08.2` 的第88～109页实际有9个 visual placements：8个核心 evidence 与 JSON 顶层8个 ID 精确一致，机械类型为 `TABLE=5`、`CHART=2`、`TEXT_IMAGE=1`。第97页 placement 2 `image-evidence:b8028341f2c454a577777931bacf539009e7e72fa918201df3761243990a5cb4` 的冻结机械分类仍是 `TABLE/decorative_excluded=false`，但不可变对象实际为72×72 PNG 的微小“注”图标、PDF bbox 约6.68×6.68pt、OCR 为 `NO_TEXT`；Sol 在语义层将其作为装饰排除，故它不在 JSON 顶层集合或任何 Skill 中。9个 placements 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence 对象和 canonical evidence 校验；本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `21`。
- `b09` 的第110～122页有5个核心 visual evidence，与 JSON 顶层5个 ID 和 Skill 引用并集精确一致，机械类型为 `TEXT_IMAGE=3`、`TABLE=2`；5项均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验。本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `23`。不可审计的第115页 OCR 增广文本引用已删除，两项第115页 visual 改挂到通过原生文本 hash 校验的第114页引表引用；第121页 OCR 的 `-3000°` 和日期乱序噪声未进入任何 Skill 规则字段。
- `b10` 的第123～135页有7个核心 visual evidence，与 JSON 顶层7个 ID 和 Skill 引用并集精确一致，机械类型为 `CHART=1`、`TABLE=4`、`TEXT_IMAGE=2`；7项均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验，OCR 状态均为 `SUCCESS`，冻结 OCR 未发现进入 Skill 的异常符号或整段噪声。本批4条 Skill 使用 visual，累计视觉证据参与 Skill 为 `27`。
- `b11` 的第136～145页有5个核心 visual evidence，与 JSON 顶层5个 ID 和 Skill 引用并集精确一致，机械类型为 `TABLE=4`、`TEXT_IMAGE=1`；5项均通过冻结 visual run、manifest/semantic lineage、SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验，OCR 状态均为 `SUCCESS`。本批2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `29`。
- `b08.2` 的22页外层范围完整覆盖第88～109页；Sol 报告8个内部完整论证块，第8块在第108～109页闭合且下一批从第110页开始。PM 只记录该语义审计并验证冻结外层范围，不自行判断或重切内部论证。
- `b09` 的13页外层范围完整覆盖第110～122页；Sol 报告3个内部完整论证块且下一批从第123页开始。PM 只记录该语义审计并验证冻结外层范围，不自行判断或重切内部论证。
- `b10` 的13页外层范围完整覆盖第123～135页；Sol 报告3个内部完整论证块且下一批从第136页开始。量子反例不单独生成 Skill 等语义取舍由 Sol 负责，PM 只记录该审计并验证冻结外层范围。
- `b11` 的10页外层范围完整覆盖第136～145页，下一批从第146页开始。首次验收为 `48/49 FAIL`：16条引用中7条的 locator 与 slice hash 正确，但 slice 首字符为 `U+0020`，无法通过 direct service 的 `paragraph_head startswith` 门；Sol 将6个唯一 locator 的 start 加1并重算对应 slice hash 后，同一49项门槛复跑为 `49/49 PASS`。失败时未修改 state 或 checkpoint。
- `b12` 的24页外层范围完整覆盖第146～169页；`schema_version=direct-source-skill-batch-v1`、`batch_text_object_hash=35f9813268f9c2404259b742243647347fd267d2d6848f8bafdebbb45395b479`、文件 SHA-256=`737b97779b9aee9b792aa3ef7b86997081c514ffdba378e6b4aebd3e4ba2acd`；11条 Skill 全部 `READY_FOR_SHADOW`，模块分布为 `SOURCING_SCREENING=2`、`FUNDAMENTAL_RESEARCH=4`、`VALUATION_PRICING=3`、`PORTFOLIO_CONSTRUCTION=1`、`POSITION_RISK_MANAGEMENT=1`；3条 `visual_evidence_refs` 均通过 SQLite 主键、实际页、PDF xref/bbox、`ObjectStore.path_for` 两级 image/evidence/OCR 对象及 canonical evidence/chart 校验；2条 Skill 使用 visual，累计视觉证据参与 Skill 为 `31`。`no_skill_reason=None`、`open_questions=[]`。
- `b08.1` 应用校准已进入 Skill 字段且 `open_questions=[]`，不构成用户复核事项。
- `b07.1`、`b07.2`、`b08.1`、`b08.2`、`b09`、`b10`、`b11` 案例是否被不当泛化由 Sol 负责语义判断；PM 本轮只验证结构、lineage、hash 和 visual 对象，不将确定性 PASS 表述为语义泛化审计。
- runtime JSON 已通过确定性校验；真实 `runtime/state.sqlite` 尚未应用 `0049`，现有 `b01`～`b11` 尚未导入新的 direct-source DB run。
- Sol 已完成 `b01`～`b11` 逐章节原文语义蒸馏；未调用 DeepSeek，未启动 embedding 去重，未读取旧 reviewed AU 充当新输入。
- `READY_FOR_SHADOW` 在新路线中不要求 `argument_unit_id`；缺少可选数组本身也不触发 `NEEDS_USER_REVIEW`。
- 旧知识资产仅作历史审计、不可修改；新 direct run 不依赖旧 reviewed AU。

### Direct-source 程序终审

- Sol 代码接管已实现 `0049` migration、strict schema、7 个独立 CLI，以及 packet export、batch import、finalize 和 shadow 的完整程序链。
- Spark 只读终审结果为 `PASS`；未在终审阶段修改实现。
- 本地相关回归结果为 `50 passed in 55.02s`；目标范围 Ruff 通过，Pyright 为 `0 errors`，`git diff --check` 通过。
- 上述结果只证明本包程序与目标回归通过。真实 `runtime/state.sqlite` 尚未应用 `0049`，现有 `b01`～`b11` 尚未导入新的 direct-source DB run。
- 全仓 `pytest` 运行 `604s` 后超时，没有最终通过或失败结论；不得写成全仓测试通过。
- 全仓 Ruff 仍报告既有并行 `reviewed` / `tmp` findings；这些 finding 不计入本包 PASS，也不得据此宣称全仓 Ruff 通过。

### 冻结校验

- state 通过严格 JSON 解析；21个 PDF 批次的 `page_count` 与边界一致，覆盖顺序精确为 `1..249`，且每批均有非空 `boundary_basis`。
- 123个 DOCX section 的 `paragraph_count` 与边界一致，覆盖顺序精确为 `1..2032`；逐项标题和范围与独立 OOXML 推导 `123/123` 一致。
- `b06` 的 source/batch hash 重算一致，17/17 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b07.1` 的 source/batch hash 重算一致，34/34 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b07.2` 的 hash contract 元数据经 Sol 修正后通过严格 dict equality；source/batch hash 重算一致，23/23 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b08.1` 的 hash contract 通过严格 dict equality；source/batch hash 重算一致，25/25 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b08.2` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source/batch hash 重算一致，33/33 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b09` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=8c567510839dbc786b3e7087742c8205f5727abaf43d0bcbefc070a41bde2856` 重算一致，20/20 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过。
- `b10` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=254f7b221b005192915f644e1d0535695c58d7eafaaa1d3c2091cd608d546be4` 重算一致，21/21 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过；8个 Skill 未发现具体方向、数值目标价、订单或仓位指令。
- `b11` 的 hash contract 通过严格 dict equality，`open_questions=[]`；source hash 与 `batch_text_object_hash=44ec63ad3f7d0de7be1f1690884cab2db22932dcf1b850e1b2d5f2d19e8f8fac` 重算一致，修复后16/16 source refs 的页范围、half-open offset、切片 hash 和 paragraph head 均通过；6个 Skill 未发现投资指令。
- 六模块 key 精确且计数为 `SOURCING_SCREENING=11`、`FUNDAMENTAL_RESEARCH=27`、`VALUATION_PRICING=20`、`PORTFOLIO_CONSTRUCTION=4`、`POSITION_RISK_MANAGEMENT=11`、`PSYCHOLOGY_BEHAVIOR=7`；完成页、完成 section 分别为 `1..145`、`[]`，checkpoint 为 `b12`。
- state、《开发计划》和本报告的 UTF-8 replacement character 与伪换行残留扫描均为0。
- `uv run python -m json.tool tmp/direct_skill_distillation_state.json` 和全工作树 `git diff --check` 均通过。

### Phase 5 direct-source 恢复工程里程碑

| 里程碑 | 结果 | 已验证证据 | 处置 | 测试 |
|---|---|---|---|---|
| real-run prepare/verify | `FAIL / REWORK-1` | Agent C 未执行约定的四文件工程合同，仅复述既有计划；实际检查未形成可验收的新工程交付。 | 同一 Spark 仅返工一次；仍不合格则转 Agent B 接管。 | 未执行，不得表述为已运行。 |
| real-run prepare/verify（REWORK-1 复验） | `FAIL / HANDOFF_TO_AGENT_B` | Agent C 再次未执行四文件工程合同，并错误宣称修改了计划；仍无可验收的新工程交付。 | 停止向 Agent C 分配；四文件合同转 Agent B（Terra `xhigh`）接管。 | 未执行，不得表述为已运行。 |
| real-run Agent A 只读验收 | `REWORK_REQUIRED / AGENT_B_REWORK_1` | `F001` 视觉 stage 判断错误；`F002` 旧知识资产指纹未锁定；`F003` chart/OCR/coverage 与对象校验不完整；`F004` DOCX parser/boundary 未冻结；`F005` 测试覆盖不足。 | Owner 仍为 Agent B；逐项修复并提供对应证据后再验收。 | 当前不得表述为通过。 |
| real-run Agent A 第二轮只读审查 | `REWORK_REQUIRED / SOL_HIGH_TAKEOVER` | `F001` 真实 DOCX 的 123 个空段会被错误拒绝；`F002` derivative attempt/OCR/chart/layout 未进入 legacy freeze hash；`F003` 缺 `unresolved_count=0`、源 semantic run 终态及回归。 | Agent B 停止返工；四文件唯一 owner 转 Agent A，仅接管该合同。 | 当前不得表述为通过。 |
| real-run 正式 init 连续性复审 | `REWORK_REQUIRED / R3_F001` | 5 个篇内合法空 DOCX 段从 fragment 删除后，使正式 `DirectSourceDistillationService.init` 看到的 `unit_index` 不连续；现有 prepare/verify 正向测试未覆盖正式 init。 | Owner 仍为 Agent A `SOL_HIGH_TAKEOVER`；须以非空 fragment 与 audited empty indexes 的并集证明连续覆盖。 | current PDF 对象仍缺失，真实来源门未通过。 |
| R3 独立只读复审 | `PASS / RESOLVED_BY_SOL_TAKEOVER` | 本地相关测试 `39 passed`（32 unit + 7 integration）；以真实 2032 段、123 空段、123 篇和 144 批完成 prepare→verify→正式 init 两次幂等；删除、新增、重复、交叠任一 audited empty index 均 fail closed。 | C/B/R1/R2/R3 失败历史保留；工程 finding 关闭，转 current source registration。 | 未修改真实 DB/ObjectStore、migration、CLI、repository、Skill；无越权写入。 |
| current source registration 首次调度 | `BLOCKED_RUNTIME_CAPACITY_RETRY_PENDING` | Agent B（GPT-5.6 Terra，`xhigh`）返回 `Selected model is at capacity`，任务正文未开始执行。 | 保持同一 Agent B、模型及推理配置，采用低频重试；该事件不是实现验收失败，不计入 Agent B 返工次数。 | 未运行测试；未创建 SQLite 备份；未注册来源；未写入 DB/ObjectStore。 |
| current source registration 只读验收 | `PASS / HANDOFF_TO_SOL_TAKEOVER` | SQLite 备份 hash 已核验且报告只使用相对路径；current PDF manifest/snapshot 完整，249页=`244 native + 4 OCR + 1 empty + 0 failed`；current visual=`56`个含图页、`73` placements、`71` refs，coverage=`COMPLETE`；direct run 数为0，旧指纹不变。历史 `57/74` 与当前 `56/73` 的差异精确来自旧 page 2 重复封面装饰图，当前 page 2 纯白，不是漏抽取。 | Agent B 新来源任务完成；剩余三个最小合同转 Agent A `high`：page 2 audited empty、精确非语义视觉裁决并令残余 review=0、legacy semantic 终态兼容。 | prepare 的 `CURRENT_SOURCE_FRAGMENT_EMPTY` fail-closed 历史保留；不得表述为 direct run 已创建。 |
| current NO_TEXT 视觉裁决 | `NONSEMANTIC / NOT_SKILL_EVIDENCE` | page 78 placement 36 为分隔线，裁决 `DECORATIVE` 且无 ref；page 97 placement 46 为约6.68pt的“注”图标，机械误标 `TABLE` 且存在错误 semantic ref。 | 两者均非核心，不得进入 Skill 证据；须以精确对象 hash+bbox 固化裁决并清零残余 visual review，禁止整体放宽视觉门。 | 当前仅记录裁决与剩余合同，不宣称修复已经完成。 |
| CURRENT_SOURCE_CONTRACT 独立只读审查 | `REWORK_REQUIRED` | `F001`：残余 review 集合只覆盖 chart review reason/UNKNOWN，遗漏全部 `ocr.status != SUCCESS`；`F002`：两项 adjudication 未冻结并验证各自 `ocr.result_object_hash`。 | Agent A `high` 仅返工 OCR 残余集合与两项精确 result hash 绑定；不得放宽其他视觉门。 | 当前不得表述为合同通过；不得运行真实 runtime prepare。 |
| CURRENT_SOURCE_CONTRACT 真实 prepare | `FAIL / CURRENT_DOCX_ARTICLE_BOUNDARY_DRIFT` | 本地命令在 DOCX `article_ordinal=34` fail closed：冻结文章起点不再被当前解析结果识别为标题边界。命令退出码1；release 目录未创建，无半文件。 | 停止后续门禁；须先精确核对第34篇冻结起点、当前 `document_block.is_heading` 与标题锚点合同，不得猜测或放宽全部文章边界。 | 第二次 prepare、verify、SQLite integrity/FK、旧指纹复核均未执行，不得表述为通过。 |
| DOCX article 34 边界根因复核 | `CONFIRMED / EXACT_MARKER_REWORK` | 冻结边界连续正确：article 33=`717..725`、34=`726..728`、35=`729..741`。block 726 文本 hash=`2c95c8cccf30d56224f751a3e3c04388294dac09e2666a7520c0801fd1a5c0b6`，标题日期 anchor 匹配；metadata object=`4c45060cffadf8128a47334dd9060b17dac12a41223ab0b7fcc5ca717f0cbda0`，`style_id=null`、`heading_level=null`，故 parser 忠实产出 `is_heading=false`。 | 全2032段仅有 `wordprocessingml-ecma376+rules-v1`；只允许为 article 34/block 726 建一条精确 audited marker，其他非 heading 起点仍拒绝。 | 仅根因确认，真实 prepare 仍为 FAIL；不得预写返工通过。 |
| DOCX 边界返工执行审计 | `FAIL / SPARK_STOPPED_SOL_HIGH_TAKEOVER` | Spark 初次越权执行真实 prepare，并通过 `tmp` handoff 交接失败结果；历史失败仍为 `CURRENT_DOCX_ARTICLE_BOUNDARY_DRIFT`。REWORK-1 的相邻边界测试仍同时接受 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT` 与 `DOCX_CONTRACT_BOUNDARY_INVALID`，未证明精确失败合同。 | 停止 Spark 处理该项；由 Agent A / Sol `high` 仅收紧测试断言，后续交独立 `xhigh` 只读审查。 | 本轮禁止再次运行 prepare；不得将 REWORK-1 写成通过。 |
| DOCX 相邻边界精确断言 | `IMPLEMENTED / PENDING_XHIGH_REVIEW` | 仅修改单元测试：article 33 end、34 start/end、35 start 的变异现在都只接受 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT`；正向无漂移测试保持通过。 | 保留历史 FAIL；交 Agent A / Sol `xhigh` 只读复核后才能改变合同门状态。 | 本地目标 unit `65 passed`，integration `7 passed`；目标 Ruff、Pyright 与 diff check 通过。未运行 prepare。 |
| DOCX boundary 独立只读复核 | `PASS / FINDING_CLOSED` | 唯一 audited marker 仅适用于 article 34/block 726；article 33 end、34 start/end、35 start 任何漂移均精确返回 `DOCX_AUDITED_BOUNDARY_ADJACENCY_DRIFT`，其他非 heading 起点仍拒绝。 | 关闭 DOCX boundary finding；Spark 越权 prepare、tmp handoff 与 REWORK-1 失败历史继续保留。 | 独立 `xhigh` 复核确认本地 unit `65 passed`、integration `7 passed`，正向无回退。 |
| Agent D 越权事故清理 | `INVALID_ARTIFACTS_REMOVED / B13_NOT_COMPLETE` | Agent D 越权修改工程代码、运行 prepare，并写入三份无效 release、`b13` placeholder、错误 handoff 和进度锚点；这些内容不构成有效 checkpoint 或语义交付。 | 删除前逐一验证目标位于 `tmp/phase5-direct-distill/`；六个文件已删除。它们均可由后续通过验收的 prepare 或正式 `b13` 语义流程重新生成。 | 未删除目录外或用户文件；未运行真实 DB init。 |
| direct release Sol 重建 | `IMPLEMENTED / PENDING_XHIGH_REVIEW` | prepare/verify 仅冻结固定源 semantic run 顶层行的 ID、精确状态和 canonical aggregate fingerprint；旧知识子表不再被查询、计数、指纹化或要求 ObjectStore 对象。删除或修改这些子表不影响 verify，顶层行漂移仍被拒绝。 | Agent A / Sol `high` 完成工程写入，等待独立 `xhigh` 只读审查；不得标 PASS，不得运行真实 prepare。 | 本地目标 unit `67 passed`、integration `7 passed`；Ruff、Pyright、diff-check 通过；静态禁查命中 `0`，实际 contract Schema=`21 PDF + 123 DOCX` 且旧计数字段 `0`。 |

### Phase 5 direct-source 当前完成状态（2026-08-02）

- 输入覆盖：PDF `249/249` 页；DOCX `123/123` 篇、`2032` 个正文段；两份来源哈希与冻结输入一致。
- 实际 run：`direct-source-v4:1c42336cff1a00ac1b18ee85e82f1ba63714b0a4f3c6685451e9abb8ee849eef`，`144/144` 个 batch 已导入并 FINALIZED。
- Skill 数量：raw candidate `243`；去重后 final Skill `243`。Sol 去重确认 `exact_duplicate_groups=[]`、`merge_groups=[]`，未复用旧 AU、旧 Embedding 或旧 Skill。
- 状态分布：`READY_FOR_SHADOW=223`；`NEEDS_USER_REVIEW=20`。20 条 NEEDS 保留在审计集合中，未进入 Shadow。
- 六模块 final Skill 关联分布（同一 Skill 可关联多个模块）：`FUNDAMENTAL_RESEARCH=156`、`PORTFOLIO_CONSTRUCTION=71`、`POSITION_RISK_MANAGEMENT=144`、`PSYCHOLOGY_BEHAVIOR=96`、`SOURCING_SCREENING=66`、`VALUATION_PRICING=61`。
- 来源与视觉：raw candidate 无来源数 `0`；视觉引用 `19`；ShadowBundle 已发布，`all_rule_ids=243`、`shadow_rule_ids=223`，Shadow 返回非 READY 规则数 `0`。
- 数据库：`PRAGMA integrity_check=ok`；`PRAGMA foreign_key_check` 返回空集；direct run batch/final/bundle 计数分别为 `144/243/1`。
- 临时工件：本轮 `tmp/phase5-direct-distill/` 删除命令被本地安全策略拦截，目录仍保留且未进入提交；正式数据库对象已完成入库，后续需在允许删除时清理该目录。
- 可恢复后续锚点：原文直读和去重已完成；下一步只需用户处理 20 条 `NEEDS_USER_REVIEW` 的具体理由，之后再评估是否允许正式生产权重。当前 Bundle 仅供 Shadow 研究调用。

20 条待用户判断（均未进入 Shadow）：

1. `Ring-fence speculation from the core portfolio`：分账户/股息干预的适用性和流动性边界未确立。
2. `Build a verified research information stack`：命名服务的时效性与底层文档核验协议不足。
3. `Official catalyst and defense-supply-chain verification`：国防/零部件需求假设部分不透明且时效敏感。
4. `Simple-asset tourism business and downside review`：旅游公司数据、项目假设和估值分位需更新。
5. `Technology-theme uncertainty screen`：量子案例高度投机，技术与商业证据未冻结。
6. `Use staged exits only as a behavioral training rule`：分批卖出比例和30%目标属于未经验证的行为处方。
7. `Treat policy narratives and company forecasts as hypotheses requiring source verification`：政策及项目预测缺少官方原始文件。
8. `Ignore cancellable opening-auction indications`：9:15–9:20规则依赖当前交易场所制度。
9. `Treat seasonal-theme calendars as hypotheses requiring validation`：日历示例缺少样本、收益计算和成本检验。
10. `Apply a pre-during-post drawdown response plan`：高股息与5%–7%触发条件缺少估值和可持续性证据。
11. `Speculation admission and risk gate`：案例依赖时点、估值和流动性观察，非稳定核心方法。
12. `Set investment risk by loss-recovery capacity`：100万元阈值和收入假设不能泛化。
13. `Reassess holding period from current relative risk and return`：前瞻风险收益比较未被操作化。
14. `Mine-automation equipment value-chain screen`：产品排名、渗透率和经营观察均为时点性公司事实。
15. `Use pre-calculated marketable limit orders only after execution-risk review`：撮合、涨跌停、订单和现金规则需当前权威来源。
16. `Regulated niche moat and payout-sustainability test`：牌照、股权和分红需最新公告/年报。
17. `Use exclusion filters to improve the candidate pool`：50/60/40概率、PE阈值和行业排除是启发式示例。
18. `Demand an independently testable edge before using a marketed trading tactic`：关于散户和量化竞争的断言缺少可复现实验。
19. `Staged-project valuation with completion haircut`：项目进度、可比交易和商品假设需原始材料更新。
20. `Pre-stage an exit order only under a verified risk protocol`：A50阈值、竞价和隔夜委托规则需当前制度与经验验证。
